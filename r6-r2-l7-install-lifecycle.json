{
  "lens": "L7",
  "lens_title": "install-lifecycle hardening + legacy-path deletion",
  "round": "R6 R2",
  "phase": "phase-4-meta-core",
  "council": "phase-close-convergence",
  "branch": "phase-4-meta-core/r6-r2-l7-install-lifecycle",
  "base_branch": "phase-4-meta-core/r6-r1-fp-integration",
  "base_sha": "3c80546f37adee8cb2c05e1257e4aa883370ab92",
  "prior_round_artifact": "r6-l7-install-lifecycle.json (R6 R1 commit 1f9b119b — 2 findings: L7-MIN-1 docstring drift + L7-MAJ-1 SHAPE-not-SUBSTANCE replay-defense e2e)",
  "depth_disciplines_applied": [
    "Same-attack-class siblings (Step 3b/3c/4 pre-mint gate cluster + Step 5/6/7 post-consent validate cluster + Step 9 mint-with-rollback)",
    "Production-arm vs sentinel-presence (would-FAIL-if-no-op'd against EVERY new R6-R1-FP hook + every carried-over R6-R1-finding-target)",
    "Cross-lens cross-confirmation (L6 capability/plugin-trust, L2 adversarial, L18 docs/ledger consumed)",
    "Ground-truth-verify per §3.5n (grepped every production install_plugin/install_module/install_verified_record_unchecked call site workspace-wide; asserted each supplies expected port shape)",
    "HARD RULE 12 NAMED-destination specificity (no 'defer to next brief' / no phantom-destinations in any finding's deferred-if-deferred field)",
    "Per-finding evidence (file:line + reproducer `grep` / `cargo` invocation in every evidence block)",
    "§3.6j sweep-completeness self-verify (verified ALL 34 InstallPorts construction sites carry the post-S3a `policy:` field via cargo check + counted policy: bindings 65 ≥ 35 InstallPorts sites; also verified 0 NEW production callers of the deprecated install_verified_record_unchecked side-door since the rename)",
    "Sibling-of-install pipeline parity audit (Engine::install_module — DESIGN-INTENTIONALLY distinct, see Q-sibling-pipeline below; admin_ui_v0 install path — routes through plugin_lifecycle::install_plugin via g24d_substantive_pipeline, full F4 wiring inherited)",
    "Workspace doc-coupling sweep (testing.rs doc-block + plugin_lifecycle.rs phantom-symbol cite + V1-FROZEN-INTERFACE-DEFERRED.md row narrative)"
  ],
  "surfaces_audited": [
    "crates/benten-platform-foundation/src/plugin_lifecycle.rs (install_plugin 826-1164 + InstallPorts 698-749 + InstallParams 763-810 + S2 dropped-Option + S3a check_install_consent step 3c + S3 docstring 'Order:' block 832-859 + Step 7a verify_upgrade_author_continuity wiring 1021-1026)",
    "crates/benten-platform-foundation/src/install_consent.rs (R6 R1 FP-F4 §S3a InstallConsentPolicy trait + AdmitAllInstallConsent + DenyAllInstallConsent)",
    "crates/benten-platform-foundation/src/testing.rs (noop_replay_check helper + DOC-BLOCK with InstallPorts example)",
    "crates/benten-platform-foundation/src/manifest_store.rs (install_verified_record_unchecked rename + #[deprecated] + #[doc(hidden)] at lines 116/306 + RedbManifestStore variant)",
    "crates/benten-engine/src/install_record_replay.rs (InstallRecordReplayStore atomic record_and_check + signing_payload_hash helper)",
    "crates/benten-engine/src/engine.rs:1035+1955+2085 (install_record_replay_store always-initialized + accessor)",
    "crates/benten-engine/src/engine_modules.rs:168 (Engine::install_module — SIBLING install pipeline; design-intentionally distinct from plugin install)",
    "tools/benten-admin-shell/src/main.rs:62 (active_session_count_for_test EXEMPT case; no install_plugin call)",
    "crates/benten-engine/tests/g_core_8_install_record_replay_atomic_record_and_check.rs (substrate exercise; STILL does NOT wire through install_plugin)",
    "crates/benten-platform-foundation/tests/r6_r1_fp_f4_s2_install_ports_drops_option_and_side_door_deprecated.rs (F4 S2 verification)",
    "crates/benten-platform-foundation/tests/r6_r1_fp_f4_s3a_install_pipeline_consults_check_install_consent.rs (F4 S3a verification; does NOT wire DenyAll through install_plugin)",
    "crates/benten-platform-foundation/tests/r6fp_a_plugin_trust_blocker_closures.rs (5 substantive install_plugin pins for consent + caller-mint-first + plugin-DID-mismatch)",
    "crates/benten-platform-foundation/tests/plugin_upgrade_rejects_peer_did_substitution_e2e.rs (T10-(a) substantive e2e — L2-R6-MAJOR-1 closure verification)",
    "crates/benten-platform-foundation/tests/tf7_g_core_7_install_lifecycle_hardening.rs (§4.20 + §4.41 substantive e2e through install_plugin)",
    "crates/benten-platform-foundation/tests/tf_g_core_0_legacy_install_path_deletion_4_33.rs (3-arm absence-pin)",
    "crates/benten-platform-foundation/tests/g24d_substantive_pipeline.rs (full real-signature install_plugin pin; admin_ui_v0 delegates here)",
    "crates/benten-platform-foundation/tests/admin_ui_v0_install_as_signed_plugin_across_two_atrium_peers.rs (delegates to g24d_substantive_pipeline)",
    "docs/V1-FROZEN-INTERFACE-DEFERRED.md Rows D-2/D-3-a/D-3-b/D-3-c/D-4/D-5/D-6/D-18",
    "docs/V1-FROZEN-INTERFACE.md item 7 + item 12",
    "docs/SECURITY-POSTURE.md Compromise #26",
    "docs/MODULE-MANIFEST.md (cross-checked: confirms ModuleManifest distinct primitive from PluginManifest)"
  ],
  "convergence_baseline": "R6 R1 → R6 R2; R6 R1 surfaced 2 must-close findings; the R6-R1 fix-pass DID NOT land their closures (both carry forward). R6 R2 also walks the R6-R1-FP-mutated surfaces (S2 drop-Option + S3a check_install_consent + S3 reordering + B's L2-R6-MAJOR-1 wiring + manifest_store rename + payload_hash hoisting) and surfaces 4 new findings — 2 of them are pim-N pattern recurrences (1 pim-18 + 1 pim-1) confirming the existing rules' load-bearingness; 2 are surgical doc/narrative gaps (1 §3.6i discipline + 1 testing.rs doc-block).",
  "questions": [
    {
      "q": "L7 R6 R1 findings closure: are L7-MIN-1 (docstring stale step-order) + L7-MAJ-1 (SHAPE-not-SUBSTANCE replay-defense e2e) CLOSED at integration HEAD 3c80546f?",
      "verdict": "NO — BOTH STILL OPEN; closures DID NOT land in R6-R1-FP",
      "evidence": "L7-MIN-1: plugin_lifecycle.rs:832-859 still document the PRE-R6-FP-A step ordering ('3. Seam 2 — validate_with_clock'; '4. Consent gate — verify InstallRecord's user-DID signature'). Implementation at lines 891-975 executes the OPPOSITE order (3=consent gate per sec-r6r1-6 R6-FP-A reorder; 4=Seam 2 clock). Docstring is silent on Step 3b §4.37 InstallRecordReplayStore (lines 920-941), Step 3c §S3a check_install_consent (lines 943-964 ADDED in R6-R1-FP-F4 §S3a; THIS round adds the gap), Step 7a verify_upgrade_author_continuity (lines 1021-1026 ADDED in R6-R1-FP-B L2-R6-MAJOR-1 closure; THIS round adds the gap), AND Step 7b §4.41 caps-grew (lines 1028-1059). L7-MAJ-1: `grep -rn 'PluginInstallRecordAlreadyApplied' --include='*.rs'` shows every assertion is at the primitive layer (install_record_replay.rs test mod + g_core_8_install_record_replay_atomic_record_and_check.rs primitive-direct exercises); NO test wires install_plugin(.., install_record_replay_check: &mut substantive_counting_check, ..) and asserts Err(PluginInstallRecordAlreadyApplied). Reproducer: `grep -rn 'PluginInstallRecordAlreadyApplied' --include='*.rs' | grep -v 'install_record_replay.rs:\\|/src/'` returns 0 hits inside install_plugin-driven test bodies."
    },
    {
      "q": "B's L2-R6-MAJOR-1 closure — verify_upgrade_author_continuity wired into install_plugin?",
      "verdict": "YES (substantive)",
      "evidence": "plugin_lifecycle.rs:1021-1026 invokes `verify_upgrade_author_continuity(&prior_manifest, &manifest)?` at Step 7a (after Step 7 DAG-descendant check, BEFORE Step 8 plugin-DID adoption). Substantive e2e at plugin_upgrade_rejects_peer_did_substitution_e2e.rs:170-185 (drives install_plugin end-to-end + asserts ErrorCode::PluginAuthorNotTrusted on the upgrade-author-substitution attack arm; first install consents under author Alice at line 112, upgrade attempt under attacker is rejected at line 185 assertion). The R5 helper `verify_upgrade_author_continuity` is now consumed at the production install entry — T10-upgrade (a) defense is LIVE."
    },
    {
      "q": "Per-DID install record TOCTOU defense (F4 S2 closure of Row D-2): is `InstallPorts.install_record_replay_check` now a non-Option `&mut InstallRecordReplayCheckFn`?",
      "verdict": "YES STRUCTURALLY (at the type level) — but with the v1-beta posture caveat captured in F-5",
      "evidence": "plugin_lifecycle.rs:723 declares `pub install_record_replay_check: &'a mut InstallRecordReplayCheckFn,` (no Option<>). Verified by F4 S2 type-level test at r6_r1_fp_f4_s2_install_ports_drops_option_and_side_door_deprecated.rs:54-68. **§3.6j SWEEP COMPLETENESS:** `grep -rn 'InstallPorts\\s*{' --include='*.rs' | wc -l` = 35 (34 test sites + 1 ignored doc example). `cargo check -p benten-platform-foundation --features testing` GREEN — so all 34 sites carry the required `install_record_replay_check` field (type system enforces). The drop-Option STRUCTURALLY forces every caller to make an explicit choice; production callers can no longer silently disable the §4.37 defense via a `None` arm. manifest_store.rs side-door correctly renamed to `install_verified_record_unchecked` + `#[deprecated]` + `#[doc(hidden)]` at lines 112-129 (in-RAM ManifestStore) + 302-328 (RedbManifestStore variant). **NEW CALLER AUDIT POST-RENAME:** `grep -rn 'install_verified_record_unchecked' --include='*.rs'` returns 5 caller sites — all in tests + 2 internal manifest_store.rs tests; ALL are marked `#[allow(deprecated)]` or explicitly exercise the deprecated side-door for drift-detection (plugin_manifest_post_install_drift_detected.rs + tf7_g_core_7_install_lifecycle_hardening.rs + r6_r1_fp_f4_s2 + manifest_store.rs:447/466). NO new production callers since the rename — rename discipline holds."
    },
    {
      "q": "F4 S3a check_install_consent wired at install pipeline step 3c — production-arm denial actually rejects via install_plugin?",
      "verdict": "PARTIAL (structurally wired; denial-arm e2e test pin missing — captured as F-3)",
      "evidence": "plugin_lifecycle.rs:957-964 invokes `ports.policy.check_install_consent(&payload_hash, plugin_did_str).is_err()` at Step 3c, mapping to typed `PluginInstallConsentDenied`. `InstallPorts.policy: &'a dyn InstallConsentPolicy` at line 748. InstallConsentPolicy trait + AdmitAllInstallConsent + DenyAllInstallConsent at install_consent.rs. **REJECTION-CODE FIDELITY NOTE:** The policy's specific Err variant is DISCARDED at line 962-963 (`.is_err()` → fixed `PluginInstallConsentDenied`). Per Δv3-3 in V1-FROZEN-INTERFACE-DEFERRED, this is the design intent (forensic-discrimination at the install_plugin layer, NOT at the policy layer); a custom policy that wanted a different typed code would need engine-side mapping support — NOT a v1-beta gap. **§3.6j SWEEP COMPLETENESS:** All 34 InstallPorts construction sites carry the new `policy:` field (verified by cargo check pass). `grep -rn 'policy:.*[Pp]olicy\\|AdmitAll\\|DenyAll\\|noauth_policy'` = 65 hits, > 35 InstallPorts construction sites confirming field-completeness. **GAP:** the dedicated §S3a test file (r6_r1_fp_f4_s3a_install_pipeline_consults_check_install_consent.rs) admits in its header at lines 11-14: 'The end-to-end install_plugin integration is exercised by the existing platform-foundation tests (they all wire AdmitAllInstallConsent post-S3a migration; with the threading verified by their compilation).' Verification is compilation-coercion only. `grep -rn 'DenyAllInstallConsent' --include='*.rs' | grep -v install_consent.rs` shows DenyAllInstallConsent is used ONLY in the §S3a trait-direct tests, never wired through install_plugin to assert pipeline-rejection."
    },
    {
      "q": "All production install_plugin call sites supply substantive InstallPorts (drop-Option mandated)?",
      "verdict": "VACUOUSLY YES (no production call sites exist at HEAD)",
      "evidence": "Reproducer: `grep -rn 'plugin_lifecycle::install_plugin\\|::install_plugin\\b\\|^use.*install_plugin\\b' --include='*.rs' | grep -v '/tests/' | grep -v -E 'uninstall_plugin|^\\s*//|/// |//!|deprecated'` returns: 1) `crates/benten-platform-foundation/src/plugin_lifecycle.rs:865:pub fn install_plugin<F, M, P>(` (the definition itself); 2) `crates/benten-platform-foundation/src/manifest_store.rs:113/303` (deprecation notes pointing TO it). ZERO production callers invoke it. Every `install_plugin(..)` invocation in the codebase is in `tests/`. tools/benten-admin-shell/src/main.rs has only `state.session().active_session_count_for_test()` at line 62 — fix-up #6 EXEMPT (legacy naming carry, R4b-tracked). This is consistent with V1-FROZEN-INTERFACE-DEFERRED.md Row D-2's FORENSIC row 'v1-beta posture: ... zero production callers supply Some(closure) at HEAD. The TOCTOU defense (parallel-install-of-same-bytes) is NOT live in shipped binaries' — verified accurate at FORENSIC narrative; F-5 captures the inconsistency with the CLOSED-block wording."
    },
    {
      "q": "Pipeline ordering invariants (3b replay → 3c consent → 4 clock-validation → 5+ cap cascade) — does the implementation actually execute in this order? Same-attack-class sibling steps verified?",
      "verdict": "YES (structurally; ordering invariants hold across all pre-mint gate siblings)",
      "evidence": "plugin_lifecycle.rs executes: line 912 (Step 3 consent gate verify_user_signature + manifest_cid + consenting_user_did match) → 920-941 (Step 3b §4.37 replay-check) → 943-964 (Step 3c §S3a check_install_consent) → 975 (Step 4 validate_with_clock) → 978-981 (Step 5 heterogeneity) → 985 (Step 6 cycle detection) → 988-994 (Step 7 DAG-descendant) → 1021-1026 (Step 7a T10-(a) verify_upgrade_author_continuity) → 1051-1059 (Step 7b §4.41 caps-grew) → 1097-1102 (Step 8 plugin-DID adoption) → 1105-1126 (Step 9 cap-cascade) → 1129-1135 (Step 10 namespace) → 1142-1156 (Step 11 library insert + set_active). **Same-attack-class sibling cluster (pre-mint gates):** Steps 3 / 3b / 3c / 4 / 5 / 6 / 7 / 7a / 7b ALL fire BEFORE Step 9's mint loop — so a denial at any of them mints zero grants. The pre-mint atomicity contract holds for the entire cluster. **Rollback paths:** rollback_step9 (lines 1171-1175) unwinds every mid-loop minted grant on Step-9 mid-loop fail, Step-10 fail, Step-11 fail. Step 11b additionally calls library.remove(expected_cid) + deprovision_private_namespace + rollback_step9. Pre-mint atomicity discipline preserved across the post-R6-FP-A re-ordering."
    },
    {
      "q": "§4.33 legacy install-path deletion — still structurally absent at integration HEAD?",
      "verdict": "YES",
      "evidence": "Reproducer: `grep -n 'pub fn install_plugin\\|pub fn install_plugin_persisting_did' crates/benten-platform-foundation/src/module_ecosystem.rs` returns 0 hits. The 3-arm absence-pin at tf_g_core_0_legacy_install_path_deletion_4_33.rs remains valid. Test files don't import the deleted symbols. plugin_lifecycle.rs is the canonical-only install path."
    },
    {
      "q": "§6.4 manifest envelope chain validation invoked at install time?",
      "verdict": "DESIGN-INTENTIONAL (same as R6 R1)",
      "evidence": "Install MINTS the root chain at Step 9 (CLAUDE.md #18 Layer-1 trace anchor); chain validator at benten-caps::manifest_envelope_chain_validation walks a DOWNSTREAM delegation chain. Install does not consult the validator by design. Production call site is sync-merge boundary via apply_atrium_merge manifest_envelope_recheck port — ProductionManifestEnvelopeRechecker WIRED at HEAD per Row D-4 closure (handshake.rs builder). Same disposition as R6 R1."
    },
    {
      "q": "Q-sibling-pipeline: Are sibling install pipelines (Engine::install_module + admin_ui_v0 install) parity-audited against F4 wiring?",
      "verdict": "YES — Engine::install_module is DESIGN-INTENTIONALLY distinct (engine-internal privileged write; Compromise #21 closure via verify_manifest_with_mode); admin_ui_v0 install ROUTES THROUGH plugin_lifecycle::install_plugin (g24d_substantive_pipeline) with full F4 wiring inherited.",
      "evidence": "engine_modules.rs:168 `Engine::install_module(manifest, expected_cid, verify_args)` is a privileged engine-internal write path (`WriteAdmissionFrame::engine_internal()` at line 256). It handles ModuleManifest (Phase-2b SANDBOX wasm module bundle; Compromise #21) NOT PluginManifest (Phase-4-Foundation plugin trust schema; CLAUDE.md #18). The trust model is intentionally different: module install is engine-author-trusted (signature-verified via verify_manifest_with_mode at lines 217-225, audience-bound for UCAN paths); plugin install is user-DID-routed with the 11-step CLAUDE.md #18 three-layer-consent pipeline. Module install correctly does NOT consult InstallRecordReplayStore (no install-record artifact), check_install_consent (no user-DID consent surface; user-anchor is implicit in engine ownership), §4.41 caps-grew (modules don't have manifest envelope `requires` widening at upgrade-time at v1-beta), or T10-(a) (no peer-DID author chain at module-author level). **Doc-cite of the distinction is implicit in docs/MODULE-MANIFEST.md vs docs/PLUGIN-MANIFEST.md but not co-located** — a non-finding observation: a 1-line cross-link from each doc to the other naming 'these are distinct primitives with distinct trust models' would help future auditors. admin_ui_v0 install routes through plugin_lifecycle::install_plugin via g24d_substantive_pipeline::full_install_pipeline_real_signatures_succeeds (full real-signature pin; InstallPorts:128-148 wires noop_replay_check + AdmitAllInstallConsent + cap-minter + ns) — inherits full F4 wiring. No sibling-parity gap."
    }
  ],
  "disposition": "APPROVE-WITH-FINDINGS",
  "summary": "Install-lifecycle hardening + §4.33 deletion remain structurally sound at v1-beta. R6 R1 must-close on L7-MIN-1 + L7-MAJ-1 DID NOT LAND — both carry forward to R6 R2 unchanged (L7-MIN-1 now also widens to include the new Step 3c §S3a + Step 7a T10-(a) gaps introduced by R6-R1-FP itself, STRICTLY WORSENING the drift). FOUR NEW findings: F-3 (§S3a check_install_consent denial-arm e2e through install_plugin missing — 2nd pim-18 §3.6f instance this lens), F-4 (phantom helper symbol `make_engine_replay_check_closure` cited in plugin_lifecycle.rs:712 doc but does not exist — pim-1 §3.5b instance), F-5 (V1-FROZEN-INTERFACE-DEFERRED Row D-2 closure-block wording inconsistent with FORENSIC narrative + Row D-3-c precedent — §3.6i discipline instance), F-6 (crates/benten-platform-foundation/src/testing.rs doc-block InstallPorts example missing `policy:` field added by R6-R1-FP-F4 §S3a — pim-1 §3.5b instance + 5th-instance recurrence in this lens). All findings FIX-NOW per HARD RULE 12: F-1 ~50 LOC docstring rewrite + F-2 ~120 LOC substantive test addition + F-3 ~100 LOC substantive test addition + F-4 ~3-10 LOC helper-add-or-doc-edit + F-5 ~5 LOC doc-narrative tighten + F-6 ~5 LOC doc-block edit ≈ ~285-300 LOC across 6 surgical fixes. Cap-cascade ordering / atomicity / rollback discipline / T10-upgrade (a) + (b) defense / F4 S2 drop-Option / F4 S3a check_install_consent structural wiring / §4.33 deletion / sibling-of-install pipeline parity (install_module distinct-by-design, admin_ui_v0 routes through canonical) all VERIFIED-CORRECT.",
  "findings": [
    {
      "id": "L7-R2-F-1",
      "title": "[CARRYOVER + WIDENED] install_plugin docstring 'Order:' block at lines 832-859 STILL documents stale pre-R6-FP-A step ordering AND now also omits new Step 3c §S3a + Step 7a T10-(a) gates added during this round's FP",
      "severity": "MINOR",
      "surface": "crates/benten-platform-foundation/src/plugin_lifecycle.rs lines 826-863 (#[allow(clippy::too_many_arguments)] pub fn install_plugin docstring 'Order:' enumeration)",
      "evidence": "Carryover from R6 R1 L7-MIN-1 (R6-R1 fix-pass did not land the docstring rewrite). At integration HEAD 3c80546f: docstring claims `3. Seam 2 — validate_with_clock(now_secs)` and `4. Consent gate — verify InstallRecord's user-DID signature`. Implementation at lines 891-975 executes: 3=consent gate (sec-r6r1-6 R6-FP-A reorder; lines 896-918), 3b=§4.37 replay check (lines 920-941), 3c=§S3a check_install_consent (lines 943-964; NEW THIS ROUND via R6-R1-FP-F4 §S3a), 4=Seam 2 validate_with_clock (line 975). Docstring is silent on Steps 3b, 3c, 7a (verify_upgrade_author_continuity NEW THIS ROUND via R6-R1-FP-B L2-R6-MAJOR-1 closure at lines 1021-1026), and 7b (§4.41 caps-grew at lines 1028-1059). Drift is now STRICTLY WIDER than at R6 R1 — the R6-R1-FP itself introduced two new pipeline steps (3c + 7a) whose addition the docstring did not capture (in addition to the original 3b/7b gaps). Reproducer: read lines 832-859 vs lines 891-1060. pim-1 §3.5b post-fix doc-coupling pre-flight FOR the R6-R1-FP-B + FP-F4 waves themselves would have caught this — every wave that mutates install_plugin's step ordering must sweep the adjacent 'Order:' docstring before push.",
      "would_fail_test_marker": "A reviewer (or post-hoc auditor) reading the docstring to understand the security pipeline ordering would form a wrong mental model: they would believe the clock-fail check fires BEFORE consent, they would not know §4.37 replay-defense exists, they would not know §S3a install-time consent exists, they would not know §T10-(a) upgrade-author continuity exists, and they would not know §4.41 caps-grew fresh-consent exists. pim-1 §3.5b discipline exists precisely to catch this; recurrence across 2 rounds + 2 new steps = 4-instance pattern within this lens alone.",
      "disposition": "FIX-NOW",
      "fix_proposal": "Rewrite the 'Order:' enumeration (lines 832-859) to match current implementation. Promote the authoritative inline section comments (`// 3. Consent gate FIRST (sec-r6r1-6 R6-FP-A reorder)` at line 896, `// **3b. G-CORE-8 §4.37 InstallRecord replay-defense ...` at line 920, `// **3c. R6 R1 FP-F4 §S3a (Row D-3-a closure) ...` at line 943, `// 7a. **T10-upgrade (a) — same-author DID continuity ...` at line 996, `// 7b. **G-CORE-7 §4.41 — caps-grew fresh-consent ...` at line 1028) into the pub-fn docstring 'Order:' block. New step list: 1=decode+content-CID; 2=trust-list; 3=consent gate (user-sig + record.manifest_cid + record.consenting_user_did); 3b=§4.37 InstallRecordReplayStore record-and-check via install_record_replay_check port; 3c=§S3a check_install_consent via policy port (typed PluginInstallConsentDenied); 4=Seam 2 validate_with_clock; 5=heterogeneity; 6=cycle detection; 7=upgrade-DAG-descendant; 7a=T10-(a) verify_upgrade_author_continuity (typed PluginAuthorNotTrusted); 7b=§4.41 caps-grew fresh-consent (decide_upgrade_consent + install_record_covers_required_caps; typed PluginInstallConsentRequired); 8=plugin-DID adoption (caller-mint-first); 9=cap-cascade with atomic rollback (rollback_step9); 10=private namespace (with deprovision on Step 11 fail); 11=insert library + set_active (with unwind on fail). ~40-55 LOC docstring edit, no code change.",
      "destination_if_deferred": "N/A — FIX-NOW per HARD RULE 12 (orchestrator-direct trivial doc edit; R6 R2 fix-pass scope; carryover from R6 R1 + 2 new gaps from R6-R1 fix-pass).",
      "convergence_disposition": "must-close"
    },
    {
      "id": "L7-R2-F-2",
      "title": "[CARRYOVER] §4.37 install-record replay-defense end-to-end test pin through install_plugin STILL missing (SHAPE-not-SUBSTANCE)",
      "severity": "MAJOR",
      "surface": "crates/benten-engine/tests/g_core_8_install_record_replay_atomic_record_and_check.rs (test `install_record_replay_rejected_pre_cap_cascade_not_post_rollback` at lines 223-283); coupled to crates/benten-platform-foundation/src/plugin_lifecycle.rs Step 3b at line 940.",
      "evidence": "Carryover from R6 R1 L7-MAJ-1 (R6-R1 fix-pass did not land the e2e test addition). At integration HEAD 3c80546f: the named test `install_record_replay_rejected_pre_cap_cascade_not_post_rollback` at lines 223-283 STILL exercises only the InstallRecordReplayStore primitive directly (`store.record_and_check(payload_hash)` at lines 262 + 273). The test's own commentary at lines 226-233 STILL says 'SHIPPED-SURFACE EXERCISE: ... exercise the SHIPPED ErrorCode discriminator on a Step-9-class error to anchor the structural contract' AND at lines 267-271 'the production install_plugin caller catches the typed Err'. Reproducer: `grep -rn 'PluginInstallRecordAlreadyApplied' --include='*.rs' | grep -v 'install_record_replay.rs\\|/src/'` returns 0 install_plugin-driven assertions. Would-FAIL scenario unchanged from R6 R1: a regression deleting `(ports.install_record_replay_check)(&payload_hash)?` at line 940 would NOT cause any test to fail.",
      "would_fail_test_marker": "Identical to R6 R1 L7-MAJ-1: a regression deleting the install_record_replay_check call at line 940 would NOT cause any test to fail (the primitive tests still pass; the substrate-proxy test still passes). The §4.37 contract's behavioral pin at the install_plugin boundary remains UNTESTED.",
      "disposition": "FIX-NOW",
      "fix_proposal": "Add ONE substantive end-to-end test in `crates/benten-platform-foundation/tests/`: (1) build a minimal valid PluginManifest + InstallRecord using `common::manifest_fixtures` helpers; (2) construct `InstallPorts { install_record_replay_check: &mut substantive_check, policy: &AdmitAllInstallConsent, cap_minter: &mut minter, private_ns: &mut ns }` where `substantive_check` is a closure capturing an `InstallRecordReplayStore` forwarding to `store.record_and_check(*hash)`; (3) first `install_plugin(..)` call succeeds (InstallOutcome.grants_minted > 0, store.consumed_count() == 1, library.len() == 1); (4) second `install_plugin(..)` call with identical install_record returns `Err(ErrorCode::PluginInstallRecordAlreadyApplied)` AND store.consumed_count() is still 1 AND library.len() is still 1 (zero double-install). Filename: `crates/benten-platform-foundation/tests/g_core_8_install_record_replay_wired_through_install_plugin.rs`. ~100-150 LOC reusing existing fixture helpers.",
      "destination_if_deferred": "N/A — FIX-NOW per HARD RULE 12 (test gap, not new scope; sibling harness exists; substrate fully-wired; carryover from R6 R1).",
      "convergence_disposition": "must-close"
    },
    {
      "id": "L7-R2-F-3",
      "title": "[NEW — pim-18 §3.6f 2nd-instance for §S3a] §S3a check_install_consent denial-arm end-to-end test pin through install_plugin missing (DenyAllInstallConsent never wired through install_plugin to assert PluginInstallConsentDenied)",
      "severity": "MAJOR",
      "surface": "crates/benten-platform-foundation/tests/r6_r1_fp_f4_s3a_install_pipeline_consults_check_install_consent.rs (the test file dedicated to §S3a verification); coupled to crates/benten-platform-foundation/src/plugin_lifecycle.rs Step 3c at lines 957-964.",
      "evidence": "The §S3a test file's own header at lines 11-14 admits the e2e pin gap: 'The end-to-end install_plugin integration is exercised by the existing platform-foundation tests (they all wire AdmitAllInstallConsent post-S3a migration; with the threading verified by their compilation).' Verification by compilation-coercion ONLY. Reproducer: `grep -rn 'DenyAllInstallConsent' --include='*.rs' | grep -v install_consent.rs` shows DenyAllInstallConsent is used ONLY in the §S3a trait-direct tests (r6_r1_fp_f4_s3a:71-75 — exercises `policy.check_install_consent` method DIRECTLY); never wired through install_plugin. `grep -rn 'PluginInstallConsentDenied' --include='*.rs' | grep -v 'errors\\|/src/'` shows only the trait test asserts it; the install_plugin pipeline at line 963 maps any policy-error to PluginInstallConsentDenied but no test exercises the mapping. Same pim-18 §3.6f SHAPE-not-SUBSTANCE pattern as L7-MAJ-1 / L7-R2-F-2, applied to a fresh wave (R6-R1-FP-F4 §S3a) — STRENGTHENS the existing-pattern recurrence count.",
      "would_fail_test_marker": "A regression that quietly removed the `ports.policy.check_install_consent(&payload_hash, plugin_did_str).is_err()` call at plugin_lifecycle.rs:957-962 (e.g., short-circuited to `Ok(())`) would NOT cause any test to fail. The §S3a check_install_consent hook would be silently inert in shipped binaries — exactly the failure-mode pattern that L7-MAJ-1 surfaced for §4.37 replay-defense.",
      "disposition": "FIX-NOW",
      "fix_proposal": "Add a substantive end-to-end test in `crates/benten-platform-foundation/tests/`: (1) build a minimal valid PluginManifest + InstallRecord; (2) construct `InstallPorts { policy: &DenyAllInstallConsent, install_record_replay_check: &mut noop_replay_check(), cap_minter: &mut minter, private_ns: &mut ns }`; (3) call `install_plugin(..)`; (4) assert `Err(ErrorCode::PluginInstallConsentDenied)`; (5) assert `minter.minted_grants().is_empty()` (no Step-9 mint), `library.is_empty()` (no Step-11 insert), `ns.provisioned_count() == 0` (no Step-10 namespace). Combine with a positive-arm: same shape with `AdmitAllInstallConsent` → `Ok(InstallOutcome)`. Filename: `crates/benten-platform-foundation/tests/r6_r1_fp_f4_s3a_install_pipeline_denies_via_check_install_consent_e2e.rs` (or extend the existing s3a file with new tests). ~80-120 LOC.",
      "destination_if_deferred": "N/A — FIX-NOW per HARD RULE 12 (test gap on a structurally-wired surface; closure cost ~80 LOC; couples to F-2 by the same pim-18 pattern).",
      "convergence_disposition": "must-close"
    },
    {
      "id": "L7-R2-F-4",
      "title": "[NEW — pim-1 §3.5b instance] InstallPorts.install_record_replay_check doc-comment cites phantom helper `make_engine_replay_check_closure` that does NOT exist in the codebase",
      "severity": "MINOR",
      "surface": "crates/benten-platform-foundation/src/plugin_lifecycle.rs:712 (the doc-comment on `InstallPorts.install_record_replay_check` field).",
      "evidence": "Line 712: `Production callers wire \\`make_engine_replay_check_closure(engine.install_record_replay_store())\\`;`. Reproducer: `grep -rn 'make_engine_replay_check_closure' --include='*.rs'` returns ONLY this doc comment itself. No `fn make_engine_replay_check_closure` is defined anywhere in the workspace. The intended helper (a one-line wrapper over `move |hash| engine.install_record_replay_store().record_and_check(*hash)`) was either renamed, deleted, or never landed. cite-drift / phantom-symbol failure mode that pim-1 §3.5b doc-coupling exists to catch. Couples to F-5 (Row D-2's similar description).",
      "would_fail_test_marker": "A reader trying to wire production install_plugin code would grep for the cited helper, find nothing, and either invent an ad-hoc closure (no shared convention) or mis-wire by mistake. The doc misleads about the production-wiring API surface.",
      "disposition": "FIX-NOW",
      "fix_proposal": "Two options (orchestrator's call): (a) ADD the helper to crates/benten-engine/src/install_record_replay.rs as `pub fn make_engine_replay_check_closure(store: &Arc<InstallRecordReplayStore>) -> impl FnMut(&[u8; 32]) -> Result<(), ErrorCode> + '_` returning the canonical `move |hash| store.record_and_check(*hash)` closure (~10 LOC; matches the cited symbol; makes production wiring trivial; couples to closing F-5). (b) REPLACE the doc phrase with the inline closure form `move |hash| engine.install_record_replay_store().record_and_check(*hash)` (~3 LOC edit). Recommend (a) per the agent-economics-prefer-thorough-cleanup memory (helper exists in design intent; landing it advances the production-wiring story; couples to closing F-5).",
      "destination_if_deferred": "N/A — FIX-NOW per HARD RULE 12 (trivial; orchestrator-direct).",
      "convergence_disposition": "must-close"
    },
    {
      "id": "L7-R2-F-5",
      "title": "[NEW — §3.6i schema discipline instance] V1-FROZEN-INTERFACE-DEFERRED.md Row D-2 closure-block wording conflates structural-type-shape closure with production-wiring; needs v1-beta posture qualifier consistent with Row D-3-c precedent",
      "severity": "MINOR",
      "surface": "docs/V1-FROZEN-INTERFACE-DEFERRED.md lines 116-128 (~~Row D-2~~ — InstallRecordReplayStore lifecycle-required wiring — CLOSED).",
      "evidence": "Row D-2's closure block at lines 116-128 says 'every install caller MUST supply a substantive closure now ... production callers wire `engine.install_record_replay_store().record_and_check`.' Verified accurate at the type level (drop-Option). But verified inaccurate at the production-call-site level: `grep -rn 'plugin_lifecycle::install_plugin\\|::install_plugin\\b' --include='*.rs'` (excluding tests + comments) shows ZERO production callers. The 'production callers wire ...' description is FORWARD-LOOKING. The (FORENSIC) Row D-2 narrative at lines 129-152 correctly captures the v1-beta posture: 'zero production callers supply Some(closure) at HEAD. The TOCTOU defense (parallel-install-of-same-bytes) is NOT live in shipped binaries.' The (CLOSED) summary at lines 116-128 should either (a) inherit that v1-beta qualifier or (b) be downgraded to 'STRUCTURALLY CLOSED' pattern consistent with Row D-3-c at line 154 ('PARTIAL CLOSED' for a similar structural-but-not-fully-consumed shape). §3.6i schema-discipline applied to deferred-row narrative authoring.",
      "would_fail_test_marker": "A reader skimming V1-FROZEN-INTERFACE-DEFERRED for v1-beta security posture would believe the §4.37 TOCTOU defense is LIVE in shipped binaries (per the Row D-2 closure block); reading further to the FORENSIC narrative would surface the contradiction. The single-glance v1-beta-posture-accuracy discipline is regressed.",
      "disposition": "FIX-NOW",
      "fix_proposal": "Update Row D-2 closure block (lines 116-128) to: '~~Row D-2~~ — InstallRecordReplayStore lifecycle-required wiring — **STRUCTURALLY CLOSED at R6 R1 FP-F4 §S2** (2026-05-24)' (mirroring Row D-3-c at line 154's 'PARTIAL CLOSED' precedent). Add v1-beta posture qualifier paragraph: 'STRUCTURAL CLOSURE: `InstallPorts.install_record_replay_check` is now a required `&mut InstallRecordReplayCheckFn` — no caller can silently disable the §4.37 defense via the `None` arm. v1-beta posture: zero production callers of `plugin_lifecycle::install_plugin` exist at HEAD; the TOCTOU defense is the type-level guarantee + the substrate primitive\\'s correctness. Live-defense in shipped binaries follows G-COMP-1 production wiring; the (FORENSIC) row below captures the unchanged v1-beta caveat.' Couples to F-4 (if F-4 lands option (a), the helper exists; description can then be tightened).",
      "destination_if_deferred": "N/A — FIX-NOW per HARD RULE 12 (trivial doc edit ~5 LOC).",
      "convergence_disposition": "must-close"
    },
    {
      "id": "L7-R2-F-6",
      "title": "[NEW — pim-1 §3.5b 5th-instance + §3.6j sweep-completeness] testing.rs doc-block InstallPorts example missing `policy:` field added by R6-R1-FP-F4 §S3a",
      "severity": "MINOR",
      "surface": "crates/benten-platform-foundation/src/testing.rs lines 34-41 (`/// ```ignore` doc-block on `pub fn noop_replay_check`).",
      "evidence": "Lines 34-41 document the canonical usage of `noop_replay_check()` in a `/// ```ignore` block: `let mut ports = InstallPorts { cap_minter: ..., private_ns: ..., install_record_replay_check: &mut noop, };` — missing the `policy:` field that R6-R1-FP-F4 §S3a added to InstallPorts at plugin_lifecycle.rs:748. The doc-block is `ignore`'d so it doesn't compile-fail, but it is STALE — a reader following the documented pattern would get an 'InstallPorts requires `policy` field' compile error. §3.6j sweep-completeness: the F4 §S3a wave (which added `policy: &'a dyn InstallConsentPolicy`) added the field at the type level + migrated all 34 test sites to thread it, but did NOT sweep the testing.rs doc-block. This is a 5th pim-1 §3.5b instance in this lens alone (counted: L7-R2-F-1 carryover original-step-3 stale + L7-R2-F-1 widening for step-3c-§S3a + L7-R2-F-1 widening for step-7a-T10 + L7-R2-F-4 phantom helper + L7-R2-F-6 testing.rs doc-block). Cross-lens cross-confirmation: this lens-of-instance count exceeds the ≥3-recurrence threshold for pim-pattern reinforcement.",
      "would_fail_test_marker": "A reader copy-pasting the doc-block as a starting template would get a compile error from rustc — 'missing field `policy` in initializer of `InstallPorts<_, _>`'. The doc's intent (template for test-fixture authoring) is regressed.",
      "disposition": "FIX-NOW",
      "fix_proposal": "Update the testing.rs doc-block at lines 34-41: add the `policy: &AdmitAllInstallConsent,` line to the InstallPorts initializer. Also (orchestrator's call): consider promoting the doc-block from `/// ```ignore` to `/// ```no_run` once the example is complete + buildable (post-this fix + F-4 helper landing) so the rustdoc tooling catches future drift automatically. Filename: same file; ~3-5 LOC edit (1 added field + optional `no_run` flip).",
      "destination_if_deferred": "N/A — FIX-NOW per HARD RULE 12 (trivial doc-block edit).",
      "convergence_disposition": "must-close"
    }
  ],
  "non_findings_explicit": [
    "Step ordering correctness: VERIFIED (consent → §4.37 replay → §S3a check_install_consent → clock-validate → heterogeneity → cycle → upgrade-DAG → T10-(a) → §4.41 caps-grew → plugin-DID adoption → cap-cascade → namespace → library insert). All rollback paths preserve the no-observable-residue invariant. Atomicity discipline: every consent/replay/clock/heterogeneity/cycle/upgrade/T10/caps-grew check fires BEFORE Step 9's mint loop, so a denial mints zero grants. R6 R1 verdict CARRIES.",
    "Same-attack-class sibling cluster (pre-mint gates 3 + 3b + 3c + 4 + 5 + 6 + 7 + 7a + 7b) all fire before Step 9 mint — no half-mint observable on any denial across the cluster. Cross-checked at the implementation by reading lines 891-1126.",
    "B's L2-R6-MAJOR-1 closure VERIFIED: `verify_upgrade_author_continuity` is wired at Step 7a (plugin_lifecycle.rs:1021-1026) AND exercised end-to-end via plugin_upgrade_rejects_peer_did_substitution_e2e.rs which drives install_plugin and asserts `ErrorCode::PluginAuthorNotTrusted`. T10-upgrade (a) defense is LIVE.",
    "F4 S2 drop-Option STRUCTURAL closure VERIFIED: `InstallPorts.install_record_replay_check: &'a mut InstallRecordReplayCheckFn` (no Option) at plugin_lifecycle.rs:723; 34 test fixture sites all carry the new field (verified by cargo check pass); F4 S2 type-level test verifies coercion. (BUT see F-5: structural closure ≠ live production defense; CLOSURE-BLOCK wording needs tightening.)",
    "F4 S2 manifest_store rename VERIFIED + NO NEW CALLERS POST-RENAME: `install_verified_record_unchecked` at lines 116-129 + 306-328, both carry `#[deprecated]` + `#[doc(hidden)]`. `grep -rn 'install_verified_record_unchecked' --include='*.rs'` = 5 caller sites, ALL in tests + 2 internal manifest_store.rs test fns + all `#[allow(deprecated)]`. Side-door discipline holds; deprecation note correctly steers callers to plugin_lifecycle::install_plugin.",
    "F4 S3a check_install_consent STRUCTURAL wiring VERIFIED: plugin_lifecycle.rs:957-964 invokes `ports.policy.check_install_consent(&payload_hash, plugin_did_str)` mapping any Err to `PluginInstallConsentDenied`. InstallConsentPolicy trait + AdmitAllInstallConsent + DenyAllInstallConsent at install_consent.rs. Routed via `&dyn` port (CRITIC-2 F-1.2 compliant). All 34 InstallPorts construction sites carry the `policy:` field (verified by cargo check pass + grep count 65 ≥ 35). (BUT see F-3: denial-arm e2e through install_plugin is missing.)",
    "F4 S3a typed ErrorCode `PluginInstallConsentDenied` VERIFIED present in catalog at benten-errors/src/lib.rs:986 + 1279-equivalent (CATALOG_VARIANT_COUNT 192→193 per the V1-FROZEN-INTERFACE-DEFERRED notes). TS class mirror + ERROR-CATALOG.md narrative — cross-checked here for completeness (L12 cross-language §3.5g lens's primary scope).",
    "Sealed-discipline preservation VERIFIED: `InstallConsentPolicy` lives in benten-platform-foundation (not benten-caps; cycle-safe per the install_consent.rs module-doc). Per CLAUDE.md baked-in #7 sealed-CapabilityPolicy refinement, the engine-internal sealed boundary is preserved; the trait is 'sealed-by-convention' (no external impls expected).",
    "§4.21 install-time consent gate every-plugin check VERIFIED: install_plugin Step 3 enforces install_record verify_user_signature + manifest_cid + consenting_user_did binding on EVERY invocation. §4.33 deletion confirms no alternate install path.",
    "§4.20 + §4.41 substantive e2e VERIFIED: tf7_g_core_7_install_lifecycle_hardening.rs exercises BOTH §4.20 (`install_with_time_bounded_manifest_under_clock_sentinel_rejected_e2e` asserts `Err(ErrorCode::UcanClockNotInjected)` AND zero grants minted) AND §4.41 (`upgrade_with_grown_requires_must_block_install_until_fresh_consent_e2e` asserts `outcome.is_err()` AND `minted_grants().len() == grants_after_v1` — substantive behavioral pins through install_plugin).",
    "§4.33 legacy install-path deletion VERIFIED structurally: 3-arm absence-pin at tf_g_core_0_legacy_install_path_deletion_4_33.rs; module_ecosystem.rs has zero `pub fn install_plugin*` symbols (verified by `grep -n`); cargo-public-api baseline reflects deletion. R6 R1 verdict CARRIES.",
    "Caller-mint-first plugin-DID contract VERIFIED: install_plugin Step 8 (lines 1097-1102) enforces `install_record.plugin_did == *params.expected_plugin_did` (typed PluginInstallRecordPluginDidMismatch) + `plugin_did_store.get(expected_plugin_did).is_some()` (typed PluginDidHandleNotPreInserted). Substantive e2e pins at r6fp_a_plugin_trust_blocker_closures.rs (5 test pins on this surface).",
    "Cap-cascade atomicity VERIFIED: §4.35 rollback_step9 helper (lines 1171-1175) unwinds every mid-loop minted grant on Step-9 mid-loop fail, Step-10 fail, OR Step-11 fail. The install_plugin call leaves zero observable residue on failure.",
    "InstallRecordReplayStore primitive thread-safety VERIFIED: parallel-thread TOCTOU test at install_record_replay.rs:193-219 (16 parallel callers, exactly 1 admit + 15 reject) + the engine integration test at lines 184-213 (32 parallel callers). The PRIMITIVE is well-tested; only the wire-up through install_plugin is the gap (F-2).",
    "Engine::install_record_replay_store() always-initialized VERIFIED at engine.rs:1955 (Arc::new construction; no Option/no lazy init); accessor at engine.rs:2085 returns `&Arc<InstallRecordReplayStore>`.",
    "apply_atrium_merge fail-CLOSED on unresolvable peer-DID VERIFIED at engine.rs:1448-1491 (literal-empty-set short-circuit returns ManifestEnvelopeRecheckUnresolvedDeny structurally BEFORE rechecker dispatch). Synthesized-fallback hardening Row D-18 closed at R6 R1 FP-F4 §S4.",
    "Manifest-envelope chain validation (§6.4) install-time non-invocation VERIFIED: install MINTS the root chain at Step 9 (CLAUDE.md #18 Layer-1); chain validator is downstream at sync-merge boundary. Per-design; R6 R1 verdict CARRIES.",
    "InstallParams shape VERIFIED: user_did + expected_plugin_did + version_chain + prior_installed_cid + user_trust_list + installer_shape + now_secs all distinct fields; no field-conflation hazard. R6-FP did not mutate this.",
    "Symmetric InstallPorts/UninstallPorts split (Surf-1 #888) VERIFIED + EXTENDED with policy field: both *Ports bundles share the symmetric pure-port shape; install legitimately omits SubscriptionRegistry (lazy-attach at first walk; documented at lines 689-697 as a scoped DISAGREE-WITH-EXPLANATION). InstallPorts now carries policy (F4 S3a addition) preserving the symmetry framework — the new field is itself a pure port.",
    "Engine::install_module sibling pipeline parity audit: ARCHITECTURALLY DISTINCT-BY-DESIGN. Module install (engine_modules.rs:168) is engine-internal privileged write (`WriteAdmissionFrame::engine_internal()`) for ModuleManifest (Phase-2b SANDBOX wasm bundle; Compromise #21); plugin install is user-DID-routed for PluginManifest (CLAUDE.md #18 three-layer-consent). Module install correctly does NOT consult InstallRecordReplayStore (no install-record artifact), check_install_consent (engine-trust model), §4.41 caps-grew (modules don't have manifest-envelope upgrade-widening at v1-beta), or T10-(a) (no peer-DID author-chain at module-author level). Module signature verification is via verify_manifest_with_mode (lines 217-225, Compromise #21 closure). Audit-trail observation (non-finding): a 1-line cross-doc link between docs/MODULE-MANIFEST.md and docs/PLUGIN-MANIFEST.md naming 'these are distinct primitives with distinct trust models' would help future auditors avoid mistakenly cross-applying F4 wiring expectations.",
    "admin_ui_v0 install path: DELEGATES to plugin_lifecycle::install_plugin via g24d_substantive_pipeline::full_install_pipeline_real_signatures_succeeds (real-signature InstallPorts at lines 132-148 with noop_replay_check + AdmitAllInstallConsent + cap-minter + ns); inherits full F4 wiring. No sibling-parity gap.",
    "tools/benten-admin-shell/src/main.rs:62 `active_session_count_for_test` production call is the R4b fix-up #6 EXEMPT case (legacy naming carry; outside L7 scope; tracked separately at R4b)."
  ],
  "spec_to_code_cross_check": {
    "plan_§1.A.FROZEN_item_7": "DELETED — verified at HEAD; INTERNALS.md retains forensic note; V1-FROZEN-INTERFACE.md item 7 matches.",
    "V1-FROZEN-INTERFACE_item_7": "Frozen — DELETION (not deprecation); cargo-public-api baseline absence-pinned; consistent.",
    "V1-FROZEN-INTERFACE_item_12": "G-CORE-8 security-surface ManifestEnvelopeRecheckOutcome four-variant freeze + #[non_exhaustive] LANDED; outcome_to_row_reject non-collapse invariant present; NoopManifestEnvelopeRechecker default + apply_atrium_merge structural fail-CLOSED on empty peer-DID resolution present; consistent.",
    "Compromise_#26": "Substantive substrate WIRED at R6 R1 FP-F4 (Rows D-1/D-2/D-3/D-4/D-6/D-18 close); substantive PluginLibrary-driven chain-walk still G-COMP-1 deferred; v1-beta posture documented end-to-end; consistent. NOTE F-5: Row D-2's closure-block wording on production wiring is slightly forward-looking; FORENSIC narrative is accurate.",
    "CLAUDE.md_#18_Layer_1": "User-DID anchors root cap-mint at Step 9 (user_did → plugin_did → req.scope grants); InstallRecord's user-sig at Step 3 is the consent anchor; consistent.",
    "CLAUDE.md_#18_Layer_2": "Manifest envelope = `requires` + `shares` is `validate_with_clock` + `verify_peer_signature` (Step 4); structural binding at Step 3 (manifest_cid == record.manifest_cid); consistent.",
    "CLAUDE.md_#18_Layer_3": "Runtime delegation-within-manifest is the SharesPolicyResolver + manifest_envelope_chain_validation path — install does not consult (install is the ROOT; downstream sync-merge / runtime delegation does); consistent.",
    "CLAUDE.md_baked_in_#7_sealed_CapabilityPolicy_refinement": "Preserved — `InstallConsentPolicy` is engine-internal (cycle-safe per install_consent.rs module-doc); Class B β sealed boundary intact.",
    "MODULE_MANIFEST_vs_PLUGIN_MANIFEST_primitive_distinction": "VERIFIED-DISTINCT at the design + implementation levels (engine_modules.rs::install_module vs plugin_lifecycle::install_plugin); non-finding observation: a 1-line cross-doc link helps future auditors."
  },
  "pim_n_pattern_signals": [
    "pim-1 §3.5b post-fix doc-coupling pre-flight: L7-R2-F-1 carryover + widening (3 origin-instances: stale-step-3-from-R6-R1 + new-step-3c-§S3a + new-step-7a-T10) + L7-R2-F-4 phantom helper symbol + L7-R2-F-6 testing.rs stale doc-block = **5-instance recurrence within this lens alone**. The §3.5b rule is CORRECT; the failure-mode is in the discipline's execution during R6-R1-FP waves themselves (FP-A reorder + FP-B L2-MAJOR-1 wiring + FP-F4 §S2 + §S3a all mutated install_plugin's adjacent doc-surfaces; none of them swept the docs). ≥3-recurrence threshold for pattern-induction-meta-sweep is met EASILY. Recommendation: NOT a new pim-N (the rule exists); add to the R6 R2 fp brief a pre-flight clause: 'when mutating install_plugin's step ordering, public surface, OR adjacent helper symbols — sweep BOTH the pub-fn `Order:` docstring AND the testing.rs/install_consent.rs doc-blocks AND any cite of helper symbols (e.g. `make_engine_replay_check_closure`) for accuracy before push.'",
    "pim-18 §3.6f SHAPE-not-SUBSTANCE pre-flight: L7-R2-F-2 (CARRYOVER for §4.37 replay) + L7-R2-F-3 (NEW for §S3a check_install_consent) = 2 active instances within this lens; coupled to the broader R6-R1-FP-F4 wave's pattern of structural-wire-up-without-behavioral-e2e (S2 + S3a both show the same shape). The pim-18 rule is CORRECT and load-bearing. Recommendation: NOT a new pim-N; add to next R6 fp wave's completion-checklist a clause: 'every new policy/port hook landed in this wave MUST have a {AdmitArm + DenyArm} substantive e2e pin through the production caller asserting the typed-code mapping; bare-primitive tests + compilation-coercion verification do NOT suffice for this discipline.'",
    "§3.6i Review/lens/mini-review JSON schema discipline applied to deferred-row narrative: L7-R2-F-5 surfaces a CLOSED-but-actually-STRUCTURALLY-CLOSED row inconsistent with Row D-3-c's PARTIAL-CLOSED wording. Single-instance at this lens (not yet ≥3-recurrence); recommendation: NOT a new pim-N at this round; flag for L18 cross-confirmation (L18 docs/ledger lens may have surfaced parallel instances in other deferred rows)."
  ],
  "convergence_outlook": {
    "expected_r3_findings": "All 6 R6-R2 findings (F-1 carryover + F-2 carryover + F-3 new pim-18 instance + F-4 new pim-1 instance + F-5 new §3.6i instance + F-6 new pim-1 5th-instance) close cleanly with surgical fix-pass: 1 docstring rewrite (F-1, ~50 LOC) + 1 substantive test addition (F-2, ~120 LOC) + 1 substantive test addition (F-3, ~100 LOC) + 1 helper-add (F-4 option-a, ~10 LOC) + 1 doc-narrative tighten (F-5, ~5 LOC) + 1 doc-block edit (F-6, ~5 LOC). Total ~290 LOC across 6 surgical fixes. R3 close expects L7 findings to drop to 0 if F-1..F-6 all land. Downstream-lens L13 test-coverage-discipline may surface adjacent F-2/F-3-flavored gaps in other §4.x install-lifecycle waves (the same SHAPE-not-SUBSTANCE pattern likely recurs at every R6-R1-FP hook-wire-up — L13's lens, not L7's).",
    "no_blockers_at_r2": true,
    "phase_close_readiness_signal": "MODERATE-for-this-lens; downgraded from R6 R1 'STRONG-contingent-on-F-1+F-2-closure' because (a) BOTH R6-R1 must-close findings carried forward unchanged (FP did not land their fixes), (b) 4 additional findings now surface (1 carryover-widening + 1 new pim-18 recurrence + 1 new pim-1 phantom-helper + 1 §3.6i discipline + 1 new pim-1 testing.rs doc-block). The install-lifecycle is structurally complete + sound at v1-beta; the gap is observable-behavioral test pinning at the install_plugin boundary + 4 doc-coupling drifts, NOT a security gap (the §4.37 + §S3a defenses ARE structurally wired in the production code path; only their tests are SHAPE-not-SUBSTANCE). R3 close after F-1..F-6 land is achievable in one orchestrator-direct fix-pass."
  }
}
