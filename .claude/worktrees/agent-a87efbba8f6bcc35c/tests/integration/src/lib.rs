//! Phase-3 workspace-level integration test package — empty lib stub.
//!
//! See `Cargo.toml` for the directory's purpose. Test files live at
//! the package root (e.g., `atrium_browser_thin_client.rs`) and run
//! as named integration tests via `[[test]]` entries in `Cargo.toml`.

#![cfg_attr(docsrs, feature(doc_cfg))]
#![deny(missing_docs)]

// Empty stub. Integration tests are hosted as separate test files
// (named in Cargo.toml) and do not import from this lib.
