{
  "lens": "L6 capability-system + plugin-trust, phase-wide",
  "round": "R6 R2",
  "phase": "Phase-4-Meta-Core",
  "reviewer_agent": "general-purpose subagent (orchestrator-direct lens-as-prompt; this file)",
  "reviewed_at_sha": "3c80546f",
  "main_at_review_time": "5ca364e7",
  "augmentation_pass": "1 (per orchestrator follow-up depth-disciplines 2026-05-24 — same-attack-class siblings + sweep-completeness + ground-truth-verify §3.5n + L6 trait-wide audit; folds sibling-lens L3 audience_pubkey same-attack-class + L11 7 phantom cite-strings findings into L6 phase-wide scope)",
  "scope": {
    "files_in_scope": [
      "crates/benten-caps/src/policy.rs",
      "crates/benten-engine/src/engine_caps.rs (delegate_capability + admit_write_chain)",
      "crates/benten-platform-foundation/src/plugin_lifecycle.rs (install_plugin + InstallPorts)",
      "crates/benten-platform-foundation/src/install_consent.rs",
      "crates/benten-engine/src/engine.rs (apply_atrium_merge per-row check_write_with_audience)",
      "crates/benten-engine/src/engine_diagnostics.rs (transaction commit per-write hook)",
      "crates/benten-engine/src/engine_wait.rs (WAIT-resume put_node)",
      "crates/benten-engine/src/primitive_host.rs (evaluator per-write cap-recheck)",
      "crates/benten-engine/src/write_boundary_chain_validator.rs",
      "docs/V1-FROZEN-INTERFACE.md item 8 / V1-FROZEN-INTERFACE-DEFERRED.md Row D-3 / ERROR-CATALOG.md / V1-BETA-BREAKING-CHANGES.md / SECURITY-POSTURE.md"
    ]
  },
  "summary": {
    "disposition": "APPROVE-WITH-FINDINGS — 2 MAJOR + 3 MINOR + 3 OBS (augmented from 1 MAJOR + 3 MINOR + 2 OBS)",
    "headline": "All 3 §8-E hook trait surfaces present + sealed-discipline preserved + Class B β intact + InstallPorts.policy port shape (CRITIC-2 F-1.2) honored. AUGMENTED FINDINGS: (a) S3a InstallConsentPolicy threading is wired AT THE PORT LEVEL but no engine-side adapter exists to project CapabilityPolicy::check_install_consent into install_plugin (sec-r6r2-l6-1 MAJOR, original); (b) NEW per same-attack-class-siblings audit: `wallclock_refresh_ceiling` — the §9.13 dual-source sibling to `iterate_batch_boundary` (which IS consumer-wired at primitive_host.rs:682-684) — has ZERO production consumer-wire site post-F4, leaving Compromise #1 wall-clock TOCTOU bound enforceable only via the iteration-count half (sec-r6r2-l6-7 NEW MAJOR). Test-shape concerns at sec-r6r2-l6-2/3/4 MINOR (now meet 3-instance recurrence threshold for pim-N codification per `feedback_3_plus_recurrence_deep_sweep`). New sec-r6r2-l6-8 OBS: InstallPorts cascade audit confirms 35/35 callers migrated to S2-Option-drop + S3a-policy-add shapes (no orphans).",
    "convergence_signal_vs_r1": "Substantial R6 R1 cluster closure (4 of 5 §S surfaces fully consumer-wired); S3a residual + L6-wide wallclock_refresh_ceiling gap now surfaced. The wallclock_refresh_ceiling sibling was NOT in F4's named substrate but IS in the L6 phase-wide lens scope — exactly the kind of finding the L6 lens is positioned to catch. Recommend FP-2 land both adapter (sec-r6r2-l6-1) AND wallclock_refresh_ceiling evaluator wire (sec-r6r2-l6-7) OR explicit doc-state update naming destinations."
  },
  "ground_truth_verify_attestation_per_3_5n": {
    "method": "Orchestrator independently grep-verified every cited file:line against `git show 3c80546f:<path>` to confirm reachability and exact line content; reviewer findings cross-checked rather than trusted. ZERO phantom cites in this artifact (cross-confirm pass with L11's 7-phantom-cite finding).",
    "verified_sites": {
      "engine_caps_rs_572_587_check_per_delegation_consultation": "VERIFIED — `git show 3c80546f:crates/benten-engine/src/engine_caps.rs | sed -n '562,587p'` shows the `if let Some(policy) = self.engine.policy.as_ref() && policy.check_per_delegation(source_principal_did.as_str(), plugin_did, resolved_scope.as_str()).is_err()` block with typed `PluginPerDelegationDenied` reject.",
      "plugin_lifecycle_rs_959_965_check_install_consent_consultation": "VERIFIED — `git show 3c80546f:crates/benten-platform-foundation/src/plugin_lifecycle.rs | sed -n '950,970p'` shows the step-3c block `if ports.policy.check_install_consent(&payload_hash, plugin_did_str).is_err()` returning typed `PluginInstallConsentDenied`.",
      "engine_rs_1413_check_write_with_audience_at_apply_atrium_merge": "VERIFIED — sed-n 1400,1430 shows the apply_atrium_merge per-row recheck calling `policy.check_write_with_audience(&ctx)` with `audience_did: None` per Δv3-2.",
      "engine_wait_rs_907_check_write_with_audience_at_wait_resume": "VERIFIED — sed-n 895,915 shows wait-resume put_node site calling `policy.check_write_with_audience(&ctx)` with `audience_did: None`.",
      "engine_diagnostics_rs_85_check_write_with_audience_at_commit": "VERIFIED — sed-n 78,90 shows the transaction-commit per-write hook calling `p.check_write_with_audience(&ctx)`.",
      "primitive_host_rs_619_check_write_with_audience_at_evaluator": "VERIFIED — sed-n 605,625 shows evaluator per-write cap-recheck calling `policy.check_write_with_audience(&ctx)`.",
      "install_consent_rs_25_adapter_reference": "VERIFIED MISSING — `git show 3c80546f:crates/benten-platform-foundation/src/install_consent.rs | head -30` shows the module-doc claim 'the engine-side adapter `CapabilityPolicyInstallConsent` ... wraps the policy'. `git grep -ln CapabilityPolicyInstallConsent 3c80546f` returns ZERO matches outside that doc-line reference itself.",
      "policy_rs_443_iterate_batch_boundary_consumer_at_primitive_host_682": "VERIFIED — `git show 3c80546f:crates/benten-engine/src/primitive_host.rs | sed -n '680,690p'` shows `fn iterate_batch_boundary(&self) -> usize { match self.policy() { Some(p) => benten_caps::evaluator_delegation::iterate_batch_boundary_for(p), None => benten_eval::DEFAULT_BATCH_BOUNDARY } }` consumer; benten-eval/src/primitives/iterate.rs:98 consumes via `host.iterate_batch_boundary().max(1)`. Wire-up complete end-to-end.",
      "policy_rs_464_wallclock_refresh_ceiling_zero_consumer": "VERIFIED MISSING — `git grep -n wallclock_refresh_ceiling 3c80546f -- 'crates/'` returns ONLY: (a) policy.rs:438+460+464 (trait method + docs); (b) caps/INTERNALS.md (doc references); (c) caps/src/lib.rs:220 (retraction-comment naming the wrapper as DELETED at Qual-1 #674 / umbrella #1154 — 'WAS a true zero-consumer wrapper'); (d) crates/benten-caps/tests/wallclock_delegation.rs + wallclock_refresh_typed_error_fires.rs (TDD red-phase pins of trait method default + override-shape). ZERO production source consumer at HEAD; method TODO at policy.rs:454 explicitly names Phase-3-backlog §2.3 (ii) as named destination.",
      "ucan_grounded_check_write_sites_565_836_excluded_per_delta_v3_7": "VERIFIED — `git show 3c80546f:crates/benten-caps/src/ucan_grounded.rs | grep -n '^#\\[cfg(test)\\]'` shows `501:#[cfg(test)]\\n502:mod tests {`. All 7 `policy.check_write(&ctx)` sites at lines 565/593/620/642/729/750/836 are inside the test module; valid Δv3-7 exclusion from the §S3c sweep."
    },
    "sweep_completeness_self_verify_per_3_6j": {
      "method": "Orchestrator ran the §3.6j-style scan that sec-r6r2-l6-3 said the test arm omits.",
      "command": "git grep -nE 'policy\\.check_write\\(' 3c80546f -- 'crates/' 2>&1 | grep -v '_with_audience\\|tests/\\|//\\|/// '",
      "result": "7 hits, ALL inside crates/benten-caps/src/ucan_grounded.rs lines 565-836 inside `#[cfg(test)] mod tests` block (verified at line 501). ZERO production direct policy.check_write( sites at HEAD outside Δv3-7's documented exclusion. The §S3c sweep IS complete in substance (the test arm just doesn't prove it).",
      "categorical_audience_did_check_per_delta_v3_2": "Confirmed: production `crates/benten-engine/src/` has ZERO `audience_did: Some(...)` populations outside (a) manifest_signing.rs's distinct `engine_audience_did` UCAN-chain audience domain and (b) compile-time tests in crates/benten-engine/tests/. The Δv3-2 design contract (audience_did=None at all sweep sites; populate-side at delegate_capability defers to G-COMP-1; runtime audience-discrimination delivered by check_per_delegation Layer-3 enforcement) is structurally honored. The brief's wording 'audience_did populated at delegate_capability cap-target site' is a slight overstatement of intent — populate-side is NOT in place at v1-beta and that is by-design per the F4 ratification."
    }
  },
  "trait_wide_method_audit_per_l6_extended_scope": {
    "method": "Enumerate every method on the `CapabilityPolicy` sealed trait (policy.rs:362-579) + verify each has a production consumer-wire site reachable from a real engine WRITE/READ/INSTALL/DELEGATE entry point.",
    "trait_methods_seven_total": [
      {
        "method": "check_write (mandatory, no default)",
        "consumer_status": "WIRED via default-delegate chain — all 4 production engine WRITE entry points route through `check_write_with_audience` (S3c) whose trait default delegates to `check_write` — so check_write is reachable end-to-end via the default impl chain.",
        "reachability": "End-to-end reachable on default policies (NoAuthBackend / GrantBackedPolicy / UcanGroundedPolicy) which do NOT override check_write_with_audience — every WRITE round-trips through check_write."
      },
      {
        "method": "check_read (default Ok(()))",
        "consumer_status": "WIRED at 9 production sites verified by ground-truth grep: engine_crud.rs:325 (check_read_gate) + engine_diagnostics.rs:402+440 + engine_subscribe.rs:366 + engine_views.rs:342 (via check_read_gate) + primitive_host.rs:196 (check_read_ctx) + 4 callers of check_read_ctx at primitive_host.rs:273/296/321/539/648.",
        "reachability": "End-to-end reachable. NOTE: no `check_read_with_audience` audience-aware sibling minted at §8-E — asymmetric with check_write_with_audience by-design (the §8-E surface deliberately adds audience only to the write side); category-correctness intentional (private-namespace caps already gate plugin-DID reads via existing check_read on `private:<plugin_did>:*` scope)."
      },
      {
        "method": "iterate_batch_boundary (default DEFAULT_BATCH_BOUNDARY=100)",
        "consumer_status": "WIRED at primitive_host.rs:682-684 (`fn iterate_batch_boundary(&self) -> usize { match self.policy() { Some(p) => benten_caps::evaluator_delegation::iterate_batch_boundary_for(p), None => benten_eval::DEFAULT_BATCH_BOUNDARY } }`) consumed by benten-eval/src/primitives/iterate.rs:98 (`let boundary = host.iterate_batch_boundary().max(1);`).",
        "reachability": "End-to-end reachable; ITERATE primitive's cap-refresh cadence IS the configured policy's value."
      },
      {
        "method": "wallclock_refresh_ceiling (default Duration::from_mins(5))",
        "consumer_status": "ZERO PRODUCTION CONSUMER (sec-r6r2-l6-7 NEW MAJOR). The previously-fronting `evaluator_delegation::wallclock_refresh_ceiling_for` zero-consumer wrapper was DELETED at Qual-1 #674 / umbrella #1154; the trait method has a TODO at policy.rs:454 naming Phase-3-backlog §2.3 (ii) as the named destination ('v1-assessment-window co-routed with §10.1 Compromise #1 TOCTOU window bound; shared iterate-batch-boundary cap-recheck cadence mechanism'). Tests at crates/benten-caps/tests/wallclock_*.rs exercise the trait method default + the override-shape but NO production code calls it.",
        "reachability": "NOT reachable end-to-end. A policy override returning Duration::from_secs(30) has zero observable effect at v1-beta. The R4b compromise #1 tightening (g4-p2-uc-2) named this as the BLOCKER for closing the wall-clock TOCTOU half of the named-compromise — that BLOCKER REMAINS OPEN."
      },
      {
        "method": "check_install_consent (default Ok(()) — §8-E NEW)",
        "consumer_status": "PARTIALLY wired: `plugin_lifecycle.rs:959-965` consults `ports.policy.check_install_consent(...)` via the InstallConsentPolicy port. BUT the engine-side adapter projecting CapabilityPolicy → InstallConsentPolicy does NOT exist (sec-r6r2-l6-1 MAJOR), so a production CapabilityPolicy override is unreachable.",
        "reachability": "PARTIAL — port reachable via direct caller-supplied InstallConsentPolicy; CapabilityPolicy-routed reachability requires the missing adapter."
      },
      {
        "method": "check_per_delegation (default Ok(()) — §8-E NEW)",
        "consumer_status": "WIRED at engine_caps.rs:572-587 directly against `self.engine.policy.as_ref().check_per_delegation(source_principal_did.as_str(), plugin_did, resolved_scope.as_str())` — no adapter needed because engine_caps lives in benten-engine where Engine::policy is in scope.",
        "reachability": "End-to-end reachable on every delegate_capability call."
      },
      {
        "method": "check_write_with_audience (default delegates to check_write — §8-E NEW)",
        "consumer_status": "WIRED at all 4 production WRITE entry points (engine.rs:1421 + engine_wait.rs:907 + engine_diagnostics.rs:85 + primitive_host.rs:619). Δv3-3 invert-the-panic verifies the wire (would-FAIL on revert).",
        "reachability": "End-to-end reachable; audience_did stays None at sweep sites per Δv3-2 (peer_did at apply_atrium_merge is transport-principal NOT cap-target; populate-side at delegate_capability defers to G-COMP-1)."
      }
    ],
    "summary": "7 trait methods total. 5 fully end-to-end reachable. 1 PARTIAL (check_install_consent — adapter missing, sec-r6r2-l6-1). 1 UNREACHABLE (wallclock_refresh_ceiling — sec-r6r2-l6-7 NEW MAJOR; preceded F4 but lands in L6 trait-wide audit scope per orchestrator depth-discipline #1 same-attack-class siblings)."
  },
  "install_ports_shape_cascade_verification_per_3_5n": {
    "method": "Workspace-walker grep for every InstallPorts struct-literal construction site post-S2 (Option drop) + S3a (policy field add).",
    "command": "git grep -A20 'InstallPorts\\s*{' 3c80546f -- 'crates/benten-platform-foundation/' | grep -c 'policy:'",
    "construction_sites_count": "35 (30 in tests/ + 1 in src/testing.rs doc-comment + 1 in src/plugin_lifecycle.rs body + 3 in src/plugin_lifecycle.rs body docs)",
    "callers_supplying_policy_field": "35 of 35 (100% — verified by grep counting `policy:` lines inside InstallPorts struct literals across all 14 caller files at HEAD)",
    "callers_supplying_substantive_replay_check": "35 of 35 (Option drop verified; every site supplies either `&mut noop_replay_check()` or a substantive closure)",
    "policy_field_value_distribution": "35 of 35 callers wire `policy: &AdmitAllInstallConsent` or `policy: &noauth_policy_*`. ZERO callers wire `&DenyAllInstallConsent` or a custom denying impl through install_plugin end-to-end — this is the sec-r6r2-l6-4 dead-uncovered deny-arm.",
    "no_orphan_caller_landed_pre_s2_s3a_without_being_migrated": "VERIFIED — every InstallPorts construction site in the workspace at HEAD contains both `install_record_replay_check:` and `policy:` fields. The cascade is complete in struct-shape; the substantive gap is the value of the policy field (always AdmitAll) + the dead deny-arm tested only at the trait level."
  },
  "cross_lens_cross_confirmation": {
    "l2_invariant_enforcement_overlap": "L2 covers invariant enforcement; the wallclock_refresh_ceiling gap (sec-r6r2-l6-7) intersects: Compromise #1 (the TOCTOU cap-refresh-window bound) has a wall-clock half + iteration-count half. The iteration-count half is enforced via iterate_batch_boundary's consumer-wire at primitive_host.rs:682; the wall-clock half is UNenforced. A TRANSFORM-heavy or CALL-heavy handler at 1 iter/10sec can blow past 10 minutes between refreshes (per R4b compromise #1 tightening, auditor finding g4-p2-uc-2). L2 + L6 jointly should flag this as a Compromise #1 partial-closure regression.",
    "l5_test_coverage_overlap": "L5 covers test-coverage gaps; sec-r6r2-l6-2/3/4 all are L5-shaped test-coverage findings on the F4 wave — the three §S production-arm tests in r6_r1_fp_f4_*.rs do NOT exercise end-to-end production entry points (delegate_capability / install_plugin / workspace-walker scan). Recommend L5 cross-confirm: pim-18 §3.6f SHAPE-not-SUBSTANCE recurrence is at 3-instance threshold across F4's three §S surfaces — pim-N codification candidate per the brief's depth-discipline #1.",
    "l7_security_posture_overlap": "L7 covers SECURITY-POSTURE.md compromise dispositions. The wallclock_refresh_ceiling MAJOR (sec-r6r2-l6-7) is a Compromise #1 disposition correctness check — SECURITY-POSTURE.md §Compromise #1 should be cross-checked against L6's finding that the wall-clock half is unenforced at v1-beta. If SECURITY-POSTURE.md claims Compromise #1 is FULLY closed at v1-beta, the doc is incorrect; PARTIAL closure is the truth.",
    "l3_audience_pubkey_same_attack_class_cross_confirm": "Sibling L3 found audience_pubkey same-attack-class as scope (both unsigned, post-mutable, NOT in binding_message). L6 same-attack-class-siblings audit (orchestrator depth-discipline #1) applied to CapabilityPolicy trait surfaces in this lens finds sec-r6r2-l6-7 wallclock_refresh_ceiling. Both are 'wave touched surface S, sibling-on-same-surface remains unaddressed' — the same class-of-failure-mode. Recommend pim-N-same-attack-class-siblings-audit-on-trait-edits codification candidate (2 instances now if L3+L6 count as one pattern; cross-confirm via R6 R3).",
    "l11_phantom_cite_cross_confirm": "Sibling L11 found 7 phantom cite-strings in wire-format inventory expansion. The L6 ground-truth-verify pass above re-checked every cited file:line + confirmed each was a real reachable site; ZERO phantom cites in this L6 artifact. The L11 phantom-cite pattern is a R6 R3 codification candidate (recommend `pim-N-reviewer-must-grep-verify-cite-before-claim` — severity-escalated to OBSERVE+ given the high severity of L11's 7-cite failure mode)."
  },
  "findings": [
    {
      "id": "sec-r6r2-l6-1",
      "severity": "MAJOR",
      "title": "§S3a install-consent: engine-side adapter `CapabilityPolicyInstallConsent` is referenced in install_consent.rs:25 module doc but does not exist; production CapabilityPolicy.check_install_consent override is dead surface at v1-beta",
      "evidence": {
        "doc_claim": "crates/benten-platform-foundation/src/install_consent.rs:25 — '...the engine-side adapter `CapabilityPolicyInstallConsent` (NOT in this crate because of the dep-cycle constraint) wraps the policy.'",
        "code_search_reproducer": "`git grep -ln CapabilityPolicyInstallConsent 3c80546f` returns ONLY the install_consent.rs reference (the doc-claim line itself). `git grep -ln 'impl.*InstallConsentPolicy for' 3c80546f` shows only the 2 helper impls in install_consent.rs + 2 test-fixture impls. No engine-side blanket impl exists in benten-engine, benten-caps, benten-platform-foundation, bindings/napi, or packages/engine.",
        "v1_frozen_interface_md_line_735_contradiction": "docs/V1-FROZEN-INTERFACE.md:735 row claim 'Net: external policy authors overriding any of the three hooks now have runtime effect at v1-beta' is FALSE for check_install_consent — without the adapter, a custom CapabilityPolicy.check_install_consent is unreachable from `plugin_lifecycle::install_plugin`'s `policy: &dyn InstallConsentPolicy` port.",
        "deferral_already_documented_inconsistently": "V1-FROZEN-INTERFACE-DEFERRED.md Row D-3 marks D-3-a 'CLOSED' at §S3a (line 154), then forensic body at line 200+ says 'A future custom CapabilityPolicy author overriding any of the three will be silently ignored' (which is now FALSE for §S3b/§S3c but REMAINS TRUE for §S3a). The doc-state is internally inconsistent.",
        "delegate_capability_contrast": "`delegate_capability` in engine_caps.rs:572-587 consults `self.engine.policy.as_ref().check_per_delegation(...)` DIRECTLY against the trait method — no adapter required because engine_caps lives in benten-engine and `&self.engine.policy` IS the configured CapabilityPolicy. The same direct-consultation pattern is NOT available at the install path because `plugin_lifecycle::install_plugin` lives in benten-platform-foundation which cannot dep-cycle into benten-caps via benten-engine."
      },
      "impact": {
        "security": "CLAUDE.md baked-in #18 Layer-2 install-time consent admission policy (e.g. a curated-DID trust-list, an institutional ratification gate) cannot be wired through the engine's CapabilityPolicy at v1-beta. The pipeline structurally consults a default-`Ok(())`-returning AdmitAllInstallConsent helper instead. A production deployment wanting install-time policy enforcement must EITHER fork the install entry point OR construct its own InstallConsentPolicy impl + pass it directly to install_plugin (which is not the documented Phase-4-Meta-Core seam shape — the documented surface is CapabilityPolicy.check_install_consent override).",
        "doc_state_consistency": "V1-FROZEN-INTERFACE.md item 8 row + V1-FROZEN-INTERFACE-DEFERRED.md Row D-3-a + install_consent.rs module-doc + the SECURITY-POSTURE.md line all read like the wiring is complete; the missing adapter contradicts the surface-as-documented.",
        "audit_trail": "When a real CapabilityPolicy author lands at G-COMP-1 / Phase-4-Meta-Composing and discovers their check_install_consent override has been silently ignored since v1-beta, the regression-window is the entire v1-beta release."
      },
      "disposition": "FIX-NOW (per HARD RULE 12) — three valid options:\n\n(a) [PREFERRED] Land the engine-side adapter at FP-2 (≤50 LOC). The adapter shape: `pub struct CapabilityPolicyInstallConsent<'a> { policy: &'a Arc<dyn CapabilityPolicy> }` + `impl<'a> InstallConsentPolicy for CapabilityPolicyInstallConsent<'a> { fn check_install_consent(&self, hash, did) -> Result<(), ErrorCode> { self.policy.check_install_consent(hash, did).map_err(|_| ErrorCode::PluginInstallConsentDenied) } }` lives in benten-engine/src/install_consent_adapter.rs (new file ~30 LOC); engine glue at the install boundary constructs `InstallPorts.policy = &CapabilityPolicyInstallConsent::new(&engine.policy)`. Plus 1 substantive end-to-end test (~50 LOC) wiring a denying CapabilityPolicy through install_plugin via the adapter, asserting the typed `PluginInstallConsentDenied` reaches the install_plugin return.\n\n(b) DEFER-NAMED-NOW per HARD RULE 12 clause (b): edit docs to acknowledge the gap. V1-FROZEN-INTERFACE-DEFERRED.md Row D-3-a status flips 'CLOSED' → 'PARTIAL CLOSED — adapter to G-COMP-1'. V1-FROZEN-INTERFACE.md:735 row text edits accordingly. install_consent.rs:25 module doc edits to acknowledge the adapter is a G-COMP-1 deliverable. Add a v1-beta breaking-changes warning so policy authors know.\n\n(c) DISAGREE: cite a Ben ratification or design constraint that makes the adapter intentionally absent — none found in the F4 design pipeline pre-work.\n\nRecommend (a) over (b) — the adapter is small + closes the gap fully + aligns the doc-state with the SHAPE-OF-PHASE-4-META-CORE v1-beta contract."
    },
    {
      "id": "sec-r6r2-l6-7",
      "severity": "MAJOR",
      "title": "[NEW per augmentation depth-discipline #1 same-attack-class siblings] `wallclock_refresh_ceiling` has ZERO production consumer wire site; the §9.13 dual-source sibling to `iterate_batch_boundary` is reachable in the trait but unenforced end-to-end; Compromise #1 wall-clock TOCTOU half UNCLOSED at v1-beta",
      "evidence": {
        "trait_method_definition": "crates/benten-caps/src/policy.rs:443 hosts `iterate_batch_boundary() -> usize` (default 100); crates/benten-caps/src/policy.rs:464 hosts `wallclock_refresh_ceiling() -> core::time::Duration` (default Duration::from_mins(5)). The dual-source §9.13 / P1 / Phase-2a G9-A design names BOTH as evaluator-consumer hooks — 'min(iteration_count, wall_clock_seconds)' enforces Compromise #1 BLOCKER ceiling.",
        "iterate_batch_boundary_consumer_wired": "VERIFIED at crates/benten-engine/src/primitive_host.rs:682-684 (`fn iterate_batch_boundary(&self) -> usize { match self.policy() { Some(p) => benten_caps::evaluator_delegation::iterate_batch_boundary_for(p), None => benten_eval::DEFAULT_BATCH_BOUNDARY } }`) + crates/benten-eval/src/primitives/iterate.rs:98 (`let boundary = host.iterate_batch_boundary().max(1);`). End-to-end reachable.",
        "wallclock_refresh_ceiling_zero_consumer": "VERIFIED MISSING via `git grep -n wallclock_refresh_ceiling 3c80546f -- 'crates/'`. Results: (a) policy.rs:438+460+464 (trait method + docs naming Phase-3-backlog §2.3 (ii) as named destination); (b) caps/INTERNALS.md:55+98 (doc references); (c) caps/src/lib.rs:220 (retraction-comment naming the wrapper as DELETED at Qual-1 #674 / umbrella #1154); (d) crates/benten-caps/tests/wallclock_delegation.rs + wallclock_refresh_typed_error_fires.rs (tests of trait method default + override-shape; pre-evaluator-consumer-wire-up RED PHASE pins). ZERO production source consumer at HEAD. The previously-fronting evaluator_delegation::wallclock_refresh_ceiling_for wrapper was DELETED as zero-consumer per the policy.rs:460 retraction-comment.",
        "trait_method_self_documentation": "policy.rs:454-462 carries a TODO with explicit named destination: 'TODO(phase-3 — wallclock-refresh-ceiling evaluator wire-up): wire into the evaluator's refresh path so the override becomes load-bearing end-to-end. Carried from Phase-2a G9-A (didn't land); registered at `phase-3-backlog §2.3 (ii)` (v1-assessment-window co-routed with §10.1 Compromise #1 TOCTOU window bound; shared iterate-batch-boundary cap-recheck cadence mechanism).' The defer IS named per HARD RULE 12 clause (b) — but the L6 phase-wide lens flags this because it sits IN the same trait as the F4 §S3 hook wiring AND is the documented same-attack-class sibling of a hook F4 chose NOT to fully address.",
        "r4b_compromise_1_tightening_evidence": "policy.rs:432-440 comment chain: 'The companion wall-clock TOCTOU ceiling (per R4b compromise #1 tightening, auditor finding g4-p2-uc-2 — a TRANSFORM-heavy or CALL-heavy handler at 1 iter/10sec pushes past 10 minutes between refreshes under iteration-count alone; the first real capability backend MUST additionally enforce a wall-clock ceiling `min(iteration_count, wall_clock_seconds)`, default ≤300s).' — the BLOCKER call-out is in-tree but the consumer is missing."
      },
      "impact": {
        "security": "Compromise #1 (the TOCTOU cap-refresh-window bound) is PARTIALLY closed at v1-beta: the iteration-count half via iterate_batch_boundary IS enforced; the wall-clock half via wallclock_refresh_ceiling is NOT. A long-running TRANSFORM/CALL/SANDBOX handler whose iteration cadence is slow (1 iter / 10 sec ≈ 17 minutes between refreshes under default boundary=100) can hold a revoked cap for the full wall-clock duration with no refresh — exactly the auditor finding g4-p2-uc-2 scenario.",
        "v1_beta_shippability": "Per CLAUDE.md baked-in #15 v1-milestone-gate, Compromise #1 disposition correctness is a v1-shippable concern. SECURITY-POSTURE.md must accurately reflect that Compromise #1 is PARTIAL-CLOSED at v1-beta — a Ben-decision point if the doc currently overclaims closure (recommend L7 cross-confirm verification).",
        "same_attack_class_siblings_audit_intent": "Per orchestrator depth-discipline #1: F4 wired three §8-E NEW hooks; L6 phase-wide lens audit reveals the F4 wave touched the trait without auditing its older sibling methods for matching consumer-reachability. This is a class-of-finding L6 is exactly positioned to surface."
      },
      "disposition": "FIX-NOW (per HARD RULE 12) — three valid options:\n\n(a) [PREFERRED for v1-beta-shippability] Land the evaluator-consumer wire-up at FP-2 alongside sec-r6r2-l6-1's adapter. Shape: extend benten-eval's `PrimitiveHost` trait with `fn wallclock_refresh_ceiling(&self) -> Duration { Duration::from_secs(300) }` defaulted method (mirrors iterate_batch_boundary); add `benten_caps::evaluator_delegation::wallclock_refresh_ceiling_for(p)` helper (revive the deleted wrapper); engine impl at primitive_host.rs delegates to the configured policy; ITERATE/CALL/SANDBOX executors enforce `min(iteration_count, wall_clock_seconds)` at refresh-decision boundary. Closes Compromise #1's wall-clock half end-to-end. Estimate ~80 LOC + 1 substantive integration test (~60 LOC) verifying a TRANSFORM-heavy handler whose policy override returns Duration::from_secs(30) refreshes within 30s of grant-revocation.\n\n(b) DEFER-NAMED-NOW per HARD RULE 12 clause (b): the named destination already exists (Phase-3-backlog §2.3 (ii)). Update SECURITY-POSTURE.md Compromise #1 disposition explicitly to 'PARTIAL-CLOSED at v1-beta — iteration-count half enforced; wall-clock half tracked at phase-3-backlog §2.3 (ii)' if the doc currently overclaims closure. Also recommend reviewing whether the §2.3 (ii) destination should be promoted from 'v1-assessment-window' (post-v1-beta) to 'v1-beta gate' given the auditor finding g4-p2-uc-2's threat-model concern.\n\n(c) DISAGREE: argue that the wall-clock half is not v1-beta-gating because (i) the default 5-minute cap is already-shipped behavior at the trait method level and (ii) no v1-beta CapabilityPolicy impl currently exercises a tighter override. This argument has merit but contradicts the policy.rs:432-440 'MUST additionally enforce' comment chain.\n\nRecommend (a) if FP-2 capacity allows; else (b) with explicit Ben-promotion question for the v1-beta gate vs v1-assessment-window placement."
    },
    {
      "id": "sec-r6r2-l6-2",
      "severity": "MINOR",
      "title": "§S3b delegate_capability test r6_r1_fp_f4_s3b_*.rs has 5 arms but NONE call `delegate_capability(...)` end-to-end — pure pim-18 / §3.6f SHAPE-not-SUBSTANCE shape",
      "evidence": {
        "test_file": "crates/benten-engine/tests/r6_r1_fp_f4_s3b_delegate_capability_consults_check_per_delegation.rs",
        "arm_audit": "Arm 1 `delegate_capability_surfaces_plugin_per_delegation_denied_code` asserts the new ErrorCode variant exists (no delegate_capability call). Arm 2 `check_per_delegation_observes_invocation` calls `policy.check_per_delegation(...)` directly on a CountingPolicy (no delegate_capability call). Arm 3 `check_per_delegation_returns_err_on_denial` calls policy directly. Arm 4 `default_check_per_delegation_admits_all` checks NoAuthBackend default. Arm 5 `check_per_delegation_is_callable_on_arc_dyn_capability_policy` checks Arc<dyn ...> dispatch. NONE construct an Engine + EngineCapsHandle + call delegate_capability(...) with a real grant + plugin DID store + denying policy + assert PluginPerDelegationDenied surfaces from the delegate_capability return.",
        "self_admission": "Arm 1's body: '// The end-to-end integration test (with a real grant + plugin DID store) is exercised by the existing G27-A/D/G24-D-FP suite; this arm pins the variant's existence + reachability.' — but the G27-A/D/G24-D-FP tests pre-date the §S3b wiring and do NOT exercise the new check_per_delegation hook path."
      },
      "impact": "Reverting the engine_caps.rs:572-587 wire-in (the `if let Some(policy) = self.engine.policy.as_ref() && policy.check_per_delegation(...).is_err()` block) would NOT be caught by this test file's assertions. The Δv3-3 invert-the-panic pattern that §S3c uses for the workspace-sweep verification is the right shape; §S3b should adopt it: a CapabilityPolicy whose check_per_delegation panics, install it on a real Engine, run delegate_capability, verify the panic fires (would-FAIL via panic IF wire-in present; would-NOT-FAIL if reverted because the policy is never called). pim-18 §3.6f exact failure mode.",
      "disposition": "FIX-NOW — add arm 6 to the existing test file: construct full Engine + grant + plugin DID handle + install an invert-the-panic CapabilityPolicy whose check_per_delegation panics; call delegate_capability; assert panic fires. ~50 LOC; mirrors §S3c arm-1 invert-the-panic discipline (per Δv3-3 ratification). Closes the §3.6f / pim-18 gap on the delegate_capability wire-in."
    },
    {
      "id": "sec-r6r2-l6-3",
      "severity": "MINOR",
      "title": "§S3c workspace-walker enumeration arm is `assert_eq!(EXPECTED_CHECK_WRITE_WITH_AUDIENCE_SITES, 4)` — does NOT scan code; brittle to silent additions",
      "evidence": {
        "test_file": "crates/benten-engine/tests/r6_r1_fp_f4_s3c_workspace_check_write_routes_through_with_audience.rs:106-110",
        "arm_3_body": "fn workspace_walker_audit_four_check_write_with_audience_sites() {\n    const EXPECTED_CHECK_WRITE_WITH_AUDIENCE_SITES: usize = 4;\n    assert_eq!(EXPECTED_CHECK_WRITE_WITH_AUDIENCE_SITES, 4);\n}",
        "intent_comment": "// future drift via a new WRITE entry point landing `policy.check_write` instead of `_with_audience` should be caught at the §3.6j sweep-completeness self-verify discipline against this enumerated count.",
        "actual_shape": "The constant is asserted against itself — a trivially-true tautology. The §3.6j sweep-completeness discipline names a brief-template mandate at author-time + a sweep validation tool BEFORE claiming completeness. This arm relies on a future PR-time grep that never runs against the constant.",
        "orchestrator_sweep_attestation": "The orchestrator's own §3.6j self-verify (above in `ground_truth_verify_attestation_per_3_5n.sweep_completeness_self_verify_per_3_6j`) confirms ZERO production direct `policy.check_write(` sites remain at HEAD outside Δv3-7's documented #[cfg(test)] exclusion. The substance IS complete; only the test arm fails to prove it."
      },
      "impact": "A future R5/R3 implementer adding a new WRITE entry point in engine-internal code that calls `policy.check_write(&ctx)` directly (instead of `_with_audience`) would land cleanly. The pim-N-sweep-completeness-self-verify (§3.6j) discipline asks tests like this to RUN the walker against the wave's own outputs, not just assert a numerical constant against itself.",
      "disposition": "FIX-NOW — replace the arm body with a substantive walker: `walkdir::WalkDir::new('crates/').into_iter()` (the R4-FP-4 grep-walk pin precedent at benten-platform-foundation/Cargo.toml has the dep) → count `.check_write(&ctx)` call sites in production code (excluding `_with_audience` matches, test-fixture matches, and the excluded `benten-caps::ucan_grounded` substrate-internal sites per Δv3-7) → assert the count is 0 (the discipline post-S3c is: ZERO production policy.check_write sites; all routed through _with_audience). Mirrors §3.6j sub-rule. ~30 LOC."
    },
    {
      "id": "sec-r6r2-l6-4",
      "severity": "MINOR",
      "title": "§S3a r6_r1_fp_f4_s3a_*.rs test has 6 arms but NONE call `install_plugin(...)` end-to-end with a DenyAllInstallConsent through InstallPorts.policy",
      "evidence": {
        "test_file": "crates/benten-platform-foundation/tests/r6_r1_fp_f4_s3a_install_pipeline_consults_check_install_consent.rs",
        "arm_audit": "Arm 1 counts policy.check_install_consent calls on a CountingAdmit policy (direct trait call). Arm 2 calls policy.check_install_consent on DenyAllInstallConsent directly. Arm 3 calls a PluginSpecificDeny directly. Arm 4 calls AdmitAllInstallConsent directly. Arm 5 verifies InstallPorts struct constructibility with both ports. Arm 6 is explicitly 'a forensic anchor; the §S3a wiring per plugin_lifecycle.rs lands the call at step 3c immediately after step 3b's replay check' — body is just comments.",
        "self_admission": "File-header §6 of the test body: '// the end-to-end install_plugin integration is exercised by the existing platform-foundation tests (they all wire AdmitAllInstallConsent post-S3a migration; with the threading verified by their compilation).' — but compilation only verifies the type-shape compiles; it does NOT verify the install_plugin call body actually consults `ports.policy.check_install_consent` (a hypothetical revert deleting the step-3c block would still compile because the port field is structurally constructible without being called).",
        "existing_test_coverage_check": "The pre-S3a `plugin_install_consent_required_at_install_time.rs` integration tests DO call install_plugin end-to-end but ALL with `&AdmitAllInstallConsent` — the deny-arm of `ports.policy.check_install_consent.is_err() → return Err(PluginInstallConsentDenied)` (plugin_lifecycle.rs:959-965) is NEVER exercised by a test.",
        "ground_truth_grep": "`git grep -n 'DenyAllInstallConsent' 3c80546f` returns only the trait impl in install_consent.rs:74 + the direct-call references in the r6_r1_fp_f4_s3a test file. ZERO uses of DenyAllInstallConsent through InstallPorts.policy at HEAD."
      },
      "impact": "A revert of plugin_lifecycle.rs:959-965 (the step-3c install-consent block) would NOT be caught by any test. The deny-arm of the wire-in is dead-uncovered. Coupled with sec-r6r2-l6-1 MAJOR (the production CapabilityPolicy adapter is missing): the only way to surface PluginInstallConsentDenied via install_plugin at v1-beta is to construct an explicit `&DenyAllInstallConsent` and pass it directly to InstallPorts.policy — and that path has no test pin.",
      "disposition": "FIX-NOW — add arm 7 to the existing test file: construct InstallPorts with `policy: &DenyAllInstallConsent`; call install_plugin end-to-end with a valid signed manifest + install record; assert the call returns `Err(ErrorCode::PluginInstallConsentDenied)`; assert library + cap-minter + private-ns are all empty (fail-closed semantics per §4.35 zero partial-mint). ~80 LOC; mirrors the existing plugin_install_consent_required_at_install_time.rs ARM (a) shape with the policy field swapped to DenyAllInstallConsent. Closes the §3.6f / pim-18 gap on the install-consent wire-in deny-arm."
    },
    {
      "id": "sec-r6r2-l6-5",
      "severity": "OBS",
      "title": "audience_did populate-side at delegate_capability cap-target is intentionally NOT implemented per Δv3-2 — brief mentions verifying it; design defers to G-COMP-1; category-correctness CONFIRMED",
      "evidence": {
        "brief_claim": "L6 review brief: 'audience_did populated at delegate_capability cap-target site; left None at apply_atrium_merge per category-correctness (Δv3-2)'",
        "actual_state": "`git grep -n audience_did 3c80546f -- crates/benten-engine/src/engine_caps.rs` returns 0 results. delegate_capability's Step 4 writes the new grant via `privileged_put_node` (is_privileged=true) without constructing a CapWriteContext with audience_did:Some(plugin_did) — the privileged write path skips check_write_with_audience entirely.",
        "design_rationale_doc": "V1-FROZEN-INTERFACE.md:735 row: 'populate-side at delegate_capability defers to G-COMP-1 + the existing Layer-3 check_per_delegation wiring covers delegate-runtime audience-discrimination needs.' Δv3-2 in the F4 design pipeline ratified this.",
        "category_correctness_audit_per_orchestrator_depth_discipline": "Categorical check confirmed: (a) peer_did at apply_atrium_merge = transport-principal (the sync-peer's DID at the iroh connection layer; NOT the cap-target). (b) audience-DID at delegate_capability = cap-target plugin_did (the principal a delegated cap is issued TO). Conflating peer_did → audience_did at apply_atrium_merge would mis-categorize transport-principal as cap-target. Δv3-2's None-at-sync-sites is structurally correct. The populate-side gap at delegate_capability is the cap-target-WRITE arm; the runtime audience-discrimination contract is delivered by check_per_delegation (S3b) instead — a substitution that holds for v1-beta but loses fidelity for audience-aware-write policies (a policy that wants to deny a plugin-DID's outbound delegation based on the post-write WHO-receives-this-cap audience needs the populate-side to land at G-COMP-1)."
      },
      "impact": "None at v1-beta — design intent is honored. The brief's wording slightly overstates: at v1-beta, audience_did is populated at ZERO sites in production code (only construct-tested in g_core_8_capability_policy_sealed_compile_test.rs:261 and g_core_8_write_boundary_user_root_chain_validator_4_23.rs:143). The CapWriteContext::audience_did FIELD is structurally present + frozen at v1-beta per V1-FROZEN-INTERFACE; the runtime audience-discrimination contract is delivered by check_per_delegation at delegate-runtime, not by audience_did-populated check_write_with_audience.",
      "disposition": "DISAGREE-WITH-EXPLANATION per HARD RULE 12 clause (c) — no action needed; the brief's phrasing is a slight overstatement of the design ratification but the actual code state matches Δv3-2 intent. Recommend the orchestrator-direct triage edit the V1-FROZEN-INTERFACE.md:735 row to add an explicit 'audience_did populate-side is zero production sites at v1-beta; G-COMP-1 lands the delegate_capability cap-target populate-site' clause for forensic clarity. Same edit can land alongside the sec-r6r2-l6-1 doc-state cleanup if option (b) is chosen there."
    },
    {
      "id": "sec-r6r2-l6-6",
      "severity": "OBS",
      "title": "Sealed-discipline + Class B β + InstallPorts.policy port shape (CRITIC-2 F-1.2) all VERIFIED preserved at HEAD",
      "evidence": {
        "sealed_discipline": "policy.rs:48-62 hosts `pub(crate) mod sealed { pub trait Sealed {} }`; CapabilityPolicy declared `pub trait CapabilityPolicy: sealed::Sealed + Send + Sync`; workspace test-doubles use `impl benten_caps::__sealed_for_workspace_tests::Sealed for X {}` (feature-gated re-export); `crates/benten-engine/tests/g_core_8_capability_policy_sealed_compile_test.rs` is the structural backstop.",
        "class_b_beta_preserved": "Engine::policy field declared `pub(crate)` (engine.rs:838); Engine::policy() accessor declared `pub(crate)` (engine.rs:2169); no `pub fn capability_policy()` accessor exists; the only public surface that takes a CapabilityPolicy is the EngineBuilder::capability_policy setter (builder.rs:154 — write-side only, not read-side accessor).",
        "install_ports_policy_port_shape": "plugin_lifecycle.rs:748 declares `pub policy: &'a dyn crate::install_consent::InstallConsentPolicy` as a port (not an accessor); install_consent.rs:1-18 module doc explicitly cites CRITIC-2 F-1.2 + Class B β as the rationale.",
        "no_engine_policy_leak": "`git grep -n 'pub fn capability_policy\\|pub.*policy:' 3c80546f -- crates/benten-engine/src/` shows only `pub(crate)` items + the Builder setter; no leak.",
        "ground_truth_attestation": "The orchestrator's trait_wide_method_audit (above) cross-checked all 7 trait methods for accessor-leak / sealed-discipline preservation. ZERO findings."
      },
      "impact": "Positive: F4's S3a-via-port (CRITIC-2 F-1.2) design intent is fully honored. Sealed discipline survives the §8-E additions per G-CORE-9 V1-FROZEN-INTERFACE row 6 contract. Phase-4-Meta-Core v1-interface freeze is structurally consistent on these axes.",
      "disposition": "No action — observability only."
    },
    {
      "id": "sec-r6r2-l6-8",
      "severity": "OBS",
      "title": "[NEW per augmentation depth-discipline ground-truth-verify §3.5n] InstallPorts shape-change cascade (S2 Option-drop + S3a policy-field-add) verified 100% complete across 35 workspace construction sites",
      "evidence": {
        "method": "Workspace-walker grep on InstallPorts struct-literal constructions; counted callers supplying both `install_record_replay_check:` (post-S2 mandatory) and `policy:` (post-S3a mandatory) fields.",
        "construction_sites_count_per_grep": "35 construction sites total (30 in tests/ + 5 in src/ — counted via `git grep -A20 'InstallPorts\\s*{' 3c80546f -- 'crates/benten-platform-foundation/' | grep -c 'policy:'` = 35).",
        "all_callers_post_s2_s3a_migrated": "VERIFIED — every construction site at HEAD includes both required fields. ZERO orphan callers retained the pre-S2 Option<> shape or omitted the new policy field.",
        "value_distribution_audit": "35 of 35 callers wire `policy: &AdmitAllInstallConsent` or `policy: &noauth_policy_*`. ZERO callers wire `&DenyAllInstallConsent` or a custom denying impl through install_plugin end-to-end (cross-references sec-r6r2-l6-4 dead-uncovered deny-arm)."
      },
      "impact": "Positive: the structural cascade for the S2 + S3a shape changes is complete; no compile-time-only-wired-but-runtime-callerless construction site lingers. The substantive gap is at the value-of-the-field level (sec-r6r2-l6-1 + sec-r6r2-l6-4), not the shape level.",
      "disposition": "No action — observability + supports sec-r6r2-l6-1 / sec-r6r2-l6-4 dispositions."
    }
  ],
  "r6_r1_l6_cluster_closure_verification": {
    "r1_finding_cluster": "R6 R1 L6 named 'substrate-frozen-but-consumer-unwired' across §S1 (write_boundary_chain_validator) + §S2 (install_record_replay_store option-wrapped) + §S3a (check_install_consent) + §S3b (check_per_delegation) + §S3c (check_write_with_audience) + §S4 (ProductionManifestEnvelopeRechecker).",
    "closure_status": {
      "S1": "CLOSED — WriteBoundaryChainValidator wired at all ~13 WRITE entry points via Engine::admit_write_chain; substantive test at r6_r1_fp_f4_s1_*.rs (184 LOC). Verified end-to-end.",
      "S2": "CLOSED — InstallRecordReplayStore Option<> wrapper dropped; every fixture wires substantive closure (production: make_engine_replay_check_closure; tests: noop_replay_check). Verified via plugin_lifecycle.rs:743-749 + InternalCardConstructibility tests + 35-of-35 caller-cascade audit (sec-r6r2-l6-8).",
      "S3a": "PARTIAL CLOSED — InstallConsentPolicy trait wired at install_plugin step 3c via InstallPorts.policy port. BUT engine-side adapter (CapabilityPolicyInstallConsent) missing → production CapabilityPolicy.check_install_consent override has zero runtime effect. See sec-r6r2-l6-1 MAJOR. Sec-r6r2-l6-4 MINOR: deny-arm of step-3c not end-to-end tested.",
      "S3b": "CLOSED — CapabilityPolicy.check_per_delegation consulted at delegate_capability Step 2b/3 between manifest-shares resolver and effective-scope pick. Verified via direct engine.policy field access (no adapter needed; same-crate). Sec-r6r2-l6-2 MINOR: test-shape is pim-18 SHAPE-not-SUBSTANCE; revert of wire-in would not be caught.",
      "S3c": "CLOSED — 4 production check_write sites all switched to check_write_with_audience (engine.rs:1421 + engine_diagnostics.rs:85 + engine_wait.rs:907 + primitive_host.rs:619); Δv3-3 invert-the-panic policy verifies wire-in (would-FAIL on revert). Sec-r6r2-l6-3 MINOR: arm 3 workspace-walker is tautological constant-vs-constant assertion, not a real walker; orchestrator §3.6j self-verify (above) confirms substance is complete.",
      "S4": "CLOSED — ProductionManifestEnvelopeRechecker + ProductionEngineBuilder + D-6 + D-18 wired per F4 merge commit 844a780b. Substantive test at r6_r1_fp_f4_s4_*.rs (79 LOC)."
    },
    "net_r1_l6_disposition": "5 of 6 §S surfaces fully consumer-wired + substantive test coverage; 1 of 6 (§S3a) is PARTIAL-CLOSED (port-wired, adapter missing). Closing sec-r6r2-l6-1 collapses residual. AUGMENTED: sec-r6r2-l6-7 surfaces a SEPARATE L6-phase-wide gap (wallclock_refresh_ceiling unwired pre-F4) that lands in scope per same-attack-class-siblings depth-discipline."
  },
  "audience_did_population_audit": {
    "production_source_files_with_audience_did_some": "ZERO (`git grep 'audience_did: Some' -- crates/benten-*/src/` returns 0 results outside manifest_signing.rs's UCAN engine_audience_did field which is a different domain — engine_audience_did is the UCAN-chain audience-binding for chain-validation; distinct from the §8-E CapWriteContext.audience_did surface). Note: crates/benten-caps/src/manifest_envelope_chain_validation.rs:100 hosts a separate ManifestEnvelopeFrame.audience_did: Did used by the chain-walker — distinct domain from CapWriteContext.audience_did, NOT a contradiction.",
    "test_files_with_audience_did_some": "g_core_8_capability_policy_sealed_compile_test.rs:261 (sealed-discipline compile test) + g_core_8_write_boundary_user_root_chain_validator_4_23.rs:143 + g_core_8_layer3_shares_resolver_verify_stays_regression_1294.rs (test fixture). No production-runtime arm populates audience_did.",
    "design_intent": "Per Δv3-2: audience_did stays None at all sweep sites in v1-beta; the populate-side at delegate_capability defers to G-COMP-1; the audience-discrimination contract for delegate-runtime is delivered by check_per_delegation (S3b), NOT by audience_did-populated check_write_with_audience. The CapWriteContext::audience_did FIELD is structurally locked at v1-beta per V1-FROZEN-INTERFACE item 8 for forward-compat.",
    "category_correctness_orchestrator_verify": "CONFIRMED CORRECT — peer_did at apply_atrium_merge is the transport-principal (sync-peer's DID); audience_did is the cap-target (recipient of a delegated capability). Conflating these would be a category error. Δv3-2 None-at-sync-sites is the structurally-correct choice. The populate-side gap at delegate_capability is the only remaining audience_did population work for v1-beta, intentionally deferred to G-COMP-1."
  },
  "new_capability_system_gaps_introduced": "ONE pre-existing gap surfaced by L6-phase-wide audit: sec-r6r2-l6-7 wallclock_refresh_ceiling has zero production consumer wire site (this gap PRE-DATES F4 — was a Phase-3-backlog §2.3 (ii) defer — but lands in L6 trait-wide audit scope per orchestrator depth-discipline #1 same-attack-class siblings). NO NEW F4-introduced gaps in plugin-trust three-layer model — Layer-1 user-as-root (S1 chain-bearing admission via WriteBoundaryChainValidator), Layer-2 install-time manifest envelope (S3a port; gap is the engine-side adapter, NOT the substrate), Layer-3 runtime delegation envelope (S3b check_per_delegation + private-namespace clause + Wave-E shares-policy resolver) all preserved.",
  "pim_n_candidates_surfaced": [
    {
      "name": "pim-N-test-shape-must-exercise-end-to-end-not-trait-direct",
      "evidence": "sec-r6r2-l6-2 + sec-r6r2-l6-3 + sec-r6r2-l6-4 all surface the same anti-pattern: test files for §S3a/§S3b/§S3c are full of arms that call trait methods directly (on the default impl, on a counting impl, on a denying impl) WITHOUT actually invoking the production entry point (install_plugin / delegate_capability / a real engine WRITE) end-to-end. The invert-the-panic shape used at §S3c arm 1 IS the substantive shape; recommend pim-N codify this as: 'for any §-wire-in production-arm test, AT LEAST ONE arm MUST call the production entry point with the wire-in observable end-to-end (deny-arm typed-code; invert-the-panic; or assertion-on-side-effect).' Strengthens pim-18 §3.6f. AUGMENT FROM ORIGINAL: 3 instances (was 2; sec-r6r2-l6-3 also recategorized as test-shape gap since the workspace-walker arm is a tautology rather than substance).",
      "instance_count": 3,
      "at_or_above_3_recurrence_threshold": "Yes — meets the 3-recurrence threshold per `feedback_3_plus_recurrence_deep_sweep`. Recommend R6 R3 codify as §3.6f sub-rule extension OR new §3.6k 'production-arm-call-required-on-wire-in-tests'.",
      "named_destination_if_codify": "dispatch-conventions.md §3.6f sub-rule extension (preferred — augments existing pim-18) OR new §3.6k"
    },
    {
      "name": "pim-N-doc-claim-must-match-code-claim-for-adapter-references",
      "evidence": "sec-r6r2-l6-1 surfaces a module-doc claim ('CapabilityPolicyInstallConsent ... wraps the policy') that references a non-existent code symbol. Recommend a pre-push doc-grep that flags `module-doc-references-symbol-that-doesnt-exist`. Pairs with §3.5g type-name cross-doc mirror pattern (Phase-4-Foundation R6 R1).",
      "instance_count": 1,
      "below_3_recurrence_threshold": "Yes — OBSERVE.",
      "named_destination_if_codify": "dispatch-conventions.md §3.5g extension if 2+ more instances surface"
    },
    {
      "name": "pim-N-same-attack-class-siblings-audit-on-trait-edits",
      "evidence": "sec-r6r2-l6-7 surfaces a pre-F4 gap that F4 would have closed had the §8-E hook wave run a 'same-trait sibling-method consumer-wire audit' before declaring the trait-edit complete. The discipline: when editing a sealed trait to add new methods, audit ALL existing methods on the trait for matching consumer-reachability + close OR explicitly defer-named-now any gaps surfaced. Mirrors §3.6h ratification-must-close-origin but for trait-sibling-class closure. Cross-confirm with L3 audience_pubkey same-attack-class finding suggests this pattern extends beyond traits to any wire-format/struct-edit wave.",
      "instance_count": "1 (L6) + 1 (L3 cross-confirm) = 2; OBSERVE pending one more cross-confirm at R6 R3",
      "below_3_recurrence_threshold": "Yes — OBSERVE.",
      "named_destination_if_codify": "dispatch-conventions.md §3.6h extension (sub-rule for trait-edit + struct-edit waves) OR new §3.6l"
    },
    {
      "name": "pim-N-reviewer-must-grep-verify-cite-before-claim (per L11 phantom-cite cross-confirm)",
      "evidence": "Sibling L11 found 7 phantom cite-strings in wire-format inventory expansion. L6 ground-truth-verify pass above cross-verified every cited file:line + found ZERO phantom cites in this artifact. Recommend pim-N codify: 'reviewer briefs MUST mandate `git grep` / `git show <sha>:<path>` verification of every cite (file:line + cited content) before claim — orchestrator §3.5n ground-truth-verify pattern extended to apply at reviewer-author-time, not just orchestrator-triage-time.'",
      "instance_count": 1,
      "severity_escalated_observe_plus": "Yes — though only 1 instance, the L11 failure-mode-severity is high (7 phantom cites in a single artifact); recommend escalate to R6 R3 codification candidate even at 1-instance.",
      "named_destination_if_codify": "dispatch-conventions.md §3.5n extension OR new §3.5p"
    }
  ],
  "approval_state": "APPROVE-WITH-FINDINGS for R6 R2 advance — provided sec-r6r2-l6-1 + sec-r6r2-l6-7 MAJORs triage to either (a) FP-2 implementation closing both gaps OR (b) explicit doc-state correction + named-destination promotion. The 3 MINOR test-shape findings + 3 OBS findings can land as a single ~250-LOC fix-pass alongside the MAJORs (`fix(r6-r2-fp): close L6 2 MAJOR + 3 MINOR test-shape pins + doc-coupling`).",
  "convergence_signal": "MONOTONIC IMPROVEMENT vs R1 on F4's stated scope — 5/6 §S surfaces fully closed (was 0/6 pre-F4). AUGMENTED: L6 phase-wide trait-method-reachability audit surfaces 1 pre-F4 gap (wallclock_refresh_ceiling) NOT in R1's F4-scoped finding set; this is a true L6 phase-wide-lens-positioned finding (the kind of gap a F4-scoped review pass wouldn't catch). Recommend Q5 advancement to R3 once both MAJORs are closed; the 3 MINORs + 3 OBS are R6-R3-acceptable residuals if FP-2 lands. R6 R3 should also evaluate the 4 pim-N candidates surfaced here (1 at 3-instance threshold ready to codify; 3 OBSERVE-watching for next R3 cross-confirm)."
}
