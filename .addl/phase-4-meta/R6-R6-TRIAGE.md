# R6 Round-6 Triage — Phase-4-Meta-Core phase-close (artifact main 011936fd)

Run `wf_682a25de-03f` (Task winnquf14, 33 agents / 19 lenses, deeper B2 adversarial probe). **VERDICT: NOT-CONVERGED — 1 BLOCKER (F-01+F-02) · 2 CONFIRMED MAJOR freeze-forks (F-04, F-07) · F-09 REFUTED · MINOR/OBS cluster.** The deeper sender-origin-auth lens found a REAL content-splice hole that 5 prior rounds missed. **Freeze MUST NOT be tagged at 011936fd.**

## ⛔ F-01 (BLOCKER) + F-02 — B2 CONTENT-SPLICE (do-it-right fix, NO Ben fork)
All 3 open paths (`open_inner` layer_c.rs:794-810, `open_group_stanza` :1293-1316, `open_membership_set_group` :2221-2253) recover `body` and rebuild `M_auth` with `body_cid` taken FROM THE WIRE — **none recomputes** `self_describing_cid(blake3::hash(&body))` against it. So a co-member holding K_Set can capture victim A's real `sender_sig` (over A's body_cid) and **re-seal a SUBSTITUTED body** under A's signature + A's original body_cid → all honest recipients accept the forged body attributed to A. (`body_aad_digest` covers only AAD_VERSION‖codepoint‖body_cid, not body.) The seal side derives the cid at :1975-1976; the open side trusts the wire. **FIX (no decision):** on all 3 open paths, after recovering body, recompute `self_describing_cid(blake3::hash(&body))` and fail-closed `SenderOriginAuthFailed` unless == the wire body_cid. **F-02:** add `f_lc_3_content_splice_rejected_membership_group` (+ `_layer_c_group`) — open an honest A-send as co-member B, re-seal a DIFFERENT body carrying A's REAL sender_sig + unchanged body_cid under the K_Set CEK, assert open REJECTS; would-FAIL-on-revert. **→ DISPATCHED as shard A (no Ben dependency — strictly more-correct, not a wire change).**

## ⛔ F-04 (MAJOR) — BEN FREEZE-FORK: codepoint band 0x6380 vs 0x63A0
`band_base()` returns 0x6380 (MLS-Application) for RotatingGroupKeyChainedMode + ChainedStateTlv, but §4.0 registry assigns them to CGKA-Commit 0x63A0..0x63AF. `chained_state_tlv_aad_binding(true)` emits `01 63 80` into AAD (byte-pinned f_nqa1_1:578 PIN 4 — tautological, doesn't detect the mismatch). One side must move (frozen AAD byte). ORCH REC: align CODE to §4.0 (move band_base→0x63A0, re-pin AAD `01 63 a0`) — §4.0 symbol-bound table is source-of-truth + CGKA-Commit is the semantically-correct band for a chained group-key mode. (Reserved codepoint, not live — but byte-pinned freeze record.)

## ⛔ F-07 (MAJOR) — BEN FREEZE-SCOPE FORK: registry omits 3 surfaces
SECURITY-PROOFS §4.1 / THREAT-MODEL §5 make a UNIVERSAL claim ("every cryptographic surface...draws from a single prefix-free registry"), but `registered_domain_tags()` (now 16) OMITS 3 live keying/AAD surfaces: (1) vault AEAD AAD `b"benten-vault:"` (vault.rs:296), (2) DAK HKDF info `b"benten-dak-v1"` (vault.rs:64), (3) recipient-seed label `b"benten-crypto-suite:recipient-seed"` (cipher_suite.rs:202). NOT a live break (all 25 literals mutually prefix-free) but these 3 sit OUTSIDE the prefix-free gate → a future adjacent mint isn't build-gated. Unlike gossip-topic, no documented exclusion. ORCH REC: (a) register the 3 (16→19 + mirror-asserts) — genuine keying surfaces; consistent with the round-5 widen; makes the universal claim TRUE + forward-gates them.

## MINOR/OBS (round-7 wave / named-carry)
F-05 (DropBundleError non_exhaustive audit-arm gap — fix-now cheap) · F-06 (re-tense "parallel CODE shard" prose → registry SHIPPED — fix-now) · F-10 (DAK return not zeroized-on-drop → fix-now ZeroizeOnDrop/SecretBox newtype, security hygiene) · F-09 REFUTED (wasm32-wasip1 lane runs tf3a — drop) · F-11 (drop "hkdf key-schedule" on 0x647a) · F-12 (INTERNALS 168→199) · F-13 (INVARIANT-COVERAGE SHA anchor 011936fd) · F-16 (deny.toml hax-lib-macros provenance) · F-17 ("mirrors not delegates") · F-14/F-15/F-18 + F-19..F-37 named-carry/OOS/disagree · F-27/F-28 = 5→18 (already the tracked Ben ratification surface).

## Sequence
Shard A (BLOCKER F-01+F-02) DISPATCHED now. SURFACE F-04 + F-07 → Ben picks → shard B (F-04 + F-07 per Ben + F-05/F-06/F-10 + fix-now cluster + named-carries). Integrate A+B → mini-review BLOCKER + forks → reconcile PR → Ben squash → re-run R6 ROUND 7 (re-point 011936fd→new main) → iterate to 0 BLK/MAJ → pre-tag bundle [5→18 · §16 · gossip · multicodec · DropContentMode] → tag (HOLD Ben).
