# Issue-Audit Report — phase-4-meta-core (main 35fb9ad6, B2 merged)

Generated by issue-audit workflow (wf_3d7af4f9-043): 223 issues, 26 shards, 130 agents, assess -> adversarial-verify. **NOTHING executed — awaiting Ben sign-off.**


## CONFIRMED CLOSE — 32 issues


### #204
**Finding:** Weekly cargo-audit flagged 2 vulnerabilities against the committed lockfile: RUSTSEC-2026-0120 (hickory-net 0.26.0-beta.4, NSEC3 unbounded-loop DoS) and RUSTSEC-2026-0119 (hickory-proto 0.26.0-beta.4, O(n^2) name-compression CPU exhaustion), both patched in >=0.26.1.
**Evidence:** origin/main d0ccf606 Cargo.lock has hickory-net 0.26.1 (checksum e2295ed2...), hickory-proto 0.26.1, hickory-resolver 0.26.1 — all satisfy the >=0.26.1 patch requirement; no 0.26.0-beta entries remain (grep returned NONE). Bump landed in ancestor commit ae69c339 (PR #1307, G-CORE-2): parent had hickory-net 0.26.0-beta.4, the commit has 0.26.1; git merge-base confirms ae69c339 is ancestor of origin/main. deny.toml lines 47-51 document: 'RUSTSEC-2026-0119 + RUSTSEC-2026-0120 ... CLOSED 2026-05-19 
**Closing comment:**
Resolved. Both flagged vulnerabilities (RUSTSEC-2026-0120 hickory-net NSEC3 unbounded-loop, RUSTSEC-2026-0119 hickory-proto O(n²) name-compression) require the >=0.26.1 patch and were detected against 0.26.0-beta.4. The current lockfile on main (d0ccf606) carries hickory-net (checksum e2295ed2...), hickory-proto, and hickory-resolver all at 0.26.1; grep for `0.26.0-beta` returns no entries. The bump landed in commit ae69c339 (PR #1307, G-CORE-2) — its parent had hickory-net 0.26.0-beta.4, the commit has 0.26.1 — via the iroh 0.98 -> 1.0.0-rc.0 upgrade pulling the patched hickory transitive chain; deny.toml lines 47-51 document the closure at G-CORE-2-FP-1. Verified the two advisory IDs appear only in a deny.toml comment, not as active ignore entries, so neither is suppressed — both are genuinely no longer detected. Closing.

### #269
**Finding:** cargo-public-api.yml drift loop iterated only 8 of 12 workspace crates, missing benten-id / benten-sync / benten-platform-foundation / benten-renderer-tauri (33% of crates could drift silently); benten-platform-foundation had no baseline at all.
**Evidence:** At HEAD (d91896b3) .github/workflows/cargo-public-api.yml iterates a 14-crate loop: 'for crate in benten-core benten-errors benten-graph benten-caps benten-ivm benten-eval benten-engine benten-dsl-compiler benten-crypto-suite benten-drop benten-sync benten-id benten-platform-foundation benten-renderer-tauri'. All 4 named-missing crates are present. The previously-missing benten-platform-foundation.txt baseline now exists (docs/public-api/benten-platform-foundation.txt, 299524 bytes) along with .
**Closing comment:**
Resolved in current main (d0ccf606). `.github/workflows/cargo-public-api.yml`'s drift-detector loop now iterates the full workspace public surface, including all four named-missing crates (`benten-id`, `benten-sync`, `benten-platform-foundation`, `benten-renderer-tauri`). The previously-absent `docs/public-api/benten-platform-foundation.txt` baseline was minted (2596 lines / ~300 KB, including the plugin-manifest surface: PluginManifest / CapRequirement / SharesPolicy / InstallRecord), and every crate in the loop now has a unified `.txt` baseline — resolving the mixed `.txt`/`.json` format gap the finding flagged.

Loop extension + platform-foundation baseline mint landed in commit 8cc4eddd (PR #1344, G-CORE-9 FREEZE wave build-out). The loop has since grown further to 15 crates (F-full added `benten-membership-set`), confirming it is actively maintained. The workflow is required-failing per `.github/branch-protection.yml:225` ("API drift detector (required-failing per G-CORE-9 FREEZE)"), so API drift in any covered crate now fails CI rather than landing silently.

Cross-crate CI-mirror codification (every new workspace crate must extend the loop in the same wave) is captured in docs/future/refinement-audit-2026-05.md as the shared #273+#269 fix-cluster recommendation. Closing.

### #307
**Finding:** Legacy deprecated module_ecosystem::install_plugin + install_plugin_persisting_did still shipping plus §4.22→§4.33 cite-drift in deprecation notes.
**Evidence:** G-CORE-0 (PR #1310, commit 48ba69b2) deleted both functions + InstallResult/InstallerShape supports. module_ecosystem.rs is now 133 LOC and contains only upgrade/trust auxiliaries (verify_install_record, decide_upgrade_consent, verify_upgrade_author_continuity, check_author_trust); the deletion is documented at module_ecosystem.rs:11-23 and INTERNALS.md:5,184,307. Dedicated verify-pass test crates/benten-platform-foundation/tests/tf_g_core_0_legacy_install_path_deletion_4_33.rs asserts the symbo
**Closing comment:**
Resolved by G-CORE-0 (PR #1310, commit 48ba69b2, merged 2026-05-22; ancestor of current main). Both legacy `module_ecosystem::install_plugin` and `install_plugin_persisting_did` — plus their `InstallResult`/`InstallerShape` supports — were DELETED per V1-FROZEN-INTERFACE item 7 / HARD-RULE-12 clause-(a). `plugin_lifecycle::install_plugin` is now the canonical-and-only public install entry point (the 11-step pipeline with full Layer-1/Layer-2/Layer-3 consent gates per CLAUDE.md #18).

Verified at current main:
- `crates/benten-platform-foundation/src/module_ecosystem.rs` (132 LOC) now hosts only upgrade/trust auxiliaries (`verify_install_record`, `decide_upgrade_consent`, `verify_upgrade_author_continuity`, `new_version_available_code`, `check_author_trust`). Deletion note at `module_ecosystem.rs:11-23`.
- Zero `#[deprecated]` attributes remain on this surface, so the §4.22→§4.33 cite-drift is moot (the attribute that carried the stale cite no longer exists).
- `grep` for `pub fn install_plugin*` in `module_ecosystem.rs` returns no function definitions; the only `install_plugin` definition crate-wide is `plugin_lifecycle::install_plugin`.
- Verify-pass guard `tests/tf_g_core_0_legacy_install_path_deletion_4_33.rs` lands with 4 non-ignored `#[test]` arms asserting the symbols are absent, the canonical symbol is present, and 4 consumer test files migrated off the legacy path.

Note for follow-up (does NOT block closure — the code defect is gone): `INTERNALS.md` is internally inconsistent — lines 5/184/307 correctly record the deletion, but lines 11 and 63 still describe the functions as "DEPRECATED canary precursors" that "remain for three pre-R4b-FP-1 integration tests." That stale prose should be retensed in a doc sweep.

Closing.

### #312
**Finding:** MaterializerError::Backend(String) and MaterializerError::Other{code,message} never constructed (pure dead enum arms) plus aspirational rustdoc claim.
**Evidence:** Commit 86e1edb3 (PR #1262, which Closes bundle #1196 that #312 was tracked under) removed both variants, their code() match arms, and the rustdoc `MaterializerError::Backend` engine-seam claim (git show 86e1edb3 -- materializer.rs shows '-Backend(String)', '-Other {', '-MaterializerError::Backend(_)', '-MaterializerError::Other { code, .. }', '-/// [`MaterializerError::Backend`] for engine-seam failures.'). Current materializer.rs:209-232 defines exactly 3 variants (SchemaMismatch, SubscribeSeam
**Closing comment:**
Resolved by commit 86e1edb3 (PR #1262, merged 2026-05-16), which `Closes #1196` — the bundle umbrella this issue was tracked under (per the 2026-05-15 comment above). The dead variants `MaterializerError::Backend(String)` and `MaterializerError::Other { code, message }` were deleted along with their `code()` match arms and the aspirational rustdoc claim (`/// [\`MaterializerError::Backend\`] for engine-seam failures.`).

Verified in current main (HEAD d91896b3):
- `crates/benten-platform-foundation/src/materializer.rs:209-232` now declares exactly the three constructed variants: `SchemaMismatch`, `SubscribeSeamFailure`, `UcanClockNotInjected`.
- `code()` at :238-246 maps only those three.
- Workspace-wide grep for `MaterializerError::Backend` / `MaterializerError::Other` across `crates/` and `bindings/` returns zero matches.
- `INTERNALS.md:93` records the removal ("the dead `Backend` / `Other` variants were removed — Hyg-1 #312, forward-fire-only per §3.6h").

Closing.

### #330
**Finding:** Nine pub fn ..._for_test() helpers plus execute_and_capture_zone_writes/execute_for_test_signal* in benten-eval are unconditionally pub and leak test-only surface into release builds (missing cfg gate).
**Evidence:** Fixed in PR #1351 (commit 4b1333b1). Every cited helper now carries #[cfg(any(test, feature = "testing"))] in current main (HEAD d91896b3): invariants/system_zone.rs:156, invariants/budget.rs:621/660/682/708, invariants/attribution.rs:85/99/125, invariants/immutability.rs:87, primitives/wait.rs:220/236/259 (execute_for_test_signal / execute_for_test_signal_with_trace / execute_and_capture_zone_writes). Verified the preceding line of every `pub fn *_for_test` / `execute_*` in src/ is the cfg attr
**Closing comment:**
Resolved in PR #1351 (commit 4b1333b1, phase-4-meta-core R6-R1 consolidate; ancestor of current main). All 10 cited rows now carry `#[cfg(any(test, feature = "testing"))]`, verified directly against current main:

- invariants/system_zone.rs:156 (`build_subgraph_reading_literal_system_cid_for_test`)
- invariants/budget.rs:621/660/682/708 (`build_chained_call_iterate_iterate_for_test`, `build_nested_iterate_for_test`, `build_call_with_callee_budget_for_test`, `build_two_path_dag_for_test`)
- invariants/attribution.rs:85/99/125 (`run_with_attribution_for_test`, `build_five_step_handler_for_test`, `build_subgraph_with_undeclared_attribution_for_test`)
- invariants/immutability.rs:87 (`build_subgraph_writing_to_literal_cid_for_test`)
- primitives/wait.rs:220/236/259 (`execute_for_test_signal`, `execute_for_test_signal_with_trace`, `execute_and_capture_zone_writes`)

A `git grep` for `pub fn *_for_test` / `execute_and_capture_zone_writes` across `crates/benten-eval/src/` returns exactly these 12 functions, each immediately preceded by the cfg attribute — no un-gated test-helper remains. The `testing` feature is declared in `crates/benten-eval/Cargo.toml` (`testing = ["benten-core/testing", "benten-caps/testing", "dep:wat"]`), so in release builds without `cfg(test)` and without the feature these symbols are excluded from the production cdylib, matching the `register_test_callee` / sec-r6r2-02 precedent. Closing.

### #609
**Finding:** plugin_lifecycle::install_plugin upgrade path did not verify peer-DID author continuity (T10-upgrade (a) defense uninstrumented); verify_upgrade_author_continuity had zero production callers.
**Evidence:** Resolved at R6 R1 fix-pass (PR #1351, commit 4b1333b1, L2-R6-MAJOR-1 / META #707). plugin_lifecycle.rs:1048-1078 adds step 7a which calls `verify_upgrade_author_continuity(&prior_manifest, &manifest)?` when prior_installed_cid is resolvable, returning ErrorCode::PluginAuthorNotTrusted on peer-DID mismatch. Production-arm e2e test crates/benten-platform-foundation/tests/plugin_upgrade_rejects_peer_did_substitution_e2e.rs drives install_plugin(...) with prior_installed_cid: Some(v1) and asserts th
**Closing comment:**
Resolved by the R6 R1 fix-pass (PR #1351, commit `4b1333b1`; standalone fix `b5a6e758`) closing L2-R6-MAJOR-1 under META #707. The preferred Option A wire-up landed: `plugin_lifecycle::install_plugin` now invokes `verify_upgrade_author_continuity(&prior_manifest, &manifest)?` as step 7a (`crates/benten-platform-foundation/src/plugin_lifecycle.rs:1073-1078`, helper imported at :53). On a within-lineage upgrade where `prior_installed_cid` is Some and the prior manifest is resolver-resolvable, a peer-DID mismatch is rejected with `ErrorCode::PluginAuthorNotTrusted` (helper at `module_ecosystem.rs:99-107`), forcing the re-install/fresh-consent ceremony per `docs/PLUGIN-MANIFEST.md` §4.3 + admin-ui-v0-threat-model.md §T10. A production-arm e2e pin drives the full `install_plugin(...)` path: `tests/plugin_upgrade_rejects_peer_did_substitution_e2e.rs::install_plugin_rejects_peer_did_substitution_on_upgrade_path` installs alice-signed v1, then attempts an attacker-signed v2 (a DAG-descendant that passes the T10-(b) check), and asserts `expect_err` → `PluginAuthorNotTrusted` (:181-188) with library state unchanged; a positive-control (`install_plugin_admits_same_peer_did_upgrade_on_upgrade_path`) guards against over-strictness. All three (wire-in, helper, test) verified present in current main. Closing this closure-pin.

### #684
**Finding:** docs/SECURITY-POSTURE.md Compromise #21 (module-manifest signing CLOSED) is bypassed at the napi cdylib deployment boundary because install_module hard-codes ManifestVerifyArgs::unsigned_development with no JS-side opt-out (#624).
**Evidence:** On main (d91896b3): #624 CLOSED. PR #1282 (6ed80b2f) added installModuleSigned at the napi cdylib boundary routing through ManifestVerifyArgs::registry (bindings/napi/src/lib.rs:1763 install_module_signed; verify_args = ManifestVerifyArgs::registry at :1823). docs/SECURITY-POSTURE.md Compromise #21 retensed (lines 1680-1699): 'Compromise #21 stays CLOSED, now honestly inclusive of the napi cdylib boundary' naming the new installModuleSigned surface. Closure-pin test: bindings/napi/tests/install_
**Closing comment:**
Closed on main (HEAD 115b0847). This META names exactly one confirmed instance, #624, which is now closed.

- Signature-verifying surface added at the napi cdylib boundary: `installModuleSigned` (`bindings/napi/src/lib.rs:1763-1833`, PR #1282 / commit 6ed80b2f) threads `ManifestVerifyArgs::registry(&registry_pubkey, &audience, now)` (lib.rs:1823) into the same enforced `Engine::install_module` path (lib.rs:1830). JS callers installing registry/third-party modules now have a non-bypass recourse; `installModule` is retained as the explicit unsigned-development opt-in (documented at lib.rs:1711-1720).
- Enforcement is real, not paper: `crates/benten-engine/src/engine_modules.rs:206-226` runs `verify_manifest_with_mode` BEFORE persistence for all non-Unsigned modes; `crates/benten-engine/src/manifest_signing.rs:466-476` performs Ed25519 `registry_pk.verify(...)` returning `RegistryInvalid` on failure.
- `docs/SECURITY-POSTURE.md` Compromise #21 retensed (lines 1680-1699): "Compromise #21 stays CLOSED, now honestly inclusive of the napi cdylib boundary."
- Closure-pin: `bindings/napi/tests/install_module_signed_napi_cdylib_enforcement_1205_closure_pin.rs` (commit 6d250fdf) — tampered registry signature rejected (would-FAIL-if-reverted) + valid registry-signed manifest admits (not degenerate deny-all), exercising the exact production seam `installModuleSigned` wraps.

#624 was closed 2026-05-18 via merged PR #1294 (stateReason COMPLETED). The single confirmed instance is resolved; closing this META.

### #702
**Finding:** materialize_format per-primitive cap_recheck fan-out loop discards a security-shaped bool and is observability-theater (qual-1 framing of safe-1 #527).
**Evidence:** crates/benten-platform-foundation/src/materializer.rs:1367-1387 — the per-primitive fan-out loop is gone; the in-source comment (lines 1376-1380) states 'The previous per-primitive fan-out loop discarded both the read scope and the gate bool (Qual-1 #702 / Safe-1 #527) ... is removed.' The single authoritative gate call now consumes its bool (line 1386-1387, consumed at 1400). Fix is present in current main (commit 7380459a squash; resolution originated in the drain/cleanup wave #1262).
**Closing comment:**
Resolved in current main. The per-primitive `cap_recheck` fan-out loop in `materialize_format` has been deleted; the materialization-layer cap-decision is now a single authoritative gate call whose bool is consumed (`crates/benten-platform-foundation/src/materializer.rs:1386-1387`, used at line 1400 to drive admit-vs-redact). The in-source comment at `materializer.rs:1376-1380` records the removal, explicitly citing this issue and #527 ("observability-theater discarding a security-shaped bool ... is removed"). A regression-guard test `per_row_gate_bool_is_consumed_not_swallowed` (`materializer.rs:1661`) invokes the production entry point and asserts a deny gate yields zero materialized rows + a denial frame + no content leak. The only remaining `let _ = (cap_recheck)(...)` text in the file is inside that test's doc-comment describing a hypothetical regression, not live code. Fix landed in commit 7380459a (PR #1271, merged 2026-05-17), which is an ancestor of current main. Closing as resolved.

### #744
**Finding:** AsyncTask/Promise mandate at napi binding: entire ~8.1k-LOC binding has zero Promise-returning #[napi] methods; ~50 hot-path methods sync-block libuv worker threads; PR-A (JsAtrium mutators) + PR-B (StreamHandleJs::next) + binding-wide #716.
**Evidence:** PR-A landed: #1299 (ed03729a) converted 11 JsAtrium async mutators (closes #648/#688/#704). PR-B landed: PR #1333 (1b99589c, batch #1340 05707357) full StreamHandleJs::next napi-rs v3 AsyncTask migration (closes #652+#1203; NextChunkTask AsyncTask at bindings/napi/src/lib.rs:2212+). Sub-issues #648/#652/#688/#704/#716 CLOSED; bundle umbrellas #1202(PR-A)/#1203(PR-B) CLOSED COMPLETED. Maintainer's last comment set the close criterion 'META #744 stays OPEN as the PR-B rollup until #1203 lands' — #
**Closing comment:**
Closing — the maintainer's stated close criterion ("#744 stays OPEN as the PR-B rollup until #1203 lands", 2026-05-18) is now satisfied, and both scoped deliverables are verified in `main` (`d0ccf606`).

- **PR-A** — #1299 (commit `ed03729a`, MERGED 2026-05-18) converted the `JsAtrium` async mutators (join/leave/rejoin/declare_device_attestation/set_local_device_*/clear_local_device_*) to Promise-returning `AsyncTask`s, closing #648/#688/#704. Verified in `main`: e.g. `LeaveTask`/`RejoinTask` `impl Task` with `block_on` now inside `compute()` on the libuv worker (`bindings/napi/src/atrium.rs:866+`), not on the JS invocation thread.
- **PR-B** — bundle #1203 (full `StreamHandleJs::next` napi-rs v3 `AsyncTask` migration, closes #652). PR #1333 was closed unmerged; its content reached `main` via batch consolidation #1340 (commit `05707357`, MERGED 2026-05-23). Verified in `main`: `pub fn next(&self) -> AsyncTask<NextChunkTask>` at `bindings/napi/src/lib.rs:2300` (`NextChunkTask`/`impl Task` at lib.rs:2211), replacing the Option-C stopgap.

Bundle umbrellas #1202 (PR-A) and #1203 (PR-B) are both CLOSED COMPLETED. The binding-wide ~50-method remainder (#716, closed tracked-under-META) was re-homed to the Phase-4-Meta forward-readiness backlog (#1026 "no AsyncTask migration story" → `docs/future/phase-4-backlog.md` §4.198 / META #1094 v1-API decision cluster); both #1026 and #1094 are themselves now CLOSED COMPLETED (#1026 landed-as-named via #1294, `39a0e732`). No open downstream sub-issue requires this rollup to stay open.

A few non-`block_on`-on-the-JS-thread items remain by design (sync atomic setters/getters, in-memory roster ops — documented as intentionally not migrated at `atrium.rs:198+`), and the deeper binding-wide migration roadmap lives in the re-homed backlog rows. The PR-B rollup obligation this META was held open for is discharged.

### #763
**Finding:** Evaluator's 5 suffix-stacked run entry points (run_with_budget / run_with_trace / run_with_trace_attributed / run_with_trace_attributed_capturing_with_budget) should collapse to a RunOptions builder.
**Evidence:** Resolved at HEAD d91896b3. The 5 suffix-stacked methods are gone; evaluator.rs now exposes only `run` (defaults, :158) and `run_with(.., RunOptions)` (:199), with a `RunOptions` builder struct (:99-143) carrying budget/attribution/collect_trace/capture_on_err axes. Docstring at evaluator.rs:75-82 + :169-177 explicitly cites 'refinement-audit #1145 / Qual-2 #763'. Landed via commit f412dbab (ST-EVAL lane, Refs #1145) consolidated through PR #1277.
**Closing comment:**
Closed: the five suffix-stacked entry points (`run_with_budget` / `run_with_trace` / `run_with_trace_attributed` / `run_with_trace_attributed_capturing_with_budget`) were collapsed to two methods — `Evaluator::run` (all-defaults) + `Evaluator::run_with(.., RunOptions)` — via a `RunOptions` builder.

Verified in current main:
- `crates/benten-eval/src/evaluator.rs:99-143` — `RunOptions` struct + builder methods (`budget` / `attribution` / `collect_trace` / `capture_on_err`).
- `crates/benten-eval/src/evaluator.rs:158` (`run`) and `:199` (`run_with`); docstrings at :75-98 and :169-198 record this finding (#763 / #1145).
- No `fn run_with_budget` / `run_with_trace*` definitions remain anywhere in the workspace (grep clean); remaining string matches are docstrings explaining the collapse.
- Both production callsites in `crates/benten-engine/src/engine.rs` (~:4037, ~:4049) now call `run_with(.., RunOptions::new()...)`.
- Regression closure-pin: `crates/benten-eval/tests/v1_api_stabilization_closure_pins.rs`.

Landed under bundle umbrella #1145 via PR #1277 (squash-merge commit 9b0f327f; ST-EVAL lane authoring commit f412dbab).

### #869
**Finding:** JsAtrium::join discarded the JS-supplied atriumId and passed EngineAtriumConfig::for_test() to engine.open_atrium regardless of caller config (production-invariant + API-surface-lie violation).
**Evidence:** Resolved at HEAD d91896b3 by commit a0d7c946 ("fix(bindings-napi): ST-NAPI lane … Refs #1187 #1206 #1204", batched in #1282): the engine-bound join path now drives `EngineAtriumConfig::production()` (atrium.rs:839, inside JoinTask::compute), closing the for_test()/Loopback production mis-wire. The original atrium-id-routing prescription is moot post-COLLAPSE: engine-side AtriumConfig is `struct AtriumConfig { mode: AtriumMode }` only (crates/benten-engine/src/atrium_api.rs:64-69) — it carries NO
**Closing comment:**
Resolved on main (d0ccf606) via the ST-NAPI lane fix, squash-merged in batch PR #1282 (6ed80b2f); umbrella bundle #1187 is CLOSED.

Verified in current main:
- The engine-bound `JsAtrium::join()` path now drives `EngineAtriumConfig::production()` (bindings/napi/src/atrium.rs:839, inside `JoinTask::compute`), closing the production-invariant violation where `for_test()` (= `AtriumMode::Loopback`) was bound unconditionally for engine-bound callers.
- The atrium-id-routing concern is moot post-COLLAPSE: engine-side `AtriumConfig` is `struct AtriumConfig { mode: AtriumMode }` only (crates/benten-engine/src/atrium_api.rs:64-69) — there is no atrium-id field to thread through. `Engine::open_atrium(config)` (engine.rs:1156) yields a fresh, independent `AtriumHandle` per call, stored per-instance in `state.engine_atrium`; atrium identity is tracked solely in the napi layer's `JsAtrium.config.atrium_id`. The original `from_id(...)` prescription no longer maps to any engine slot.
- The closure is documented in the `join` docstring at atrium.rs:335-351, and a regression pin (bindings/napi/tests/atrium_join_production_mode_1187.rs, tracked in main) asserts `production().mode == AtriumMode::Production` distinct from `for_test()` (= Loopback), so a revert of the mis-wire is caught. (Note: the pin asserts the constructor-mode contract the call site depends on rather than driving `join()` directly — carried as an integration test because the lib-test target is blocked on a pre-existing-on-main `benten_engine::testing` residual in wait.rs.)

Closing.

### #888
**Finding:** asymmetric InstallContext (2 ports + 7 fields) vs UninstallContext (3 pure ports) — composability defect + missing SubscriptionRegistry port at install.
**Evidence:** crates/benten-platform-foundation/src/plugin_lifecycle.rs — InstallContext is gone; replaced by the recommended split InstallPorts<'a, M, P> (line 698) + InstallParams<'a> (line 777), and UninstallContext symmetrized to UninstallPorts<'a, R, P, S> (line 164). The split doc explicitly cites Surf-1 #888 (lines 695, 775). The 'missing SubscriptionRegistry port at install' sub-point received an in-source scoped DISAGREE-WITH-EXPLANATION (lines 689-697: install attaches SUBSCRIBE handles lazily; addi
**Closing comment:**
Resolved in current main (d0ccf606). The asymmetry is closed via the recommended *Ports/*Params split: `InstallContext` no longer exists — it is replaced by `InstallPorts<'a, M, P>` + `InstallParams<'a>` (crates/benten-platform-foundation/src/plugin_lifecycle.rs:698, 777), and `UninstallContext` is symmetrized to `UninstallPorts<'a, R, P, S>` (line 164), so both lifecycle entry-points now share a pure-`*Ports` / `*Params` shape a future install-then-rollback (#635) can compose against. The split docs explicitly cite Surf-1 #888 (lines 159, 683, 775). The 'missing SubscriptionRegistry port at install' sub-point is a scoped, documented DISAGREE-WITH-EXPLANATION (lines 689-697): the v1 install path attaches a plugin's SUBSCRIBE handles lazily at first subgraph walk (uninstall terminates them, which is why uninstall carries the port), so an install-time registry port would be dead surface (Hyg-2) — a valid HARD-RULE-12 disposition. The InstallContext→InstallPorts/InstallParams rename landed in commit 86e1edb3 (#1262, the #1199 bundle / #286 drain cleanup) and is in current main. Closing as resolved.

### #986
**Finding:** KVBackend trait sync-only — v1-stabilization fork (lock sync vs convert to RPITIT) was unsurfaced and risked silent SemVer-lock at v1.
**Evidence:** The forward-decision this issue asked to surface has been made and frozen in the phase-4-meta-core v1-beta interface freeze: docs/V1-FROZEN-INTERFACE.md §4.63 (landed via G-CORE-9 R6, PR #1356) reads 'KVBackend: Send + Sync — DECIDED sync (NOT RPITIT) ... future-Composing-async migration is an additive AsyncKVBackend trait.' Path A is explicitly locked. Backlog row docs/future/phase-4-backlog.md §4.43/§4.63 + lines 828/988 carry the AsyncKVBackend post-v1 plan. Umbrella #1207 closed via #1240 (a
**Closing comment:**
Closing — this closure-pin's forward decision is now surfaced AND frozen. #986 was kept open as a `closure-pin` (Refs-only, named-deferred at PR #1240/#1237; umbrella #1207 closed via ac745e6) so the v1-stabilization fork would be adjudicated rather than silently locked. That fork has now been decided and frozen.

`docs/V1-FROZEN-INTERFACE.md` §4.63 (section 3, Backend-trait SemVer-locks; row authored in PR #1344 / G-CORE-9 FREEZE wave, consolidated through PR #1356, both ancestors of current main) records: `crates/benten-graph/src/backend.rs:306` `KVBackend: Send + Sync` — DECIDED sync (NOT RPITIT); future-Composing-async migration is an additive `AsyncKVBackend` trait. That is Path A from this finding. Code at HEAD matches: `pub trait KVBackend: Send + Sync` with sync `fn get(&self, key: &[u8]) -> Result<...>` at backend.rs:306.

The post-v1 plan is tracked at docs/future/phase-4-backlog.md §4.63 (lines 983-991) + the §4.43 v1-API-stabilization sub-bullet (line 828, "KVBackend async-shape fork — Path A ... + AsyncKVBackend post-v1"). The fork is no longer silent.

### #1014
**Finding:** Registry trait surface forward-architecture-shipped at Phase-4-Foundation locks publish-of-body/closed-enum/single-error-code shape pre-Atrium-substrate — v1-API-freeze hazard.
**Evidence:** crates/benten-platform-foundation/src/registry.rs:12-23 + 68-73 — the pub trait Registry was RETRACTED ('No `Registry` trait at v1 (Fwd-2 #1014 RATIFIED Path A, 2026-05-15)'). grep confirms there is no `pub trait Registry` in the file. The trait is no longer part of the v1 public API surface, removing the shape-lock-in this issue names; the concrete data shapes (RegistryEntry/DiscoveryQuery/DiscoveryResult) remain as reserved anchors and a trait will be introduced only if/when a real second impl
**Closing comment:**
Resolved in current main (`d91896b3`). The paper-only `trait Registry` this issue flagged — the surface that froze a publish-of-body / closed-enum / single-error-code shape pre-Atrium-substrate — has been retracted per the ratified Path A decision ("match docs to code", Ben 2026-05-15). Verified in the HEAD blob: `git show HEAD:crates/benten-platform-foundation/src/registry.rs | grep "trait Registry"` finds no trait definition; the module-doc (registry.rs:12-23) and the in-file NOTE (registry.rs:68-73) explicitly document the retraction ("No `Registry` trait at v1 (Fwd-2 #1014 RATIFIED Path A)").

The concrete data shapes (`RegistryEntry`, `DiscoveryQuery`, `DiscoveryResult`) + the reserved `E_REGISTRY_DISCOVERY_TIMEOUT` ErrorCode (`timeout_error_code()`) deliberately remain as stable anchors — this is exactly Path A, which kept the data shapes while removing the shape-locking trait. The forward-trajectory hardening (CID-keyed announce, `#[non_exhaustive]` DiscoveryQuery, richer error vocabulary, introduce a trait only if a genuine second impl materializes) is recorded as the Phase-4-Meta task at `docs/future/phase-4-backlog.md §3.1` (Registry-trait-reconsideration paragraph), to be decided against the actual Phase-8 trajectory rather than the retracted v1 abstraction.

The v1-API-freeze trait-shape hazard this issue named is removed. Closing as resolved. (Note: the earlier closure-pin comment referenced bundle #1198, which doesn't resolve to a PR; closing on resolved-in-code instead.)

### #1065
**Finding:** InstallResult vs InstallOutcome v1-public-API duplication + module_ecosystem deprecated install path scheduled Phase-4-Meta deletion = breaking surface change at Phase-4-Meta opening.
**Evidence:** PR #1310 (G-CORE-0, commit 48ba69b2) deleted module_ecosystem::install_plugin + install_plugin_persisting_did + their InstallResult / InstallerShape supports (commit body + diff shows '-pub struct InstallResult', '-pub fn install_plugin', '-pub fn install_plugin_persisting_did'). In current main, crates/benten-platform-foundation/src/module_ecosystem.rs contains no pub fn install_plugin and InstallResult appears only once, in a doc-comment recording the deletion (lines 13-23). The dual-install-p
**Closing comment:**
Resolved in current main (HEAD d91896b3). PR #1310 (G-CORE-0, merge commit 48ba69b2, per plan §1.A.FROZEN item 7 / docs/future/phase-4-backlog.md §4.33) deleted the legacy `module_ecosystem::install_plugin` + `install_plugin_persisting_did` functions and their `InstallResult` / `InstallerShape` supports via HARD-RULE-12 clause-(a) (delete, not just deprecate — strictly stronger than the `#[deprecated]` minor fix this issue proposed).

Verified in current main:
- `crates/benten-platform-foundation/src/module_ecosystem.rs` contains no `pub fn install_plugin`, no `pub fn install_plugin_persisting_did`, no `pub struct InstallResult`, no `module_ecosystem::InstallerShape`. The module now exposes only upgrade-time/trust auxiliaries (`verify_install_record`, `decide_upgrade_consent`, `verify_upgrade_author_continuity`, `new_version_available_code`, `check_author_trust`). The sole remaining `InstallResult` token (line 19) is inside the G-CORE-0 deletion-note doc-comment (lines 11-23).
- The canonical install path is `plugin_lifecycle::install_plugin` (`plugin_lifecycle.rs:917`) returning `InstallOutcome` (`plugin_lifecycle.rs:828`); `InstallerShape` has a single canonical home at `plugin_lifecycle.rs:594`.
- The dual install path + the `InstallResult`-vs-`InstallOutcome` parallel-return-type duplication this issue flagged no longer coexist in the public surface; the silent-semver-break liability is moot because `InstallResult` is gone entirely.
- 4 legacy test files were migrated to the canonical path; the §4.33 backlog row is marked CLOSED at G-CORE-0; the verify-pass arms in `tests/tf_g_core_0_legacy_install_path_deletion_4_33.rs` are un-ignored and green.

Closing as resolved (closure-pin → bundle PR #1310 merged).

Note (non-blocking, separate doc residual): `crates/benten-platform-foundation/INTERNALS.md:63` still carries a stale paragraph describing module_ecosystem as hosting `InstallResult`/`InstallerShape` with the legacy functions "remain[ing]" — this contradicts the correct deletion record at INTERNALS.md:184 and is a doc-coupling cleanup item, not part of this finding's code-level defect.

### #1143
**Finding:** benten-caps chain-validator + UCAN reorder bundle: Fwd-1 #946 per-step clones + Safe-4 #641 N+1-KV-gets snapshot race.
**Evidence:** Both leaf issues CLOSED. #946: commit 2b5713db (drained via batch PR #1290 / merge 20fb5c78, ancestor of HEAD) added step_outside_envelope(&DelegationStep) helper consolidating the two StepOutsideEnvelope construction sites — verified at crates/benten-caps/src/manifest_envelope_chain_validation.rs:226-231 + call sites :313,:346. Clones remain on the denial path only (owned Did/String for 'static ErrorCode). #641: DISAGREE-WITH-EVIDENCE (durable revocation is append-only/monotonic so the N+1 race
**Closing comment:**
Bundle fully dispositioned on current main — closing.

Both constituent leaves are CLOSED:

- Fwd-1 #946 (Track 1, clone consolidation): DELIVERED and verified live. The two `StepOutsideEnvelope` construction sites in `validate_chain_with_manifest_envelope` are consolidated into one `step_outside_envelope(&DelegationStep)` helper — `crates/benten-caps/src/manifest_envelope_chain_validation.rs:226-231`, called at `:313` (chain-integrity `next`-attribution) and `:346` (`OutsideEnvelope` `step`-attribution). These are the only two production construction sites; the happy path stays zero-alloc and the owned `Did`/`String` clones (required for the `'static` `ErrorCode` boundary) remain on the denial path only. Code reached main via the per-crate full-drain batch PR #1290 (merge 20fb5c78, ancestor of HEAD); the pre-squash standalone commit was 2b5713db.
- Fwd-1 #946 (Track 2, cap-policy critical-path bench-coverage gap): named-deferred to Phase-4-Meta perf-pass at `docs/future/phase-4-backlog.md` §4.84 (HARD-RULE clause-(b) destination).
- Safe-4 #641: dispositioned DISAGREE-WITH-EXPLANATION — durable revocation is append-only/monotonic (no un-revoke API), so the N+1-gets-without-snapshot pattern is fail-CLOSED-only (a revoke landing mid-walk can only make a LATER token reject the chain, never admit one that should reject). Locked by regression pin `durable_revocation_is_monotonic_so_n_plus_1_race_is_fail_closed_only_1143_641` at `crates/benten-caps/tests/refinement_audit_2026_05_caps_lane_resolved_on_main_pins.rs:100` (exercises the real `UCANBackend::revoke`/`is_revoked` arm; passes on current main).

Both leaf issues #946 and #641 were formally closed on 2026-05-18 as `phase-4-meta-deferred` close-as-named destinations via PR #1294 (main 39a0e732). With both leaves closed and the work dispositioned (Track-1 delivered, Track-2 named-deferred, #641 disagreed-with-pin), this bundle umbrella is complete — closing per the bundle-umbrella precedent (#1214/#1221/etc.).

### #1144
**Finding:** Add #[non_exhaustive] to WriteAuthority, ChangeEvent, and OperationNode (pre-v1 SemVer-readiness; completes the 3-instance benten-core pim-N pattern).
**Evidence:** All 3 sub-issues CLOSED (#837, #1015, #1024). Verified in current main d91896b3: #[non_exhaustive] present on WriteAuthority (lib.rs:122-123), ChangeEvent (change_stream.rs:123-124), and OperationNode (subgraph.rs:195-196). #1015/#1024 closed by PR #1290; #837 closed in-lane. Full bundle scope landed.
**Closing comment:**
Resolved in main (d0ccf606). All three `#[non_exhaustive]` attributes are present in benten-core:
- `WriteAuthority` enum — src/lib.rs:122 (landed in PR #1295, a15d6af4)
- `ChangeEvent` struct — src/change_stream.rs:123, with SemVer-rationale docstring (landed in the ST-CORE lane, #1262/86e1edb3)
- `OperationNode` struct — src/subgraph.rs:195 (landed in PR #1295, a15d6af4)

All three sub-issues are closed: #1015 (via #1262), #837 and #1024 (code landed in #1295; closed-as-named 2026-05-18). Each shape carries the documented SemVer-readiness rationale per the acceptance criteria. Full bundle scope landed — closing.

### #1148
**Finding:** benten-caps DoS-via-unbounded-decode + silent-error hardening bundle (META #629 sub-cluster): #543 #546 #559 #492 #497.
**Evidence:** All 5 leaf issues CLOSED; landed via commit e2b03deb (batch PR #1290 / merge 20fb5c78, ancestor of HEAD). Verified in current main: #543 MAX_CHAIN_DEPTH=64 const + ChainValidationOutcome::ChainTooDeep + CapChainTooDeep mapping (manifest_envelope_chain_validation.rs:154,163,199,280); #492 tracing::warn on un-decodable proofs in iter_installed_proofs (backends/ucan.rs:356,380); #497 typed_cap_permitted_by_proof disambiguates BackendStorage failure (fail-CLOSED) from denial (ucan_grounded.rs:311); 
**Closing comment:**
All five constituent leaves are closed and verified against current main (landed via PR #1290, squash-merge commit 20fb5c78, an ancestor of HEAD; the underlying fix work is e2b03deb "fix(benten-caps): drain #1148").

- #543 (DoS chain-depth cap): `MAX_CHAIN_DEPTH = 64` named constant; the `chain.len() > MAX_CHAIN_DEPTH` check fires BEFORE any per-step manifest lookup, returning `ChainValidationOutcome::ChainTooDeep` which maps to `ErrorCode::CapChainTooDeep` (E_CAP_CHAIN_TOO_DEEP) — O(1) on the attack input, fail-CLOSED. Existing error code reused intentionally (no new mint), satisfying the "typed error on exceed" intent. crates/benten-caps/src/manifest_envelope_chain_validation.rs:154,163,199,280.
- #492 (silent cbor-decode drop): `iter_installed_proofs` now emits `tracing::warn!` (target benten_caps::ucan_backend, error_code, key, error) on un-decodable durable entries; the non-fatal skip is preserved but now forensically observable. crates/benten-caps/src/backends/ucan.rs:356,380.
- #497 (swallowed chain-validation error): `typed_cap_permitted_by_proof` now disambiguates a `CapError::BackendStorage` failure (propagate + warn, fail-CLOSED) from a genuine security denial (iterate to next proof) — pre-fix `is_err(){continue}` swallowed both. crates/benten-caps/src/ucan_grounded.rs:385.
- #546 (fail-OPEN audience-less fallback): `principal_did_from_context` returns 3-state `PrincipalResolution`; a present-but-malformed `actor_hint` resolves to `Malformed` and fails CLOSED with `CapError::UcanAudienceMismatch` + warn instead of silently degrading to the audience-less walk. crates/benten-caps/src/ucan_grounded.rs:112-167,466-479.
- #559 (dev_revoke_key cross-namespace collision): `dev_revoke_key` is fully deleted from src (zero source hits; remaining references are doc/regression-pin entries confirming the deletion under the COLLAPSE spine #1251). Revocation is now per-UCAN-CID via the single `g14b:revoked:<ucan_cid>` BLAKE3-keyed marker — no untrusted DID prefix.

Substantive closure-pins accompany each fix (exercise the real arm, fail-on-revert). Bundle complete — closing.

### #1149
**Finding:** Hyg-1 dead-code sweep: delete Subgraph::is_deterministic duplicate, 4 unused DagVersionChain accessors, and SubscriberId::into_cid.
**Evidence:** All 3 sub-issues CLOSED (#284, #287, #290) in-lane via #1262 (86e1edb3). Verified in current main d91896b3: the Subgraph-level is_deterministic duplicate is gone (only PrimitiveKind::is_deterministic — a distinct semantic classifier — remains; Subgraph::is_declared_deterministic kept at subgraph.rs:344); DagVersionChain in version_chain.rs no longer exposes descendants/all_cids/is_singleton/root (#287); SubscriberId::into_cid has zero occurrences crate-wide (#290).
**Closing comment:**
Resolved in main (d0ccf606); all three sub-issues (#284/#287/#290) are CLOSED. Verified directly in code:

- #284: there is no `Subgraph::is_deterministic` method. The only `is_deterministic` in benten-core is `PrimitiveKind::is_deterministic` (subgraph.rs:128) — a distinct Invariant-9 primitive-determinism classifier, not the deleted duplicate. The canonical `Subgraph::is_declared_deterministic` is retained at subgraph.rs:344. The `Subgraph::is_deterministic` alias deletion is recorded in commit 86e1edb3.
- #287: none of `descendants`/`all_cids`/`is_singleton`/`root` exist as methods on `DagVersionChain` (version_chain.rs fn set: code/new/add_version/is_ancestor_of/is_descendant_of/tips/len/is_empty/current/set_current). Remaining textual hits are stale module-level doc comments only.
- #290: zero `into_cid` occurrences anywhere in crates/. `SubscriberId` exposes only `from_cid` and `as_cid` (change_stream.rs:53-69).

The bundle's sub-issues were formally closed via the ST-CORE lane (commit d58a68c9), which landed in main through the squash-merged batch PR #1277 (merged 2026-05-17); that commit notes the deletions were already on main from prior PRs. Closing the umbrella.

### #1151
**Finding:** benten-caps hygiene + dead-code sweep bundle: Hyg-1/2/3/4 (#298 #300 #303 #358 #453 #458 #480).
**Evidence:** All 7 leaf issues CLOSED; landed via commit ee0759e6 (batch PR #1290 / merge 20fb5c78, ancestor of HEAD). Verified on main: #298 fn cid_of deleted (grep zero hits in backends/ucan.rs; graph_backend retained — now consumed by #492 pin); #300 with_authority gone (zero src hits); #303 manifest_delegates_anything gone (zero src hits); #453 brittle line-cite removed from the wasm32 compile_error mirror comment (manifest_envelope_chain_validation.rs:50-55 now symbol-based); #358/#458/#480 stale narrat
**Closing comment:**
All seven constituent leaves are CLOSED and verified gone in current main (d0ccf606). The hygiene sweep landed via commit ee0759e6 ("drain #1151") on batch/d2-d10-megabatch, squash-merged as PR #1290 (merge 20fb5c78, an ancestor of current main). The umbrella stayed open only because PR #1290 cited the leaves individually, not this bundle.

Re-verified on main:
- #298: dead `pub UCANBackend::cid_of` deleted (zero callers; no fn in crates/benten-caps/src/backends/ucan.rs). The other two methods are no longer dead: `validate_chain_with_durable_revocations` was DELETED by the COLLAPSE spine (#1230 perpetual-DoS BLOCKER closure; only a module-doc note at ucan.rs:29 records the removal), and `graph_backend` is now consumed by the #492 closure-pin (retained at ucan.rs:259).
- #300: `WriteContext::with_authority` gone from caps src (swept by the #1269 CapWriteContext rename) — zero src hits.
- #303: `manifest_delegates_anything` deleted from caps src — zero src hits.
- #453: the brittle line-number cite was replaced with a symbol reference to `validate_chain_with_manifest_envelope` in the wasm32 compile_error mirror comment (crates/benten-caps/src/manifest_envelope_chain_validation.rs; note recorded ~line 70).
- #358: bench wallclock_toctou_refresh.rs has a real, clean module-doc — no stale R3 "returns todo!()" narrative.
- #458: phantom test-file cite gone from grant_backed.rs; current cites point to real files.
- #480: device_dispatch.rs module-doc (in tests/, not src/) states (c) was relocated to benten-engine + is GREEN + no longer #[ignore]'d.

Bundle complete — closing.

### #1152
**Finding:** p0 boundary hardening (META #629 slice): recursion-depth + size caps on untrusted decode, wasm32 checked-arithmetic on base32 decode, handle_of misroute fix, and explicit error arms for 5 saturating numeric conversions.
**Evidence:** Three of four subs fixed and verified in main d91896b3: #554 depth cap (MAX_VALUE_DECODE_DEPTH in value.rs:102+, budgeted ValueVisitor) plus tests/boundary_decode_caps.rs; #560 checked_mul in base32_lower_nopad_decode (lib.rs:704); #495 handle_of now returns Option<NodeHandle> (subgraph.rs:407). Closed via #1277 (9b0f327f). But #506 (5 saturating *::try_from().unwrap_or(MAX) sites at subgraph.rs lines 727/788/835/849/897) was closed as 'Phase-4-Meta close-as-named' (phase-4-backlog Wave-E W20 vi
**Closing comment:**
All four sub-issues are landed and verified in main (115b0847). Closing the umbrella.

META #629 boundary-decode core (PR #1277, 9b0f327f) plus the depth-cap arm (536c9208) and the #506 builder closure (PR #1325, 132dde91, G-CORE-6a verify-pass):

- #554 — Value decode now enforces a structural recursion-depth cap `MAX_VALUE_DECODE_DEPTH = 64` on the budgeted ValueVisitor (value.rs:139), in addition to the 16 MiB `Subgraph::MAX_DECODE_BYTES` ceiling before decode (subgraph.rs:519/533). Substantive pins at tests/boundary_decode_caps.rs cover both the depth-cap and size-cap arms (each fails on revert).
- #560 — `base32_lower_nopad_decode` capacity calc uses `checked_mul` (lib.rs:704); `Cid::from_str` rejects over-length strings before decode (wasm32 usize-overflow / unbounded-alloc defense).
- #495 — `SubgraphBuilder::handle_of` returns `Option<NodeHandle>` (subgraph.rs:407) instead of silently misrouting to NodeHandle(0).
- #506 — closed not as a doc-only deferral but code-fixed in #1325: the three semantic-property-bearing saturating sites (push handle-slot, iterate `max_iterations`, iterate_parallel `parallel_fanout`) now record a deferred `CoreError::ValueOutOfRange { field, value, bound }` (lib.rs:849, mapped to `ErrorCode::ValueOutOfRange` / `E_VALUE_OUT_OF_RANGE` with a TS class mirror) surfaced at the single fallible point `SubgraphBuilder::build(self) -> Result<Subgraph, CoreError>` (subgraph.rs:1221). The two remaining `i64::try_from(...).unwrap_or(i64::MAX)` sites are `wait_signal_with_timeout` / `wait_duration`, explicitly categorized benign-leave-as-is per #506's own per-site disposition (a Duration exceeding i64::MAX ms is ~292M years; documented inline at subgraph.rs:992/1018).

All four sub-issues are CLOSED. The benten-core slice of META #629 is complete.

### #1154 ⚠️COMMENT-FLAGGED(acc=False)
**Finding:** benten-caps Qual-1 + Qual-2 simplicity sweep bundle: #661 #668 #674 #683 #793 #803 #816.
**Evidence:** All 7 leaf issues CLOSED. Self-contained items via commit 517fe4f1 (batch PR #1290): #816 ChainAnchor enum deleted (no constructor in src; lib.rs:180 + manifest_envelope_chain_validation.rs:105-109 document the zero-consumer delete); #803 derive_scopes_for_pending_ops extracted from check_write (grant_backed.rs:328); #674 wallclock_refresh_ceiling_for handled (note: later re-introduced at lib.rs:236 for a real post-FP-B consumer — orthogonal); iterate_batch_boundary_for kept DISAGREE-WITH-EVIDEN
**Closing comment:**
All seven constituent leaves are closed with documented dispositions, verified on current main.

Self-contained caps-only items landed via PR #1290 (squash 20fb5c78, drain commit 517fe4f1):
- #816 — dead pub `ChainAnchor` enum deleted; zero consumers crate-wide (no enum def / no `ChainAnchor::` constructor in src; documented at crates/benten-caps/src/manifest_envelope_chain_validation.rs:105-109, lib.rs:180).
- #803 — `derive_scopes_for_pending_ops` extracted verbatim from `check_write` (crates/benten-caps/src/grant_backed.rs:328, consumed at :469; 4 closure-pins).
- #674 — zero-consumer `evaluator_delegation::wallclock_refresh_ceiling_for` wrapper deleted (later re-introduced at lib.rs:236 for a real post-FP consumer — orthogonal); `iterate_batch_boundary_for` retained DISAGREE-WITH-EVIDENCE as a live `benten_engine::primitive_host` accessor (lib.rs:222).
- #668 — DISAGREE-WITH-EVIDENCE: the dead `with_config` it named is already gone; the two remaining `GrantReaderChain` test constructors (default-convenience + explicit-config) are the idiomatic pair and were kept intentionally.

Cross-crate / production-wiring-gated items:
- #793 — actually code-fixed (not deferred): the misleading `with_now_for_test` production seam was renamed to `with_now_secs` via PR #1295 (commit 0948d203, P-II naming sweep); `with_now_for_test` survives only in a historical doc-comment at crates/benten-caps/src/ucan_grounded.rs:255.
- #661 — `AsAttenuationScope` -> `AsRef<str>` is a true cross-crate cascade (benten-eval call site); dispositioned BELONGS-NAMED-NOW to the P-II rename queue, home at docs/future/phase-4-backlog.md §4.85 / §4.145 (trait intentionally retained pending P-II).
- #683 — `testing::WallclockProbe` is a live test+bench surface; full retire is production-wiring-gated (WriteContext::now threading); BELONGS-NAMED-NOW to docs/future/phase-4-backlog.md §4.85.

Every leaf is closed via code-fix, DISAGREE-WITH-EVIDENCE, or HARD-RULE-compliant BELONGS-NAMED-NOW. Bundle complete — closing.

### #1155
**Finding:** Surf-2 doc-completeness: add foundational-type doctests, enumerate ChangeStream # Errors codes, strip phase-internal narrative from ChangeEvent docstring, delete dead import, and document idempotent-duplicate-edge behavior.
**Evidence:** All 5 subs CLOSED (#892, #894, #896, #897, #902). Verified in main d91896b3: `cargo test --doc -p benten-core` runs 12 doctests (9 pass, covering Cid, Node, Hlc, Subgraph, Value, Edge, DagVersionChain, VersionDag) satisfying the >=7 foundational-type criterion (#892); ChangeStream trait has # Errors sections on its methods (change_stream.rs:230/248/266) (#894); no R6FP/wave-8c/Round-2 process narrative remains in change_stream.rs (#896); 'use alloc::string::String' is gone from version.rs (#897)
**Closing comment:**
Resolved in current main (d0ccf606). Independently verified in code, all five sub-issues closed:

- #892 — `cargo test --doc -p benten-core` now runs 12 doctests (9 pass / 3 ignored), covering 8 foundational types: Cid (lib.rs:396), Node (lib.rs:175), Hlc (hlc.rs:194), Edge (edge.rs:43), Value (value.rs:50), Subgraph (subgraph.rs:260), DagVersionChain (version_chain.rs:100), VersionDag (version_dag.rs:219) — meeting the >=7 criterion.
- #894 — ChangeStream trait methods enumerate catalog codes in `# Errors` (change_stream.rs:230/248/266): subscribe lists E_SUBSCRIBE_PATTERN_INVALID / E_SUBSCRIBE_DELIVERY_FAILED; next_event lists E_SUBSCRIBE_CURSOR_LOST / _REPLAY_WINDOW_EXCEEDED / _REVOKED_MID_STREAM / _DELIVERY_FAILED; unsubscribe lists E_SUBSCRIBE_DELIVERY_FAILED.
- #896 — phase-internal process narrative (R6FP / wave-8c / Round-2 / Instance 6) removed from the ChangeEvent docstring; it now carries only the multi-label-matching contract and SemVer rationale.
- #897 — dead `use alloc::string::String;` deleted from version.rs.
- #902 — add_version documents its idempotent-duplicate-edge behavior under `# Idempotent on duplicate edges` (version_chain.rs:144).

Closing.

### #1156
**Finding:** benten-caps mutex-poison + rate-limit + WriteContext disambiguation bundle: #503 #626 #631 #885.
**Evidence:** All 4 leaf issues CLOSED; landed via ST-CAPS lane (commits 54ed3d1f / 0bba1d6b, batch PRs #1282 / #1269, ancestors of HEAD). Verified on main: #503/#631 five .expect("rate-limit state mutex poisoned") sites replaced with .unwrap_or_else(std::sync::PoisonError::into_inner) recovery (rate_limit.rs:283,315,347,381,460), matching engine_caps discipline; regression pin poisoned_state_mutex_recovers_and_keeps_enforcing (rate_limit.rs:444). #626 clock sampled INSIDE the mutex critical section (let now 
**Closing comment:**
All four constituent leaves are CLOSED and verified on current main.

- **#503/#631 (mutex-poison recovery):** all four `InMemoryRateLimitPolicy` lock sites use `.unwrap_or_else(std::sync::PoisonError::into_inner)` graceful recovery, matching the `engine_caps` discipline (`crates/benten-caps/src/rate_limit.rs:283,315,347,381`). No `.expect()`/`.unwrap()` on `.lock()` remains anywhere in the crate. Regression pin `poisoned_state_mutex_recovers_and_keeps_enforcing` (rate_limit.rs:444) poisons the mutex, asserts it is poisoned, then asserts continued budget enforcement — documented to FAIL if `.expect("rate-limit state mutex poisoned")` were restored.
- **#626 (clock inside mutex):** the clock is now sampled inside the state-mutex critical section (`let now = (self.clock)()` at rate_limit.rs:284, immediately after lock acquisition at :280-283), eliminating the sub-window-boundary discrepancy; injection seam is `with_clock` (:254).
- **#885 (WriteContext disambiguation):** `benten_caps::WriteContext` renamed to `CapWriteContext` (`crates/benten-caps/src/policy.rs:158`); `benten_graph::WriteContext` retained (`crates/benten-graph/src/lib.rs:943`), resolving the dual-type name collision (276 refs across 55 files migrated).

Landed via merged Strategy-C batch PRs #1282 (ST-CAPS lane, commit 54ed3d1f) and #1269 (engine.caps() canonical surface, commit 0bba1d6b), both in main. Bundle complete — closing.

### #1158
**Finding:** Five Cid/Anchor/ChangeEvent v1-API-stabilization decisions: rewrite Cid drop-in-migration docstring (#995), delete u64-id Anchor surface (#1003), reconsider Cid::from_blake3_digest name (#1033), rename ChangeEvent::legacy_minimal (#990), unify three Anchor shapes (#849).
**Evidence:** Three subs implemented and verified in main d91896b3: #1003 u64-id Anchor surface deleted (lib.rs:893 deletion comment + tests/u64_anchor_surface_deleted_1003.rs) via #1290 (b3d3bbd4); #990 legacy_minimal removed (zero occurrences); #995 Cid docstring rewritten to 'not a drop-in swap' (lib.rs:388). But #1033 (Cid::from_blake3_digest still present at lib.rs:456) and #849 (three Anchor shapes) are both labelled phase-4-meta-deferred and closed-as-named — NOT code-fixed. PR #1290 lists #849 as belo
**Closing comment:**
All five sub-decisions are ratified and discharged; closing the umbrella. Verified in current main:

- #995 (Cid drop-in framing) — docstring rewritten to state migration is NOT a drop-in swap (Copy-vs-heap shape ripple), byte layout frozen either way (crates/benten-core/src/lib.rs:384-392). Code-fixed.
- #1003 (u64-id Anchor surface) — the legacy crate-root u64-id Anchor surface (Anchor / append_version / current_version / walk_versions / U64_CHAINS / ANCHOR_COUNTER) was DELETED with no deprecated shim (PR #1290, b3d3bbd4); behavioural regression pin at crates/benten-core/tests/u64_anchor_surface_deleted_1003.rs. Code-fixed.
- #990 (ChangeEvent::legacy_minimal) — renamed to ChangeEvent::minimal with workspace callsite migration (PR #1295); zero legacy_minimal occurrences remain (now crates/benten-core/src/change_stream.rs:165). Code-fixed.
- #849 (three coexisting Anchor shapes, no shared trait) — SHIPPED, not deferred: the unified version_dag::VersionDag composability surface (one type + opt-in Mode::{Strict,Dag} + shared VersionChain trait + one CURRENT semantic) landed via PR #1309 (G-CORE-5, merged 2026-05-22, "Closes #849"); crates/benten-core/src/version_dag.rs. The two underlying per-pattern impls remain for existing callers; callsite migration is downstream G-CORE-6/9 work. Backlog §4.79 marks this SHIPPED.
- #1033 (Cid::from_blake3_digest codec-in-name) — RATIFIED Option B = keep the name as a deliberate settled choice; baked-in commitment #5 permanently settles BLAKE3/DAG-CBOR/CIDv1, so the codec in the constructor name is intentional, not a leak. Zero-LOC API change; nothing to fix (DECISION-1158 doc; backlog §-row).

Every sub-issue is closed and every fork has a ratified disposition, so the decision-batch umbrella is fully discharged.

### #1160
**Finding:** Inv-14 / Inv-13 string-mirror discipline: single source of truth for ATTRIBUTION_PROPERTY_KEY re-exported to benten-eval, route 4 LABEL_CURRENT/LABEL_NEXT_VERSION consumers through the constant, add drift-defense test pin.
**Evidence:** Both subs CLOSED (#597, #601) via #1277. Verified in main d91896b3: ATTRIBUTION_PROPERTY_KEY declared once (subgraph.rs:49) and re-exported via `pub use benten_core::ATTRIBUTION_PROPERTY_KEY` in benten-eval (invariants/attribution.rs:35), with drift-defense test tests/attribution_mirror.rs present (#597); LABEL_CURRENT/LABEL_NEXT_VERSION defined once (lib.rs:902/905) and consumers route by name through benten_core::LABEL_NEXT_VERSION (ivm algorithm_b.rs:123-124, views/version_current.rs:66/182, 
**Closing comment:**
Resolved in main (d0ccf606) via PR #1277 (commit d58a68c9), which closed both sub-issues #597 and #601.

Verified in current main:
- #597 — `ATTRIBUTION_PROPERTY_KEY` has a single source of truth in benten-core (`crates/benten-core/src/subgraph.rs:49`, `pub const ATTRIBUTION_PROPERTY_KEY: &str = "attribution"`), re-exported into benten-eval via `pub use benten_core::ATTRIBUTION_PROPERTY_KEY` (`crates/benten-eval/src/invariants/attribution.rs:35`). No independent redeclaration remains.
- #601 — `LABEL_CURRENT` / `LABEL_NEXT_VERSION` are defined once in `crates/benten-core/src/lib.rs:902/905`; production consumers route through the constants by name (benten-ivm `algorithm_b.rs:123-124`, `views/version_current.rs:66/182`, `subgraph_spec.rs:177`, and benten-platform-foundation `plugin_library.rs:104` `EDGE_CURRENT`). The only remaining `"CURRENT"`/`"NEXT_VERSION"` string literals are the constant definitions themselves plus a docstring example in `edge.rs:61` — no production string-literal duplicates.
- Drift-defense pin present and substantive: `crates/benten-core/tests/attribution_mirror.rs` freezes the wire-string values for the attribution key and both version edge labels (would-FAIL-on-drift).

All acceptance criteria met. Closing the umbrella.

### #1161
**Finding:** benten-id trait extraction bundle: CanonicalBytes trait (#759) + RevocationSet trait (#787) plus Fwd-1 perf members (#971/#973/#976).
**Evidence:** In main d91896b3, the CanonicalBytes trait IS extracted (crates/benten-id/src/canonical_bytes.rs:42, landed via ST-ID lane / #1290). The RevocationSet trait does NOT exist — grep for 'trait RevocationSet' across crates/benten-id/src returns zero hits. So only one of the two named traits in the bundle was extracted; the #787 RevocationSet half + the coupled Fwd-1 perf members are not landed.
**Closing comment:**
Closing — all five member issues of this Pattern-F bundle are resolved and CLOSED.

- **#759 CanonicalBytes (Qual-2)** — EXTRACTED. `pub trait CanonicalBytes` at `crates/benten-id/src/canonical_bytes.rs:42` (re-exported at crate root), implemented for `RotationAttestation` / `UcanClaims` / `CredentialClaims` / `DeviceAttestation`, byte-identical to the prior per-site encodings. Landed via PR #1282 (ST-ID lane; "Closes #759").
- **#787 RevocationSet (Qual-2)** — resolved as moot, no trait extracted by design. Post-COLLAPSE the substrate basis eroded: `Acceptor::nonce_store` was DELETED (no `nonce_store` in `crates/benten-id/src`) and the `validate_chain_with_*` walkers relocated to `benten-caps` (`crates/benten-caps/src/chain_authority.rs`). Only `RevocationRegistry` (`vc.rs:385`) + `RotationLog` (`did_rotation.rs:206`) remain — an insufficient 2-substrate basis for a forced trait. Documented at `crates/benten-id/INTERNALS.md:226`.
- **#971 / #973 / #976 (Fwd-1)** — CLOSED. #976's `Acceptor::nonce_store` key-narrowing and #973's `validate_chain_with_device_revocations` nested-loop are moot (those code sites were deleted / moved to benten-caps); #971's `canonical_bytes` recompute seam is covered by the CanonicalBytes extraction.

All five (#759/#787/#971/#973/#976) are CLOSED (reason=COMPLETED, 2026-05-17/18, batch consolidation #1282). The umbrella tracks only those members, so it is fully resolved. (Note: `docs/future/phase-4-backlog.md:1424` still lists these as "open" — that line is stale relative to the issue tracker and the INTERNALS.md reconciliation.)

### #1204
**Finding:** index.d.ts regenerate + CI parity gate (Surf-2 MAJOR): .d.ts was stale at HEAD and there was no CI gate failing on napi-surface change without .d.ts regen.
**Evidence:** Both acceptance criteria satisfied at HEAD d91896b3. (a) The CI parity gate landed: commit 13322df4 ("ci(packages-engine): #1204 TS-side public-API parity gate (G-CORE-9 row 2; JS analog of cargo-public-api)") added .github/workflows/ts-public-api.yml, whose header documents it as the '#1204 TS-side public-API parity gate', built+committed+FREEZE-FLIPPED at G-CORE-9 and now REQUIRED-FAILING (R6-FP-D), with branch-protection updated to require 'TS API drift detector'; baseline packages/engine/etc
**Closing comment:**
Resolved on main (d0ccf606). The #1204 TS-side public-API parity gate landed via commit `13322df4` as `.github/workflows/ts-public-api.yml` — the JS analog of `.github/workflows/cargo-public-api.yml` — with a baseline at `packages/engine/etc/public-api.txt`. The gate was FREEZE-FLIPPED from informational-only to REQUIRED-FAILING at R6-FP-D (2026-05-24): the diff step now `exit 1` on drift, and `.github/branch-protection.yml` (line 228) lists `TS API drift detector (required-failing per G-CORE-9 FREEZE)` in `required_status_checks`. The freeze record `docs/V1-FROZEN-INTERFACE.md` lists `13322df4` as delivering build-backlog row 2.

Scope note: at G-CORE-9 the original `bindings/napi/index.d.ts` framing was authoritatively reframed (item 10) to lock the exported `@benten/engine` public TS API; the gate diffs the exported surface across `dist/*.d.ts`. `bindings/napi/index.d.ts` itself is a gitignored napi-rs v3 build artifact (regenerated on every build, not git-tracked), and item 10's "What's NOT frozen" explicitly scopes out the `bindings/napi/src/*.rs` Rust source and non-exported internal interfaces. The §3.5g TS mirror for the new `installModuleSigned` surface is present at `packages/engine/src/engine.ts:252` (it lives on the non-exported `NativeEngine` interface, so it is not itself in the parity baseline, consistent with the item-10 scope). Closing.

### #1218
**Finding:** Bundle: complete the Strategy::C -> Strategy::Reserved rename (sub-issues #913 cross-doc cite-drift + #1086 ViewStrategyCReserved error-catalog variant).
**Evidence:** Both sub-issues resolved in main (d91896b3). #913: cross-doc cite-drift closed via Strategy-C batch PR #1235 (merged 336178a). #1086: the error-catalog rename was completed in phase-4-meta-core at Row D-19 G-COMP-1 Cohort 8 (PR #1356, commit 462dfe7f, ancestor of HEAD) as an atomic 4-surface rename, CATALOG_VARIANT_COUNT in-place. Verified in source: ErrorCode::ViewStrategyReserved + 'E_VIEW_STRATEGY_RESERVED' at crates/benten-errors/src/lib.rs:340,1607,2471; EngineError::ViewStrategyReserved at
**Closing comment:**
Both bundle sub-issues are resolved in main; closing this umbrella as drained.

#913 (cross-doc cite-drift) — CLOSED via Strategy-C batch PR #1235 (merged 336178a).

#1086 (Strategy::C error-catalog variant) — completed during phase-4-meta-core at Row D-19 G-COMP-1 Cohort 8 (PR #1356, commit 462dfe7f, verified ancestor of HEAD) as an atomic 4-surface §3.5g rename:
- benten-errors/src/lib.rs:340,1607,2471 — `ErrorCode::ViewStrategyReserved` + wire string `E_VIEW_STRATEGY_RESERVED` (variant + Display arm + parse arm)
- benten-engine/src/error.rs:122,572 — `EngineError::ViewStrategyReserved`
- packages/engine/src/errors.generated.ts:108,1325 — TS class `EViewStrategyReserved` + code `E_VIEW_STRATEGY_RESERVED`
- public-api baselines regenerated (docs/public-api/benten-errors.txt:192, benten-engine.txt:4461/12142) now lock `ViewStrategyReserved`

The Strategy variant was renamed in place (no new variant for the rename); the CATALOG_VARIANT_COUNT 194→197 bump pinned in benten-errors/tests/stable_shape.rs:1058 comes from the 3 co-landed DSL ErrorCode mints, not this rename. Regression-pinned by benten-dsl-compiler/tests/row_d19_first_class_catalog_mirrors.rs:114 (asserts the post-rename canonical wire string).

No `ViewStrategyCReserved` remains in crate src. Residual `Strategy::C` / `E_VIEW_STRATEGY_C_RESERVED` strings are all legitimate: rename-provenance docstrings, round-trip test arms, and the forensic pre-closure / breaking-change migration entries in V1-FROZEN-INTERFACE-DEFERRED.md + V1-BETA-BREAKING-CHANGES.md (plus stale packages/engine/dist/ build artifacts).

### #1225
**Finding:** Bundle: AlgorithmBView::materialize triple-source-of-truth documentation (#662) + dispatch_for verb-only naming (#758).
**Evidence:** Both sub-issues resolved in main (d91896b3). #662: materialize's three sources (walk_observable / inner.read / typed_output_projection) are now documented as each-load-bearing for a specific consumer (crates/benten-ivm/src/algorithm_b.rs docstring ~1246-1276, 'each load-bearing for a SPECIFIC consumer (not redundant)'; landed crate-drain 64b33e0d). #758: resolved by the D1 CanonicalViews seam in G-CORE-4 PR #1311 (commit c9c11c56) — the public engine-facing seam is now noun-bearing CanonicalView
**Closing comment:**
Both bundle sub-issues are resolved in current main (d0ccf606) and the bundle can be closed as drained. Both sub-issues are already CLOSED-as-completed.

#662 (CLOSED 2026-05-17): `AlgorithmBView::materialize`'s three sources are each documented as load-bearing for a specific consumer (not redundant) in `crates/benten-ivm/src/algorithm_b.rs` (~L1244-1276): walk_observable = canary observable (only emitted bytes; cross-registration-path determinism), inner.read = shape confirmation only (bytes discarded, variant asserted), typed_output_projection = register-time contract selecting the KernelOutput variant. This satisfies the acceptance criterion "each source documented as load-bearing for a specific consumer." Landed via the #1290 per-crate full-drain consolidation (20fb5c78).

#758 (CLOSED 2026-05-18): resolved by the D1 CanonicalViews seam in G-CORE-4 PR #1311 (c9c11c56). The verb-only `dispatch_for` is narrowed to `pub(crate)` as a crate-internal shim (algorithm_b.rs:358); the published engine-facing surface is now the noun-bearing `CanonicalViews` type (algorithm_b.rs:193) with `dispatch()` (L303), `lookup()` (L272), `is_canonical()` (L287) methods. Engine production call sites in `crates/benten-engine/src/engine_views.rs` use `benten_ivm::CanonicalViews::registry()` (the only remaining `dispatch_for` mentions outside benten-ivm are historical-provenance doc comments, not call sites).

### #1241
**Finding:** J8 ceiling-AND completion pin: add cap.resource discrimination to the envelope-ceiling, unified with the #669 manifest ceiling as ONE predicate, landing with COLLAPSE P5.
**Evidence:** The named completion pin landed exactly as scoped, in PR #1276 (MERGED, commit f68d94fd 'COLLAPSE P5 ... + #1241 cap-predicate'). The unified predicate envelope_ceiling_rejects_cap is the single shared helper (crates/benten-caps/src/chain_authority.rs:119), called with &cap.resource by both the device-envelope walker (:172) and the manifest walker (:277). The #1241 completion fn envelope_ceiling_first_rejected_resource (chain_authority.rs) is production-wired in the engine recheck at crates/bent
**Closing comment:**
Closing — the Ben-ratified v1 scope (2026-05-16: "(a) zone-scoped for v1 + THIS named completion pin landing with P5") is satisfied in merged main.

Landed in PR #1276 (MERGED 2026-05-17, commit f68d94fd):
- ONE unified ceiling predicate `benten_caps::chain_authority::envelope_ceiling_rejects_cap` (crates/benten-caps/src/chain_authority.rs:119) — NOT a parallel pipe (build-constraint iii). Consumed by both the device-envelope chain-walk (`validate_chain_with_envelope_ceiling`, :172) and the #669 plugin-manifest chain-walk (`validate_chain_with_manifest_ceiling`, :277).
- The #1241/F2 cap-predicate completion `envelope_ceiling_first_rejected_resource` (chain_authority.rs:325) discriminates on the writer's actual delegated `cap.resource` (not the synthetic `{zone}:write` scope the F2 finding flagged inert), feeding the SAME predicate.

Accuracy correction to the closure record: the cap-resource-complete engine seam `envelope_ceiling_admits_cap_resources` (crates/benten-engine/src/manifest_envelope_recheck.rs:473, which delegates to `envelope_ceiling_first_rejected_resource` at :480) is the READY shared mechanism but is NOT yet wired onto the live `apply_atrium_merge` path. The production inbound-merge path (crates/benten-engine/src/engine.rs:1620) continues to call the (a) zone-scoped `envelope_ceiling_admits_row` (row_scope = "{zone}:write") for v1, exactly as designed — the gap is verified INERT (F2-exploitability-investigation.md: the inbound data-zone write path never dispatches a SANDBOX host-fn). Threading the inbound writer's full UCAN delegation chain to the per-row merge seam is a new wire surface that was always §4b-deferred past v1; the completion (the unified predicate) is the deliverable, the wire-threading is the §4b-deferred mechanical residual. No second predicate remains to write when that future wire-threading lands.

### #1298
**Finding:** Weekly cargo-audit flagged the same 2 vulnerabilities as #204 — RUSTSEC-2026-0120 (hickory-net 0.26.0-beta.4) and RUSTSEC-2026-0119 (hickory-proto 0.26.0-beta.4), patched in >=0.26.1 — plus informational unmaintained/unsound warnings (GTK3 gtk-rs crates, paste, unic-*, proc-macro-error, glib).
**Evidence:** Same closure as #204: origin/main d0ccf606 Cargo.lock has hickory-net/hickory-proto/hickory-resolver all at 0.26.1; no 0.26.0-beta entries remain; bump landed in ancestor commit ae69c339 (PR #1307); deny.toml lines 47-51 document closure 2026-05-19 at G-CORE-2-FP-1 via iroh 0.98 -> 1.0.0-rc.0. The two hickory advisories are the only entries in vulnerabilities.list (count=2); the remaining items are informational warnings (unmaintained/unsound), which do not gate the audit and are not the actiona
**Closing comment:**
Resolved against current main (d0ccf606). The two gating vulnerabilities in this report — RUSTSEC-2026-0120 (hickory-net NSEC3 unbounded-loop DoS) and RUSTSEC-2026-0119 (hickory-proto O(n²) name-compression CPU exhaustion), both affecting 0.26.0-beta.4 and patched in >=0.26.1 — are no longer present. The committed Cargo.lock carries hickory-net, hickory-proto, and hickory-resolver all at 0.26.1, with zero 0.26.0-beta entries remaining.

The bump landed transitively in commit ae69c339 (PR #1307, "G-CORE-2 signature-agility integration crate"), an ancestor of main, via the iroh 0.98 -> 1.0.0-rc.0 upgrade which pulled hickory-resolver 0.26.0-beta.4 -> 0.26.1. See deny.toml lines 47-51 documenting the closure at G-CORE-2-FP-1 (2026-05-19) and the matching note in .github/workflows/supply-chain.yml's cargo-audit step.

The remaining entries in the report are informational unmaintained/unsound warnings only (gtk-rs GTK3 stack atk/atk-sys/gdk/gdk-sys/gdkwayland-sys/gtk/gtk-sys/gtk3-macros, paste, proc-macro-error, the unic-* family, atomic-polyfill — all unmaintained; glib — unsound). These are non-gating: cargo-deny surfaces advisories as warnings, and the belt-and-suspenders cargo-audit job runs `cargo audit --deny warnings` with every one of these IDs in its --ignore list (each transitive via tauri/iroh and tracked for closure in docs/future/phase-3-backlog.md §9 and phase-4-backlog.md §3.6). No actionable vulnerability remains against the current lockfile; the audit is green. Closing.


## CONFIRMED COMMENT — 71 issues


### #268
**Finding:** branch-protection.yml required-status-checks contexts frozen at Phase-2a/2b/3 era, missing always-on Phase-4-Foundation workflows (esp. bench.yml's two (required)-labelled jobs) plus an incomplete Excluded-and-why block and undecided msrv.yml disposition.
**Comment:**
Status update against current main (115b0847):

Partial progress — the contexts set is no longer frozen at the Phase-2a/2b/3 era. PR #1351 (commit 4b1333b1) added a "Phase-4-Meta-Core G-CORE-9 FREEZE additions" section promoting cargo-public-api.yml ("API drift detector (required-failing per G-CORE-9 FREEZE)", .github/branch-protection.yml:225) and ts-public-api.yml ("TS API drift detector (required-failing per G-CORE-9 FREEZE)", :228) to required-failing per the V1-FROZEN-INTERFACE freeze contract. Both context strings match the rendered job names verbatim (cargo-public-api.yml:42, ts-public-api.yml:54).

The core MAJOR finding remains OPEN, however:

1. (action item 1, still open) bench.yml triggers on every PR with no paths filter (.github/workflows/bench.yml:55) and its two jobs self-label "(required)" ("bench compile gate (required)" line 82; "bench CRUD fast-path APFS timing (required)" line 104), but neither context string appears in required_status_checks::contexts (grep -c = 0). A PR that breaks the CRUD fast-path APFS timing can still land cleanly.

2. (action item 2, still open) The "Excluded (and why)" block (branch-protection.yml:161-176) still enumerates only codeql, the path-filtered set, and schedule-only workflows; it does not enumerate the informational-by-design workflows (coverage / devserver-vitest / napi-vitest / packages-engine-vitest / cargo-vet / cite-drift) nor admin-shell-e2e.

3. (action item 3, still open) msrv.yml exists (context "cargo build --workspace --locked @1.95", .github/workflows/msrv.yml:44) and is not in the contexts set; the keep-or-retire disposition is unratified.

Keeping open.

### #349
**Finding:** 6+ pub test/bench-only helpers on Engine ship un-cfg-gated in the production cdylib.
**Comment:**
Status update (verified at main `d0ccf606`): partially addressed by the R6 R1 FP-A cfg-gating sweep (PR #1351, merged 2026-05-25). The 6 F-full crypto commits between the sweep and current HEAD do not touch these helpers.

Now resolved:
- `Engine::fabricate_test_suspend_envelope` + `..._with_attribution_cid_bytes` — gated `#[cfg(any(test, feature = "test-helpers"))]` (engine_wait.rs:929/978).
- `DidKeyedSession::consumed_nonce_count_for_test` — gated (thin_client.rs:548).
- `DidKeyedSession::active_session_count_for_test` (thin_client.rs:540) — reclassified as production-consumed (tools/benten-admin-shell/src/main.rs:62 boot-banner) and now an explicit `EXEMPT_PUB_ITEMS` entry, pinned by `tests/phase_3_workspace/for_test_symbols_are_feature_gated.rs`. (v1-GM follow-up: drop the misleading `_for_test` suffix.)

Still un-gated at HEAD (no `#[cfg]`, only `#[must_use]`/`#[doc(hidden)]`):
- `Engine::benchmark_helper_subgraph_cache_{cold,prewarm,warm,reregister_and_miss}` (engine_wait.rs:1444/1453/1473/1483).
- `cap_recheck::allow_all` (cap_recheck.rs:166) / `cap_recheck::deny_all` (cap_recheck.rs:178). (Note: their docstrings claim NoAuthBackend / fail-closed deployment uses — worth confirming these are genuinely test-only before gating, vs the original finding's assumption.)
- `cap_snapshot_hash::verify_legacy` (cap_snapshot_hash.rs:206) — `compute_legacy` is production-bound (engine.rs:2596, engine_wait.rs:857) but `verify_legacy` has only test callers.
- `WriteSpec::test_inject_failure` (subgraph_spec.rs:331).

The two `subgraph_cache_*` `[[bench]]` rows in `crates/benten-engine/Cargo.toml` (lines 399/403) still lack `required-features = ["test-helpers"]`.

These non-`_for_test`-suffixed helpers are not caught by the suffix-pattern pin (`for_test_symbols_are_feature_gated.rs` greps `_for_test*` only), so they remain in the production cdylib. Keeping open as a closure-pin for the residual (tracked under meta-issue #356).

### #355
**Finding:** Speculative pub surface minted ahead of consumers that never materialized — ~50 dead-pub sub-issues across 11 crates.
**Comment:**
Status update against current main (115b0847). The bulk of this Hyg-1 class was swept by the refinement-audit batch campaign that pre-dates phase-4-meta-core: of the 55 sub-issues tracked in the table above, 51 are now CLOSED (merged via #1282 spine4 / commit 6ed80b2f, #1290 d2-d10 per-crate full-drain, #1294 wave-e). Four remain OPEN: #296, #348, #312, #310.

Re-verifying the four against current main:

- #296 (benten-sync) — residual PRESENT: `MstDiffFrame` + `MST_DIFF_WIRE_VERSION` + `MstDiffSession::enqueue_frame` at `crates/benten-sync/src/mst_proto.rs:190,209,310` have no production consumer (only INTERNALS.md + the in-file test at line 451).
- #348 (benten-engine) — residual PRESENT: `SubgraphSpecBuilder::iterate` + `IterateBody` at `crates/benten-engine/src/subgraph_spec.rs:189,263` are still the Phase-1 no-op stub (`_max`/`_body` unused, returns `self`). The only compiling reference is the `#[ignore]`'d `tests/integration/compromises_regression.rs:47`; no production caller.
- #310 (bindings/napi) — residual PRESENT: `pub mod testing` at `bindings/napi/src/lib.rs:2406` is still the Phase-1 shim returning `Err(CoreError::NotFound)`/sentinels.
- #312 (benten-platform-foundation) — defect ALREADY FIXED IN CODE; issue is stale-open. #312's specific finding was the dead `MaterializerError::Backend(String)` + `MaterializerError::Other { code, message }` arms. The current enum at `crates/benten-platform-foundation/src/materializer.rs:209` has only the three constructed variants — `SchemaMismatch`, `SubscribeSeamFailure`, `UcanClockNotInjected` — and `code()` (line 238) has no `Backend`/`Other` arms. The dead variants are gone. #312 should be closed by SHA reference under its bundle umbrella (#1196).

Also note: #324 (benten-id) is CLOSED, but its surface persists in code — `RotationLog::append` at `crates/benten-id/src/did_rotation.rs:223` sits beside `accept_rotation_event` (line 268) with no production caller. If #324's closure was a dedupe, the surface itself is still a dead-pub residual.

phase-4-meta-core (G-CORE-9 freeze wave / F-full) targeted the v1-beta interface freeze, not a dead-pub sweep; none of the above were swept by it. Keeping this meta OPEN as a closure-pin until the genuine residuals (#296, #348, #310, the #324 surface) are removed and #312 is closed against the code that already landed its fix.

### #356 ⚠️acc=False
**Finding:** Test/bench-only pub helpers landing without the sec-r6r2-02 cfg-gate across benten-eval + benten-engine + bindings/napi.
**Comment:**
Status update against current main (origin/main = d0ccf606). This META stays OPEN — its closure is pinned to sub-issues #330 (benten-eval) and #349 (benten-engine), both still OPEN. (#335 and #314 are CLOSED.)

Progress confirmed — these cited helpers ARE now cfg-gated:
- `fabricate_test_suspend_envelope` / `..._with_attribution_cid_bytes` — `#[cfg(any(test, feature = "test-helpers"))]` at `crates/benten-engine/src/engine_wait.rs:930` / `:979`.
- `build_*_for_test` family in `crates/benten-eval/src/invariants/budget.rs` — `#[cfg(any(test, feature = "testing"))]` (e.g. `:621`).
- The `_for_test`/`_for_testing`-suffixed helpers in benten-eval and `Engine::resolve_subgraph_cid_for_test` (`crates/benten-engine/src/testing.rs:1162`, via the `#[cfg(any(test, feature = "test-helpers"))]` impl block at `:1096`) are gated and enforced by `tests/phase_3_workspace/for_test_symbols_are_feature_gated.rs`.

Residual gaps (still present, NOT yet fixed) — correcting an earlier draft that claimed the benchmark helpers were gated:
- `Engine::benchmark_helper_subgraph_cache_cold/prewarm/warm/reregister_and_miss` at `crates/benten-engine/src/engine_wait.rs:1444/1453/1473/1483` are STILL un-gated. They sit in a plain (non-cfg) `impl Engine` block; the `#[cfg(any(test, feature = "test-helpers"))]` at `:1416` belongs to the preceding `grant_read_capability_for_testing`, not these helpers. They escape the `for_test_symbols_are_feature_gated.rs` regression test only because that scanner matches the `_for_test`/`_for_testing` substring, which these names lack.
- The two coupled benches `subgraph_cache_hit` and `subgraph_cache_hit_80_20_mixed` (`crates/benten-engine/Cargo.toml:398` / `:402`) still lack `required-features = ["test-helpers"]` — exactly the "2 `[[bench]]` entries need required-features" item #349 named. This is why the helpers can't simply be gated (the benches that consume them don't opt into the feature).
- `SubgraphSpecBuilder::test_inject_failure` at `crates/benten-engine/src/subgraph_spec.rs:331` is STILL un-gated; its docstring still promises "Phase-2 gates behind `#[cfg(...)]` once the integration layout stabilises (R-minor-04)", which never happened.

No merged PR or in-flight PR (#1366 is sealed-sender, unrelated) closes #330/#349/#356. Keep open until the remaining un-gated benten-engine helpers + the two `[[bench]]` `required-features` entries land and #330/#349 close.

### #362
**Finding:** Stranded scaffolding from past R6 fix-pass + canary-promotion waves (benten-sync + benten-ivm + benten-id) left uncleaned.
**Comment:**
Status update against current main (115b0847). The cited stranded surfaces are all still present and unfixed:

- `handshake::MessageKind` local enum still defined at `crates/benten-sync/src/handshake.rs:92` (no `use crate::mst_proto::MessageKind` import in that file). Its own doc-comment at `handshake.rs:36-40` still reads verbatim: "when G16-C lands, `MessageKind` becomes a re-export from `crate::mst_proto::MessageKind` (single source of truth) and this local definition is deleted." The canonical `mst_proto::MessageKind` exists (`crates/benten-sync/src/mst_proto.rs:53`), and its own doc-comment at lines 23-24 even asserts "both modules import [`MessageKind`] from this file so the enum + ordering live in exactly one place" — which is currently FALSE for `handshake.rs`. The G16-C reconciliation has not landed.
- `Endpoint::bind_with_relay_url` canary-scope placeholder still at `crates/benten-sync/src/transport.rs:550`; body still returns the "G16-A canary scope" typed error (no real relay binding) even though the wave-6b promotion to `peer_discovery::bind_atrium_peer` landed (`crates/benten-sync/src/peer_discovery.rs:204`).
- `MstDiffFrame` (`crates/benten-sync/src/mst_proto.rs:190`) + `MST_DIFF_WIRE_VERSION` (`mst_proto.rs:209`) + `MstDiffSession::enqueue_frame` still present.

Sub-issue status: #289 and #302 were CLOSED in the 2026-05-15 dedupe sweep, not by substantive fixes — both closing comments state "Consolidated under META #362 ... Closure happens at the META level ... This sub-issue is closed to reduce tracker noise." #296 remains OPEN (closure-pin, priority:p1). That META-level closure has not yet occurred. phase-4-meta-core (current main; phase-4-meta-core-close tag not yet landed) included no wave-promotion sibling sweep against these surfaces. Keeping open as a closure-pin.

### #365
**Finding:** DSL parse_primitive emits underscore-prefixed property keys (_predicate/_target/_body/_topic/etc.) for 7 of 12 primitives that diverge from the eval-side runtime property names, so DSL-authored handlers are reserved-but-not-fired / not executable on the engine.
**Comment:**
Status update against current main (`d91896b3`).

The underscore-prefixed namespace half of this finding is RESOLVED: #604 (closed via `0e22f517` / PR #1269, "normalize DSL compiler canonical-byte scheme pre-v1") eliminated the `_`-prefixed property keys. The DSL compiler now emits non-underscore canonical key constants mirroring `benten_core::SubgraphBuilder` — `crates/benten-dsl-compiler/src/lib.rs:136-162` (`KEY_LABEL="label"`, `KEY_PREDICATE="predicate"`, `KEY_CALL_HANDLER="handler"`, `KEY_TRANSFORM_BODY="body"`, `KEY_EMIT_TOPIC="topic"`, `KEY_SUBSCRIBE_PATTERN="pattern"`, `KEY_ITERATE_BODY="iter_body"`), emitted at `parse_primitive` (`lib.rs:858-953`).

The deeper executability gap (disposition A) REMAINS OPEN: the eval-side runtime primitives still read different property names than the DSL/SubgraphBuilder canonical keys, and no translation layer bridges them (`benten-dsl-compiler` is a standalone sibling crate with no `benten-engine`/`benten-eval` dep):
- `branch.rs:39,49,53,102` reads `condition_value`/`match_value`/`cases`/`conditions` (DSL emits `predicate`)
- `call.rs:116,125` reads `target`/`call_op` (DSL emits `handler`/`args`)
- `transform.rs:72,95,96` reads `expr`/`input`/`result` (DSL emits `body`)
- `emit.rs:55,56,61` reads `channel`/`payload`/`handler` (DSL emits `topic`)

Confirmed at HEAD: no eval-side primitive reads any DSL canonical key (`grep '"predicate"|"topic"|"iter_body"'` over `crates/benten-eval/src` returns nothing; transform eval does not read `"body"`; call eval does not read `"handler"`/`"args"`). A handler authored purely via the Rust DSL compiler is therefore still not directly executable for these primitives.

No commit/doc/backlog references #365; the issue carries `closure-pin` + `priority:p1` (Path A wire-up), not `phase-4-meta-deferred`. Keeping OPEN for the DSL-to-eval property-name reconciliation decision (disposition A align-to-eval vs B explicit-non-runnable framing).

### #549
**Finding:** Ucan::prf recursive deserialization has no depth bound; stack-overflow DoS reachable from untrusted input at typed-CALL dispatch + UCAN durable backend.
**Comment:**
Partially addressed; keeping open. The depth-bounded entry point landed in benten-id (commit d8abab51, umbrella #1172): `MAX_UCAN_PROOF_DEPTH = 32` (crates/benten-id/src/ucan.rs:85), `Ucan::from_canonical_bytes_bounded` with a non-recursive CBOR nesting pre-walk (ucan.rs:186), and the typed `UcanError::ProofChainTooDeep { depth, max }` variant — closure-pinned in crates/benten-id/tests/dos_unbounded_decode_safe2_1172.rs.

However, the actual DoS surface is NOT yet closed. Both untrusted-input call sites this finding named still decode via the unbounded path in current main:
- crates/benten-engine/src/typed_call_dispatch.rs:365 — `serde_ipld_dagcbor::from_slice(bytes)` for the `ucan_validate_chain` typed-CALL op.
- crates/benten-caps/src/backends/ucan.rs:377 — `cbor::from_slice::<Ucan>(value)` in `iter_installed_proofs`.

A workspace grep confirms `from_canonical_bytes_bounded` has zero production callers (only its own definition + a doc reference). Note PR #1290 added the `UcanError::ProofChainTooDeep` arm to caps `cap_err_from_ucan` error-mapping (plumbing for the future migration), but the decode call itself is still the unbounded `cbor::from_slice::<Ucan>` (its only change there was the #492 warn-on-skip observability).

The call-site migration is tracked at docs/future/phase-4-backlog.md §4.80 (P-II post-COLLAPSE workspace sweep), which states the surface is not closed until both sites adopt `Ucan::from_canonical_bytes_bounded` and `serde_ipld_dagcbor::from_slice::<Ucan>` has zero remaining production callers. Stays open until that migration lands.

### #563
**Finding:** identity.rs uses an unchecked DID constructor for 7 issuance/verify sites, signing invalid did:key strings into VC + device-attestation envelopes without validation.
**Comment:**
Status update (verified against main `d0ccf606`): the underlying `benten-id` surface changed but the napi-side input-validation defect remains — keeping open.

What changed:
- `benten_id::did::Did::from_string_unchecked` was demoted to `pub(crate)` (crates/benten-id/src/did.rs:155), so it is no longer reachable from the napi crate.
- A checked constructor `Did::parse_validated` now exists (crates/benten-id/src/did.rs:186); it round-trips the input through `Did::resolve()` and surfaces a typed `DidError` on malformed `did:key` strings.

Why the defect is NOT closed:
- The `identity.rs` issuance sites were migrated to `RustDid::from_string_for_test_fixture(...)` rather than `parse_validated`. That constructor's own docstring states **"NEVER call from production code. Production callers MUST use `Did::parse_validated`"** (crates/benten-id/src/did.rs:171-172).
- No `.resolve()` / `parse_validated` validation precedes the issuance constructors, so untrusted JS-supplied invalid `did:key` strings still get signed into VC / device-attestation envelopes:
  - bindings/napi/src/identity.rs:191 — `JsVerifiableCredential::issue` (VC subject) → signed at the `.sign(&issuer.inner)` below.
  - bindings/napi/src/identity.rs:333 — `JsDeviceAttestation::issue`
  - bindings/napi/src/identity.rs:354 — `JsDeviceAttestation::issue_with_runtime_check`
  - bindings/napi/src/identity.rs:412 — `JsDeviceAttestation::issue_with_authority` (a 5th issuance site added since the issue was filed)
  - bindings/napi/src/identity.rs:426 — `JsDeviceAttestation::issue_for_browser_target`
  The signing path (`benten_id::device_attestation::DeviceAttestation::issue` → `issue_at` → `issue_with_nonce`, crates/benten-id/src/device_attestation.rs:177/198/216) does no DID validation — it stores `device_did.as_str()` and signs the canonical bytes directly.
- By contrast, the verify path DOES validate: `verify_signature` (identity.rs:110) and `JsDeviceAttestation::verify_signature` (identity.rs:459) both call `.resolve()` on the next line, which catches malformed input. The issuance sites are exactly the ones missing that guard.

Note on the OPEN PR #1366 (sealed-sender ORIGIN-AUTH): it touches `crates/benten-id/src/did.rs` but does NOT touch `bindings/napi/src/identity.rs`, so it does not close this defect even when merged.

Fix shape: re-point the issuance sites (identity.rs:191/333/354/412/426) to `Did::parse_validated`, returning a typed `Error` to the JS caller on invalid `did:key` input.

### #593
**Finding:** Meta closure-pin: read-side capability check + signature-verify enum-narrowing auth-bypass cluster (5 sub-instances: #534 #509 #336 #525 #499).
**Comment:**
Status update (verified against current main `115b0847`): 4 of the 5 sub-instances are now closed and the fixes are present in main; this META stays OPEN as a closure-pin on the remaining one.

Closed and verified-in-main:
- **#534** — closed via PR #1290 (merge `20fb5c78`, ancestor of HEAD). `read_node_as` now fails CLOSED on every non-permit `check_read` outcome, not only `DeniedRead` (`crates/benten-engine/src/engine_crud.rs` ~172 + the canonical fail-CLOSED gate at ~363-373; Option-C `DeniedRead` collapse). Non-`DeniedRead` variants (Revoked/Denied/etc.) now reject instead of falling through to a permitted read.
- **#509** — closed 2026-05-15. The authenticity gate is now in `accept_rotation_event` (`crates/benten-id/src/did_rotation.rs:278-282`): `previous_did` is resolved to the OLD public key and `verify_signature_with(&old_pk)?` runs BEFORE any ordering/replay check; resolution failure or signature mismatch both map to `BadSignature`.
- **#336** — closed 2026-05-15 by elimination: the `DeviceRevocation` / `Acceptor` device-revocation acceptance pipe was DELETED (COLLAPSE P3, per DECISION-RECORD §4 RATIFIED — see `crates/benten-id/src/device_attestation.rs:121-123` + 431-442). Revocation collapsed to user-root UCAN revocation (`benten_caps::revoke`), so the string-compare-only revocation chain-walk surface no longer exists.
- **#499** — closed via PR #1235 (commit `336178a3`), renderer rung-2 cap lookup.

Remaining open:
- **#525** — the silent fail-OPEN sentinel is STILL present at `bindings/napi/src/lib.rs:1371` (`Err(_) => Ok(None)`). It is now `cfg(feature = "test-helpers")`-gated (production cdylib builds return `E_PRIMITIVE_NOT_IMPLEMENTED` instead), but the enum-narrowing defect is unchanged: all engine errors (lock-poisoned / backend-corrupt / cap-deny) are still conflated with the "no metrics record yet" `null` sentinel on the gated path.

This META remains open as a closure-pin until #525 lands.

### #595
**Finding:** Meta closure-pin: silent observability gap / false rustdoc tracing claims (9 sub-instances across 7 crates).
**Comment:**
Status update (verified against origin/main d0ccf606).

Of the 8 primary sub-instances, only #536 remains OPEN. Of the 7 closed, the closures split two ways:

GENUINELY CODE-FIXED (tracing/observability added at the cited sites):
- #511 — benten-sync crdt now emits tracing on silent-skip arms (8 `tracing::` calls in `crdt.rs`); closed via bundle #1179 (PR #1261).
- #492, #497 — benten-caps observability added in `backends/ucan.rs` + `ucan_grounded.rs`; closed via bundle #1148 (PR #1290, merge 20fb5c78).
- #508 — benten-graph `subscriber_count` migrated to the `lock_recover()` idiom in `browser_backend.rs`; closed via PR #1277.

CLOSED-AS-NAMED / DEFERRED (HARD-RULE clause-(b); defect intentionally carried to Phase-4-Meta, NOT yet fixed in code):
- #528, #537, #541 — closed 2026-05-18 with the underlying defects deferred to `docs/future/phase-4-backlog.md` (Wave-E W20 §-rows, landed via PR #1294). The code is unchanged: e.g. #537's `change.rs:218-219` still documents "panics are caught and discarded. Phase 2 revisits once the `tracing` dep lands" with a bare `let _ = std::panic::catch_unwind(...)` at line 249 and zero `tracing::` calls (mirror at `emit_broadcast.rs:165/177`).

Remaining OPEN defect (#536): the false-rustdoc-vs-tracing-emission gap is still present. `rehydrate_handler_version_chains_from_zone` at `crates/benten-engine/src/handler_versions.rs:265` documents "logged + skipped" while the function body contains zero `tracing::` calls and silently `continue`s on every malformed-node branch (lines ~271-289).

This META stays OPEN as a closure-pin: it tracks both the lone unfixed-and-open sub-issue (#536) and the three deferred-to-backlog sub-issues (#528/#537/#541) whose code defects remain. Reopen/close when those land.

### #600
**Finding:** G27-D manifest_scope functions (check_scope_within_envelope + manifest_requires/shares_to_scope + private_namespace_scope_admits_actor) have ZERO production consumers; Layer-2 audience-side write-path envelope check is paper-only.
**Comment:**
Status materially changed, but this finding's specific defect is NOT closed in current main (d0ccf606). Keeping open.

What changed:
- Sibling Layer-3 issuance-side gap (#596, `delegate_capability` AllPermit shim) is CLOSED — wired via a new `SharesPolicyResolver` port consumed in `EngineCapsHandle::delegate_capability` (engine_caps.rs:429, resolver consulted ~533-584). Landed in main via PR #1294 (merge 39a0e732); the standalone authoring commit 0dbfa438 is not itself an ancestor of main.
- Layer-2 manifest-envelope enforcement now EXISTS in production, but via a DIFFERENT mechanism than the functions this finding named: the `ManifestEnvelopeRechecker` port + `chain_authority::{manifest_to_envelope_ceiling, validate_chain_with_manifest_ceiling, envelope_ceiling_rejects_cap}` (COLLAPSE P5, PR #1276 / f68d94fd), consumed at the sync-merge boundary (engine.rs:1453) with `ProductionManifestEnvelopeRechecker` wired in `production_engine_builder.rs`.

What is still open (this row's specific defect):
- The four `manifest_scope` functions named here — `check_scope_within_envelope`, `manifest_requires_to_scope`, `manifest_shares_to_scope`, `private_namespace_scope_admits_actor` — still have ZERO production consumers. In current main they are referenced only by manifest_scope.rs itself (def + unit tests), the lib.rs re-export, INTERNALS.md / docs/public-api/benten-caps.txt, and crates/benten-caps/tests/*.
- `GrantBackedPolicy::check_write` (grant_backed.rs:421) does NOT invoke `check_scope_within_envelope`; it consults `has_unrevoked_grant_matching` only — no envelope second-step. The audience-side write-path envelope check via these named functions remains paper-only.

The Layer-2 invariant (Inv-14) is therefore partially enforced through the rechecker path, but the specific G27-D `manifest_scope` audience-side functions remain dead/paper. Consistent with this row's closure-pin status under META #669 (still OPEN): the bundle umbrellas #1146/#1197 are closed (they wired the issuance + chain-validation halves), but #600 was intentionally left open as the substantive anchor for the audience-side `manifest_scope` wire-up (or a decision to delete-as-superseded-by-the-rechecker-path).

### #602
**Finding:** Meta closure-pin: silent-success — Ok-return masks real failure (5 sub-instances; bandwidth-budget bypass).
**Comment:**
Status update (verified against main `115b0847`). This META stays OPEN as a closure-pin: 2 of 5 sub-instances are closed; the 3 MAJOR sub-instances remain open with the cited defects still present in main.

CLOSED:
- #493 (benten-graph, MINOR) — actually code-fixed: `scan_edge_index` (`crates/benten-graph/src/transaction.rs:743`) now propagates `GraphError::Decode` on corrupt/short index keys via `.ok_or_else(...)` / `Cid::from_bytes(...).map_err(...)` + `?` instead of the old silent `_ => continue`. Closed under bundle umbrella #1210.
- #521 (benten-platform-foundation, MINOR) — closed-as-named-deferred (refinement-audit Wave-E W20, per CLAUDE.md baked-in #15), tracked in `docs/future/phase-4-backlog.md`, landed via PR #1294 (main `39a0e732`). Administratively closed per HARD-RULE clause (b)/(c), not a code fix.

STILL OPEN (defects present in main):
- #516 (benten-sync, MAJOR) — `MerkleProof::approximate_bytes` (`crates/benten-sync/src/mst.rs:476-477`) is still `serde_ipld_dagcbor::to_vec(self).map_or(0, |v| v.len())`, returning 0 on encode failure (light-client bandwidth-budget fail-OPEN). `PeerId::to_dag_cbor_bytes` (`crates/benten-sync/src/peer_id.rs:110-114`) still uses `.unwrap_or_default()`.
- #519 (benten-eval, MAJOR) — WAIT suspend-side still swallows persistence Results: `let _ = store.put_wait(...)` and `let _ = store.put_envelope(...)` at `crates/benten-eval/src/primitives/wait.rs:411` and `:424` (note: file is `primitives/wait.rs`, not the `wait.rs:392+405` cited in the META body). WAIT still returns `Suspended` even when persistence failed.
- #517 (benten-platform-foundation, MAJOR) — `PluginLibrary::try_append_anchor_chain` (`crates/benten-platform-foundation/src/plugin_library.rs:324`) still `let _ = append_version(anchor, &tip, &new_cid)`, swallowing all `VersionError` variants (including `UnknownPrior`, not just the intended `Branched`).

No merged PR and no in-flight PR (#1366 sealed-sender B2) touches mst.rs / peer_id.rs / primitives/wait.rs / plugin_library.rs. This META closes only when #516 / #519 / #517 land.

### #606
**Finding:** Meta closure-pin: bindings/napi G19-B JSON envelope bypass (~30 sites; anchored at #529; siblings #525 #522 #531).
**Comment:**
Status update (verified against main `d0ccf606`): of the referenced sub-instances, only #531 (raw-`format!` JSON construction) is closed (2026-05-18). The primary anchor #529 (~30-site G19-B JSON-envelope mass-rewrite) remains open — raw non-envelope error construction is still live, e.g. 14× `Error::from_reason(format!(...))` in `bindings/napi/src/identity.rs`. Siblings also remain open: #522 (mutex lock-poison panics — 13× `.lock().expect("atrium state mutex")` in `bindings/napi/src/atrium.rs`) and #525 (silent fail-OPEN sentinel, still present at `bindings/napi/src/lib.rs:1371` as `Err(_) => Ok(None)`). No merged PR closes #529/#522/#525. This META stays open as a closure-pin until the bindings/napi G19-B cleanup wave lands.

### #608
**Finding:** Rust DSL accepts empty/whitespace handler_id while TS DSL rejects it with EDslInvalidShape — cross-language rule-mirror gap.
**Comment:**
Status update against current main (`d91896b3`). The code fix for this finding has landed via PR #1325 (merge `132dde91`, an ancestor of HEAD; PR body: "closes #545 #608 ...").

`validate_shapes` now rejects empty / whitespace-only `handler_id` with `E_DSL_INVALID_SHAPE` at `crates/benten-dsl-compiler/src/lib.rs:1438-1447` (wired into `compile_str` at line 1361), mirroring the TS-side `EDslInvalidShape("handlerId must be a non-empty string")` at `packages/engine/src/dsl.ts:800`. Production-path regression tests exist at `crates/benten-dsl-compiler/tests/tf10_dsl_chunk1_closures.rs` (empty + whitespace-only rejected; normal still compiles; would-FAIL on revert).

The implementation defect described here is resolved in code. Leaving OPEN per the `phase-4-meta-deferred` / `closure-pin` labels and the META-level tracking (#660 / #707) — disposition belongs to the phase-4-meta-core close (tag not yet landed).

### #623
**Finding:** Three napi sites (policy.rs grant issuer, lib.rs delegate_capability plugin_did, atrium.rs set_local_device_did) persist unvalidated DID strings into Inv-14 / Compromise #25/#26 load-bearing surfaces.
**Comment:**
Status update (verified against main d0ccf606): 1 of the 3 sites is now closed; the other 2 remain.

CLOSED — Site 2 `delegate_capability` `plugin_did`: now validated at `bindings/napi/src/lib.rs:834-868` (the `#1206 closure` block). The DID is constructed via `Did::from_string_for_test_fixture(plugin_did)` and validated with `candidate.resolve()`, rejecting malformed audience DIDs with an `E_PLUGIN_DID_MALFORMED:`-prefixed `InvalidArg`. Landed in commit 6ed80b2f (PR #1282, merged 2026-05-17, confirmed ancestor of main). Note the closure was tracked under the sibling issue #1206 (same `delegate_capability` site).

STILL OPEN — the two residual sites are unchanged in main:
1. `policy.rs::parse_grant_json` reads the grant `issuer` field as a bare string (`obj.get("issuer").and_then(|x| x.as_str()).map(String::from)`) with no DID validation (`bindings/napi/src/policy.rs:110`).
2. `atrium.rs::set_local_device_did` stores `device_did` verbatim — only an is-empty check, then "any non-empty string is stored verbatim" (`bindings/napi/src/atrium.rs:627-637`).

Both still admit malformed DID strings into their persistence/emission targets. Keeping open as the closure-pin for the two residual sites.

### #629
**Finding:** Campaign flagship meta closure-pin: DoS-via-unbounded-decode (26 instances across 9 crates).
**Comment:**
Status update (verified against main 115b0847): substantial progress on this flagship closure-pin META.

Closed (14 of the 26 primary sub-instances): the load-bearing leaf-decoder #554 (PR #1290, commit 20fb5c78; benten-core::Value now threads an explicit remaining-depth budget through visit_seq/visit_map via MAX_VALUE_DECODE_DEPTH — crates/benten-core/src/value.rs ~102-281, with depth charged at lines 205-207 / 259-261) plus #543, #545, #553, #560, #562, #567, #574, #575, #580, #581, #585, #586, #591.

Still open (12): #496, #549, #558, #566, #568, #571, #572, #573, #577, #578, #583, #589. Note #549 has its canonical entry point already landed (benten-id Ucan::from_canonical_bytes_bounded + MAX_UCAN_PROOF_DEPTH=32 + UcanError::ProofChainTooDeep, commit d8abab51) but stays open as a closure-pin: the two cross-crate untrusted-decode call sites (benten-engine typed-CALL dispatch + benten-caps UCAN backend) are migration-deferred per docs/future/phase-4-backlog.md §4.79. The benten-platform-foundation install/schema/manifest concentration (#568/#572/#578/#589) and the napi cluster (#558/#566/#571) are the bulk of the residual.

Correction to the prior #999 note: the #999 legitimate-workload-growth sub (Fwd-1 lens addition, not part of the original 26-count) was dispositioned NOT-closed-by-this-wave on 2026-05-15, but it has since been CLOSED (2026-05-16, PR #1261). It is no longer an open out-of-scope item under this META.

This META stays open as a closure-pin until the remaining caps land.

### #635
**Finding:** install_plugin (11 steps) and uninstall_plugin (5 steps) cascades are non-atomic with no rollback on mid-cascade failure.
**Comment:**
Status update (partial closure). The install-side leakable states described here (failures after step >= 9) are resolved by G-CORE-7, PR #1312 (commit 8883f789, MERGED 2026-05-22; §4.21 rollback + §4.35 atomicity). `plugin_lifecycle::install_plugin` now makes the Step-9 cap-cascade all-or-nothing (mint-loop tracks every minted CID and unwinds via `rollback_step9`, `crates/benten-platform-foundation/src/plugin_lifecycle.rs:1157-1178` + helper `:1223-1227`); a Step-10 private-NS provision failure unwinds Step 9 (`:1184-1187`); a Step-11 library-insert / `set_active` failure removes the just-inserted library entry, deprovisions the namespace, and unwinds Step 9 (`:1201-1208`). Steps 1-8 are pure validation with no cross-port observable mutation, so the rollback covers every state-minting step. Pinned (un-ignored, GREEN) in `crates/benten-platform-foundation/tests/tf7_g_core_7_install_lifecycle_hardening.rs` (`install_step10_provision_failure_rolls_back_step9_minted_grants_zero_residual`, `install_step9_cap_cascade_is_atomic_midloop_failure_unwinds_prior_grants`). PR #1312 cites the §-rows but does not name this issue.

The UNINSTALL-side concern remains OPEN. `uninstall_plugin` (`crates/benten-platform-foundation/src/plugin_lifecycle.rs:200-249`) still drives its 5-step cascade with bare `?` propagation and no compensation: a mid-cascade failure at step 2 (`cascade_revoke_grants_with_issuer`, `:223-226`) or step 3 (`terminate_subscriptions_for`, `:228-231`) after step 1 (held-caps revoke, `:220-221`) succeeds still leaves the half-revoked state described here (held caps revoked but downstream delegations / live subscriptions still firing, private-NS rows un-deleted, library entry not removed, plugin-DID not revoked). No rollback/unwind code or docs exist for the uninstall path in current main, and there is no §-row delivery for it.

Keeping OPEN as the closure-pin anchor for the uninstall-side rollback work (coupled with #640 per `docs/future/refinement-audit-2026-05.md` §15 / lines 1282-1286).

### #643
**Finding:** Meta closure-pin: identity-binding + _unchecked-constructor + V1-verify-skip cluster (6 NEW Wave-6 instances).
**Comment:**
Status update (verified against main `115b0847`):

Closed sub-instances:
- Wave-6: #555, #569, #588, #592 (all COMPLETED).
- Wave-5 (shared with #593): #336, #499, #509, #534 (all COMPLETED).

Remaining open (defects re-verified present in main):
- #557 — `benten-sync` `Handshake::respond_with_window` still constructs the responder session via `Session::new_authenticated(local_did, initiate_frame.peer_did.clone(), ...)` at `crates/benten-sync/src/handshake.rs:626-632`. The frame signature is verified against `initiate_frame.peer_id`'s pubkey (`:517-523`), but nothing binds `peer_did` to the `did:key` derived from `peer_id`. Identity-binding gap unchanged.
- #563 — `bindings/napi/src/identity.rs` no longer calls `Did::from_string_unchecked` (that fn was made `pub(crate)` by the #835 crypto-agility discharge, PR #1307). The unvalidated-DID issuance defect persists via the renamed open-unsafe constructor `Did::from_string_for_test_fixture` (def: `crates/benten-id/src/did.rs:147`, body `Self(s)`, no validation), now at 9 sites (`identity.rs:110,191,233,245,333,354,412,426,459`). The issuance sites (`:191` VC subject, `:333/:354/:412/:426` device, `:459` parent) still bake user-supplied DID strings into signed VC/DeviceAttestation envelopes without validating; the verify-side `:110-111` resolves after construction. Cite the renamed symbol when scoping the fix.
- #525 — `bindings/napi/src/lib.rs:1371` still maps `Err(_) => Ok(None)` in `describe_sandbox_node_napi` (now under `#[cfg(feature = "test-helpers")]`; the non-test-helpers arm returns an error). Shared with #593.

This META stays open as a closure-pin until #557/#563 (and shared #525) land.

### #650
**Finding:** Meta closure-pin: asymmetric-defense / half-a-rule-applied pattern (10 instances across 5 crates).
**Comment:**
Status update (verified against main 115b0847).

This META references 10 sub-instances (6 primary + 4 sibling). Current state: 6 closed, 4 still open.

Closed (all COMPLETED):
- #570 (benten-graph SnapshotBlobBackend::get Cid parse swallow) — via PR #1290 merge commit 20fb5c78, confirmed in main
- #584 (benten-eval table_growing uncapped)
- #582 (bindings/napi user_view_drain_updates negative-clamp)
- #580 (benten-eval UcanValidateChain unbounded)
- #509 (benten-id accept_rotation_event unauthenticated)
- #336 (benten-id verify_signature_with zero callers)

Still open — defects re-verified present in main:
- #576 MAJOR (benten-eval) — `shapes_match` `Value::List` arm still `e.iter().zip(a.iter()).all(...)` with no length check, at crates/benten-eval/src/primitives/wait.rs:584-586; expected `[A,B,C]` zip-truncates to match actual `[]`.
- #564 MAJOR (benten-sync) — `HandshakeFrame::from_canonical_bytes` (crates/benten-sync/src/handshake_wire.rs:136-142) does a bare `from_slice` with no wire-version check, asymmetric with `MstDiffFrame::from_canonical_bytes` (crates/benten-sync/src/mst_proto.rs:254-261) which rejects on version mismatch.
- #587 MAJOR (benten-engine) — migrations-require-persistence rejection at crates/benten-engine/src/engine_modules.rs:177-184 is gated `#[cfg(target_arch = "wasm32")]`; no `not(wasm32)` arm, so native silently accepts a manifest carrying MigrationStep[] that wasm32 rejects (CLAUDE.md baked-in #17 deployment-shape asymmetry).
- #551 MINOR (benten-dsl-compiler) — `validate_shapes` (crates/benten-dsl-compiler/src/lib.rs:1463-1475) enforces only `n >= 0`; no upper bound, so `fuel: i64::MAX` compiles.

This META stays open as a closure-pin until the 4 asymmetric arms above are closed (and the pim-N codification lands per the body).

### #660
**Finding:** Meta closure-pin: 5 DEPLOYED invariant violations at HEAD — Inv-2/8/10/13/14 (p0 v1-platform-shippable BLOCKER cluster).
**Comment:**
Status update (verified against main `115b0847`): 2 of the 5 deployed invariant violations are now CLOSED with the fix present in main; 3 remain OPEN with the defect re-confirmed at HEAD. This BLOCKER-cluster META stays open.

CLOSED & verified fixed:
- Inv-10 (#604) — closed via PR #1269 (merge `0e22f517`). `crates/benten-dsl-compiler/src/lib.rs` now emits canonical keys `handler`/`module`/`body`/`signal` (no underscore prefix), matching `SubgraphBuilder`; the `_target`/`_module`/`_body` references that remain are explanatory comments about the removed scheme. Cross-surface CID divergence resolved.
- Inv-13 (#615) — closed via PR #1290 (merge `20fb5c78`). `Transaction::put_node_with_attribution` (`crates/benten-graph/src/transaction.rs:362`) now probes CID existence inside the write txn (lines 409-412) and routes through the Inv-13 5-row dispatch matrix: `User`-authority re-puts of an already-present CID return `Err(GraphError::InvImmutability)` (lines 423-426). 68 `probe_cid_exists`/`WriteAuthority`/`InvImmutability` refs wired in benten-graph.

Still OPEN — defect present at HEAD:
- Inv-2 (#618) — `validate_subgraph` finalized path still uses raw longest-path length (`crates/benten-eval/src/invariants/structural.rs:153`, `longest.len()`); only `validate_builder` counts CALL primitives per ENGINE-SPEC §4 (`structural.rs:432-441`). `Engine::register_subgraph` → `sg.validate` → `validate_subgraph` (`crates/benten-eval/src/subgraph_ext.rs:197`) runs the broken raw-len arm.
- Inv-8 (#621) — `crates/benten-eval/src/invariants/budget.rs:183` and `:215` resolve the CALL `handler` property via `crate::lookup_test_callee` reading `TEST_CALLEE_REGISTRY`; the mutation surface `register_test_callee` is `cfg(any(test, feature = "testing"))`-gated (`lib.rs:120`), so the production registry is always empty and any composed-handler CALL rejects with `E_INV_REGISTRATION`.
- Inv-14 (#630) — `PendingHostOp::DeleteNode` carries only `{ cid }` (`crates/benten-engine/src/primitive_host.rs:167`); at `engine.rs:4224` the delete path calls `tx.delete_node(cid)` with no attribution while `PutNode` threads the full `actor/handler/grant` triple via `put_node_with_attribution`. `ChangeEvent.actor_cid` is `None` on deletion.

No commits closing #618/#621/#630 exist in main. META stays OPEN until those three land.

### #669
**Finding:** CLAUDE.md baked-in #18 plugin-trust Layers 2+3 + T10-upgrade defenses are typed/tested but paper-only at HEAD with zero production callers (AllPermit shim, manifest-scope zero-consumers, verify_upgrade_author_continuity unwired).
**Comment:**
Status (partial-landing + named G-COMP-1 deferral; META stays OPEN). Verified against current main.

LANDED on main (Path A, Ben-ratified 2026-05-15):
- #596 / #612 / #619 closed. The `AllPermit` policy-view shim is deleted; `EngineCapsHandle::delegate_capability` now consults `Engine::shares_policy_resolver` fail-CLOSED (`Denied`/`NoManifest` reject with `PluginDelegationOutsideManifestEnvelope`) plus the always-on private-namespace clause (`engine_caps.rs:516`, PR #1294 / commit `39a0e732`).
- T10-upgrade (#609): `plugin_lifecycle::install_plugin` now calls `verify_upgrade_author_continuity` (`plugin_lifecycle.rs:1077`) and `decide_upgrade_consent` (`:1106`) on the upgrade arm; closure-pin `plugin_upgrade_requires_same_author_did.rs`. (#609 is genuinely wired — it is open only as a tracking lag; it can be closed.)
- Unified envelope-ceiling (PR #1276 / `f68d94fd`) consumed in `apply_atrium_merge` (`engine.rs:1620` `envelope_ceiling_admits_row`).

NOT yet live on main (the heart of #600 and the META's "paper-only" claim — deferred, not closed):
- The substantive per-resolvable-DID Layer-2/Layer-3 manifest-`shares` recheck is NOT live in shipped binaries. The default `SharesPolicyResolver` is `NoopSharesPolicyResolver`, which classifies every principal `NotPluginPrincipal` — the code itself states this is "observably identical to the deleted `AllPermit` shim" (`shares_policy_resolver.rs:135`, `engine_caps.rs:538`). No production `SharesPolicyResolver` adapter exists (only a test impl in `layer3_shares_envelope_enforcement_1197_closure_pin.rs`; `ProductionEngineBuilder` does not install one).
- `ProductionManifestEnvelopeRechecker` hardens only the synthesized/empty-DID arm (`node-id:N` -> `UnresolvedDeny`); for resolvable peer-DIDs it returns `NotApplicable` (admit). `docs/V1-FROZEN-INTERFACE-DEFERRED.md` Row D-4 (FORENSIC): "the per-resolvable-DID substantive recheck is NOT live in shipped binaries" — deferred to G-COMP-1.

Why KEEP OPEN: `docs/SECURITY-POSTURE.md` (row 27) keeps Compromise #27 RESERVED + OPEN, "tracking via META #669 + #1118", with the row body landing only "when META #669 closure or honest-disclosure mint lands." The Compromise #27 mint task #1118 (which "consolidates META #669") is also OPEN, and the G-COMP-1 substantive chain-walk is still deferred. This META is the live tracking anchor for that as-yet-unlanded disclosure/wire-up; closing it now would orphan the reserved registry row. Recommend: close #609 (wired), and keep #669 + #600 open until either the G-COMP-1 substantive recheck lands or Compromise #27 is minted.

### #671
**Finding:** validate_shapes' two SANDBOX-int error arms duplicate ~22 LOC of Diagnostic construction; collapsible to a single helper.
**Comment:**
Status update against current main (`d0ccf606`). The refactor for this finding has landed via PR #1325 (merge `132dde91`, ancestor of main; G-CORE-DSL chunk-1 R5, commit body "close #545 #608 #671 #841 #848").

The two duplicate SANDBOX-int-prop error-construction arms in `validate_shapes` are now collapsed into a single `sandbox_int_err` helper closure at `crates/benten-dsl-compiler/src/lib.rs:1454-1462`, called from both match arms (`lib.rs:1469-1470`) — single source of truth for the typed `E_DSL_INVALID_SHAPE` message body. Regression pin: `crates/benten-dsl-compiler/tests/tf10_dsl_chunk1_closures.rs::dsl_671_both_sandbox_int_error_arms_share_canonical_message_format` exercises both arms via `compile_str` and asserts they share the canonical message format (would-FAIL if the arms drift).

The code cleanup described here is done. Leaving open per the `closure-pin` + `phase-4-meta-deferred` disposition (closes via the deliberate phase-4-meta-core close-as-named bulk step, not piecemeal).

### #707
**Finding:** Dominant Safe-3 finding-class: invariant/contract wired correctly on the trafficked path but bypassed/divergent on a parallel less-trafficked entry point — 10 instances across 5 crates.
**Comment:**
Status update: the trust-subset of this META is closed; the non-trust invariant-coherence residual is not yet complete. Keeping open.

Closed (7/10): #634, #615, #617, #620, #594, #622, #605. The COLLAPSE work (PR #1251, commit 73009f05 `fix(#707-trust-subset)`) closed the trust-subset by collapsing the parallel revocation/recheck pipes into the single chain-validation seam. The design doc (`.addl/refinement-audit-2026-05/impl-design-COLLAPSE.md` §5, line 187) is explicit: "instances 1+10 are the trust subset; COLLAPSE closes them by construction; 2-9 are the non-trust §3.6k residual cross-linked but not in COLLAPSE PRs."

Still OPEN with the defect verified present in current main (d0ccf606 era; re-checked at HEAD 115b0847) — the non-trust residual the design doc cross-linked but did NOT carry in the COLLAPSE PRs (no commit grepping #633 / #618 / #630 exists):

- #633 — `Engine::update_node` (`crates/benten-engine/src/engine_crud.rs:182`, body lines 190-193) goes straight to `tx.delete_node` + `tx.put_node` inside the transaction closure and still skips the `is_system_zone_label` probe that `create_node` runs before the closure (`engine_crud.rs:68-77`, surfacing typed `E_INV_SYSTEM_ZONE`). So `update_node` still surfaces the storage-layer `E_SYSTEM_ZONE_WRITE` (TxAborted-wrapped) rather than the user-facing `E_INV_SYSTEM_ZONE`.
- #618 — `Engine::register_subgraph_aggregate` (`crates/benten-engine/src/engine.rs:3510`) still calls `validate_subgraph(&sg, &cfg, true)` (`:3517`), which uses raw `longest.len() > max_depth` (`crates/benten-eval/src/invariants/structural.rs:152-153`), vs `validate_builder`'s spec-correct CALL-primitive `call_count > max_depth` (`structural.rs:433-441`, per ENGINE-SPEC §4).
- #630 — `PendingHostOp::DeleteNode { cid }` (`engine.rs:4224-4226`) still emits no attribution (`tx.delete_node(cid)`), vs `PutNode`'s `put_node_with_attribution(... actor_cid, handler_cid, capability_grant_cid)` (`engine.rs:4215-4221`).

The proposed invariant-entry-point-coherence pim-N is also not yet codified in `.addl/dispatch-conventions.md` (note: the §3.6k label proposed in the issue body is already taken there by an unrelated iterate-to-convergence rule, so the codification needs a fresh section). Keeping open until the three residual instances and the codification land.

### #713
**Finding:** MaterializerOutput ships multi-row vocabulary (Vec<Cid> + filter_rows + delivered_row_cids alias) for a single-row walk — premature plurality.
**Comment:**
Status update (verified against current main, materializer.rs).

The dead-code half of this finding is closed. The zero-caller `delivered_row_cids` accessor (coupled to #315) was removed by the materializer API-surface simplification commit `768a3eb` (Refs #702 #713 #730 #732 #312 #315 #527; landed via the refinement-audit drain, bundle #1196 now CLOSED). That same commit also closed the coupled cluster siblings #315 (dead alias), #527/#702 (cap-recheck observability-theater loop), #730 (4 Value-walkers → 1 generic visitor), and #732 (redundant per-variant code field).

The headline multi-row vocabulary is RETAINED by design — effectively the DISAGREE/Option-C path from the original finding. Still present in `crates/benten-platform-foundation/src/materializer.rs`:
- `materialized_cids: Vec<Cid>` (line 344) — NOT collapsed to `Option<Cid>`
- `materialized_row_cids() -> &[Cid]` (line 382)
- `filter_rows_at_materialization(rows: Vec<Cid>, ...)` (lines 440-450)

The `768a3eb` change added a defending docstring (lines 377-380) stating the v1 walk is single-row (`content_cid` is singular, holds at most one element) and that Phase-4-Meta IVM-view materialization (one view = N rows) is where a true multi-row return materializes — the forward-architectural context for which the G-CORE-4 materializer recursive walk + IVM view path was wired (#1311, MERGED). The ST-PF lane disposition recorded #713 as "FIX-NOW (scoped): dead-alias half closed (#315); multi-row trait has live test consumers, documented as v1 single-row."

Keeping OPEN as a closure-pin (consistent with still-open cluster siblings #312 + #702): the dead-code sub-claim is resolved, but the Vec<Cid>-vs-Option<Cid> shape was decided in favor of retention rather than collapse, and the phase-4-meta-core freeze has not yet tagged.

### #735
**Finding:** Lock-duration META: locks held longer than needed across .await / persistent-store I/O / CPU-bound user callbacks / loop-redundant reacquire — 9+ instances across 6 crates; fix-shape snapshot-drop-act-reacquire-mutate.
**Comment:**
Status update: partial. The leaf-crate lock-duration instances closed; the core engine/eval lock-duration cleanup wave has not yet landed. Keeping the META open.

Closed (7/10): #675, #685, #626, #632, #644, #657, #641.

Still open with the defect verified present in current main (115b0847):
- #659 — `Engine::apply_atrium_merge` still calls `atrium.resolve_peer_dids` redundantly: once outside the per-row loop and again inside (`crates/benten-engine/src/engine.rs:1247, 1286, 1377, 1422`). mega6 PR #1277 (9b0f327f) explicitly notes #659 is "COLLAPSE/engine Lock-Duration META — out of the ST-SYNC lane", i.e. not closed there.
- #664 — the sweep entry points still hold the `wait_ttl_tracked_envelopes` + `wait_ttl_gc_stats` `Mutex` guards across the redb `get_wait`/`delete` I/O in `run_event_driven_sweep` (`engine.rs:4955-4972` holding guards across `wait_ttl_gc.rs:168-170, 188`).
- #649 — `with_active_handle` still holds the `ACTIVE_HANDLES` global `Mutex` across the user closure with `.expect()` (`crates/benten-eval/src/primitives/subscribe.rs:709-714, 1262`).

The proposed snapshot-drop-act-reacquire-mutate pim-N is not yet codified. All three remain `closure-pin` and are queued for the lock-duration cleanup wave per `docs/future/refinement-audit-2026-05.md` (§ around 1167-1180). Keeping open.

### #737
**Finding:** Pattern F flagship: Qual-1 structural-simplification refactors are the canonical fix for coupled Safe-1/Safe-2/Safe-3 real-bug findings at the same site — prefer the structural refactor over the surgical patch; 6 coupled bundles.
**Comment:**
Status update: the three *confirmed* Pattern-F coupled bundles are CLOSED and their fixes are live on `main`. This META is retained OPEN as the Phase-4-Meta Pattern-F tracking row per `docs/future/phase-4-backlog.md` §4.86.

Confirmed coupled bundles (verified in main):
- Bundle 1 (engine ReadContext helper) #534 + #695 — closed via PR #1290 (merge `20fb5c78`).
- Bundle 2 (sync CBOR encoding) #511 + #667 — closed via PR #1261 (merge `55f13baf`). `crdt.rs` now uses `StampedValueCodec` + `LoroValue::Binary` DAG-CBOR (`crates/benten-sync/src/crdt.rs:83-174`); the old `\x1f`/`splitn(4,':')` flat-string survives only in doc-comments.
- Bundle 3 (renderer-tauri parallel-array → typed slice) #499 + #611 + #670 + #681 + #693 — closed via PR #1235 (merge `336178a3`). `crates/benten-renderer-tauri/src/lib.rs:105` defines `IpcMethod` and line 127 the typed `IPC_METHODS: &[IpcMethod]` slice; the old `IPC_METHOD_NAME_ALLOWLIST` / `IPC_METHOD_CAP_BINDING` / `IpcAllowlist` survive only in doc-comments.

(Note: §4.86 attributes all three to `20fb5c78`; per git the three bundles actually closed across PRs #1290/#1261/#1235 — only Bundle 1 is `20fb5c78`.)

Remaining tracked items keep this row open:
- Sibling real-bug halves still open: #516 (benten-sync), #702 (benten-platform-foundation).
- #1161 benten-id trait-extraction bundle still open.
- The `phase-4-meta-deferred` siblings #700/#527/#505/#698 are closed.
- The proposed `pim-N-coupled-bundle-fix-pass-priority` codification is not yet landed in `.addl/dispatch-conventions.md` (§3.6k is the unrelated iterate-to-convergence rule).

Per §4.86 (landed via PR #1294) this row closes only when its remaining tracked sub-items close at Phase-4-Meta. Keeping open as the tracking row.

### #739
**Finding:** Lock-poisoning recovery posture asymmetry across the workspace — 4 sub-classes (.expect() panic, into_inner recover, typed-error, silent-swallow) diverge; silent-swallow at no-Result trait boundaries is the load-bearing concern; 4+ instances / 4 crates.
**Comment:**
Status update: all four primary instances are dispositioned, but the META's load-bearing concerns (uniform poisoning-posture discipline + codification) are not yet landed. Keeping open.

Closed (verified on main `115b0847`):
- #627 — `BrowserBackend::snapshot` and all `BrowserBackend` Mutex sites now use `lock_recover()` (`crates/benten-graph/src/browser_backend.rs:259-598`); the silent-swallow snapshot arm is gone.
- #637 — 16 `BrowserBackend` Mutex sites converted to `lock_recover()` (`crates/benten-graph/src/browser_backend.rs`).
- #631 — rate-limit `.expect("... poisoned")` replaced with `unwrap_or_else(std::sync::PoisonError::into_inner)` recovery (`crates/benten-caps/src/rate_limit.rs:280-283`); closure-pin test `poisoned_state_mutex_recovers_and_keeps_enforcing` (`rate_limit.rs:444`).
- #705 — closed by DOCUMENTING the intentional disposition (return `None`/`false`/`Vec::new()`/`0` = "store is dead", `bindings/napi/src/wasm_browser.rs:180-184`), NOT by the META's proposed class-(d) `tracing::warn!` + counter discipline. The silent-swallow arms still stand at `bindings/napi/src/wasm_browser.rs:179-237` (`Err(_) => None/false/Vec::new()/0`); no `tracing::warn!` or counter is present.

Still outstanding:
- Cross-ref #522 (the load-bearing class-(1) napi concern) remains OPEN — 13 `state.lock().expect("atrium state mutex")` panic-abort sites are still present in `bindings/napi/src/atrium.rs` (lines 321/491/510/522/533/585/784/830/847/855/877/914/973). Its fix-pass error emission must route through the #606 G19-B JSON envelope.
- The class-(d) silent-swallow `tracing::warn!` + counter convention was not adopted at #705.
- `pim-N-mutex-poisoning-recovery-posture-discipline` is still only a candidate (enumerated in `.addl/refinement-audit-2026-05/triage-safe-4.md`); not codified into memory / §3.5g, and no pre-push grep gate for the class-(d) drift exists.

Keeping open until the discipline / codification and the #522 cross-ref land.

### #822
**Finding:** Cross-crate Qual-2 pattern: 17 public fn/variant/type names promise one contract while the body carries a different one.
**Comment:**
Status update against current checkout HEAD `115b0847` (branch `phase-4-meta-core/orchestration-2026-05-26`; the phase-4-meta-core-close tag has NOT landed). All 17 sub-issues were closed 2026-05-15 as META-consolidation ("Tracking + closure happens at the META level"), so this META remains the live tracker; closing depends on the residual name-vs-body gaps, not on the sub-issue state.

MAJOR instances — substantively resolved in code:
- #794 `ExecutionStateEnvelope::envelope_cid()` is now infallible, returning `Cid` directly (`crates/benten-eval/src/exec_state.rs:402`; commit `f31ef18e`; docstring cites refinement-audit #794). Positive closure-pin: `crates/benten-eval/tests/v1_api_stabilization_closure_pins.rs::envelope_cid_is_infallible_accessor_794`.
- #750 `Did::from_string_unchecked` is now `pub(crate)` (`crates/benten-id/src/did.rs:128`); #835 discharge marker enforced in `crates/benten-crypto-suite`.
- #738 an honestly-named `from_dag_cbor_envelope` companion was added (`crates/benten-id/src/keypair.rs:335`). Note the originally-misnamed `from_seed_bytes` remains `pub` at `keypair.rs:319` (retained as DISAGREE-WITH-EXPLANATION — live napi/cross-crate surface).
- #745 the variant docstring was narrowed to handshake-decode (`crates/benten-sync/src/errors.rs:103-117`). Note the variant-name gap itself persists: `crates/benten-sync/src/mst_proto.rs` still constructs `AtriumTransportError::HandshakeWireFormat` for MST-diff wire-format failures (e.g. `mst_proto.rs:88/160/174/236/250/255`).
- #769 `Acceptor::with_parent_lookup` is deleted from source (only DELETED-marker test references remain).

Minor instances — name-vs-body gap still present in current main:
- #749 `GraphError::Redb(String)` is still constructed at non-redb sites, e.g. `crates/benten-graph/src/in_memory_backend.rs:91` ("in-memory: lock poisoned") and `transaction.rs` "after commit/abort" sites; `crates/benten-graph/src/backend.rs:238` still describes the variant as one that "lies about where the error came from" (the `KVBackend` error-polymorphism trait offers a way out but `InMemoryBackend` still uses `GraphError::Redb`).
- #811 `validate_chain_no_time_check` is retained and consumed at three production call sites in benten-caps (`crates/benten-caps/src/chain_authority.rs:94/164/267`) plus benten-sync handshake (`handshake.rs:574/699`).
- #756 `get_node_label_only` is still `pub fn` (`crates/benten-graph/src/lib.rs:144`; docstring at :133 openly admits "label-only describes the return shape, not a byte-bounded header read"). This symbol is named in the ratified §4.43 v1-API-stabilization sweep (`docs/future/phase-4-backlog.md`: `Engine::get_node`/`put_node`/`get_node_label_only`/`resolve_subgraph_cid_for_test` → `pub(crate)`, applied atomically at the G-CORE-9 v1-API freeze) — not yet applied at HEAD.

Keeping open as the v1-API-stabilization tracker until the residual minor instances are closed or explicitly dispositioned at the phase-4-meta-core freeze.

### #841
**Finding:** Diagnostic.error_code is &'static str but the 4 valid values were pub(crate) consts, forcing consumers to hardcode string literals.
**Comment:**
Status update against current main (`d91896b3`).

The fix for this finding has landed via PR #1325 (merge `132dde91`, ancestor of HEAD; commit body explicitly lists `close ... #841`). The four `E_DSL_*` error-code constants are now `pub const` (promoted from `pub(crate)`) at `crates/benten-dsl-compiler/src/lib.rs:696,698,700,711`, so downstream consumers can match against the typed constants instead of duplicated string literals. The promotion rationale is documented at `lib.rs:685-695`, which also records that the enum-typed-code refactor (finding paths (b)/(c)) is intentionally kept out of scope to keep the v1-API surface narrow — consistent with this finding's recommendation of path (a) as the smallest-blast-radius pre-v1 landing.

The `&'static str` shape of `Diagnostic::error_code` is preserved (a wire-stability constraint). Note: one consumer (`tools/benten-dev/src/lib.rs:589`) still matches the literal `"E_DSL_INVALID_SHAPE"` rather than the now-`pub` constant; that consumer-side migration is additive/optional, not part of the original defect (which was the missing typed export — now present).

The API-coherence concern described here is addressed in code. Leaving open per the `phase-4-meta-deferred` / `closure-pin` label disposition pending phase-4-meta-core closure.

### #848
**Finding:** id_for fallback `_ => "op"` is a silent default on new #[non_exhaustive] PrimitiveKind variants — defense-in-depth gap that could silently CID-collide future variants.
**Comment:**
Status update against current main (`d0ccf606`). The fix for this finding has landed via PR #1325 (merge `132dde91`, ancestor of main; body: `closes ... #848`).

`id_for`'s silent `_ => "op"` fallback is gone (no `=> "op"` remains in the file). It is now a loud `unreachable!` panic at `crates/benten-dsl-compiler/src/lib.rs:1515-1521`, naming #848 + CLAUDE.md #1's 12-primitive irreducibility, with explicit per-variant 2-char prefixes for all 12 kinds at `lib.rs:1488-1499` (inside the `match` opened at `lib.rs:1487`, in `fn id_for` at `lib.rs:1479`). A 13th `PrimitiveKind` variant landing without a matching arm now fails loudly instead of silently CID-colliding under an `op` prefix — fix path (a) from this issue.

The defense-in-depth gap described here is closed in code. Leaving OPEN per the `phase-4-meta-deferred` label (intentionally not closed during this audit; closes on the phase-4-meta-core closure / deferred-label disposition).

### #857
**Finding:** async/sync convention is inconsistent across analogous napi surfaces; finding asked for (Phase A) a documented bucket convention and (Phase B) AsyncTask conversion of blocking ops.
**Comment:**
Status update (verified against main `d0ccf606`; napi tree byte-identical at orchestration tip `d91896b3`).

The **Phase B (AsyncTask conversion) half** of this finding has substantively landed — the premise that the binding has "zero AsyncTask instances in production method signatures" no longer holds. Atrium's blocking mutators now return `AsyncTask`, with the `block_on` moved inside `Task::compute()` off the JS thread: `join` (atrium.rs:358 → `JoinTask`), `leave` (:397), `rejoin` (:429), `declareDeviceAttestation` (:567), plus the local-device setters/clearers (`SetLocalDevice{Did,Keypair,Attestation}Task`). `StreamHandleJs::next` is also AsyncTask-based (lib.rs:2300 → `AsyncTask<NextChunkTask>`). This work landed via PR #1299 (META #744 PR-A) and the G-CORE-10 PR-B StreamHandle migration (#1203 / #652) — note neither PR references or closes #857.

What remains **OPEN is the Surf-1 composability/convention half**, which is the actual subject of this finding. INTERNALS.md §6g/§4 now documents the async-vs-sync status of each migrated surface case-by-case, but there is still:
- no named normative convention (the proposed "synchronous-pure / asynchronous / handle-returning" three-bucket labelling); no `bucket` / `synchronous-pure` / `handle-returning` terminology exists anywhere in `bindings/napi/src/` or INTERNALS.md, and
- no per-method rustdoc bucket label or structural invariant keeping analogous surfaces aligned.

Keeping open for the convention-documentation (Phase A) work.

### #871 ⚠️acc=False
**Finding:** 11 public benten-engine enums (and 7 structs) with named-field variants lack #[non_exhaustive], a v1 SemVer hazard; apply the attribute before v1 tag.
**Comment:**
Partially addressed by the META #907 `#[non_exhaustive]` sweep in the G-CORE-9 freeze wave (V1-FROZEN-INTERFACE.md §11 "MAXIMALIST application"; audit pin at `crates/benten-engine/tests/g_core_9_non_exhaustive_audit.rs`, landed via #1347/#1351/#1356).

Of this finding's enums, 5 now carry the attribute at main `d0ccf606` (re-verified):
- `AtriumMode` (atrium_api.rs:109)
- `ResumePayload` (engine_wait.rs:173)
- `SuspensionOutcome` (engine_wait.rs:192)
- `ManifestEnvelopeRecheckOutcome` (manifest_envelope_recheck.rs:105)
- `ThinClientError` (thin_client_subscribe.rs:114)

Still MISSING the attribute at `d0ccf606` — and explicitly in the §11 must-APPLY set (line 995), NOT carved out:
- `UserViewInputPattern` (outcome.rs:39)
- `TraceStep` (outcome.rs:365)
- `Transport` (thin_client.rs:597)
- `SubscribeCursor` (engine_subscribe.rs:72)
- `StreamCursor` (engine_stream.rs:150)

The 7 public-field structs in the finding are neither applied nor carved out (§11's struct-apply set covers CapWriteContext/ReadContext + benten-graph WriteContext, not these): `EngineConfig`/`SandboxSection` (engine_config.rs:82/90 — note §11 line 994 "engine_config::* YES" refers to the `EngineConfigError` enum at line 195, not these serde structs), `AtriumConfig`/`SyncStatus` (atrium_api.rs:64/128), `Challenge`/`SessionToken`/`SessionConfig` (thin_client.rs:166/193/230). `SandboxSection` is the load-bearing one (serde-deserialized from operator `engine.toml`).

The audit test only pins AtriumMode / NextChunkPoll / SuspensionOutcome / ManifestVerifyMode — none of the 5 missing enums or 7 structs. META #907 remains OPEN. Keeping this closure-pin open until the remaining 5 enums + 7 structs are applied (or explicitly carved out in the §11 carve-out registry).

### #899
**Finding:** Systemic CLAUDE.md #5 deprecated-alias pattern: 5 retained aliases/shims across benten-engine + bindings/napi.
**Comment:**
Status update against `origin/main` d0ccf606 (phase-4-meta-core-close tag NOT yet landed; v1-beta untagged).

Four of the five named aliases are deleted from production source (all four sub-issues CLOSED; code re-verified):
- #821 `create_user_view` — no `fn create_user_view` in `crates/benten-engine/src/` (only `register_user_view` at `engine_views.rs:700`); remaining hits are docstrings/comments.
- #864 `register_crud_with_grants` — gone; only `register_crud` remains (`crates/benten-engine/src/engine.rs:3566`).
- #866 `with_policy_allowing_revocation` + `with_grant_backed_policy` — gone (zero matches); only `capability_policy_grant_backed` remains (`crates/benten-engine/src/builder.rs:171`). `benten-caps/src/ucan_grounded.rs:9` notes the alias was removed.
- #868 `envelope-cache-test-grade` Cargo feature — deleted; `crates/benten-engine/Cargo.toml [features]` no longer declares it, and guard test `crates/benten-engine/tests/envelope_cache_test_grade_feature_retired.rs` asserts both engine + napi Cargo.toml stanzas are gone.

Not yet done — the napi `createUserView` alias (#861) is STILL LIVE at `bindings/napi/src/lib.rs:912-913`, forwarding to `register_user_view`, docstring still reading "Held through one transition window ... remove in a follow-up." #861 was CLOSED 2026-05-18 as a HARD-RULE clause-(b) deferral (label `phase-4-meta-deferred`, priority:p2) to the §4.43 v1-API-stabilization sweep (`docs/future/phase-4-backlog.md:1352`, napi binding slice), NOT as a deletion. That sweep has not landed.

Keeping this META open until the napi `createUserView` alias is retired in the §4.43 v1-API-stabilization sweep, pre-v1-tag (the META's stated hard deadline: any alias surviving the v1 freeze becomes SemVer-permanent).

### #907 ⚠️acc=False
**Finding:** Inconsistent #[non_exhaustive] discipline on public enums/structs across the workspace; v1 SemVer hazard.
**Comment:**
Status update against main `d0ccf606` (re-verified, all `#[non_exhaustive]` checks read from source + cross-checked against `docs/public-api/*.txt`).

**Landed (origin sites + cross-crate siblings now CLOSED):**
- benten-core: `WriteAuthority` (lib.rs:122), `ChangeEvent` (change_stream.rs:123, #1015), `OperationNode` (subgraph.rs:195, #1024) all carry the attribute — #837/#1015/#1024 CLOSED.
- benten-caps: `TypedCapGroup` (typed_cap_mapping.rs:46, #998) — CLOSED.
- benten-ivm: `AlgorithmError` (algorithm_b.rs:464, #917) — CLOSED. `Strategy` (strategy.rs:59) intentionally exhaustive per D8 (Path A keep-closed, #1085 CLOSED) — out of scope.
- benten-eval: `PrimitiveHost` (#1008) — CLOSED.

**Still outstanding — benten-engine (#871, still OPEN):** 10 public enums (not 5) lack `#[non_exhaustive]` — 17 of 27 top-level `pub enum` in `benten-engine/src` carry it. Missing:
- `UserViewInputPattern` (outcome.rs:39; docstring already says "the surface widens in Phase 3")
- `TraceStep` (outcome.rs:365)
- `Transport` (thin_client.rs:597)
- `StreamCursor` (engine_stream.rs:150; docstring notes `Persistent` mode is deferred to Phase 3 — future-widening intent)
- `SubscribeCursor` (engine_subscribe.rs:72)
- `DeviceAuthError` (layer_d/device_auth.rs:44)
- `DeviceLinkError` (layer_d/device_link.rs:136)
- `GrantRejection` (layer_d/grant_acceptance.rs:31)
- `PermissionOperation` (layer_d/remote_permission.rs:83)
- `SecretStoreError` (layer_d/secret_store.rs:26)

All 10 are genuinely public (`pub mod layer_d`; several re-exported at crate root, e.g. `benten_engine::StreamCursor`, `benten_engine::TraceStep`).

**Still outstanding — other crates (workspace meta scope):**
- benten-sync (#1072, OPEN): 6 enums lack it — `MessageKind` (handshake.rs:229), `HandshakePayload` (handshake.rs:323), `BootstrapMode` (peer_discovery.rs:55), `TransportKind` (transport.rs:116), `TransportStatus` (transport.rs:132), `GenError` (two_cid_store.rs:274).
- benten-graph (#997, OPEN): field/variant-level surfaces still pending.

Keeping open until the residual benten-engine + benten-sync + benten-graph surfaces are swept (or each is ratified as a sealed set per the `pim-N-cross-crate-non-exhaustive-discipline` candidate). Note: this meta is broader than #871 (which is scoped to named-field-variant enums); the residual benten-engine set above includes unit/tuple-variant enums that are in #907's scope but outside #871's title scope.

### #923
**Finding:** Layering/extensibility invariants asserted in docs but not type-system-enforced; 6+ paper-only instances across 5 crates (P0).
**Comment:**
Status update against current main `115b0847` (this P0 META is a closure-pin BLOCKER cluster; partial progress, several constituent instances still open).

Closed instances (all via real merged PRs, verified in main code):
- #883 (the load-bearing dep-direction doc-lie) — closed. The benten-caps doc-comment now explicitly retracts the stale "MUST NOT depend on `benten-platform-foundation` in production" invariant and documents the real native-only (`cfg(not(wasm32))`) prod edge added at G27-D (`crates/benten-caps/src/manifest_envelope_chain_validation.rs:27-49`, matching `Cargo.toml:104`). Closed by #1269.
- #830 — closed. `GrantReader` now has a real production consumer: the engine's `BackendGrantReader` wraps `Arc<RedbBackend>` and impls `GrantReader` (`crates/benten-engine/src/builder.rs:828-870`), injected as `Arc<dyn GrantReader>` (`builder.rs:563`); documented in `crates/benten-caps/INTERNALS.md:43`. Closed by #1290.
- #832 / #836 / #860 (benten-graph shape-lock trio) — all CLOSED COMPLETED via #1290.
- #850 — closed as a ratified crypto-agility decision (Ed25519/did:key as v1 default, #1290). Note: `MultiSigSurface` now has TWO impls in benten-id (`Ed25519SingleKey` at `crates/benten-id/src/multi_sig.rs:96`, plus a compile-only `ThresholdMultiSig` extension-point placeholder at `multi_sig.rs:144` that returns `MultiSigError::PostPhase3`). There are still ZERO cross-crate impls and the load-bearing surfaces remain hardcoded-Ed25519; the concrete recovery-protocol choice is the v1-assessment-window Kith item.

Still open / unenforced (keeping this META open):
- #856 — `Renderer::backend_name()` still returns free-form `&'static str` (`crates/benten-platform-foundation/src/materializer.rs:561` trait decl, `:592` impl returns `"browser-wasm32"`) with no type-level mapping to the `RendererBackend` enum (`plugin_manifest.rs:527`).
- #846 — napi binding still reaches directly into engine impl modules: `benten_engine::atrium_api::*` + `benten_engine::engine_sync::*` (`bindings/napi/src/atrium.rs:54-55`, `:985`), `benten_engine::manifest_signing::*` (`bindings/napi/src/lib.rs:1726`, `:1823`), and still carries the `benten-eval` dev-dep workaround for the missing `AttributionFrame` re-export (`bindings/napi/Cargo.toml:171`, used at `trace.rs:299`).

Keeping this BLOCKER-cluster META open until the remaining paper-only instances (#856, #846) are either type-system-enforced or have their docs retracted.

### #930 ⚠️acc=False
**Finding:** INTERNALS.md HEAD-pin/state-header self-drift from PR #250: 8 new stale instances across the 12 refreshed files.
**Comment:**
Status update against main `d0ccf606`.

The tracked crate INTERNALS.md sweep landed: the stale author-time SHAs (`c589ffe` / `85ecb69`) are gone from all `crates/*/INTERNALS.md` (git grep finds zero), headers are retensed to post-tag framing ("Last refreshed: 2026-05-24 against main HEAD `a0b75637` ... post `phase-4-foundation-close` tag baseline"), and the four stale phase-state headers (benten-engine / benten-eval / benten-ivm / benten-platform-foundation — the "in CI at PR #242" / "R6-FP-4 close" / "R6 R3 ready" framings) are corrected (all three phrases now grep to zero in `crates/*/INTERNALS.md`). The quick-wins-2 commit `95bd14f` referenced above is not in main history; the fix reached main via another path.

Not yet done: `bindings/napi/INTERNALS.md` — the sub-site this issue's table named (row "bindings/napi | INTERNALS.md:3+L185+L282") and the closure comment flagged as out-of-scope-for-batch-2 — is still drifted in main. It carries three stale `c589ffe` cites (now at lines 3, 185, 301; the file has grown since the original audit so the third moved from L282 to L301) plus a stale catalog count ("168 throwable variants at HEAD `c589ffe`", line 185) against the current `CATALOG_VARIANT_COUNT = 199` (`crates/benten-errors/tests/stable_shape.rs:1091`; also pinned at `docs/V1-FROZEN-INTERFACE.md:97`). Keeping open until the napi file is swept.

### #997
**Finding:** 6 pub struct/variant surfaces (GraphError's 4 struct-variants + WriteContext + ChangeEvent) lack field-level #[non_exhaustive], locking field shapes at v1.
**Comment:**
Partial-closure status (verified against main d0ccf606). 4 of the 6 cited surfaces now carry `#[non_exhaustive]`:

- `GraphError::BackendNotFound` (lib.rs:512)
- `GraphError::SystemZoneWrite` (lib.rs:521)
- `GraphError::InvImmutability` (lib.rs:555)
- `WriteContext` struct (lib.rs:941) — landed at G-CORE-9 freeze row 8c, commit 75a1d33a (7 test-construction sites migrated to the builder pattern).

(Umbrella #1207 closed but did not auto-close this issue — these 3 GraphError variants landed in #1207; WriteContext landed later at the G-CORE-9 freeze.)

Two surfaces remain open:

1. `GraphError::TxAborted` (lib.rs:585-595) is deliberately NOT marked. The inline comment cites cross-crate PRODUCTION construction (`crates/benten-engine/src/engine_diagnostics.rs:87,105` — verified) and defers the apply-plus-constructor-plus-migrate work to `docs/future/phase-4-backlog.md §4.43`. However V1-FROZEN-INTERFACE.md §4.43 (line 314) directs "APPLY defensively ... The freeze MUST not ship without these." This doc-vs-code discrepancy should be reconciled before the v1-beta tag.

2. `benten_graph::store::ChangeEvent` (store.rs:485, re-exported as `benten_graph::ChangeEvent` at lib.rs:97) still lacks the attribute (9 pub fields, `#[derive(Debug, Clone)]` only). The §4.43 row applied/kept `#[non_exhaustive]` on `benten_core::change_stream::ChangeEvent` (change_stream.rs:123) — a DISTINCT type from the benten-graph `ChangeEvent` this finding cited; the freeze-doc table row that marks "benten-graph ChangeEvent (re-export): YES" appears to conflate the two. The benten-graph local struct is not covered.

Keeping open for these two surfaces.

### #1002
**Finding:** No installModuleSigned napi surface — Phase-4-Meta plugin install path with manifest envelope/registry trust anchor had no napi entry point.
**Comment:**
Status update (verified against `origin/main` d0ccf606): the core gap — no signed napi install surface — is closed. A new `installModuleSigned` method is live at `bindings/napi/src/lib.rs:1763-1832`, landed in main via PR #1282 (merged 2026-05-17, ST-NAPI lane consolidation; the lane references #1206 as a partial — "Refs #1206", not a full Closes). The method:
- validates a 32-byte Ed25519 registry pubkey (`E_REGISTRY_PUBKEY_MALFORMED`),
- validates the engine audience DID via `did:key` resolution (`E_PLUGIN_DID_MALFORMED`),
- threads the publisher-registry trust anchor through `benten_engine::manifest_signing::ManifestVerifyArgs::registry(&registry_pubkey, &audience, now)`,

closing the cdylib unsigned-install bypass (Compromise #21 / #684 face) for the registry-signed path. `installModule` remains the explicit unsigned-development opt-in.

Keeping OPEN (this is a closure-pin against umbrella #1206, which #1282 only Refs):
- The richer structured return the finding proposed is not yet surfaced — `installModuleSigned` returns only the installed CID string; no minted plugin-DID, no composition-cycle result, no library-subgraph version-anchor / install-record CID.
- No composition-cycle detection at this surface: `E_PLUGIN_COMPOSITION_CYCLE` exists only as a reserved code in `benten-platform-foundation/INTERNALS.md` + the errors catalog, not wired through the napi binding.
- UCAN-chain / dual verification modes (`ManifestVerifyArgs::ucan_chain` / `::dual`) and the recommended `installModule` → `installModuleUnsignedDevelopment` rename are named as a multi-crate follow-on (P-II), not landed.

### #1010
**Finding:** delegateCapability accepted plugin_did as a raw, unvalidated String; no install-time-mint surface for the plugin-DID.
**Comment:**
Status (verified on current main d0ccf606): the specific defect this issue names — `delegateCapability` accepting `plugin_did` as an unvalidated raw String — is RESOLVED. `delegate_capability` now constructs a `Did` from the supplied `pluginDid` and calls `Did::resolve()` (W3C did:key prefix + base58btc + Ed25519 multicodec + 32-byte pubkey round-trip), rejecting malformed audience DIDs with a stable `E_PLUGIN_DID_MALFORMED:` error before forwarding to the engine seam (bindings/napi/src/lib.rs:853-866). This landed on main via the ST-NAPI lane consolidation PR #1282 (6ed80b2f).

The sibling Layer-3 gap #1018 is closed (PR #1294): the napi binding routes through `inner.caps().delegate_capability(..)` unconditionally with no `shares` bypass, and the engine-side validator fails closed with `ErrorCode::PluginDelegationOutsideManifestEnvelope` (crates/benten-caps/src/manifest_envelope_chain_validation.rs:202/208). `installModuleSigned` also now exists on main (lib.rs:1763), though post-COLLAPSE it takes the audience DID as a caller-supplied input (validated the same way) rather than minting-and-returning a typed plugin-DID newtype — so the body's gap #1 (mint-and-return surface) and gap #3 (`listInstalledPlugins`) were dissolved by that design pivot rather than built; the residual install-surface work tracks under the still-open #1002.

Leaving this open per the closure-pin discipline: it is bundled under umbrella #1206 (still OPEN, with #1002 outstanding), and the standing instruction above is to close when the bundle PR merges. The validation defect itself is no longer present.

### #1035
**Finding:** SANDBOX host-fn extensibility needs explicit ratification of whether plugin-defined host-fns are permanently forbidden or opt-in at Phase 5+; the HOST_FN_NAMES const + regression pin lock the shape but the trajectory commitment was not enumerated.
**Comment:**
Status update — decision RATIFIED, but the issue's tracked follow-through is incomplete; keeping open.

The trajectory question is settled: per Ben's ratification comment (2026-05-15) and `docs/future/refinement-audit-2026-05.md §15.3 item 9`, option (a) is RATIFIED — the SANDBOX host-fn set is permanently closed at exactly four (`time` / `log` / `kv:read` / `random`) per CLAUDE.md baked-in #16; plugin-level extension lands only via the CLAUDE.md #19 engine-extension category (recompile-your-engine), never as plugin-defined SANDBOX host-fns. The shape is locked in code at `crates/benten-eval/src/sandbox/host_fns.rs:152` (`const HOST_FN_NAMES: &[&str] = &["time", "log", "kv:read", "random"]`) and pinned by `crates/benten-eval/tests/host_fn_no_storage_mutating_per_baked_in_16.rs`.

NOT YET DONE: the ratification's mandated codification — "Codify in `.addl/dispatch-conventions.md` to prevent future re-litigation" (§15.3 item 9) — has not landed (no host-fn / baked-in-#16 / SANDBOX-closed-set entry present in `.addl/dispatch-conventions.md` at main `d91896b3`). This issue carries `arch-anchor` ("RATIFIED architectural decision anchor; pending substantive implementation") + `priority:p1`; sibling §15.3 arch-anchors are closed only once the residual lands (e.g. #1016 closed after commit `2f80134`/PR #1166) or via an explicit HARD-RULE clause-(b) named destination (#1027 → phase-4-backlog via #1294). Neither has occurred here, so keeping this open to track the dispatch-conventions codification.

### #1064
**Finding:** META roll-up: hot-loop allocation-per-step pattern across 9 sub-issues in 6 crates; perf-fix-pass anchor.
**Comment:**
Status update against current main (`d0ccf606`).

5 of the 9 named sub-findings are now CLOSED (state COMPLETED):
- #983 + #984 (benten-engine)
- #926 + #932 (benten-core TIER-1 per-WRITE alloc reduction — landed via PR #1236, squash `ac745e62`)
- #954 (benten-caps derive_write_scope/derive_read_scope)

Still OPEN — defects re-confirmed present in main:
- #1021 (benten-eval `Evaluator::run_inner`, `crates/benten-eval/src/evaluator.rs:222` — per-step `HashMap::new()` + `op.id.clone()`)
- #938 (benten-ivm `apply_event`, `crates/benten-ivm/src/subscriber.rs:286` — `String::from(view.id())` logging-only alloc at :292)
- #945 (benten-ivm `AlgorithmBView::materialize`, `crates/benten-ivm/src/algorithm_b.rs:1293` — always-on `assert!` should be `debug_assert!` + redundant canonical recompute; note this sub-issue re-scoped from the original `walk_writes` framing in the table above)
- #975 (bindings/napi `trace_step_to_json`, `bindings/napi/src/trace.rs:28` — ~12+ per-field `.to_string()`/Map allocs per step)

Keeping this META OPEN as the perf-fix-pass anchor until the remaining four sub-issues close. Per `docs/future/phase-4-backlog.md:1131`, the sub-issue set is the intentional Phase-4-Meta perf-pass window (pre-Phase-5-reference-app baseline); the umbrella stays OPEN, not force-closed. The proposed `pim-N-hot-loop-allocation-per-step-audit` codification is not yet ratified (no memory file exists).

### #1070
**Finding:** META: Pattern F 3-axis convergence (Qual/Safe/Fwd) extension — 6 cross-lens bundles; pim-N codification candidate extending #737.
**Comment:**
Status update against current main (115b0847).

This META bundle-pr/closure-pin anchor tracks the Pattern-F 3-axis (Qual ↔ Safe ↔ Fwd) convergence bundles. Progress so far:

Fully CLOSED bundle:
- benten-renderer-tauri Bundle-3 expansion — #933 landed via PR #1235 (Wave-1 Strategy-C renderer Pattern-F batch).

Partially CLOSED bundles (named perf members landed, bundle not fully drained):
- benten-id: #968 + #973 + #976 CLOSED (PRs #1256/#1282).
- bindings/napi: #1052 + #1055 CLOSED (PRs #1255/#1260/#1261); #975 still OPEN (p1).
- benten-sync LoroDoc: #999 + #1011 CLOSED (PRs #1261/#1277).
- benten-caps cap-policy cascade: #928 + #946 + #954 CLOSED (PRs #1282/#1294/#1290); #936 still OPEN (p1) — confirmed in code: crates/benten-caps/src/backends/ucan.rs:356 iter_installed_proofs still does a full KV_GRANT_PREFIX scan + per-entry CBOR decode on every check, no proof-by-required-cap index.

Still OPEN within the named bundles (all priority:p1 closure-pins):
- #975 (napi trace_step_to_json hand-rolled JSON), #948 + #981 (benten-platform-foundation cycle-detect), #936 (benten-caps proof-scan index).

pim-N codification: the 3-axis extension of Pattern F (#737, still OPEN) remains an uncodified candidate (subsequently noted as extendable to a 4-axis doc-layer form at cross-crate triage); not yet ratified.

Per docs/future/phase-4-backlog.md:1415, this issue stays OPEN as the Phase-4-Meta 3-axis Pattern-F tracking row until the remaining bundle members land. Keeping open as the bundle-coordination + codification anchor.

### #1093 ⚠️acc=False
**Finding:** META (Pattern α): forward-architectural shapes named in baked-in docs but paper-only (no seam) at v1 tag — 6+ instances across 4 crates.
**Comment:**
Status update against current main (HEAD 115b0847).

Pattern-alpha instance roll-up:
- CLOSED: #1056, #1060, #1069 (benten-id SelfRevocation / plugin-DID subgraph projection / RotationLog::is_superseded), #1083 (benten-ivm materializer + SubgraphSpec), #827 (renderer Renderer-trait dead-stub).
- #1014 (benten-platform-foundation Registry trait): the paper-only `trait Registry` was RETRACTED per RATIFIED Path A 2026-05-15. registry.rs at HEAD has no `trait Registry` (only the concrete data shapes RegistryEntry/DiscoveryQuery/DiscoveryResult remain, intentionally Phase-4-Meta-reserved with a no-production-callsites test pin). The fix-bundle #1198 ("Registry trait pull from v1, RATIFIED Path A #1014") is CLOSED-COMPLETED (PR #1262, registry.rs +INTERNALS retense). #1014 is now a stale closure-pin that never auto-closed; its substantive forward-shipped-trait defect is resolved in merged code. The doc-drift sibling #1107 stays OPEN separately.
- #838 (per-plugin IPC method registration seam) remains OPEN but is labelled phase-4-meta-deferred — an accepted deferral, not outstanding fix-now debt. IPC_METHOD_NAME_ALLOWLIST is still the single closed-const authoring path.

The proposed pim-N (`pim-N-forward-architectural-shape-paper-only-pre-v1`) is NOT yet ratified: it appears only in refinement-audit-2026-05 triage candidate lists; the suggested §3.6k destination is already occupied by an unrelated convergence-cadence rule, so no codification has landed in .addl/dispatch-conventions.md.

Keeping OPEN as the pre-v1-tag forward-architecture sweep coordinator (the phase-4-meta-core-close tag has not landed and the pim-N is unratified).

### #1095
**Finding:** META: phase-rename in-source trajectory drift (stale 'Phase 3+' / 'Phase 8 marketplace' strings) — workspace-wide sweep; 6+ instances across 4 crates.
**Comment:**
Status update against current main (115b0847).

The benten-eval slice of this sweep landed. The benten-eval-scoped bundle umbrella #1166 ("benten-eval: phase-rename in-source trajectory sweep") closed via PR #1277 (Strategy-C mega6 batch, squash 9b0f327f, merged 2026-05-17): sub-issues #1016, #1027, #1084, #1023, #1017 are all closed, and crates/benten-eval/src/sandbox/manifest.rs:14/46/312/333 now reframe the references as the historical "Phase 8 marketplace" framing (pre-dates v1-scope).

However the workspace-wide in-source sweep this META coordinates is NOT complete. Stale "Phase 3+" trajectory strings still ship outside benten-eval, including in production code (not just test narrative):
- crates/benten-graph/src/redb_backend.rs:303
- crates/benten-ivm/src/view.rs:81 (and the live runtime payload literal `deferred_to_phase: "Phase 3+"` at crates/benten-ivm/src/testing.rs:66, flagged at testing.rs:54)
- crates/benten-engine/src/outcome.rs:54, src/error.rs:117, src/engine_views.rs:678, src/engine.rs:303
- crates/benten-errors/src/lib.rs:198 and :335

Additionally #1035 (the SANDBOX host-fn extensibility Phase-5+ trajectory ratification) remains open, and the pim-N codification deliverable (`§3.5k phase-rename in-source trajectory sweep`) has not landed in .addl/dispatch-conventions.md (the §3.5 chain goes 3.5h → 3.5i → 3.5j → 3.5l, no 3.5k).

Keeping open as the pre-v1-tag sweep coordinator until the remaining in-source phase-rename strings are swept (FIX-NOW string-edits where behavior is unchanged; sharpen-to-wave-target where the deferral is real) and the §3.5k codification lands.

### #1096
**Finding:** META #669 ext: Layers 2+3 plugin-trust paper-only — 3 napi BLOCKERs (#1002/#1010/#1018) + IVM-view gap (#1086); 8+ sites.
**Comment:**
Status update against current main (`d0ccf606`).

The substantive Path-A napi wire-up that this cluster called for has largely LANDED via PR #1294 (merged to main 2026-05-18 as merge commit `39a0e732`):

- **#1018 — CLOSED** (by #1294). Real Layer-3 manifest-`shares` enforcement is now wired into the engine delegation path: the production `AllPermit` shim is deleted and replaced by `SharesPolicyResolver` (`crates/benten-engine/src/shares_policy_resolver.rs`, threaded through `engine.rs` + `engine_caps.rs`). `AllPermit` now survives only as a `#[cfg(test)]` test-double. Regression pin: `bindings/napi/tests/cap_delegate_napi_layer3_shares_enforcement_1018_closure_pin.rs`.
- **napi `delegateCapability` (the #1010 surface)** now parses + validates `plugin_did` via `Did::resolve()` and rejects malformed DIDs with `E_PLUGIN_DID_MALFORMED` (`bindings/napi/src/lib.rs:854`, comment marks it "#1206 closure"). The raw-String-forwarded-with-no-inspection defect is gone in production.
- **A signed-install entry point now exists** at `installModuleSigned` (`bindings/napi/src/lib.rs:1763`), doing real `ManifestVerifyArgs::registry` verification (`bindings/napi/src/lib.rs:1823`) rather than `unsigned_development()` — this addresses the #1002 "no installModuleSigned surface" finding (note: the original `installModule` still defaults `unsigned_development()`; the signed sibling is now its companion).
- **#1056 + #1060 — CLOSED**, but as close-as-named DEFERRALS to Phase-4-Meta (HARD-RULE clause-b), with destinations in `docs/future/phase-4-backlog.md` (Wave-E W20) landed via #1294 — NOT substantive wire-up.
- **#1086 — CLOSED** (by #1290). (The actual #1086 is the `Strategy::C → Strategy::Reserved` rename, not the IVM `register_user_view` principal-binding gap the cluster body describes — the body's sub-issue numbering predates renumbering.)

Keeping OPEN as the BLOCKER-cluster anchor: bundle umbrella #1206 is still OPEN (its checklist tracks #1002/#1010/#1018), #1002 and #1010 remain OPEN as closure-pins under it, and parent META #669 is still OPEN. The remaining work is bundle-bookkeeping closure rather than missing production code.

(Earlier draft cited sub-branch commits `0dbfa438` / `5c22ee2d` — those are on the pre-rebase `wave-e/held` branch and are NOT in main; the landed form is merge commit `39a0e732` / PR #1294.)

### #1099
**Finding:** META: module-doc vs INTERNALS vs substance divergence (3+ at renderer-tauri); §3.6f extension pim-N candidate.
**Comment:**
Status update against current main (origin/main `d0ccf606`; phase-4-meta-core NOT yet closed). KEEP-OPEN — both closure conditions for this META remain unmet.

**Per-instance state vs. underlying divergence (the distinction matters for a META):**

- #827 (renderer `Renderer`-trait structurally dead) — CLOSED 2026-05-18.
- #1009 (dsl-compiler INTERNALS §8 "could happen" forecast) — issue marked CLOSED 2026-05-15, **but the named divergence is still live in main.** `crates/benten-dsl-compiler/INTERNALS.md:170` + `:180-184` still present schema-driven-rendering as an open forecast ("nothing schema-rendering-shaped has landed … until that decision is made"; "If the plan instead chooses a sibling crate … a `benten-schema-compiler` crate appears next to it"). In reality the sibling-crate path **already shipped**: `benten-platform-foundation::schema_compiler` (G23-A, ~1.7k LOC, `compile(bytes) -> Result<SchemaSubgraphSpec, _>`, per `crates/benten-platform-foundation/INTERNALS.md:69-83`). Forecast-narrative vs SHIPPED-narrative drift, exactly the §3 sub-class this META names.
- #1016 (eval `register_runtime` "Phase 8 marketplace" stale framing) — issue marked CLOSED 2026-05-16, the code-doc was retensed (`crates/benten-eval/src/sandbox/manifest.rs:8-16` now says the historical "Phase 8 marketplace" framing pre-dates the CLAUDE.md #15 widening and is a Phase-4-Meta concern), **but the INTERNALS did not follow**: `crates/benten-eval/INTERNALS.md:121` still says "until Phase-8 marketplace work" and `:213` still says "`register_runtime` (Phase-8-reserved)". An in-crate module-doc-vs-INTERNALS divergence — the "INTERNALS retenses but module-doc doesn't, or vice-versa" shape from §2.
- #838 (renderer IPC-method extensibility seam) — remains OPEN.

**Renderer module-doc instance:** the "one-line `Cargo.toml` swap" prose is still present at `crates/benten-renderer-tauri/src/lib.rs:55-57`.

**pim-N codification:** `pim-N-cross-doc-claim-vs-substance-coherence` (the §3.6f extension this META proposes) is NOT yet ratified in a tracked artifact. `.addl/dispatch-conventions.md §3.6f` (SHAPE-not-SUBSTANCE pre-flight) has one sub-rule extension (regression-guard substantive-arm, 2026-05-25) but no cross-doc-triangle coherence clause; the pim-N name appears only as a candidate in audit triage notes.

Keeping open as the anchor for (1) the §3.6f-extension codification and (2) the still-live INTERNALS divergences at benten-dsl-compiler and benten-eval (closing the per-instance issues did not retense those INTERNALS sections).

### #1102
**Finding:** SECURITY.md:36 cites '18 sync-attack-N fixtures' (only 3 attack_*.rs exist) + bare-cites archived 'admin-ui-v0-threat-model.md'; both fail HEAD verification.
**Comment:**
Status at main `d0ccf606`: the specific `SECURITY.md:36` defect is UNCHANGED and the issue stays open.

- The line still reads "18 named adversarial fixtures `sync-attack-1..18`" while `crates/benten-sync/tests/` contains exactly 3 `attack_*.rs`-prefixed files (`attack_hlc_skew_revocation_ordering.rs`, `attack_loro_op_log_inv_13.rs`, `attack_mst_diff_cid_mismatch.rs`).
- It still bare-cites `admin-ui-v0-threat-model.md` (twice); that file does not exist at root or under `docs/` at main (archived). The cite is structurally broken.
- `SECURITY.md` has not been touched since #1271 (commit `7380459a`, 2026-05-17), which predates this audit. The open PR #1366 touches `docs/SECURITY-PROOFS.md`, not `SECURITY.md`, so no fix is pending there.

What HAS changed (underlying coverage, not the cite): PR #1340 (commit `05707357`, G-CORE-8 R5) landed `crates/benten-engine/tests/g_core_8_sync_attack_fabric_15_of_18.rs`, which enumerates the full 18-vector sync-attack-fabric class per R2 §2 (A-2) / §4.58 backlog row: 8 substantive inline `#[test]` pins (V-1/2/3/4/5/6/9/12, of which 5 are still `#[ignore]`'d as "G-CORE-8 partial-close") + 10 BELONGS-NAMED-NOW/OUT-OF-SCOPE dispositions (V-7/8/10/11/13/14/15/16/17/18) pointing to sibling files. It is NOT "15 substantive pins"; the R2 mandate was ">=15 substantive pins across the sync-attack class" counting the demoted destinations' own arms. Separately, `docs/history/PHASE-3.md:29` was retensed to "3 of the 18 landed at HEAD" naming the 3 benten-sync files, and `docs/future/phase-4-backlog.md:956-958` tracks the remaining 15 vectors (§4.58).

The `SECURITY.md` cite-fix (false count + working path for the threat-model doc) remains outstanding. Bundle with #1098 (still OPEN — X11 META archived-doc cluster + ATTACK-SURFACE-MATRIX Part 3 reconciliation) so the cite lands on a stable destination. Keeping open.

### #1113
**Finding:** ATTACK-SURFACE-MATRIX Part 3 T-row closure-claims contradicted at HEAD — T1/T3/T5/T10 'Defended' paper-only/bypassed; T2/T7 unqualified vs Compromise #26 PARTIAL.
**Comment:**
Status at main 115b0847: scope has materially narrowed but the META is not closed; the `docs/ATTACK-SURFACE-MATRIX.md` Part 3 table itself is unchanged (still 10 Defended + 2 Mitigated-by-construction, no honest-disclosure retense, no Reality column).

Resolved since filing:
- T3 — #596 and #612 are CLOSED; the `AllPermit` shim is documented DELETED in the production delegation path (`crates/benten-engine/src/engine_caps.rs:372`, `crates/benten-engine/src/shares_policy_resolver.rs:17`) and `SharesPolicy::permits_delegation` now has production callers (`crates/benten-caps/src/plugin_delegation.rs:117`, `crates/benten-caps/src/manifest_scope.rs:253`). NOTE the residual gap: with no resolver installed the default is `NoopSharesPolicyResolver` — "observably identical to the old `AllPermit` shim — zero v1 behavior delta until the platform layer installs the adapter" — so the real fail-CLOSED enforcement still rides on the platform/napi wire-up (#669 / #1206).
- T1 — #624 is CLOSED; the napi cdylib now exposes `installModuleSigned` (`bindings/napi/src/lib.rs:1764`), so the signed install path is no longer paper-only at the boundary. (UCAN-chain / dual-path verify modes are still not surfaced at napi per the doc-comment there.)
- T5 — the production read path is now `pub(crate) fn read_node` (`crates/benten-engine/src/engine_crud.rs:168`); `pub fn get_node` survives only in `crates/benten-engine/src/testing.rs:1109`. The "Mitigated-by-construction via `pub(crate)`" claim now matches HEAD.
- #1075 (`fabricate_test_suspend_envelope*` cdylib leak, cited as T1/T4 reality) is also CLOSED.

Still outstanding (keeping OPEN):
- T2 — `NoopManifestEnvelopeRechecker` remains the default mount (`crates/benten-engine/src/engine.rs:2050`).
- T10 — `docs/ADMIN-UI.md:176` still self-discloses "full T3 defenses → Phase 4-Meta" while the T10 row claims unqualified "Defended".
- Parent METAs #669, #684, #1054 and the napi shares-enforcement wire-up #1206 are still OPEN, and the Part 3 table has not been retensed. This issue is a `closure-pin` consolidated under META #1140 (also OPEN); closure remains gated on the Path A vs Path B decision tracked there.

### #1117 ⚠️acc=False
**Finding:** 4 of 12 admin-ui-v0 threat-model classes (T3/T8/T11/T12) carry zero test pins; 3 more (T5/T6/T7) only 1 pin each.
**Comment:**
Status at `origin/main` d0ccf606 (phase-4-meta-core not yet tagged). Of the 4 zero-coverage classes, 3 now have executing (non-`#[ignore]`) pins citing the threat-model class:

- T3 — `crates/benten-renderer-tauri/tests/webview_csp_locked_no_unsafe_eval.rs` (cites threat-model §T3; 2 executing arms; landed via G24-E #218 then #1271)
- T8 — `crates/benten-engine/tests/g_core_8_write_boundary_user_root_chain_validator_4_23.rs` (cites threat-model T8; 3 executing arms exercising `validate_chain_with_manifest_envelope`; landed via #1340/#1351)
- T11 — `crates/benten-platform-foundation/tests/materializer_pipeline_without_clock_injection_surfaces_e_ucan_clock_not_injected.rs` (cites threat-model T11; executing fail-closed + substance companion arm)

Still open:
- T12 — only an `#[ignore]`'d pin (`crates/benten-engine/tests/admin_ui_v0_uninstall_revokes_active_session_subscriptions.rs:44`, DESTINATION-REMAPPED at R6-FP-BF to `docs/future/phase-4-backlog.md §4.22` — the engine-side session-subscription cascade depends on the §4.22 thin-client bridge surface, which is not yet wired).
- T6 — its single pin (`crates/benten-platform-foundation/tests/admin_ui_v0_atrium_share_unattested_peer_rejected.rs:41`) is `#[ignore]`'d (redirected to Phase-4-Meta `accept_atrium_share` cross-peer install seam), so T6 remains effectively uncovered, not merely thin.
- T7 — 1 executing pin (`admin_ui_v0_private_namespace_isolated_from_other_plugins.rs`); still thin.
- T5 has grown to 4 executing pins (substitution / post-install drift / signature replay / bundle integrity) and is no longer thin.

The proposed mechanical sweep workflow (`workflows/threat-model-coverage.yml`, grep each `T<N>` header against tests + fail CI on 0 pins) does NOT exist; `.github/workflows/coverage.yml` is a generic cargo-llvm-cov job, not threat-class coverage enforcement. Keeping open for the T12 executing pin (gated on §4.22), the T6 un-ignore, and the coverage-sweep workflow.

### #1120
**Finding:** T-class threat coverage is shape-agnostic; CLAUDE.md #17 three deployment shapes lack per-shape T-row partition; META #684 proposal extended.
**Comment:**
Status at main `d0ccf606`: the core ask remains OPEN. `docs/ATTACK-SURFACE-MATRIX.md` Part 3 (Phase-4-Foundation, lines 314-337) is still a flat shape-agnostic T1..T12 table — no per-deployment-shape column, no new Part 4 partition, and none of the proposed new T-rows (Shape-(b) BrowserBackend verify-on-read; cross-shape authenticated thin-client protocol; Shape-(c) Tauri-shell→webview IPC napi inheritance) have landed. The "full T3 defenses → Phase 4-Meta" self-disclosure in `docs/ADMIN-UI.md` (Tauri embedded-webview row) still drifts from the T10 "Defended" claim in the matrix.

One cited Shape-(b) sub-gap IS resolved: #620 is CLOSED (2026-05-17). `BrowserBackend::get_node` now performs verify-on-read via `benten_core::Node::load_verified` at `crates/benten-graph/src/browser_backend.rs:207` and `:349` (production fix in #1277/#1290; closure-pin test 6e81248f), closing the wasm32 cache-tamper-produces-wrong-but-decodable-Node hole. Note the corresponding T-row in the matrix was NOT minted, so the doc-level enumeration gap for this surface persists even though the code defect is gone.

The doc restructure + remaining cross-shape T-row work stay tracked under META #1140 (OPEN; this issue is one of its 3-confirmation evidence anchors per §3.6h closure-pin discipline). Keeping open.

### #1124
**Finding:** Workspace-wide INTERNALS.md coherence drift across 6 sub-clusters (~30+ instances); synthesis consolidating #500 + #930 + #935 + new findings.
**Comment:**
Status update at main d0ccf606 (independently re-verified; the prior assessor's d91896b3 pin is a divergent commit, not an ancestor of main).

Progress since filing:
- Sub-cluster A (c589ffe HEAD-pin self-drift): cleared from all 14 tracked *crate* INTERNALS.md files. It now persists ONLY in `bindings/napi/INTERNALS.md` (3 cites: lines 3, 185, 301). NOTE — unlike at filing time, `bindings/napi/INTERNALS.md` is now TRACKED at main (blob 3d71bfee), so this is a tracked residual, not an untracked working-state artifact.
- Sub-cluster D (TauriRender vs TauriRenderer mirror drift): RESOLVED — no bare `TauriRender` remains in `crates/benten-platform-foundation/INTERNALS.md` (all occurrences are `TauriRenderer` / `BrowserRender`).
- Sub-cluster E (mid-phase state-header framing): RESOLVED for all 4 originally-named instances. `benten-platform-foundation/INTERNALS.md:3` ("R6 R3 ready"), `benten-ivm/INTERNALS.md:8` ("post R6-FP-4 close"), `benten-engine/INTERNALS.md:3` ("R6 R2 closed; R6-FP-2 in CI at PR #242"), and `benten-eval/INTERNALS.md:7` ("R6 R2 close-pending") have all been re-headed to "Last refreshed: 2026-05-24 against main HEAD a0b75637" with a Phase-4-Meta-Core delta section. The remaining "R6-FP" mentions across INTERNALS files are legitimate past-tense historical changelog references (e.g. "R6-FP-3 defensive-return hardening landed PluginDidHandleDuplicate"), not stale mid-phase framing.

Still open:
- Sub-cluster B (aggregate-count drift): `crates/benten-platform-foundation/INTERNALS.md:49` still reads "~5.0k LOC across 12 files + 2 submodule trees" vs actual 9355 LOC / 20 .rs files (11 top-level + 2 submodule trees) at main.
- Sub-cluster A residual: the 3 c589ffe cites in the now-tracked `bindings/napi/INTERNALS.md`.

Consolidated under META #1140 (OPEN); this issue is a closure-pin evidence anchor per the §3.6h ratification-must-close-origin discipline. Keeping open for the remaining sub-clusters.

### #1128
**Finding:** 3-4 new T-class categories surfaced by campaign (DoS-unbounded-decode #629; asymmetric-parallel-entry #707; cdylib-leaked test API #1075; plugin-DID-forgery) without T-row destinations.
**Comment:**
Status at main d0ccf606: the doc-level deliverable of this issue — minting T13 (untrusted-input volume exhaustion / DoS-unbounded-decode), T14 (invariant entry-point coherence drift), T15-or-T4-expansion (cdylib-leaked synthetic-attribution test API), and the T1 plugin-DID-mint split — has NOT landed. `docs/ATTACK-SURFACE-MATRIX.md` Part 3 still enumerates exactly T1..T12; there is no T13/T14/T15 and no T1 split. Keeping open.

Pattern C is resolved at the code layer (but not yet reflected as a T-row): #1075 is CLOSED (2026-05-18) and both `fabricate_test_suspend_envelope` (`crates/benten-engine/src/engine_wait.rs:930`) and `backend_for_test` (`crates/benten-engine/src/engine.rs:2350`) are now `#[cfg(any(test, feature = "test-helpers"))]`-gated, so the default-feature production cdylib compiles them out (D1 #1153 closure; rationale recorded in the `fabricate_test_suspend_envelope` docstring).

Pattern A (META #629), Pattern B (META #707), and Pattern D (plugin-DID-forgery) remain OPEN with no T-row destinations. This issue stays open as an evidence-anchor closure-pin under META #1140 (campaign-level phase-close-retense-self-verify gate) per the existing comment.

### #1136 ⚠️acc=False
**Finding:** 4-identity + Layers 1/2/3 plugin trust model has uneven T-class coverage; T1/T3 'Defended' contradict META #669 paper-only; no T-row for content-CID drift / caller-mint-first / cross-fork install.
**Comment:**
Status at main `d0ccf606` (phase-4-meta-core NOT yet tagged): scope narrowed, but the doc deliverable is NOT done and the Layer-3 framing in the prior assessment was overstated. Keeping open.

**Doc retense — NOT done.** `docs/ATTACK-SURFACE-MATRIX.md` Part 3 at HEAD: T1 (line 320) and T3 (line 322) still carry unqualified `Defended`. No T-row retense, no new T-rows for plugin content-CID drift / caller-mint-first / cross-fork install (no T13–T17). Bucket total unchanged at 12. `closedByPullRequestsReferences` is empty.

**Layer-3 (T3) — refactored, not substantively resolved.** PR #1294 (`wave-e/held`, closing #1197/#1146/#596) deleted the production `AllPermit` shim from `delegate_capability` and introduced a fail-closed `SharesPolicyResolver` port (`crates/benten-engine/src/engine_caps.rs:541`). #596 and #612 are CLOSED, and `SharesPolicy::permits_delegation` now has production callers (`crates/benten-caps/src/plugin_delegation.rs:117` blanket impl + `crates/benten-caps/src/manifest_scope.rs:253`). BUT the default resolver is `NoopSharesPolicyResolver` (`crates/benten-engine/src/engine.rs:2085`), whose `resolve_delegation` returns `NotPluginPrincipal` → admit (`crates/benten-engine/src/shares_policy_resolver.rs:151,88`). The only non-Noop `SharesPolicyResolver` impl and the only `set_shares_policy_resolver` caller are in a test (`crates/benten-engine/tests/layer3_shares_envelope_enforcement_1197_closure_pin.rs`); no production code installs a real adapter. The source comment itself states the default is "observably identical to the deleted `AllPermit` shim — zero v1 behavior delta until the platform layer installs the real adapter." `SECURITY-POSTURE.md` at HEAD keeps Compromise #27 OPEN (reserved for META #669) and describes Layer-3 as "declared-but-not-live." So T3's `Defended` claim remains substantively contradicted at the v1-default level — the shim became a seam, not a live defense.

**Layer-2 (T1) — paper-only.** #600 still OPEN; `check_scope_within_envelope` has zero production consumers at HEAD (only `#[cfg(test)]` in `crates/benten-caps/src/manifest_scope.rs` and `tests/` call it). META #669 and META #1140 remain OPEN. T1/T3 row retense and new-T-row work remain outstanding under META #1140.

### #1137
**Finding:** 11 ErrorCode catalog variants have ZERO production construction sites at v1-API-freeze (wire-now-or-delete cluster).
**Comment:**
Status update (verified at main `115b0847`): substantially still open.

Of the 11 zero-construction-site variants, only **1 has been wired** since this audit:
- `PluginInstallConsentRequired` (catalog lib.rs:992) now has a real production fire site at `crates/benten-platform-foundation/src/plugin_lifecycle.rs:1110` (`return Err(ErrorCode::PluginInstallConsentRequired)` inside the upgrade-consent guard chain; doc-comment notes it closes the production-path e2e wiring gap).

The other **10 still have ZERO production construction sites** (every match is in `tests/`; catalog decls unchanged):
- Cat-2 host-boundary: `HostNotFound` (lib.rs:213), `HostWriteConflict`, `HostCapabilityRevoked`, `HostCapabilityExpired`
- Cat-4 sync/wait: `WaitSignalShapeMismatch` (lib.rs:247), `SyncCapUnverified` (lib.rs:876)
- Cat-3 plugin: `PluginContentPeerKeyRotated` (lib.rs:982), `PluginDeviceAttestationForged` (lib.rs:1052), `PluginLibraryIndexTamper` (lib.rs:1054) — note `plugin_manifest.rs:331` references `E_PLUGIN_CONTENT_PEER_KEY_ROTATED` only in a doc-comment describing a future strict-mode opt-in (`ValidationOutcome::ValidWithWarning` is what actually surfaces today), so it remains paper-only
- Cat-1 TS-only fire (legitimate): `StreamHandleLeaked` (lib.rs:305) fires JS-side via `packages/engine/src/stream.ts:173` + `packages/engine/src/engine.ts:676`; the Rust catalog reservation is correct, disposition is doc-retense only

None of the 10 carry an explicit `KEEP_FOR_FORWARD_COMPAT` disposition in the catalog. Keeping OPEN as a closure-pin (10 of 11 remain wire-now-or-delete at v1-API-freeze).

### #1138 ⚠️acc=False
**Finding:** Production code bypasses the ErrorCode catalog via ErrorCode::Unknown(String) at 16+ sites (10 distinct strings, 6 crates), including 3 non-E*-prefixed identifiers.
**Comment:**
Status re-verified against current main (d0ccf606). This finding's specific defect is still present; keeping OPEN.

Catalog-bypass write sites still using `ErrorCode::Unknown(...)` (all confirmed by grep at d0ccf606):
- `E_MODULE_BYTES_CID_MISMATCH` — `crates/benten-graph/src/backends/blob_backend.rs:390`, `bindings/napi/src/browser_blob_store.rs:383`, `crates/benten-engine/src/engine.rs:2806` and `:2826`
- `E_SANDBOX_NODE_UNKNOWN` — `crates/benten-engine/src/engine_sandbox.rs:213` and `:262`
- `E_EVAL_BACKEND` — `crates/benten-eval/src/lib.rs:486`, `crates/benten-engine/src/primitive_host.rs:1413`
- `E_MODULE_MANIFEST_ENCODE_FAILURE` / `E_MODULE_MANIFEST_DECODE_FAILURE` — `crates/benten-engine/src/engine_modules.rs:634` and `:638` (encode also at `manifest_signing.rs:326`)
- wrong-path `E_GRAPH_INTERNAL` — `bindings/napi/src/browser_blob_store.rs:387` (should use `ErrorCode::GraphInternal` directly)
- non-`E_*`-prefixed strings — `engine_internal` at `engine_diagnostics.rs:120`, `builder_missing_path` at `builder.rs:387`, `g20a3_testing_helper` at `testing.rs:697` and `:754` (still ungated in the cdylib)
- `E_STORAGE_DOM_EXCEPTION_{}` (dynamic/formatted) at `bindings/napi/src/browser_indexeddb.rs:214` — flagged as possibly-legitimate-dynamic; needs explicit doc-exemption rather than a static mint.

None of the named variants this finding recommends are in the catalog (`crates/benten-errors/src/lib.rs`): `ModuleBytesCidMismatch`, `EvalBackend`, `ModuleManifestEncodeFailure`, `ModuleManifestDecodeFailure`, `SandboxNodeUnknown` all return 0 hits. Note `ModuleManifestCidMismatch` (`E_MODULE_MANIFEST_CID_MISMATCH`) exists but is a distinct code (manifest-CID, not the bytes-CID this finding targets).

Two corrections vs the original finding scope:
1. The `E_MATERIALIZER_BACKEND` bypass site (originally cited at `benten-platform-foundation/src/materializer.rs`) is GONE — `MaterializerError` now maps to first-class catalog variants (`MaterializerCapDenied` / `MaterializerSchemaMismatch` / `MaterializerSubscribeSeamFailure`, added in Phase-4-Foundation G23-B). A `MaterializerBackend` mint is no longer needed; this sub-item is resolved.
2. A separate r6-err-4 pass (`crates/benten-engine/src/error.rs:562`) promoted a DIFFERENT set of engine `Unknown` strings to first-class variants (`DuplicateHandler`, `NoCapabilityPolicyConfigured`, etc.), so the general pattern was partly addressed — but none of this finding's remaining enumerated sites were touched.

The umbrella #1205 (referenced in the prior pin comment) closed, but it covered Compromise #21 napi cdylib wire-up, not the ErrorCode catalog bypass; its closing PR #1294 did not retarget any of these sites. Substantive closure still pending.

### #1139
**Finding:** META X6: cross-crate public type-name collisions — 4 pairs + 2 sub-patterns across 5 crates; v1-API-stabilization sweep.
**Comment:**
Status update against current main (d0ccf606).

One of the four named collision pairs is resolved; the rest persist.

RESOLVED — Pair A `WriteContext`: the benten-caps type is now `CapWriteContext` (crates/benten-caps/src/policy.rs:158, `#[non_exhaustive]`), eliminating the collision with `benten_graph::WriteContext` (crates/benten-graph/src/lib.rs:943). #885 was closed COMPLETED (2026-05-17) under the engine.caps() canonical capability-mutation surface work.

STILL COLLIDING in main:
- Pair B `SubgraphSpec`: benten-engine/src/subgraph_spec.rs:101 + benten-ivm/src/subgraph_spec.rs:114.
- Pair C `PendingOp`: benten-caps/src/policy.rs:100 + benten-graph/src/transaction.rs:74.
- Pair D `ChangeEvent`/`ChangeKind`: benten-core/src/change_stream.rs:124 / :86 + benten-graph/src/store.rs:485 / :446. The `ChangeEvent as PortChangeEvent` workaround is still in place (now at benten-eval/src/primitives/subscribe.rs:70 — the file was renamed from subgraph_resolution.rs since this issue was filed).
- Sub-pattern E `ReadContext`: still bare at benten-caps/src/policy.rs:258.

Keeping open for the v1-API-stabilization rename sweep (closure-pin).

### #1140
**Finding:** META: phase-close retense PRs themselves introduce regressions — 3 confirmations (X13 TauriRender, X11 T-row schism, X14 7-doc registry drift); pim-N codification.
**Comment:**
Status update against current main (`115b0847`; `phase-4-meta-core-close` tag not yet landed).

This META stays OPEN as the Phase-4-Meta retense-gate codification + origin-pin tracking row (consistent with the W19 tracking destination at `docs/future/phase-4-backlog.md` §4.86 line 1416, which explicitly keeps it open).

Closed + verified (3 of the enumerated closure-pins):
- #1112 (Confirmation 1, X13 `TauriRender`→`TauriRenderer`): resolved. Actual type is `pub struct TauriRenderer` at `crates/benten-renderer-tauri/src/lib.rs:423`; `crates/benten-platform-foundation/INTERNALS.md` is clean (correct `TauriRenderer` at lines 23/103/296, zero bare `TauriRender`). The only remaining bare `TauriRender` strings are in historical/tracking records — `docs/history/PHASE-4-FOUNDATION.md` (4) and `docs/future/phase-4-backlog.md` (1) — which document the past drift, not live code/doc drift.
- #1121, #1125 (Confirmation 3 residual, stale headers): CLOSED.

Still OPEN — defects re-verified present at HEAD:
- #1098 + #1113 (Confirmation 2, X11 ATTACK-SURFACE-MATRIX T-row schism): `docs/ATTACK-SURFACE-MATRIX.md` Part 3 "Phase-4-Foundation" still carries the net-new T1..T12 enumeration (lines 314-333) with "Defended" claims (T1/T3/...), while the archived `.addl/_archive/phase-4-foundation/admin-ui-v0-threat-model.md` is still cited by 39 production test files (plus SECURITY/source). No closing commit exists for either.
- #1107 (Confirmation 3, decentralized-registry SHIPPED-vs-RESERVED drift): still present. `crates/benten-platform-foundation/src/registry.rs` is Phase-4-Meta-RESERVED (zero production call sites), yet SHIPPED-style claims persist in `CLAUDE.md:317` (Phase-4-Foundation status row) + `CLAUDE.md:344` (baked-in #15) + `docs/PRIMER.md:54` + `docs/VISION.md:108` + `docs/FULL-ROADMAP.md:98/105/115` + `docs/HOW-IT-WORKS.md:142` (the latter internally contradicted by `HOW-IT-WORKS.md:119`, which correctly scopes the registry to Phase-4-Meta) + `docs/CRATES-DEEP-DIVE.md:423/459`.
- #1116 (Confirmation 3): `docs/INVARIANT-COVERAGE.md:7` still claims "14 of 14 invariants enforced".

The pim-N codification (`pim-N-phase-close-retense-self-verify-gate`) has not landed — it lives in the local-only/gitignored `.addl/dispatch-conventions.md` (`.addl/` is gitignored at `.gitignore:65`); a grep confirms no occurrence of the rule there yet. Per §3.6h, the single dispatch-conventions ratification PR closes this META together with its remaining origin pins. Keeping open.

### #1142 ⚠️acc=False
**Finding:** Bundle to canonicalize the Subgraph/SubgraphBuilder/Anchor surface: delete trivial alias accessors, collapse the 7 _for_validator methods into a BuilderValidatorView projection, drop quad-aliased encode/decode helpers, and normalize _for_* suffixes.
**Comment:**
Status (current main `115b0847`): umbrella stays OPEN. Of the 9 sub-issues, only ONE alias was actually deleted in code; the rest are either deferred or were closed without deleting the cited alias.

Closure ground-truth:
- #757 (legacy crate-root u64-id `Anchor` surface) — DELETED in PR #1290 (`b3d3bbd4`, `Closes #757`); replaced by `version::Anchor` (Cid-head) + `version_chain::DagVersionChain`; closure pin `tests/u64_anchor_surface_deleted_1003.rs`. This is the only genuine code-fix of an alias in this bundle.
- #697 (`OperationNode::primitive_kind`) — closed via PR #1262 (`86e1edb3`) but the ALIAS WAS NOT DELETED. Only the stale "back-compat for Phase-2a tests" docstring was corrected; the collapse is explicitly DEFERRED (Surf-1 #697) because there are production cross-crate callers (`benten-platform-foundation/src/schema_compiler/emit.rs:82`, `mod.rs:140`). The alias still lives at `subgraph.rs:238`.
- #766 (`iterate`/`iterate_parallel` `max` param drift) — RESOLVED via PR #1262 (`86e1edb3`): params renamed `max`→`max_iterations` / `parallel_fanout` for disambiguation (the differing u64-vs-usize types are intentional per RATIFIED #506-class).
- #748 (`_for_test`/`_for_testing`/`_for_validator` suffix inconsistency) — closed 2026-05-15 consolidated under META #823 (not part of the W20 set).
- #666, #678, #741, #775, #786 — closed 2026-05-18 as Phase-4-Meta close-as-named (HARD-RULE clause-b), landed via PR #1294 (`39a0e732`); destination is phase-4-backlog §4.191 (benten-core symbol slice folded into the §4.43 v1-API-stabilization rename sweep, co-routed with §4.205).

Residual aliases STILL PRESENT in main (`subgraph.rs`), pending the §4.43 sweep + the deferred `BuilderValidatorView` collapse:
- `OperationNode::primitive_kind` alias — line 238 (#697)
- `Subgraph::primitive_count` 2-LOC alias — line 469 (#775)
- `canonical_subgraph_bytes` free fn duplicating `Subgraph::to_canonical_bytes` — line 605 (#786)
- 7 `*_for_validator` accessors with NO `BuilderValidatorView` projection — lines 1271-1307 (#678)
- `set_property_for_test` (production-consumed) — line 1070 (#741)

Umbrella stays open until the §4.43 v1-API-stabilization benten-core slice (BuilderValidatorView collapse + remaining alias deletions) lands.

### #1150
**Finding:** Umbrella bundle for benten-eval v1-API-stabilization: 9 sub-issues (#794 #802 #813 #807 #785 #817 #1042 #1008 #878) on Evaluator/ExecutionState/PrimitiveHost/Outcome API shapes.
**Comment:**
Ground-truth status against current main (115b0847; phase-4-meta-core not yet tagged-closed). All 9 sub-issues are CLOSED on the tracker, but the underlying work is NOT uniformly landed — 7 are genuinely resolved in code, 2 are still open defects routed to backlog §4.78 / G-CORE-9.

RESOLVED in code (verified):
- #794 — `ExecutionStateEnvelope::envelope_cid()` is now an infallible `Cid` accessor (crates/benten-eval/src/exec_state.rs:402); landed f412dbab (consolidated #1277 / 9b0f327f).
- #785 — `ChunkSinkError`/`SubscribeError` now expose `code()` (no `error_code` outliers remain); chunk_sink.rs:157, subscribe.rs:270.
- #813 — `WaitOutcome::state_cid()` docstring now states "total (never panics) by design", matching behavior (wait.rs:103-111); regression closure-pin.
- #817 — renamed `load_verified_with_cid` (subgraph_ext.rs:192/226); regression closure-pin.
- #878 — `Outcome::Suspended(SuspendedHandle)` double-nest collapsed (lib.rs:221); landed f412dbab.
- #1008 — PrimitiveHost SemVer discipline expressed as a written additive-default contract (host.rs:52-73), DISAGREE on literal `#[non_exhaustive]` (not valid for traits + would break CLAUDE.md #19 out-of-tree impls) — correctly resolved.
- #807 — canonical-encoding name fork settled on `to_canonical_bytes` (PR #1295); verify-pass complete.
Closure-pins at crates/benten-eval/tests/v1_api_stabilization_closure_pins.rs.

STILL OPEN in code (sub-issues closed as named-deferral to docs/future/phase-4-backlog.md §4.78, NOT closed-as-fixed):
- #802 — PrimitiveHost still mixes READ verbs: `read_node` + `read_view` vs `get_by_label` + `get_by_property` (host.rs:80/87/94/165). The ~27-file cross-crate rename was explicitly NOT done in the ST-EVAL lane (f412dbab) and routed to §4.78 → G-CORE-9.
- #1042 — `Outcome::Err` still leaks the heap-string variant: `EvalError::Backend(_) => ErrorCode::Unknown(String::from("E_EVAL_BACKEND"))` at lib.rs:486. No typed `ErrorCode::EvalBackend` variant exists in benten-errors and `CATALOG_VARIANT_COUNT` is still 168 (the §4.78 work specifies 168→169). Routed to §4.78 → G-CORE-9.

Keeping this umbrella OPEN: its acceptance criteria ("All 9 sub-issue closure-pins fire end-to-end") is not met in substance while #802 and #1042 remain unlanded, and §4.78 is not marked SHIPPED. Close once the G-CORE-9 PrimitiveHost READ-verb rename (#802) and the typed `ErrorCode::EvalBackend` catalog mutation (#1042) land.

### #1159
**Finding:** benten-caps v1-API-stabilization cluster bundle: Fwd-2 #993 #998 #1005 #1017 + Surf-1 #883 #884 #886 #887.
**Comment:**
Status (ground-truthed against main 115b0847). All 8 constituent leaves are CLOSED, but via two different dispositions — not a single bundle-PR auto-close:

SUBSTANTIVELY FIXED + verified in main:
- #998 — `TypedCapGroup` carries `#[non_exhaustive]` at crates/benten-caps/src/typed_cap_mapping.rs:46-47 (not authorization_grant.rs).
- #993 — `CapabilityPolicy` HARD-SEALed via the private `sealed::Sealed` supertrait: `pub trait CapabilityPolicy: sealed::Sealed + Send + Sync` at crates/benten-caps/src/policy.rs:375 (rationale + `testing`-gated re-export at policy.rs:15-52).
- #886 — `[features]` section with `testing = []` at crates/benten-caps/Cargo.toml:20-26.
- #884 — the 3 modules are re-exported at crate root (`pub use` for manifest_envelope_chain_validation / manifest_scope / plugin_delegation at crates/benten-caps/src/lib.rs:184-197).
- #883 — paper-only layering doc-lie retracted in the doc-comment at crates/benten-caps/src/manifest_envelope_chain_validation.rs:28-47 (prod dep on benten-platform-foundation now acknowledged; option-(b) edge-retirement tracked at backlog §4.43).
- #887 — permissive `check_read` default resolved as a documented permit-all seal-decision with an explicit production-author WARNING + opt-in requirement (crates/benten-caps/src/policy.rs:399-424; default body still `Ok(())` by design).

The mechanical `engine.caps()` cluster (the only part of this bundle PR #1267 touched) landed via commit 0bba1d6b ("engine.caps() as sole canonical capability-mutation surface (#820/#1195/#885 + #1159/#1158 **mechanical**)"), merged in the Strategy-C batch PR #1269 (merge commit 0e22f517).

DEFERRED to Phase-4-Meta (NOT code-fixed) — closed as named per HARD RULE clause-(b) via #1294 (main 39a0e732), reopened/actioned when Phase-4-Meta scope is scheduled:
- #1005 — `WriteContext`/`CapWriteContext::actor_hint: Option<String>` is STILL present (policy.rs:174,265); `actor_cid: Option<Cid>` was added alongside it but the Phase-1-shaped `String` resolution + `TODO(phase-3)` (policy.rs:78) remain.
- #1017 — `TODO(phase-3)` markers + `LegacyUcanStubBackend` retention STILL present (policy.rs:78; ucan_stub.rs:32; lib.rs:157).

Net: 6 of 8 leaves substantively closed in the G-CORE-9 freeze wave; 2 (#1005, #1017) deferred-within-Phase-4-Meta and their acceptance criteria ("Phase-3 stale markers swept", actor resolution lift) are NOT yet met in main. phase-4-meta-core is not yet tagged. Holding the umbrella open pending the deferred-leaf sweep decision.

### #1165
**Finding:** Residual Surf-1 tightening: implement Cid FromStr/From/TryFrom and drop inherent from_str (#840), split VersionDagError::UnknownParent/UnknownCurrent into distinct ErrorCodes (#842), and remove the Inv-14-bypassing with_node/with_edge surface (#843).
**Comment:**
Partial status (verified against main `d0ccf606`):

- #840 (Cid `FromStr`) — DONE. `Cid` now implements `core::str::FromStr` (`crates/benten-core/src/lib.rs:587`, body at :608), `From<[u8; 32]>` (:636), and `TryFrom<&[u8]>` (:644); the shadowing inherent `Cid::from_str` is deleted. Regression pin: `crates/benten-core/tests/cid_from_str.rs`.
- #843 (Inv-14 attribution bypass) — DONE. `Subgraph::with_node`/`with_edge` were renamed to `push_node_raw`/`push_edge_raw` (`crates/benten-core/src/subgraph.rs:443/457`; rename documented at :437 "Renamed from `with_node` (#843, Surf-1 ratified)"). The old names are gone entirely. Pins: `tests/with_node_renamed_inv14_bypass_pin.rs` + `tests/tf1_843_989_byte_equiv_and_cid_regression_guard.rs`. Landed in the ST-CORE lane (commit `d58a68c9`, "#1165-partial").
- #842 (ErrorCode split) — NOT DONE. `VersionDagError::UnknownParent` and `UnknownCurrent` both still map to `ErrorCode::VersionUnknownPrior` (`crates/benten-core/src/version_chain.rs:83` and `:85`); no `VersionUnknownCurrent` catalog code was minted (absent workspace-wide; `benten-errors/src/lib.rs:204` still has only `VersionUnknownPrior`). #842 was closed-as-named to Phase-4-Meta (`docs/future/phase-4-backlog.md:1332`, §4.43 v1-API-stabilization slice), with the destination landed via the W20 close-as-named batch (PR #1294).

Umbrella stays OPEN until the #842 ErrorCode split lands (catalog mint + TS mirror per §3.5g cross-language rule-mirror).

### #1168
**Finding:** Add compile-time Send+Sync static assertions for Hlc / version::Anchor and a multi-thread test pinning the documented concurrent-correctness claims.
**Comment:**
Status: substantive scope COMPLETE in current main (d0ccf606), but leaving this umbrella OPEN per the bundle-umbrella convention.

Sole tracked sub-issue #636 is CLOSED, landed via PR #1262 (commit 86e1edb3, ST-CORE lane). Verified in current main:
- `version::Anchor` Send+Sync compile-time pin — `const _: fn()` block at crates/benten-core/src/version.rs:68-71 (`assert_send_sync::<Anchor>()`).
- `Hlc` Send+Sync compile-time pin — `const _: fn()` block at crates/benten-core/src/hlc.rs:218-221, tagged "Safe-4 #636".
- Multi-thread integration test crates/benten-core/tests/concurrent_correctness_hlc_anchor_smoke.rs exercises the documented concurrent path (8 threads x `Hlc::now()`, asserts distinct/monotonic stamps).
- The third surface from finding #636, the `U64_CHAINS` process-global, was DELETED under #1003 (RATIFIED 2026-05-17), so no pin is required there; the test header documents this.

All acceptance criteria are met. PR #1262 deliberately scoped this umbrella to "stay open" (it closes individual sub-issues with SHA evidence while umbrellas span out-of-lane work); keeping it open here is consistent with sibling umbrellas #1149/#1155/#1144/#1165/#1142/#1158. Closing is an owner/umbrella-management call rather than an automated one.

### #1172 ⚠️acc=False
**Finding:** benten-id DoS-via-unbounded-decode bundle (META #629 sub-cluster): #549 prf depth bound, #555 Did deserialize TOCTOU, #561 Acceptor nonce-store growth.
**Comment:**
Bundle is in mixed states — keep open. Verified against current main (d0ccf606); the benten-id DoS hardening landed via the #1290 Strategy-C batch (commit 20fb5c78; the original work was d8abab51).

#549 — depth-bound primitive landed: `benten_id::ucan::Ucan::from_canonical_bytes_bounded(bytes, max_depth)` + `MAX_UCAN_PROOF_DEPTH = 32` + `UcanError::ProofChainTooDeep` typed variant + a non-recursive CBOR pre-walk (crates/benten-id/src/ucan.rs:85,186; closure-pinned in crates/benten-id/tests/dos_unbounded_decode_safe2_1172.rs). STILL OWED: the two untrusted call sites have not migrated and still bare-decode — crates/benten-engine/src/typed_call_dispatch.rs:365 (`serde_ipld_dagcbor::from_slice`) and crates/benten-caps/src/backends/ucan.rs:377 (`cbor::from_slice::<Ucan>`). Tracked as the P-II post-COLLAPSE workspace sweep at docs/future/phase-4-backlog.md §4.80.

#555 — FIX-NOW landed: `Did::from_string_unchecked` is now `pub(crate)` and `Did::parse_validated` was added (crates/benten-id/src/did.rs:155,186); the attacker-controlled DID/capability strings in `UcanError` Display now route through `sanitize_untrusted` (escapes non-printable bytes, bounds rendered length — crates/benten-id/src/errors.rs:34). The deserialize-side `Did` validation is DISAGREE per a v1-wire design decision: Deserialize deliberately does NOT validate the `did:key:z` prefix to collapse to one unvalidated boundary (crates/benten-id/src/did.rs:64-73, prose at :70), with callers routing through `parse_validated` / `resolve` to validate explicitly.

#561 — RESOLVED-ON-MAIN: `Acceptor` / `accept_at` / nonce_store were deleted by the COLLAPSE P3 rework (DECISION-RECORD §4 RATIFIED, #1238); no struct/fn/field remains in crates/benten-id/src (only deletion-explaining comments). The unbounded-growth surface no longer exists.

Keeping open: the #549 call-site migration at §4.80 remains owed.

### #1180
**Finding:** Umbrella bundle for benten-errors v1-API-stabilization sweep: 10 sub-issues incl. dead-code removal, const renames, FromStr, Hash+Ord derives, EdgeLabel enum, [features] stanza.
**Comment:**
Status update: all 10 sub-issues of this v1-API-stabilization umbrella are now CLOSED, but the disposition is mixed — the bundle's full acceptance criteria were NOT all implemented, so the umbrella is left OPEN as a tracking row rather than auto-closed.

Mechanical work that genuinely landed and is verified in current main (`crates/benten-errors/src/lib.rs`):
- #283 — `parse_cap_string` + `CapString` removed (no hits in `src/`).
- #286 — inherent `ErrorCode::code()` removed (no `fn code`).
- #291 — bare-`str` `PartialEq` removed; only `impl PartialEq<ErrorCode> for &str` remains (lib.rs:2674).
- #736 — consts renamed to `FIRING_CODES_AT_PHASE_2A_SNAPSHOT` / `RESERVED_CODES_AT_PHASE_2A_SNAPSHOT` (lib.rs:1461, 1486).
- #733 — converted to `impl core::str::FromStr for ErrorCode` returning `Result<Self, ParseErrorCodeError>`; infallible inherent `from_str` deleted (lib.rs:2371).
- #1007 — `ErrorCode` now derives `Hash`, `PartialOrd`, `Ord` (lib.rs:61).
Closure-pins live across `crates/benten-errors/tests/` (`stable_shape.rs`, `phase_2a_error_codes_present.rs`, the per-domain `*_codes_present.rs` files using `FromStr`). The code landed via PR #1277; per that PR's adjudication these sub-issue closures were manual repo-owner triage and #1180 was carried as Refs (not Closes).

NOT implemented in this sweep (resolved as close-as-named, work still pending):
- #658 (typed `EdgeLabel` enum) and #825 (gated `serde` `[features]` flag) — closed as Phase-4-Meta close-as-named-deferred; named destination is `docs/future/phase-4-backlog.md §4.125` (committed via #1294). Verified still pending in code: `routed_edge_label` still returns `Option<&'static str>` (lib.rs:1826); `Cargo.toml [features]` has only `default = []`.
- #734 and #824 — closed as tracker-noise consolidation under META #828 and META #903 respectively (verb/accessor naming-drift), not as a benten-errors code change in this bundle.

Net: the bundle's two architectural acceptance items (#658 EdgeLabel enum, #825 serde feature) were deferred to Phase-4-Meta rather than completed, so this umbrella should remain OPEN until those backlog rows resolve (or be closed at maintainer discretion with #658/#825 tracked via §4.125).

### #1182
**Finding:** Umbrella bundle for benten-errors decentralized-registry-readiness: 4 sub-issues (#996 DecodeLimits/ErrorCode pre-alloc, #1019 cohort-mint rule, #1030 plugin-registered ErrorCode contract, #1053 Tauri IPC codes).
**Comment:**
Status update: 3 of the 4 sub-issues are now closed; the umbrella stays OPEN because #1030 is unresolved.

- **#1019** (cohort-mint subset-closure rule-mirror) — genuinely codified as §3.5g item 5 "Cohort-mint subset-closure mirror" in `.addl/dispatch-conventions.md` (present at current main). Closed by PR #1277 (squash `9b0f327f`).
- **#996** (META #629 DecodeLimits + 6-8 ErrorCode pre-allocation) — closed-as-named-deferred 2026-05-18 (Ben-ratified Q3, Disposition B): do NOT mint the codes speculatively; mint them in the same PR that wires their first real construction site (the META #629 substantive-closure pass). Confirmed in code: only a single static `E_INPUT_LIMIT` exists in `crates/benten-errors/src/lib.rs`; the 6-8 `E_DECODE_*`/`E_INPUT_LIMIT_*` variants are intentionally not yet minted.
- **#1053** (Tauri IPC + webview ErrorCode reserves) — closed-as-named-deferred 2026-05-18 to the Phase-4-Meta admin-UI IPC work (HARD-RULE clause-(b); tracked destination `docs/future/phase-4-backlog.md` §4.73, landed via #1294).
- **#1030** (plugin-registered ErrorCode extension contract) — remains OPEN and ratification-gated. Ratification #5 (2026-05-15) selected option (b) suffix-routing (`E_PLUGIN.<plugin-DID>.<plugin-code>`) for v1, but it is NOT yet implemented. Current main carries only static `E_PLUGIN_*` compile-time variants plus the `Unknown(String)` forward-compat fallback in `crates/benten-errors/src/lib.rs` — there is no dynamic plugin-registered/install-time ErrorCode extension or suffix-routing in `routed_edge_label`.

Per the tracked destination at `docs/future/phase-4-backlog.md` §4.73, umbrella #1182 stays OPEN until the remaining ratification-gated arch decision (#1030) resolves; #996 and #1053 are administratively closed to their named Phase-4-Meta / META #629 destinations rather than substantively implemented.

### #1185
**Finding:** Umbrella bundle for benten-errors file-split inline reshuffle + bench-baseline: 3 sub-issues (#1039 file-split/reshuffle, #939 Criterion benches, #944 from_str memcmp watch-item).
**Comment:**
Status update — all 3 sub-issues are now CLOSED; this umbrella's scope is fully adjudicated and can be closed at maintainer discretion.

- #939 (zero Criterion bench coverage) — DONE. `crates/benten-errors/benches/catalog_baseline.rs` provides `bench_as_static_str_all_variants` + `bench_from_str_roundtrip` (registered as `[[bench]]` `catalog_baseline` in Cargo.toml; compiles clean; informational/non-gated). Landed in PR #1262 (commit `86e1edb3`, batch-c4 drain) — that is the PR cited in #939's closure comment.
- #944 (`from_str` length-bucketed-memcmp watch-item) — DONE. `crates/benten-errors/INTERNALS.md:115` §Performance documents the 168-arm length-bucketed-memcmp cost (~150ns/call) with the Phase-5+ >500-variant watch threshold and the `phf`/sorted-slice escalation note. Also landed in #1262.
- #1039 (2051-LOC lib.rs file-split / inline reshuffle) — RESOLVED as DISAGREE-WITH-EXPLANATION (Ben-ratified Q3, 2026-05-18). The inline reshuffle was deliberately declined: a section-comment pass that must preserve mint-order delivers marginal scannability for real diff-churn and decouples nothing; a true module-split is deferred to Phase-5+ catalog growth (~3500+ LOC).

Note: the original RATIFIED #6 framing (inline reshuffle + defer split) was superseded by the later DISAGREE resolution. `lib.rs` therefore retains its chronological mint-order organization (phase/wave banner comments such as `// ----- Phase 4-Foundation G24-D — FULL plugin manifest (15 codes) -----`); the family-grouped reshuffle was intentionally not performed. PR #1277 (mega-batch) referenced this umbrella under Refs only (not Closes) — the bench/INTERNALS work was the #1262 lane.

Recommendation: close this umbrella, noting the reshuffle acceptance criterion was intentionally declined (DISAGREE) rather than implemented.

### #1206
**Finding:** Bundle/umbrella PR scope for napi delegateCapability + installModuleSigned shares-enforcement wire-up, covering child closure-pins #1018 (Layer-3 shares bypass), #1010 (raw plugin_did String / no mint surface), and #1002 (no installModuleSigned surface).
**Comment:**
Status update against current main (d0ccf606).

This bundle is partially complete. PR #1294 (merge 39a0e732) used `Refs #1206` (not `Closes`) and closed only #1018 from this cluster (plus siblings #1205/#624). Verified states: #1018 CLOSED; #1010 OPEN; #1002 OPEN.

- #1018 (Layer-3 shares bypass): CLOSED. Engine `delegate_capability` (crates/benten-engine/src/engine_caps.rs, fn at :429) now enforces the manifest-`shares` envelope via the installed `SharesPolicyResolver` (enforcement block :533-558; crates/benten-engine/src/shares_policy_resolver.rs), surfacing `ErrorCode::PluginDelegationOutsideManifestEnvelope` (fail-closed; the prior `AllPermit` shim is deleted). The napi binding routes through `inner.caps().delegate_capability(..)` unconditionally with no bypass (bindings/napi/src/lib.rs:865-868); pinned by bindings/napi/tests/cap_delegate_napi_layer3_shares_enforcement_1018_closure_pin.rs (installs a `DenyHostileResolver`, asserts the typed denial through the napi-consumed seam).

- #1010 (raw `plugin_did` String): PARTIAL — remains OPEN. The DID-format-validation gap (gap 2) is closed: napi `delegate_capability` now validates via `Did::resolve()` and rejects malformed input with `E_PLUGIN_DID_MALFORMED` (bindings/napi/src/lib.rs:852-864). Still absent: the install-time mint-and-return surface (`mintPluginDid` / a signed-install path returning the freshly-minted plugin-DID) and the per-plugin enumeration surface (`engine.listInstalledPlugins()`) — gaps 1 and 3 of the finding (grep finds no `mintPluginDid` / `listInstalledPlugins` / `PluginDid` in bindings/napi/src).

- #1002 (no `installModuleSigned` surface): PARTIAL — remains OPEN. The signed-install surface now exists with registry signature verification (bindings/napi/src/lib.rs:1763-1832, threading `ManifestVerifyArgs::registry`), closing the core #624/#1205 security gap (verification no longer bypassed at the cdylib boundary; pinned by install_module_signed_napi_cdylib_enforcement_1205_closure_pin.rs). Still outstanding versus the recommended close-shape: `installModuleSigned` returns a bare CID String (`Ok(installed.to_base32())`) rather than the structured `{ pluginCid, pluginDid, anchorCid, versionTag, installRecordCid, replaced? }`; there is no plugin-DID mint return; no `E_PLUGIN_COMPOSITION_CYCLE` surfacing (grep finds none in napi); and unsigned `installModule` was not renamed to `installModuleUnsignedDevelopment` (lib.rs:1698).

Keeping this umbrella open until the remaining #1010 / #1002 forward-readiness surfaces (mint-and-return, plugin enumeration, structured install record, composition-cycle error surfacing) land.

### #1219
**Finding:** Bundle: 4 v1-SemVer-locking View-trait/Subscriber plugin-shape decisions (#1089 rebuild docstring, #1082 Subscriber scale, #1091 plugin-DID binding, #1092 View method-addition policy).
**Comment:**
Status: partially drained — keep open.

#1089 is resolved in code: View::rebuild's docstring was corrected to clear-and-reset (not event-replay) semantics — crates/benten-ivm/src/view.rs:322-334 ("it is a clear-and-reset, not an event-replay"), landed via commit 64b33e0d (ST-IVM lane, PR #1277).

The other three are RATIFIED Phase-4-Meta-named-defers (.addl/refinement-audit-2026-05/RATIFIED-decisions-2026-05-17.md: "#1082 / #1091 / #1092 → Phase-4-Meta-named") tracked at docs/future/phase-4-backlog.md §4.81 ("benten-ivm View-trait + Subscriber Phase-5+ plugin-shape cluster"). Each still requires Ben architectural ratification before close, and none has landed in main:
- #1082 — Subscriber many-view-scale design-doc, must land pre-v1-tag (absent).
- #1091 — Engine::register_user_view principal-bound analog (`register_user_view_as`) OR documented Phase-5 blocker; no such surface exists in the tree.
- #1092 — View trait method-addition policy (sealed vs non_exhaustive vs additive-default); the View trait remains unsealed/unpolicied (the existing `#[non_exhaustive]` is on the ViewError enum, not the trait method surface).

phase-4-meta-core-close has not been tagged. Keep open until the §4.81 ratifications land.

### #1220
**Finding:** Bundle: SubgraphSpec name-collision + materializer pipeline trajectory + pub-export narrowing (6 sub-issues: #921, #922, #1083, #1085, #1087, #1088).
**Comment:**
Status: partially drained — keep open. All 6 sub-issues are CLOSED, but 2 (#1083 + #1087) are genuine Phase-4-Meta deferrals, not fixes.

Resolved in main:
- #921 — `pub mod testing` v1-intentionality documented (`crates/benten-ivm/src/testing.rs`, "refinement-audit #921 (v1 SemVer disposition)" block) and #922 — `SubgraphSpec` field-set drift-defense pin (`crates/benten-ivm/tests/subgraph_spec_field_set_drift_defense.rs`). Both landed via PR #1277 (commit 9b0f327f, mega6 batch consolidation of the ST-IVM lane).
- #1085 — DISAGREE, ratified per design-D8: `#[non_exhaustive]` is deliberately omitted on `Strategy` (documented at `crates/benten-ivm/src/strategy.rs:53`; D8 pins the closed `{A, B, Reserved}` set).
- #1088 — 3 crate-root leaked helpers (`dispatch_for` / `hardcoded_label_for_id` / `is_canonical_view_id`) narrowed to `pub(crate)` (now at `crates/benten-ivm/src/algorithm_b.rs:334-358`, removed from the crate-root `pub use`) by the D1 `CanonicalViews` seam — PR #1311 (commit c9c11c56), whose body explicitly cites "#911/#1088 closure".

Still deferred to Phase-4-Meta (verified at origin/main d0ccf606):
- #1083 — `AlgorithmBView::register_subgraph` (`crates/benten-ivm/src/algorithm_b.rs:1130`) still has NO production call-site; every consumer is a test or bench. The wired production path is `Engine::register_subgraph` (`crates/benten-engine/src/engine.rs:3041`), which reaches the IVM kernels via `AlgorithmBView::for_id`, not `register_subgraph`. Tracked at `docs/future/phase-4-backlog.md` §4.83 (materializer-pipeline wave).
- #1087 — `SubgraphSpec` name-collision unresolved: both `benten_ivm::SubgraphSpec` (`crates/benten-ivm/src/lib.rs:60`) and `benten_engine::SubgraphSpec` (`crates/benten-engine/src/subgraph_spec.rs:101`) remain `pub`. Tracked at §4.81 (rides the D1 `CanonicalViews` seam window).

Keep open until #1083 + #1087 land in the Phase-4-Meta materializer-pipeline / v1-API-stabilization wave.

### #1234
**Finding:** Post-COLLAPSE audit of all surviving device-DID/device-grain attribution uses under the unified principal model.
**Comment:**
## Status — audit done, recommended re-home still unexecuted (keep open)

The audit this issue requested is **complete**: results recorded in the 2026-05-16 comment (12 surfaces classified — 5 elegant-as-is, 6 genuinely device-grain, 1 should-re-home; full report `.addl/refinement-audit-2026-05/audit-1234-device-attribution.md`, audited at `43365b96`).

The one gap the audit surfaced — the thin-client device-revocation gate — is **still a standalone bare-device-DID HashSet in current main** (verified at HEAD `115b0847`):
- `crates/benten-engine/src/engine.rs:939` — `revoked_device_dids: Mutex<HashSet<String>>`
- `crates/benten-engine/src/thin_client_subscribe.rs:361` `revoke_device_did` / `:386` `is_device_did_revoked` (bare `HashSet::contains` match, line 391)
- connect-path (`:200`) still consults the bare set, not a user-root-anchored seam

The audit recommended folding this re-home into the **#1230 / #1171** unified-revocation spine. Both have since closed (#1230 closed 2026-05-16 via #1251; #1171 closed 2026-05-15) **without re-homing the thin-client gate** — the only post-audit commit touching the file (#1271, COLLAPSE P2 caps consolidation) left the HashSet intact. The HARD-RULE-12 clause-(b) named destinations are now closed, so the re-home recommendation is orphaned.

**Keeping open.** Either re-home the thin-client gate now (make it a consumer of the durable user-root-anchored revocation seam rather than a second device-grain HashSet, per DECISION-RECORD §4b) or re-point this issue at a live destination before treating it as done.

### #1308
**Finding:** Multi-part post-G-CORE-2 dep-bump tail: (a) iroh rc.0->stable, (b) RC->stable promotion verification, (c) #835 from_string_unchecked visibility flip + ~100 fixture-caller migration, (d) TF-2 un-ignore (self-noted closed).
**Comment:**
Status update against current main (d0ccf606):

- (c) #835 `Did::from_string_unchecked` visibility flip + fixture-caller migration is substantially complete, landed at G-CORE-2-FP-1 (PR #1307, commit ae69c339). `Did::from_string_unchecked` is now `pub(crate)` (crates/benten-id/src/did.rs:128). The public fixture path `Did::from_string_for_test_fixture` (did.rs:147) is in use at ~90 call sites across benten-caps / benten-engine / benten-id / benten-platform-foundation, with zero remaining external `from_string_unchecked` function callers (only the `pub(crate)` definition itself plus doc-comment / discharge-marker / INTERNALS.md references remain). Effective resolution is option (c-i) (migrate callers to the named fixture loader); the loader was NOT deleted, so the stronger (c-ii) was not taken.
- (d) TF-2 un-ignore remains closed — no active `#[ignore]` attributes remain in the TF-2 test files (only doc-comment prose references the prior staging). Consistent with the issue-body note that (d) was discharged at G-CORE-2-FP-1.
- (a) iroh 1.0.0-rc.0 -> 1.0.0 stable is still pending: the pin remains `1.0.0-rc.0` at crates/benten-engine/Cargo.toml:259 and crates/benten-sync/Cargo.toml:106, and Cargo.lock resolves iroh 1.0.0-rc.0. (Not in flight via the open PR #1366, which is sealed-sender auth and does not touch the engine/sync iroh pins.)
- (b) RC->stable promotion verification tracks alongside (a).

Remaining scope is now effectively the upstream-gated iroh stable bump (a) + (b). Keeping open per phase-4-meta-deferred.