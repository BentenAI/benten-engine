I'll consolidate the 8 lens reviews into a triage-ready surface. Let me work through the deduplication and convergence analysis carefully.

# R1 Triage — F-full R0 Plan-Doc Critic Council Consolidation

**Base SHA reviewed:** `2172cb6d` (R0 verification log) · **HEAD at consolidation:** `f0fd6897` (+13 commits, all `docs(orchestration)` — crypto/invariant facts unchanged). **8 lenses, all returned.** All 8 verdicts = **APPROVE-WITH-FIXES** (zero NEEDS-REVISION, zero REJECT).

---

## 1. Convergence Read

**Deduplicated severity counts** (a single underlying issue raised by multiple lenses is counted ONCE at its highest severity):

| Severity | Count | Status |
|---|---|---|
| **BLOCKER** | **3** | OPEN — blocks advance to R2 until dispositioned |
| **MAJOR** | **20** | OPEN |
| **MINOR** | **15** | OPEN |
| **OBS** | **9** | informational / NAMED-deferred |

**Raw (pre-dedup) lens-finding tally for reference:** 3 BLOCKER + 27 MAJOR-labeled + 20 MINOR-labeled + 14 OBS across the 8 lenses. Dedup collapses the heavy clustering around (a) the #31/#30 compromise-numbering ground-truth, (b) the Sealed-Sender default contradiction, (c) the R1-Q-4 timestamp-decoupling gap, and (d) the codepoint collision.

**Does R1 need another round?** **YES — R1 has NOT converged.** Per the iterate-to-convergence discipline (R1 is in scope: gates + mutates the R0 artifact, not re-reviewed by a downstream converging tier until R6), and per the strict Q5 termination rule (any open BLOCKER/MAJOR blocks advance):

- 3 open BLOCKERs + 20 open MAJORs.
- The R0 must be **revised** (most BLOCKER/MAJOR items are R0-author edits), then **R1.2 re-reviews the EDITED R0** until a full round returns 0 BLOCKER/MAJOR.
- **However** — three of the open items (Sealed-Sender default, the codepoint collision value-choice, the X-Wing corrective Option-A-vs-B fork) are **architectural forks for Ben**, not orchestrator-resolvable. These must be surfaced to Ben BEFORE R1.2, because the R0 revision depends on his rulings.

**Recommended sequence:** Surface the 3-5 load-bearing items (§4) to Ben → apply Ben's rulings + all FIX-NOW R0 edits → R1.2 re-review of edited R0 → converge.

---

## 2. Deduplicated Findings Table

### BLOCKERS (3)

| # | Lenses | Issue | Proposed disposition |
|---|---|---|---|
| **B-1** | **L2 (DS-1)** | `benten-sync` is **missing** from the `benten-membership-set` §6.1 crate-dependency plan and the §7.3 wave-DAG, yet it owns HLC ordering, Loro CRDT merge, MST diff, and the iroh transport the entire MembershipSet sync model runs on. A membership-set crate that does the AAD-bound snapshot but cannot reach HLC-LWW merge or transport cannot converge a fork. | **FIX-NOW** — add `benten-sync` to §6.1 deps + the DAG edge; decide+state direction (sync upstream → membership-set depends on sync); resolve where iroh-gossip code physically lands (R2/R3). |
| **B-2** | **L5 (WF-1)**, corroborated by **L7 (F-SEQ-1)**, **L8 (SI-6)** | **In-tree Compromise #31 is INVERTED in the R0's renumbering plan.** Ground-truth: #31 = "Drop bundle revocation reach" (summary row :91 + callout :30 + detail :2712); the LAMPS section at :2433 has **NO summary-table row** (orphaned). The R0 asserts "#31 = LAMPS in-tree; revocation → #62" — the opposite of reality. The entire §5 renumbering spine is built on this inverted premise. **L7 adds:** #30 is **already occupied** (unaudited-PQ); the re-point touches THREE sites not two. **L8 adds:** four loci must retense atomically or it self-collides. | **FIX-NOW** — re-derive the collision from actual in-tree state. **Note lens disagreement on direction** (see below). Enumerate exact edit sites in §9.1 exit-criterion 7. |
| **B-3** | **L8 (SI-1)** | **`0x6380`/`0x6390` codepoint collision between the two ratified source docs.** 9-eyes registry (U13): `0x6380..0x638F`=MLS-Application, `0x6390..0x639F`=MLS-Welcome, Sealed-Sender=`0x6510`. M-CONS-FINAL (copied into R0 §4.2): `0x6380`=MembershipSetEncryption, `0x6390`=Sealed-Sender. **Same integers, mutually-exclusive purposes.** R0 internally cites Sealed-Sender at BOTH `0x6390` (§4.2) and `0x6510` (§4.1). Once G-CORE-9 freezes, a wrong assignment is a permanent wire-break. | **FIX-NOW** — produce ONE authoritative §4 codepoint table reconciling both source docs; resolve Sealed-Sender canonical value + relocate MembershipSetEncryption out of the MLS bracket; state supersession per-codepoint. **Surface to Ben** (value choice is wire-frozen). |

> **⚠️ Lens disagreement on B-2 (the #31 collision direction) — do not paper over.** **L5 (WF-1)** says revocation-reach should KEEP #31 (it's the in-tree occupant) and LAMPS gets a fresh number; the R0's "LAMPS keeps #31" plan is wrong. **L7 (F-SEQ-1)** and **L8 (SI-6)** agree the in-tree #31 is double-used but conclude the R0's *direction* (LAMPS keeps #31, revocation→#62) is **correct and authoritative** because CLAUDE.md baked-in #5 cites "#31 = LAMPS" 3×. **This is a genuine fork:** honor the tracked-doc occupant (L5) vs honor the CLAUDE.md narrative (L7/L8). **Both agree the R0's in-tree *description* is wrong and must be re-derived against HEAD.** The direction-choice should go to Ben (it determines whether CLAUDE.md #5 or the tracked SECURITY-POSTURE.md row is the source of truth).

### MAJORS (20)

| # | Lenses | Issue | Proposed disposition |
|---|---|---|---|
| **M-1** | **L1 (CRYPTO-1)**, **L2 (DS-6)**, **L4 (MD-3/MD-4)**, **L3 (NQ-2)** | **R1-Q-4 timestamp-decoupling claim is partly false-as-written + never asserted in the R0.** Replay defense for DeviceLink/RemotePermission rides on the **outer UCAN nonce-cache**, NOT generation counters (counters defend key-staleness/substitution; nonce-cache defends replay). The 1-hour privacy bucket coarsens the SAME `valid_until` epoch U5 uses for the functional replay-window. Grep for "decoupl" in R0 = **0 hits**. | **FIX-NOW** — rewrite R1-Q-4 resolution with the honest decoupling map; assert the matrix (HLC / UCAN nbf-exp / key-retention / generation-counters all separate from the 1-hr bucket); **name the nonce-cache as a load-bearing v1-beta REQUIREMENT** (scope/retention/durability/multi-device); pin a test that intra-hour replay is rejected by the nonce-cache. |
| **M-2** | **L3 (TM-1)**, **L4 (MD-1)**, **L8 (SI-1/R1-Q-2)**, **L1**, **L2**, **L5**, **L7** (all touch) | **Sealed-Sender DEFAULT vs additive-slot — R0 is self-contradictory AND the source docs conflict.** R0 §2.3/§2.5 record "Am4 Sealed-Sender DEFAULT (Ben-ratified)"; R0 §4.1 lists it as CODEPOINT-RESERVE impl-at-Composing/v1-GM (the OLD additive-slot disposition) + cross-refs the WRONG question number ("R1-Q-7" should be R1-Q-2). 9-eyes leaned additive-slot HIGH-confidence (~5-8 wave-days spam-mitigation cost). If DEFAULT stands: it MUST ship at Core (wire-affecting), pulls spam-mitigation/#59 into Core scope+timeline, and materially improves #43 metadata posture. | **SURFACE TO BEN** (arch-fork) + **FIX-NOW** the R0 internal §4.1 contradiction + renumber to R1-Q-2 regardless of ruling. **L3 explicitly REFUTES** the R0's "recorded as ratified" framing — flag loudly. |
| **M-3** | **L3 (TM-3)** | **`ExecuteWorkflow` — Ben's headline rented-compute use case — is ABSENT from both the active PermissionRequest operation set (§3.4 = `Decrypt\|SignUcanDelegation\|RemoteUnlock`) and the §4.1 codepoint-reserve inventory.** 9-eyes U21 minted it as a v1-beta CODEPOINT-RESERVE precisely because a post-freeze wire-break is expensive. Highest-impact operation (arbitrary handler subgraph on untrusted compute with bounded decrypt) was dropped. | **FIX-NOW** — add `ExecuteWorkflow` to §4.1 as CODEPOINT-RESERVE (slot-lock operation-variant + `max_decrypt_count` + `executor_did` AAD-binding now; runtime enforcement post-v1-beta); add executor-trust threat entry. |
| **M-4** | **L1 (CRYPTO-5)**, **L8 (SI-4)** | **X-Wing corrective is ambiguous between two materially-different fixes:** "re-label the existing HKDF-SHA256 combiner" (~24 LOC) vs "re-implement to real X-Wing SHA3-256 combiner" (construction change → new derived keys → all golden vectors regen). §3.2 prose implies real X-Wing; the "~24 LOC" estimate implies re-label. **L8 adds:** in-tree is already self-contradictory (aead.rs says HKDF-SHA256, codepoint.rs says sha3-based); `draft-connolly-cfrg-xwing-kem` is cited zero times in-tree. | **FIX-NOW + SURFACE** — DECIDE Option A (real X-Wing at `0x647A`, +KAT vectors, interop-faithful — both lenses' recommendation) vs Option B (re-label to Benten-private codepoint). Revise LOC estimate to match. Ground-truth which combiner aead.rs actually computes in Wave-0 before sizing. |
| **M-5** | **L1 (CRYPTO-2)** | **The encryption-arc Compromise table omits in-tree Compromise #30** (unaudited PQ primitives, OPEN, MITIGATED-by-hybrid, CLOSES-at-v1-GM, code-anchored to `0x647a`+`0x0001`). This is the canonical audit-gating compromise the whole arc rests on; the planned #32 (ML-KEM Decap side-channel) is a narrower sibling. An auditor reading the F-full cluster won't see the unaudited-primitives posture is already tracked. | **FIX-NOW** — add §5.2 row cross-referencing #30 (KEEP, do-not-move); state #32 is a refinement-of-#30 at the Decap axis; make #30↔#32↔C-GM-AUDIT chain explicit. |
| **M-6** | **L1 (CRYPTO-3)** | **The 9-eyes registry's own flagged gap** — no formal-methods lens on HPKE-mode-base[MLKEM768-X25519] IND-CCA2 tractability under the **adversarially-chosen-recipient-seed** model — is surfaced nowhere in the R0. The device-link/remote-permission flows DO admit a chosen-recipient-pubkey surface (malicious device B supplies adversarial pubkey). Connects to #45 (MAL-BIND) + #59 (KEM-key-confirmation). | **FIX-NOW** — add §10.2 risk row + §9.3 audit-deliverable line; it is an R0-INPUT the consolidation dropped. |
| **M-7** | **L2 (DS-2)** | **Inv-21 fork-tie-break ("lexicographically-SMALLER HLC wins") is the OPPOSITE direction from the in-tree LWW CRDT rule ("LARGER HLC wins", `crdt.rs:533`).** R0 asserts byte-equivalence; it is a deliberate second rule (oldest-anchor-wins for forks vs last-writer-wins for properties). Also `created_at_hlc` (Inv-21) vs `admitted_at_hlc` (MemberEntry) relationship is undefined. | **FIX-NOW** — §3.8/Inv-21 must state the asymmetry explicitly + justify it; define `created_at_hlc` and its relation to `admitted_at_hlc`; downgrade the "byte-equivalent" framing (this lens IS the reviewer the R0 deferred to). |
| **M-8** | **L2 (DS-3)** | **Inv-21 tie-break is non-TOTAL:** `smaller MembershipSetId` can never disambiguate two concurrent forks off the SAME anchor (they share `membership_set_id`). When `created_at_hlc` ties, no discriminator remains. The mandated kani convergence proof requires totality. | **FIX-NOW** — make the terminal discriminator the event Version-Node CID (always distinct) or the HLC `node_id` component, NOT `MembershipSetId`. |
| **M-9** | **L2 (DS-4)** | **Federation `KSetAcquisitionPath` is FROZEN wire but its depth-4 + cycle-detect traversal semantics aren't pinned at freeze.** Risk: freezing a recursive-reference shape that can't express the depth/cycle invariant offline → wire-break to fix later (forbidden by frozen-crypto rule). | **FIX-NOW** — specify the minimal frozen `KSetAcquisitionPath` fields making depth-4+cycle-detect offline-decidable; state whether cycle-detect is receiver-local or path-carried. |
| **M-10** | **L2 (DS-5)** | **iroh-gossip convergence-correctness risk under-weighted.** In-tree `Transport` has connect/accept/send/recv but NO pub-sub/topic/broadcast — gossip is genuinely net-new. The interaction of (gossip's no-causal-delivery) × (HLC causal order) × (Inv-21 partition rule under out-of-order/duplicate delivery) is the actual hard problem and is unaddressed. Existing MST anti-entropy is almost certainly the convergence backstop (gossip = liveness/notification only) — naming it de-risks the wave. | **FIX-NOW** — add the convergence-vs-gossip-delivery analysis to §3.9 + seed R2 test-family. |
| **M-11** | **L3 (TM-2)**, **L7 (R1-Q-9)** | **R1-Q-9 under-scoped:** ship-all-5 RoleId makes Moderator+Invitee permission-SEMANTICS load-bearing at v1-beta, not optional "confirm." An active role with undefined permission-set is a security hole. Two hard constraints: **Invitee derives ZERO content** (else not-yet-accepted parties get plaintext) + **Moderator ⊊ Admin** (no admit/kick/rotate/governance). | **FIX-NOW** — sharpen R1-Q-9 + add Invitee-zero-content + Moderator-strict-subset to §9.1 exit-criterion 5. |
| **M-12** | **L3 (TM-4)** | **The §6.7 pre-merge security mini-review is NON-NEGOTIABLE but has no gate-owner, pass-criteria, or audit-Node-integrity sub-question.** An unscoped gate is a skippable gate (highest single-piece security risk per §10.2). Missing: the grant must be REJECTED if `audit_node_cid` absent/unresolvable (so "grant without audit trail" is non-constructible). | **FIX-NOW** — specify: owner = threat-model lens (Pattern 6); six pass-classes (e2r's 5 + audit-Node-binding); make §9.1 item-4 read "mini-review passed, clean on all six." |
| **M-13** | **L3 (TM-5)** | **RoleId ordinal changed silently between M-CONS-FINAL (Viewer=0, Invitee=1) and R0 (Viewer=1, Invitee=0), and the ordinal is AAD-keying-bound.** A silent swap of a frozen-wire value is exactly the drift that produces non-interoperable bytes. R0's Invitee=0-as-floor is defensible but unflagged. | **FIX-NOW** — add explicit "ordinal supersedes M-CONS-FINAL per ship-all-5" note; pin exact table at canary with golden-vector test. (L5 WF-9 + L4 MD-9 confirm the same stale "3 active, 2 reserved" Inv-20 clause-j.) |
| **M-14** | **L4 (MD-2)** | **Cross-source wire-inconsistency: does `DropToRecipient` carry `sealed_at`/`valid_until` at all?** 9-eyes U5 says Vault+Drop EXCLUDED; L6 lens shows `DropToRecipient { sealed_at_epoch_seconds, valid_until_epoch_seconds }` rated HIGH-leak. R0 adopts the exclusion but never states it, leaving L6's HIGH-leak finding dangling — an implementer following L6's struct leaks ~1-sec timestamps the bucket never reaches. | **FIX-NOW** — state explicitly: DropToRecipient carries NO `sealed_at`/`valid_until` (drops forever-valid per #62; freshness rides recipient-key-generation + nonce-cache); the 1-hr bucket is Layer-D ONLY; L6's drop-timestamp finding is closed-by-exclusion. |
| **M-15** | **L5 (WF-2)**, **L7**, **L8** | **Inv-15 claimed "in-tree, partially-enforced" — it is NOT in `INVARIANT-COVERAGE.md`** (header says "14 of 14"; highest = Inv-14; grep Inv-15 = zero). It's a design-mint like Inv-16..22, not an in-tree fact. §9.1 criterion 7 must REGISTER Inv-15 too. The §5.0 "Inv-1..15 in-tree" baseline is off-by-one. | **FIX-NOW** — reclassify Inv-15 as design-mint-to-register; add "REGISTER Inv-15" to §9.1 + doc-wave; correct INVARIANT-COVERAGE.md header count. |
| **M-16** | **L5 (WF-4)** | **§4.3 + §9.1 cite "V1-FROZEN Rows D-28/D-29" as extant; in-tree max is D-27.** D-28/D-29 are design-mints; §4.3 reads as "already there," violating HARD-RULE-12(b) named-destination-must-receive-entry-now. (L8 SI-5 adds: R0 cites TWO different filenames for the destination doc.) | **FIX-NOW** — mark D-28/D-29 "(NEW — minted at doc-wave)" everywhere; name the single destination doc explicitly + the global D-NN namespace. |
| **M-17** | **L5 (WF-5)** | **`audit_log_query` is FREEZE while the audit log is GRAPH-NATIVE — the frozen-crypto rule does not cleanly cover "ops over graph-native data."** A READ op over graph data is not AAD-bound, doesn't gate K_Set, isn't crypto-verify — yet marked FREEZE via a silent implicit 4th clause. The "24 ops" count is unsourced (not in M-CONS-FINAL). | **FIX-NOW** — either add an explicit 4th limb to the frozen-crypto rule (public op-signature → freeze signature, graph-native the data) OR demote `audit_log_query` to GRAPH-NATIVE (consistent with §3.8's own "thin scoped-READ" framing). They currently contradict. (L6 GNC-1 corroborates: the `audit:<set_id>:*` scope string has no in-tree arm — must be `RestrictedScope`.) |
| **M-18** | **L5 (WF-6)** | **`AeadEnvelope`→`EncryptedEnvelope` is a rename+structural-lift+typed-AAD-introduction of a SHIPPED in-tree type, not a greenfield mint** (grep `EncryptedEnvelope`/`BindingContext` = zero; shipped is `AeadEnvelope` flat with untyped `&[u8]` AAD). Three V1→V2 migrations riding the same bump as Wave-0; R0 treats them as fresh mints — risks two independent V2 bumps. | **FIX-NOW** — state the rename+lift+typed-AAD as ONE V1→V2 migration co-scheduled with Wave-0's BE/X-Wing bump (one format-version step). |
| **M-19** | **L5 (WF-7)** | **BE-endianness migration blast radius understated as "~1 wave-day."** `to_le_bytes`/`from_le_bytes` span ≥6 AEAD sites (incl. AAD chunk_index/total_chunks/recipe_index) + `aead_wrap.rs` + platform-foundation + ≥4 golden-vector/conformance test files. Must flip codepoint AND AAD length/index fields to BE consistently (mixed-endianness AAD = correctness bug) + regen every swap-matrix vector. Multi-crate, test-heavy. | **FIX-NOW** — re-scope Wave-0: "all multiformats-framed integer fields on wire → BE (codepoint + AAD length-prefixes + index fields), single audit pass + full golden-vector regen"; raise estimate; add conformance test asserting NO `to_le_bytes` survives on any wire/AAD path. |
| **M-20** | **L7 (F-SEQ-2)** | **Wave-0 (X-Wing/BE-V2) is "independent, lands first" but is NOT a structural upstream of the encryption canaries in the §7.3 DAG** — nothing forces Wave-0 to MERGE before a canary authors LE/V1-shaped envelope bytes, forcing a second migration before freeze. | **FIX-NOW** — add a hard dependency edge `[Wave-0] ──► [Canary-ENC-1/ENC-2]` for the format-version/endianness convention; all new codepoints authored at V2+BE from first commit. |

### MINORS (15)

| # | Lenses | Issue | Proposed disposition |
|---|---|---|---|
| **m-1** | L1 (CRYPTO-4) | X-Wing info-tag `"x-wing-v1-benten-0x647a"` hardcodes codepoint as ASCII; Q2 LE→BE must state info-tag string is NOT endianness-affected (else re-derive silently diverges). Three distinct `0x647a` encode-sites. | FIX-NOW — enumerate the 3 encode-sites in §3.2/§7.1 Wave-0; per-site touch statement. |
| **m-2** | L1 (CRYPTO-6) | libcrux-ml-kem swap (Q1) + LE→BE (Q2) both change bytes at `0x647A`; need a FIPS-203 byte-compatibility KAT cross-check (RustCrypto `ml-kem 0.2` ↔ libcrux) as a Wave-0 exit gate, not assumption. | FIX-NOW — one exit-criterion line in §9.1/§7.1 + `check-secret-independence` in CI. |
| **m-3** | L1 (CRYPTO-7) | Inv-17 "no pure-PQ KEM mintable" wording tension with in-tree reserved `PURE_PQ_MLKEM768_ONLY = 0x647c` (reserved-named/typed-rejected). | FIX-NOW — sharpen Inv-17 to "no pure-PQ codepoint LIVE/selectable; reserved-typed-rejected arms permitted for swap-matrix conformance, audit-gated." |
| **m-4** | L1 (CRYPTO-9) | Layer-A vault §3.1 specifies 12-byte ChaCha20 nonce; U32 prescribes XChaCha20 (24-byte) for re-encryption-heavy sites (K_principal rotation hits 2^32 birthday bound). | FIX-NOW — §3.1 pin which AEAD nonce-width the vault uses + nonce-reuse rationale. |
| **m-5** | L3 (TM-6) | Coerced-approving-device (Layer-D `RemoteUnlock`/`SignUcanDelegation` coercion) not in Compromise table; distinct from #33 password-coercion; the audit-Node makes a coerced grant look legitimate forever. | FIX-NOW — extend #33 disclosure scope to the Layer-D approval-coercion vector. |
| **m-6** | L3 (TM-7) | #60 (role-transition doesn't invalidate prior UCANs) not connected to its dangerous compositions under ship-all-5 + remote-permission (ephemeral UCAN survives role-downgrade; only `exp` bounds it). | FIX-NOW — composition note on #60 cross-linking #52 + tight-exp default mandate in §3.4. |
| **m-7** | L3 (TM-8) | Inv-20 clause-d "per-recipient unlinkability = network-observer-only" scoping not surfaced in the audit-log threat narrative (reader could over-read it as protecting against malicious admin). | FIX-NOW — carry network-observer-only-vs-insider scoping into §3.8 + THREAT-MODEL.md skeleton. |
| **m-8** | L4 (MD-4) | R0 never states replay/freshness rides on generation-counters + nonce-cache (not the coarsened time field) — the load-bearing justification for the whole U28 1-hr disposition is absent. | FIX-NOW — one sentence in §3.10 citing Compromise #25 (shipped nonce-cache substrate). (Overlaps M-1.) |
| **m-9** | L4 (MD-5) | Jitter-vs-round-down + fixed-vs-variable-bucket sub-fork silently pre-decided; jitter↔clock-skew interaction can double-widen the replay window. | FIX-NOW — surface as explicit R1/R3 sub-question under R1-Q-4; default = round-down-no-jitter; note jitter↔skew interaction. |
| **m-10** | L4 (MD-6) | 1-hour granularity is the correct knee of the curve but the R0 inherits U28 without recording WHY (33→14 bits/year; coarser degrades Layer-D UX; finer wasted given nonce-cache). | FIX-NOW — record granularity rationale in SECURITY-PROOFS.md (planned doc-wave). |
| **m-11** | L4 (MD-7) | D6 gossip topic-rotation freshness source (`generation_summary`, NOT time-bucketed) + OOB-bootstrap first-contact metadata residue both implicit. | FIX-NOW — §3.9 state rotation rides generation-counters; cross-link OOB-bootstrap to #43/#62. |
| **m-12** | L5 (WF-8) | `0x6400` arm (classical-only X25519 downgrade) is in-tree (`aead.rs:300/354`) but absent from the §4.1 codepoint inventory — Inv-18 registry-completeness gap at freeze. | FIX-NOW — add `0x6400` row with lifecycle state; confirm BE-migration + golden-vector coverage. |
| **m-13** | L5 (WF-9), L8 (SI-7) | MembershipSet codepoint families + reserves scattered across §3.9/§4.1/§4.2/§10 with no single allocation table; intra-`0x6100..0x6FFF` non-collision never asserted; "IANA-disjoint from WHAT" unstated. | FIX-NOW — add single Benten codepoint allocation table (canonical home = `CRYPTO-CODEPOINTS.md`); name which registries it avoids + that selectors are a private namespace. |
| **m-14** | L5 (WF-10) | `RecoveryHook` "FREEZE (trait shape)" freezes a trait signature before its protocol is designed (social-recovery vs Shamir vs hardware-token differ structurally). | FIX-NOW — defer trait signature to Composing; reserve only a `RecoveryArtifact` DAG-CBOR codepoint slot at Core (the wire byte IS frozen-crypto-covered). |
| **m-15** | L6 (GNC-2/GNC-3/GNC-4/GNC-5/GNC-7) | Graph-native precision cluster: (GNC-2) "free tamper-evidence" overstates — attribution triple is `Option`, bare `put_node` leaves it unset (enforced-path needed); (GNC-3) IVM view is a compiled `const` SSoT edit = Rust-plugin LOC, not "free data"; (GNC-4) EP-1 seam roster drift (`GraphBackend` unlisted; IVM=enum not trait); (GNC-5) AAD 9-tuple assembly must pass opaque bytes to crypto-suite (no reverse dep); (GNC-7) `MemberRef` variant is Kind-determined not a per-member nature discriminator (Inv-22 boundary). | FIX-NOW — five one-line precision edits across §2.6/§2.7/§3.7/§3.8/§6.1/§8.1. |

### OBS (9 — informational / NAMED-deferred)

| # | Lenses | Issue | Proposed disposition |
|---|---|---|---|
| O-1 | L1 (CRYPTO-8) | `secrecy` crate is a new Layer-A dependency (only `zeroize` vendored today). | BELONGS-NAMED-NOW → §6.2 dep list + Compromise #39 supply-chain row. |
| O-2 | L1 (CRYPTO-10) | §3.2 rationale should name both the adversary-axis AND capability-axis for Layer-A/B. | BELONGS-NAMED-NOW → §3.2 design-rationale note. |
| O-3 | L2 (DS-7) | In-tree DUAL-CID precedent (`TwoCidStore`/`TwoCidMap`, G-CORE-3e) unreferenced; reads as net-new. | FIX-NOW (cheap) — §0.3 verification-log row + reframe §3.3 as "extends in-tree two-CID mapping." |
| O-4 | L2 (DS-9) | Revocation-propagation-lag (revoked device exercises stale grant during partition) inherited unbounded. | BELONGS-NAMED-NOW → sub-clause of Compromise #41. |
| O-5 | L2 (DS-10) | `generation_summary` in HMAC-blinded topic under-specified for lagging-member rendezvous (different generations → different topics → can't meet). | FIX-NOW — one definitional sentence in §3.9 (set-generation not per-member-vector for steady-state). |
| O-6 | L3 (TM-9/TM-10) | Blast-radius ladder (`Decrypt`=1 Node < `SignUcan`=attenuated-exp-bounded < `RemoteUnlock`/device-link=full K_principal-permanent) + trust-tier×operation matrix. | BELONGS-NAMED-NOW → `docs/THREAT-MODEL.md` skeleton (§7.1 doc-wave). |
| O-7 | L4 (MD-8) | DUAL-CID per-recipient unlinkability sound; `plaintext_cid_local` never-serialized needs an R5 kani/property test. | OUT-OF-SCOPE-for-R0 → R3 test-landscape captures. |
| O-8 | L5 (WF-11/WF-12) | §4.4 reduction-tally double-counts/uses inconsistent units; §15.c HALT-AND-SURFACE guard referenced not specified. | FIX-NOW (trivial) — single canonical reduction tally + inline the §15.c one-liner. |
| O-9 | L7 (F-SEQ-3/F-SEQ-4/F-SEQ-5/F-SEQ-6), L8 (SI-8/SI-9) | Sequencing/cost/standards refinements: DAK-unlock parallelizable with Layer-C (not after); §8.1 cost units non-additive; remote-permission-mini-review NEEDS-REVISION contingency (wire-freeze forecloses deferral); McMillion-hpke PQ-KEM-extensibility + libcrux-API-stability unbuffered; did:key hybrid-pubkey multicodec prefix; full HPKE (KEM,KDF,AEAD) triple. | Mix of FIX-NOW (one-line DAG/cost/uncertainty edits) + BELONGS-NAMED-NOW (§3.3/§4.1/§6.2 R5 freeze-rows) + R2 questions. |

---

## 3. The 9 R1-Question Resolutions

| Q | Council ruling | Confidence | vs orch my-pred | Flag |
|---|---|---|---|---|
| **R1-Q-1** (Layer-B AEAD vs Layer-A DAK — duplicative?) | **CONFIRM — KEEP BOTH.** Not duplicative: Layer-A defends the root secret vs offline-at-rest adversary lacking the password; Layer-B exists PRIMARILY to enable Cryptree selective sharing (recipient gets K(N) for a subtree without K_principal). Dropping Layer-B destroys selective-sharing. (L1, L3 both confirm; L1 adds adversary-axis vs capability-axis refinement → CRYPTO-10.) | **HIGH** | **CONFIRMS** my-pred (KEEP both). | — |
| **R1-Q-2** (Sealed-Sender DEFAULT vs additive-slot — #43 posture) | **⚠️ DIVERGENT / DISPUTED.** **L3 REFUTES** the R0's "recorded as ratified DEFAULT" framing — the two source docs conflict (9-eyes leaned additive-slot HIGH-conf; brief records Ben ratified DEFAULT) AND R0 §4.1 is self-contradictory (lists CODEPOINT-RESERVE + wrong cross-ref). **L4 REFINES** — treat DEFAULT as ratified per brief but fix every downstream surface (§4.1 → FREEZE+SHIP-at-Core, value `0x6510`, pull spam-mitigation into Core). **L8 says NOT orchestrator-resolvable — surface to Ben.** All lenses agree: wire-affecting, scope-affecting, metadata-posture-affecting; if DEFAULT stands it MUST ship at Core (not Composing). | **HIGH** (that it's a real fork) | **REFINES/PARTLY-REFUTES** — my-pred deferred to Ben + flag divergence; council confirms divergence is real, sharper, and the R0 internally encodes the LOSING disposition. | **🚩 LOUD FLAG — surface to Ben; do NOT let R2 proceed on an assumed default.** |
| **R1-Q-3** (per-layer-distinct envelope; §14.1 unified stays NAMED-deferred) | **CONFIRM — NO CONTRADICTION.** L1 traced the C-1 NO-GO to its root: Option-F+ rejected because feeding a password-derived seed into ML-KEM `KeyGenDerand` exposes the SampleNTT variable-time rejection-sampling side-channel (Arriaga "Tempo" IACR 2025/1399). This does NOT touch the ratified design (Layer-A = symmetric AEAD, no ML-KEM keygen; Layer-D keys = fresh OsRng). §14.1 unified-envelope IS the forbidden pattern → correctly NAMED-deferred. One adjacent gap → CRYPTO-3 (M-6). | **HIGH** | **CONFIRMS** my-pred. | — |
| **R1-Q-4** (DAG-CBOR framing load-bearing? + timestamp-decoupling + replay-rides-on-counters) | **(a) DAG-CBOR outer framing = LOAD-BEARING/FREEZE** (L1/L2/L4/L5 concur; subsumes U3 TLV-injectivity; CID-aligned). **(b) Decoupling PARTLY FALSE as written + never asserted** — HLC/UCAN-nbf-exp/key-retention/generation-counters ARE cleanly decoupled; the **replay-window itself is NOT** (U28 coarsens the same `valid_until` epoch U5 uses functionally; sub-hour replay rides the **nonce-cache, NOT generation-counters**). **(c) 1-hour = correct knee CONDITIONAL on pinning the nonce-cache (durability/scope/multi-device).** | **HIGH** (L2 MED on the timing-curve judgment) | **REFINES** my-pred (both LOAD-BEARING) — bucket is load-bearing, but the decoupling sub-claim must be rewritten + nonce-cache promoted to a named v1-beta REQUIREMENT. | **🚩 FLAG — the "replay rides on generation counters not time-precision" premise is WRONG for DeviceLink/RemotePermission; nonce-cache is the actual mechanism and is currently an assumption, not a spec.** |
| **R1-Q-5** (audit-event version-chain substrate at Core; query-tooling at Composing) | **CONFIRM — substrate at Core, tooling at Composing.** Substrate is genuinely cheap re-use of shipped pieces (version-chain, `ChangeEvent` triple, `CANONICAL_VIEW_META`). (L2/L6/L7 all confirm.) **Refinements:** call it "cheap additive" not "free" (L6 GNC-3: IVM view = Rust-plugin LOC); convergence gated on Inv-21 being TOTAL (L2 — DS-3); attribution-integrity needs enforced-path (L6 GNC-2). | **HIGH** | **CONFIRMS** my-pred (yes). | — |
| **R1-Q-6** (headline timeline: honest ~7-15wk vs optimistic ~4-5wk) | **CONFIRM — headline IS ~7-15 weeks (~2-4 months); ~4-5wk is the AI-agent-dispatch-only floor.** L7 ground-truthed e2r §12.2: "~4-5wk" = agent-dispatch tempo bounded by the 3-person-week external audit; ~7-15wk includes ADDL convergence variance (Phase-4-Foundation R6 took 8 rounds) + Layer-D glue + audit-remediation buffer. **Refinement: invert the §8.2 presentation** (currently LEADS with optimistic number before the correction). | **HIGH** | **CONFIRMS** my-pred (honest framing). | — |
| **R1-Q-7** (Garden/Grove governance = signed-config-Node content, not wire-codepoints) | **CONFIRM — HOLDS.** Live in-tree precedent: `system:GovernanceInheritance` Node + registered IVM view (L6 ground-truthed). Moderation policy isn't AAD-bound / K_Set-gating / crypto-verify → graph-native by the frozen-crypto rule. −2 sub-codepoint shrink justified. **Tripwire (L6/L2/L8):** if any future governance semantic gates K_Set/decryption (not just authorization), it crosses the rule and becomes wire-format — re-surface then. | **MED-HIGH → HIGH** | **CONFIRMS** my-pred (holds). | — |
| **R1-Q-8** (mint #56 journalist-FS, #59 KEM-key-confirm, #61 fingerprint-leak at v1-beta?) | **CONFIRM — MINT ALL THREE.** All are honest-disclosure compromises (near-zero engineering cost); an external audit WILL surface each. #61 is already CLOSED by the D6 HMAC-blinded topic (minting records the closure, honors ratification-must-close-origin). #59 is a direct HPKE/RFC-9180 concern. (L1/L3/L8 all confirm.) **Refinement:** keep #56 sharply framed as a SEPARATE design-class from #42/#52 (per-message-FS vs group-key-rotation-FS) so the audit doesn't read it as a duplicate. | **HIGH** | **CONFIRMS** my-pred (mint all). | — |
| **R1-Q-9** (define Moderator+Invitee permission-set semantics at Core?) | **CONFIRM — YES, and STRONGER than "confirm" (load-bearing, not optional).** `role_assignments_generation` is AAD-bound + `E_ROLE_STALE_AT_VERIFY` rejects stale-role stanzas → meaningless unless the 5 roles are DEFINED at Core (L2). Two hard constraints: **Invitee = zero content**, **Moderator ⊊ Admin** (L3). Frozen surface = ordinal+generation; semantics = UCAN templates (graph) (L5/L7). Governance workflows defer to Composing. | **HIGH** | **CONFIRMS + STRENGTHENS** my-pred (yes) — it is a v1-beta exit-criterion, not a free Composing choice. | **🚩 minor flag — "all-5-active" risks shipping as ordinals-without-semantics unless the concrete UCAN-template ability-sets land at Core.** |

**Net:** 7 of 9 CONFIRM the orch my-pred. **R1-Q-2 is genuinely DIVERGENT (refute/dispute — surface to Ben).** **R1-Q-4 REFINES (decoupling sub-claim wrong; nonce-cache must be promoted).** R1-Q-9 confirms-but-strengthens.

---

## 4. The Most Load-Bearing Items to Surface to Architects FIRST

These are the items that (a) block other work, (b) freeze wire bytes permanently, or (c) require a Ben ruling the R0 revision depends on. Ordered by leverage:

1. **🚩 Sealed-Sender DEFAULT vs additive-slot (M-2 / R1-Q-2) — BEN ARCH-FORK.** The two ratified source docs directly contradict; the R0 internally encodes the losing disposition; it is wire-format-affecting, scope-affecting (pulls ~5-8 wave-days spam-mitigation + #59 into Core if DEFAULT), and metadata-posture-defining (#43). **L3 refutes the "already-ratified" framing.** R2 cannot proceed on an assumed default. *Present Ben: Option A (plaintext-sender default + reserved Sealed-Sender sibling, #43 accepted-with-roadmap, lower v1-beta risk) vs Option B (Sealed-Sender DEFAULT, closes #43-by-default, +~5-8 wave-days, ships at Core).*

2. **🚩 The two ground-truth INVERSIONS — Compromise #31 (B-2) + Inv-15 (M-15).** Both are claimed "in-tree" but are actually inverted/absent. The entire §5 compromise-renumbering spine + the SUF-CMA-gap-closure argument rest on these. **They prove the §0.3 verification log was not exercised against the tracked docs** (it trusted the CLAUDE.md narrative). *The #31 direction-choice (LAMPS-keeps-31 per CLAUDE.md #5, per L7/L8, vs revocation-keeps-31 per the tracked doc, per L5) needs a Ben ruling on which is source-of-truth.* This is the cleanest single item to surface because it casts doubt on the verification-log discipline broadly → re-run §0.3 against HEAD.

3. **🚩 The codepoint collision (B-3) + the unified registry table (m-13).** `0x6380`/`0x6390` are double-booked between the two source docs; Sealed-Sender is cited at two values. **A single authoritative `CRYPTO-CODEPOINTS.md` table must be authored EARLY (R2, before any wire-minting canary)** — L8's strong recommendation. Wire-frozen; a wrong assignment is a permanent break. Couples to item #1 (Sealed-Sender's value).

4. **The R1-Q-4 nonce-cache promotion (M-1 / R1-Q-4).** The central R1-Q-4 hypothesis ("replay rides on generation counters not time-precision") is **wrong for DeviceLink/RemotePermission** — it rides on the outer UCAN nonce-cache, which the R0 treats as an assumption, not a spec. The 1-hour bucket's safety is conditional on the nonce-cache being durable/full-hour/multi-device-correct. This is freeze-affecting (the replay window can't be retrofitted post-freeze) and converges 4 lenses (L1/L2/L3/L4).

5. **`benten-sync` missing from the crate plan (B-1) + the iroh-gossip convergence question (M-10).** The 15th-crate dependency DAG cannot converge a fork without the crate that owns HLC/CRDT/MST/transport. Coupled: gossip is genuinely net-new (no in-tree pub-sub), and the convergence-under-out-of-order-delivery problem is unaddressed — almost certainly MST anti-entropy is the backstop and gossip is liveness-only, but the R0 must say so. This is the single largest *structural* + *wall-clock-risk* item (R1-Q-6 timeline rests on it).

*(Runner-up worth naming: the X-Wing corrective Option-A-vs-B fork (M-4) — also wire-frozen + a Ben-adjacent decision, but lower-leverage than the five above since both lenses already recommend Option A.)*

---

## 5. New Questions for R2 / R3

**Wire-format / freeze (highest priority — these gate the G-CORE-9 freeze):**
- **NQ-W1 (one V2 bump or several?)** — Wave-0 BE+X-Wing + the `AeadEnvelope`→`EncryptedEnvelope` lift + typed-`BindingContext` are all V1→V2 migrations. R2 must ratify ONE `ENVELOPE_FORMAT_VERSION_V2` step (L5/L7).
- **NQ-W2 (codepoint registry authority + early authoring)** — Is `CRYPTO-CODEPOINTS.md` authored at R2 (prerequisite) rather than the trailing doc-wave? What CI scanner enforces intra-`0x6100..0x6FFF` non-collision + IANA-disjointness at freeze? (L5/L8)
- **NQ-W3 (frozen op-surface enumerated?)** — The "24 ops" figure is unsourced. Produce the actual frozen-op list OR rule that `audit_log_query` is graph-native and NO op signatures freeze beyond the 12-primitive surface. The frozen-crypto rule needs either a 4th limb or this ruling. (L5)
- **NQ-W4 (`members_table` canonical bytes)** — Pin the exact length-injective (U3) byte encoding of the AAD-bound `BTreeMap<Did, MemberEntry>` snapshot (`MemberEntry` fields + `Option<SigPubKey>` presence + `Hlc` encoding) or two engines materialize divergent AAD for the same membership. (L5/L3 NQ-4)
- **NQ-W5 (`RecoveryHook`: artifact-codepoint-only at Core, trait at Composing?)** — Confirm we don't freeze a trait shape ahead of its undesigned protocol. (L5/L7)

**Distributed-systems (gate the kani convergence proof):**
- **NQ-D1 (iroh-gossip placement + convergence model)** — Does gossip code land in `benten-sync` (new `GossipTransport` trait) or `benten-membership-set`? Is convergence backed by existing MST anti-entropy (gossip = liveness-only) or does gossip carry convergence? (L2 — strong lean: MST is the backstop.)
- **NQ-D2 (total Inv-21 tie-break)** — Precise total ordering + the exact relationship between `created_at_hlc`/`admitted_at_hlc`/version-chain HLC stamp (must be total for kani). (L2)
- **NQ-D3 (`KSetAcquisitionPath` frozen field set)** — Minimal frozen fields making depth-4+cycle-detect offline-decidable; cycle-detect receiver-local or path-carried? (L2)
- **NQ-D4 (`generation_summary` definition)** — Precise enough that steady-state lagging members rendezvous while genuine forks rotate the topic; + how losing-fork members re-converge onto the winning fork's NEW topic mid-life. (L2)

**Threat-model (gate the §6.7 security mini-review):**
- **NQ-T1 (audit-Node encryption + replication)** — Is the `PermissionGrant` audit-Node encrypted, and under whose key? Must grants replicate the audit-Node to all of the user's devices so a malicious device can't grant-and-hide? (L3)
- **NQ-T2 (`valid_until` clock vs the 1-hr privacy bucket on the remote-permission surface)** — Confirm the U28 1-hour bucket is decoupled from the `valid_until` enforcement clock (else a coarsened `valid_until` widens the coercion/replay window from 60s to ≤1hr — a threat-model regression). This is R1-Q-4 re-checked specifically at Layer-D. (L3 NQ-2)
- **NQ-T3 (`ExecuteWorkflow` no-egress enforcement)** — Does the engine enforce the rented executor cannot exfiltrate decrypted plaintext beyond `result_recipient_pubkey` (via EMIT/WRITE)? The AAD-bound scope (`executor_did`, `max_decrypt_count`, `result_recipient_pubkey`) is frozen at v1-beta and must be SUFFICIENT to express the constraint even if enforcement is post-v1-beta. (L3 — SANDBOX-escape class re-asked at the rented-compute boundary.)
- **NQ-T4 (nonce-cache spec)** — Scope (per-device vs user-global), retention (must cover full 1-hr bucket), durability (survives restart), multi-device-shared semantics (does device C reject a nonce device B consumed)? Freeze-affecting under-spec. (L2/L3/L1)

**Crypto / standards-interop:**
- **NQ-C1 (HPKE KEM-extensibility)** — Does Brendan McMillion's `hpke` crate accept a custom/PQ KEM (so X25519MLKEM768 plugs into a real RFC-9180 context) or does Benten supply the KEM and reuse only the HPKE KDF/AEAD/key-schedule? Determines whether "HPKE-RFC-9180" is byte-accurate or aspirational; resolve before Canary-ENC-2. (L8/L7)
- **NQ-C2 (libcrux ↔ RustCrypto ml-kem FIPS-203 KAT)** — Does the swap preserve exact encap/ciphertext/decap-key serialization so golden vectors survive? Cross-impl KAT gate. (L1)
- **NQ-C3 (cross-ecosystem conformance vectors)** — Should v1-beta verify a LAMPS Composite ML-DSA sig from BouncyCastle/OpenSSL-3.5/OpenPGP-PQC against Benten's verifier (and vice versa)? The swap-matrix tests internal round-trips only; the "ecosystem interop" justification for LAMPS is asserted but untested. (L8)
- **NQ-C4 (did:key hybrid-pubkey multicodec)** — What multicodec prefix do the PQ-hybrid sig/KEM public keys use in `did:key`? If no registered multiformats value exists, what private-value-with-fallback is reserved at G-CORE-9? Wire-affecting. (L8/L6 R2-Q-B)
- **NQ-C5 (jitter ↔ clock-skew)** — If `sealed_at` jitters earlier and `valid_until` jitters later, the effective replay window exceeds 1 hour by ≤2×jitter+skew — does the nonce-cache window need widening, or does round-down-no-jitter avoid it cleanly? (L4)

**Audit-gate sequencing:**
- **NQ-A1 (audit on frozen wire → re-freeze contingency)** — Does audit baseline = `phase-4-meta-core-close` mean a high/critical wire-format finding requires re-opening the freeze + a new tag? The remote-permission mini-review NEEDS-REVISION path can't be a post-tag wire-break (L7 F-SEQ-5) — name the conservative-reserve-codepoints-implement-minimal fallback explicitly.
- **NQ-A2 (assessment-window placement)** — CLAUDE.md #15 places the v1-assessment-window BETWEEN `phase-4-meta-close` and `v1-beta`, but R0 §9.2 folds it into Composing exit-criteria. Confirm the tag sequence (`phase-4-meta-close` → assessment-window → `v1-beta`) is unambiguous. (L7 NQ-5)
