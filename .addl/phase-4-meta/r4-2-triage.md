# F-full R4.2 convergence re-review — triage (NOT-CONVERGED)

Panel 15/15 · adversarial-verify 7 CONFIRMED / 2 PARTIAL · 2 BLOCKER + 7 MAJOR + 11 MINOR + 24 OBS

R4.1's 8 BLOCKER all verified CLOSED; R4.2 surfaced 2 NEW freeze-gating BLOCKERs.

## Converge call

Both confirmed:

- **F4-008**: `TIER1_OPEN_SEAMS: [&str; 7]` includes `"DeviceAuthBackend"` and asserts `len()==7` + `contains("DeviceAuthBackend")`, while it ALSO sits in `TIER2_SEALED_SEAMS` with the invented "straddles Tier-1 AND Tier-2" comment (line 201). R0.4 §2.7 freezes the open roster at SIX (no DeviceAuthBackend) and places it in the sealed tier (#7, no external impl). A sealed trait cannot be an OPEN object-safe seam. fix-now-at-R4.

- **F4-007**: EXPECTED_HEX decodes `role` as int `04` (RoleId has `#[serde(into="u8")]` at line 106) but `member_ref` as text-string `67 55736572446964` = "UserDid" (MemberRef has no `into="u8"`, line 124). Asymmetric unratified representation frozen into the golden. fix-now-at-R4.

I have sufficient ground-truth verification. The verdict is clear: **NOT-CONVERGED** — 2 CONFIRMED BLOCKERs (F4-001, F4-002) + multiple CONFIRMED MAJORs including 2 freeze-gating completeness gaps. Here is the consolidated R4 convergence decision.

---

# R4 CONVERGENCE CONSOLIDATION — F-full R3 Red-Phase Corpus

## 1. CONVERGENCE CALL: **NOT-CONVERGED**

**Panel integrity: COMPLETE** — 15 of 15 lenses returned + adversarial-verify pass (9/9 BLOCKER/MAJOR verdicts) + fresh completeness-critic gap-hunt. I may certify on the merits. (Had any lens been a rate-limited silence, I would have called PANEL-INCOMPLETE; it was not.)

**Why NOT-CONVERGED:** 2 CONFIRMED BLOCKERs + 8 CONFIRMED MAJORs (6 from the panel + 2 freeze-gating completeness gaps), all independently ground-truth-verified by me against the real corpus on `phase-4-meta-core/f-full-r4-fix` and the R0.4 plan on `phase-4-meta-core/f-full-r0-plan-r04`. Convergence requires zero CONFIRMED BLOCKER **and** zero CONFIRMED MAJOR. The corpus needs an **R4-fix pass + re-review of the edited corpus** before R5.

Two of the MAJORs (F4-006 coarse_epoch, plus the completeness #64-mint) carry **R0.4-internal contradictions** that require an orchestrator/Ben ruling, not just a mechanical corpus edit — see §4.

---

## 2. TRIAGE TABLE (CONFIRMED findings; REFUTED/downgraded noted)

### BLOCKERs — both CONFIRMED, fix-now-at-R4
| ID | Family | Final | Disposition | Core defect (verified) |
|----|--------|-------|-------------|------------------------|
| **F4-001** | F-AAD-2 / F-LC-9 | BLOCKER | **fix-now-at-R4** | Line 166 binds plaintext `sender_did` **unconditionally** (no codepoint guard) on the DEFAULT 0x6600/0x6610 MembershipSet group path; frozen 151-byte golden contains `did:key:zAAA` inline as plaintext sender; arm-1 (line 283) asserts it byte-bound as a *positive* property — permanently leaks sender-DID metadata, defeating Sealed-Sender (R0.4 §3.10 / BR-1 / F-LC-9) for every group send. The F4-003 fix applied to the Layer-C sibling was **not propagated** here. |
| **F4-002** | F-W0-1 / F-W0-2 | BLOCKER | **fix-now-at-R4** | X-Wing combiner documented with label **PREPENDED** (`SHA3-256(X-Wing-label ‖ ss_M ‖ ...)`) at f_w0:67 AND R0.4 §3.2(a) line 435 — the superseded v01-v02 order. Final draft-connolly (v06/v10, what 0x647A is IETF-reserved for) **APPENDS** the label. KAT is a `[0x11;32]` sentinel so nothing executable catches it. R5 would freeze a non-interoperable construction, defeating the entire BR-3 corrective. |

### MAJORs — CONFIRMED
| ID | Family | Final | Disposition | Verified |
|----|--------|-------|-------------|----------|
| **F4-004** | F-LC-2/3 vs F-AAD-2 | MAJOR | **fix-now-at-R4** | Layer-C pushes `ENVELOPE_FORMAT_VERSION`(=2) into a byte its own docstring (line 189) names `aad_version`; MembershipSet pushes `AAD_VERSION`(=0x01). Same §4.1 prefix contract, frozen 0x02 vs 0x01. |
| **F4-006** | F-LC-3 | MAJOR | **fix-now-at-R4 + Ben ruling** | `coarse_epoch:u64` on drop bindings/seal-fns contradicts R0.4 §4.1/M-14 ("DropToRecipient carries NEITHER") while #43/line 484/R2 bless "audience+coarse-epoch on wire." F-LD-8 stub models drop *without* coarse_epoch — two stubs of the same wire disagree. **R0.4-internal contradiction → escalate.** |
| **F4-007** | F-AAD-1 / F-MS-3 | MAJOR | **fix-now-at-R4** | Golden freezes `member_ref` as CBOR text-string "UserDid" (`67 55736572446964`) but `role` as int ordinal `04`; asymmetric, unratified; file's own M-20 comment (line 72) calls member_ref a "big-endian integer," contradicting the bytes. |
| **F4-008** | F-CRATE-1 | MAJOR | **fix-now-at-R4** | `TIER1_OPEN_SEAMS[7]` includes `DeviceAuthBackend` + asserts `len()==7`, while it's also in `TIER2_SEALED_SEAMS` with an invented "straddles" comment. R0.4 §2.7 freezes 6 open seams; §6.3 makes DeviceAuthBackend sealed (#7, no external impl). |
| **C-MAJOR-1** (#64) | F-LD-5 / F-DISC-1 | MAJOR | **fix-now-at-R4 + Ben ruling** | §10.5 mandates "mint Compromise #<next>" — never numbered; §5.2 stops at #63; doc-cascade hard-codes "#32–#63"; F-DISC-1 PIN 1 hard-codes `for n in 30u32..=63` as the "closed range." The frozen catch-net forecloses the mandated #64. Also: no positive-control arm proves the pre-sync admit window the Compromise discloses. **Needs Ben to assign #64.** |
| **C-MAJOR-2** (clause-h) | Inv-20 clause-h / F-LB-1 | MAJOR | **fix-now-at-R4** | `grep clause-h` across all corpus tests = ZERO. R2 mis-maps clause-h→F-AUDIT-3 (audit gradation, never touches K(N)). The FROZEN (§4.2:933) per-member K(N) walk-scope confinement property — central Layer-B confidentiality claim — has no negative-confinement pin. |

### F4-003 / F4-005 — REFUTED-to-MINOR by adversarial verify (do NOT block)
| ID | Verdict | Final | Disposition |
|----|---------|-------|-------------|
| **F4-003** (XWingLabel byte-pin) | PARTIAL→MINOR | MINOR | **R5-fill-named** — label is a construction input subsumed into the byte-for-byte KAT (F-W0-2 line 275 transitively catches a wrong label); R0.4 itself defers to "the label bytes per the draft" rather than freezing a literal. Documentary gap, not a frozen-wrong byte. (Optional: add 6-byte XWingLabel docstring constant at author-convenience.) |
| **F4-005** (TLV width u16 vs u32) | PARTIAL→MINOR | MINOR | **R5-fill-named** — drop-AAD (u16) and MembershipSet-AAD (u32) are *distinct, separately-frozen, codepoint-discriminated per-object* AADs, not one shared encoder; no "break the other golden" hazard. Residual = spec-note canonicalizing per-object widths + bounded-decode annotation on u32. |

### MINOR / OBS panel findings — NON-BLOCKING (named carries)
- **fix-now-at-R4 (cheap, do in the same pass)**: F4-010 (reserve codepoint integers absent from §4.0 table), F4-011 (4 NQ-gated Layer-D docstrings stale "OPEN-SPEC" — RATIFIED at §10.5/§10.6), F4-012 (F-W0-3 under-specified swap path), F4-017 (Inv-22→Inv-20-clause-d attribution typo), F4-018 (length-prefix width §4.1 note), F4-020 (F-LD-6 roll-up tautology — delete or invoke). Also completeness **OBS**: F-LD-5 header retense ("NQ-T4 RATIFIED 2026-06-02").
- **R5-fill-named**: F4-009/F4-030 (decode-side bounded-decode family per wire surface — META #629 class), F4-013, F4-014, F4-015, F4-016, F4-019, F4-022, F4-023, F4-024, F4-025, F4-026, F4-027, F4-028, F4-029, F4-031, F4-032, F4-034, F4-036, F4-040, F4-041; completeness MINOR F4-`key_retention_window_secs` (#62 retention default pin).
- **disagree (per HARD-RULE-12, with explanation)**: F4-021 (matrix-trust lesson — gap already CLOSED by F-DROP-NO-KSET mint; recorded not re-opened), F4-026 (CRDT full-tie — faithful clone of in-tree rule, tie-path unreachable under HLC-uniqueness invariant owned by F-HLC family; optional 1-line precondition note).
- **out-of-scope**: F4-033, F4-035, F4-037, F4-038, F4-039, F4-042, F4-043, F4-044 (verification-positive / correct red-phase shape / cross-family-by-design); completeness OBS NQ-A2 (tag-sequencing, correctly test-free — confirm deliberate).

---

## 3. R4-FIX WORK-LIST (must change before R5) vs R5-FILL CARRY-LIST

### A. R4-FIX — CORPUS edits (mechanical, orchestrator-led W0 fix-pass)
1. **F4-001** — `f_aad_2`: gate plaintext-`sender_did` binding to the **non-default** plaintext-sender codepoint ONLY; on 0x6600/0x6610 bind the *sealed-inner-sender-DID*; regenerate `EXPECTED_AAD_HEX` without inline sender-DID; convert arm-1 byte-bound assert to a **default-path-no-plaintext-sender wire-scan** (mirror F4-003's Layer-C fix); update stale R0.3 §3.10 header cite → R0.4 §3.10.
2. **F4-002** — `f_w0:67` docstring AND R0.4 §3.2(a) line 435 (+ §134 desc): change to **appended-label** `SHA3-256(ss_M ‖ ss_X ‖ ct_X ‖ pk_X ‖ XWingLabel)`; pin exact `XWingLabel=0x5c2e2f2f5e5c`.
3. **F4-004** — `f_lc`: give Layer-C a dedicated `AAD_VERSION` const = MembershipSet's `0x01` (separate from `ENVELOPE_FORMAT_VERSION`), OR add explicit §4.1 reconciliation note pinning the single canonical `aad_version`.
4. **F4-007** — ratify member_ref CBOR repr in R0.4 §4.1/§4.2; make member_ref/role consistent in the golden; correct the M-20 comment (line 72) to describe a text-string variant-name encoding.
5. **F4-008** — set `TIER1_OPEN_SEAMS` to the 6-seam R0.4 §2.7 roster (`len()==6`), keep DeviceAuthBackend ONLY in `TIER2_SEALED_SEAMS`, drop the object-safe-Tier-1 assertion + "straddles" comment.
6. **C-MAJOR-2 (clause-h)** — add an F-LB-1 (or F-MS) **negative-confinement arm**: principal granted K(X) CANNOT compute K(Y) for non-descendant sibling Y; correct R2 §2.1's clause-h→F-AUDIT-3 mis-attribution.
7. **Cheap same-pass**: F4-010, F4-011 (+F-LD-5 header), F4-012, F4-017, F4-018, F4-020.

### B. R4-FIX — needs **Ben/orchestrator RULING first**, then corpus edit
8. **F4-006 (coarse_epoch)** — Ben rules the R0.4-internal tension: does `DropToRecipient` carry `coarse_epoch` on-wire? (#43/line 484/R2 say yes; §4.1/M-14 say "carries NEITHER".) Then harmonize the two stubs (add to F-LD-8 OR remove from F-LC-3) + amend §4.1/M-14/FREEZE-table to match.
9. **C-MAJOR-1 (#64)** — Ben assigns **Compromise #64** for the NQ-T4 best-effort-eventual cross-device nonce-rejection window; add the row to R0.4 §5.2 + the §9.1-7 doc-cascade registration list; change F-DISC-1 PIN 1 range to `30u32..=64`; add the **positive-control pre-sync-admit arm** to F-LD-5 (device C ADMITS B's just-consumed jti *before* sync arrives — the honest gap the Compromise discloses), pairing the existing post-sync rejection arm.

### C. R5-FILL CARRY-LIST (name in the R5 brief; do NOT do at R4)
- **META #629 decode-side bounded-decode family** (F4-009/F4-030) — hostile length-prefix → typed-reject pre-allocation, per wire surface (HpkeMultiBase / PermissionRequest / DropBundlePayload / TokenBindingAad). The strongest non-blocking carry.
- **Primitive-SWAP regens** (heavier than rebase — flag in canary commits): F4-014 (K(V) BLAKE3→structural HKDF), F4-024 (gossip topic BLAKE3-keyed-hash→HMAC), F4-025/F4-047 (synthesized KAT sentinels → real published draft-connolly/NIST corpora).
- **Single-source-of-truth collapses**: F4-023 (tie-break stub copied 3+ files), F4-022 (F-CP-2 full IANA range + all reserves incl. sig 0x00xx namespace + experimental band).
- **Stub-shape reconciliation at R5 canary**: F4-015 (CID 36-byte CIDv1 vs 32-byte digest), F4-036 (BindingContext variant union — settle F4-006 first), F4-013 (valid varint placeholders).
- **Behavioral-strengthen at R5**: F4-016 (#52 revocation-priority — real arrival-order permutation + revoked-DID identity, not LWW), F4-019 (legacy-combiner args from real keypair), F4-032 (real ability_template + source grep-defense), F4-034 (DeviceMesh admin user-DID typing).
- **Traceability/doc at R5**: F4-031 (Inv-20 clause a/b/g/h citations), F4-030/F-DISC-2 (don't mention Inv-23+ in prose until minted), F4-026/F4-027 (precondition/associativity docstring scope), F-`key_retention_window_secs` pin.
- **CI**: F4-040 (add `benten-membership-set/testing` to all CI feature lists at the R5 canary).

---

## 4. SUMMARY FOR ORCHESTRATOR + BEN

**The R3 corpus is NOT converged.** The red-phase test corpus is overwhelmingly well-built — the per-family panel is solid, 8 of the 10 BLOCKER/MAJOR-class items are *single-file mechanical fixes*, and the bulk of the 44 findings are correct red-phase shapes correctly dispositioned as R5-fill. But there are **2 confirmed BLOCKERs and 8 confirmed MAJORs** that, if left, would have R5 faithfully implement permanently-frozen-WRONG wire formats. I independently ground-truth-verified every blocking finding against the real corpus and the R0.4 plan — they are real, not stale-view artifacts.

**The two BLOCKERs are the same class of bug — a freeze-gating contradiction of a Ben-ratified rule:**
1. **F4-001**: the default MembershipSet *group send* AAD binds the sender-DID **in plaintext**, defeating Sealed-Sender — the exact thing your 2026-06-02 ruling (group sends honor Sealed-Sender, F-LC-9) forbids. The fix exists in the Layer-C sibling; it just wasn't copied across.
2. **F4-002**: the X-Wing combiner freezes the **old (v01-v02) label order** (prepended), but codepoint `0x647A` is IETF-reserved for the *final* draft (label appended). Freezing this defeats the whole X-Wing corrective (BR-3) and breaks interop with every real X-Wing stack. The KAT is a sentinel so nothing catches it today.

**Two items need YOUR ruling before the fix can land** (R0.4-internal contradictions, not orchestrator-decidable):
- **F4-006**: Does the drop-to-recipient wire carry a `coarse_epoch` byte? Your §4.1/M-14 say "carries NEITHER timestamp"; your Compromise #43 + the Sealed-Sender design text say "audience + coarse-epoch on the wire." Two corpus stubs already disagree. One reading must be blessed before R5 freezes a wire byte.
- **#64 mint**: Your NQ-T4 ratification mandated minting "Compromise #<next>" for the cross-device nonce-rejection window, but it was never numbered. The corpus catch-net hard-codes `#30..#63` as closed, so at R5 the mandated disclosure would be silently excluded. Assign it **#64** and I'll widen the range + add the positive-control arm.

**Recommended path**: orchestrator-led **W0 fix-pass** on the 6 mechanical corpus edits + 6 cheap same-pass MINORs (§3.A), surface the 2 rulings to Ben (§3.B), then **re-review the edited corpus** (the iterate-to-convergence discipline — R4 gates and its triage mutates the corpus, so a single pass never re-reviews the post-fix state). On a clean re-review round (0 BLOCKER / 0 MAJOR), advance to R5 with the carry-list (§3.C) baked into the R5 brief. The completeness critic's parting caution stands: R2's "89/89 families, zero uncovered" was independently falsified twice (F-DROP-NO-KSET mint at R4-fix; clause-h here) — do not over-trust the coverage matrix at R5.

**Disk note**: 8 `r3/w*` red-phase worktrees are still on origin (disk was ~87% at last reading); they harvest/reclaim on the post-fix-pass consolidate, not now. No tags — all HOLD pending Ben.

## Completeness critic

Definitively confirmed. The ONLY mention of the NQ-T4-mandated new Compromise anywhere in R0.4 is the §10.5 ratification text itself (line 1418: "mint Compromise #<next>"). It was never:
- assigned a number (#64),
- added to the §5.2 canonical table (stops at #63),
- added to the §9.1-7 doc-cascade registration list (line 25: "Compromise #32–#63"),
- pinned in the corpus,

and F-DISC-1's PIN 1 hard-codes `#30..#63` as the closed range, so at R5 the disclosure-coherence sweep will neither require nor admit #64.

I now have a complete, well-grounded gap report. Let me write it up.

---

**LENS: COMPLETENESS CRITIC — fresh independent gap-hunt on the F-full R4-FIXED corpus**

I read the full R2 89/95-family catalog + coverage matrix + freeze-priorities, the R0.4 plan (§3.x, §4.0/§5.1/§5.2, §9.1, §10.5–§10.8), and spot-checked the real corpus (57 test files; every R2 F-ID is referenced somewhere). The panel's per-family review is solid; what it MISSED is at the seams between the 2026-06-02 Ben rulings, the R0.4 doc body, and the frozen test ranges. My findings:

---

`[MAJOR]` cross-cutting | F-LD-5 / F-DISC-1 | DEFECT: The Ben-RATIFIED NQ-T4 ruling (R0.4 §10.5, 2026-06-02) explicitly mandates **"mint Compromise #<next>" for the "best-effort-eventual cross-device nonce-rejection window"** — but this new Compromise was never assigned a number, never added to the §5.2 canonical table (which stops at **#63**), never added to the §9.1-7 doc-cascade registration list (line 1425: "Compromise #32–#63"), and is pinned nowhere in the corpus. The only trace of it anywhere is the §10.5 mandate sentence itself (plan line 1418). | WHY: This is a frozen-completeness hole. F-DISC-1 PIN 1 (`f_disc_1_all_compromise_30_through_63_rows_present`) HARD-CODES `#30..#63` as "the F-full closed range" — so at R5, when the doc-wave mints #64 per the ratified ruling, the disclosure-coherence sweep will neither require nor admit it, and the catch-net (whose entire §5-mitigation purpose is to auto-include every Compromise row) silently misses the one row a Ben ruling specifically mandated. A ratified security disclosure goes un-disclosed and un-tested. | DISPOSITION: fix-now-at-R4 — (a) assign #64 + add the row to R0.4 §5.2 + the §9.1-7 registration list; (b) change F-DISC-1 PIN 1 range to `#30..#64`; (c) add a behavioral positive-control arm to F-LD-5 that proves the pre-sync window EXISTS (device C ADMITS B's just-consumed jti *before* sync arrives — the honest-gap the new Compromise discloses), pairing the existing post-sync rejection arm. The corpus currently tests only the post-sync rejection (`f_ld_5_multi_device_shared_rejection_nq_t4_gated`), never the disclosed pre-sync admit.

`[MAJOR]` coverage | Inv-20 clause-h | DEFECT: Inv-20 clause-h ("per-member K(N) walk-scope") has ZERO dedicated behavioral pin in the corpus. R2's coverage matrix (§2.1 Inv-20 row) attributes clause-h to **F-AUDIT-3**, but F-AUDIT-3 pins audit-log read-gradation (`AdminOnly`/`PublicAllMembers`, `audit:<set_id>:*` RestrictedScope parsing) — confirmed by reading all its arms; it never touches K(N) or walk-scope. The whole panel inherited this R2 mis-mapping. The closest real pin, F-LB-1, asserts path-divergence (different predecessor → different key) but NOT the load-bearing confinement claim: that a member holding K(X) **cannot** derive K(N) for a non-descendant sibling Node Y outside their granted walk-scope. | WHY: Clause-h is the central confidentiality property of the Layer-B structural-KDF chain and is a FROZEN wire-affecting surface (R0.4 §4.2 line 933: "ex-F28; backward-compat dispatch; bytes unchanged; re-labeled"). §9.1-5 exit-criterion implicitly relies on it. Freezing the keying surface without a negative-confinement pin means R5 could ship a chain where a member can over-derive sibling keys and no test fails. | DISPOSITION: fix-now-at-R4 — add an F-LB-1 (or F-MS) negative arm: a principal granted scope at Node X is given K(X) but CANNOT compute K(Y) for a Y outside X's descendant set (Cryptree sibling-confinement); and correct R2's §2.1 Inv-20 clause-h→F-AUDIT-3 attribution (it is wrong; clause-h needs its own home).

`[MINOR]` coverage | §3.10 `key_retention_window_secs` | DEFECT: The frozen default `key_retention_window_secs = 604800s (7d)` (R0.4 §3.10) is asserted nowhere in the corpus — grep finds only R2-citation comments, no pin. | WHY: It is a user-definable soft default (not a wire byte), so low risk; but it is a named freeze-relevant default that gates the #62 revocation-reach retention-window disclosure and the drop-forever-valid-within-retention semantics. An unpinned default can silently drift at R5. | DISPOSITION: R5-fill (named) — pin the default value + its role in the #62 retention window in an F-LC-7 or F-LD-8 arm at R5; acceptable to defer because it is non-wire and user-overridable, but name it so it isn't lost.

`[OBS]` coverage | F-LD-5 header staleness | DEFECT: F-LD-5's header (lines 20–26) still states "NQ-T4 ... is UNRESOLVED at R2" and "the EXACT ... semantics are finalized at R5," but NQ-T4 was RATIFIED by Ben 2026-06-02 (R0.4 §10.5: jti-keyed + durable + ≥1-hr retention; per-device-durable GUARANTEED, user-global best-effort-eventual-via-sync). | WHY: Not a test defect (the arms match the ratified default), but the "OPEN-SPEC / gated" framing is now factually wrong and will mislead the R5 implementer into thinking the spec is still open when it is closed — and it is precisely this closed ruling that carries the #64-mint obligation in the MAJOR above. | DISPOSITION: fix-now-at-R4 — retense the F-LD-5 header to "NQ-T4 RATIFIED 2026-06-02" and replace the "finalized at R5" language with the ratified rule, so the #64-mint obligation surfaces at the un-ignore.

`[OBS]` cross-cutting | NQ-A2 / assessment-window | DEFECT: R2 §2.4 flags NQ-A2 (assessment-window tag-sequencing) as intentionally test-free, but R2 §5.A-2 left open whether F-DISC-2 should carry a one-line "exit-criteria docs name the window" doc-conformance arm. The corpus's F-DISC-2 was not checked by me to confirm that decision was made either way. | WHY: Low stakes (a tag-sequencing ratification, not a wire byte); flagged only so the deliberate no-pin decision is recorded rather than accidentally dropped. | DISPOSITION: out-of-scope (tag-sequencing ratification, correctly test-free) — but confirm at R4-triage that the no-pin is deliberate, not an oversight.

---

Spot-checks that came back CLEAN (panel coverage confirmed, no gap): F-LC-9 group-send Sealed-Sender posture (PIN 1 negative-leak arm uses a UNIQUEMARKER sender DID so the negative genuinely fires; PIN 2 recovers inner-sender post-decrypt; PIN 3 `0x6610`≠`0x6520` strict-reject — all substantive, the GAP-1f design hole is properly closed per Ben's ruling); F-LD-6 six-class mini-review (all 6 classes incl. NQ-T1 grant-and-hide via unresolvable `audit_node_cid`, with confused-deputy-before-time ordering); F-INV16-1 U3 length-injectivity (the known-weak pass-against-stub item is genuinely fixed by R4-fix F4-002 — the cross-variant collision now fires red against the stub); F-DISC-1 word-boundary disposition-class matcher (R4-fix F4-043 correctly closes the "MIT inside committed" substring-false-positive, with a baseline arm proving the matcher rejects embedded substrings); F-AAD-1 golden hex (a real CBOR byte-string with embedded role ordinals, not self-referential); tight-`exp` #60 default (F-MS-9 enforces a real CLAMP, not a tautology). The #33 coerced-device path is correctly disclosure-only (OOS class via F-DISC-1), not a missing behavioral pin.

LENS VERDICT: FINDINGS-RAISED — 0 BLOCKER / 2 MAJOR / 1 MINOR / 2 OBS

## All findings (structured)

- **F4-001** [BLOCKER] F-AAD-2 / F-LC-9 `f_aad_2_nine_tuple_injectivity_opaque_boundary.rs` — The MembershipSet AAD 9-tuple assembler binds the plaintext sender_did as a length-prefixed field (U4) in the canonical AAD for the DEFAULT MembershipSet set-keying codepoint 0x6600 (~line 167 lp(&mut buf, t.sender_did.as_bytes())), and fre
- **F4-002** [BLOCKER] F-W0-1 / F-W0-2 `f_w0_envelope_v2_migration.rs:67 (+ R0.4 plan §3.2(a) line 435)` — The X-Wing combiner is documented with the XWingLabel PREPENDED — SHA3-256(X-Wing-label ‖ ss_M ‖ ss_X ‖ ct_X ‖ pk_X) — but final draft-connolly-cfrg-xwing-kem (v03-v10, what codepoint 0x647A is IETF-reserved for) APPENDS the label: SHA3-256
- **F4-003** [MAJOR] F-W0-2 / F-KAT-1 / F-KAT-4 `f_w0_envelope_v2_migration.rs:88-95 (+ f_kat_1, f_kat_4)` — The XWingLabel byte value (0x5c2e2f2f5e5c, ASCII '\.//^\') is never pinned anywhere in the corpus — git grep for XWingLabel|5c2e2f2f5e5c across all test files returns ZERO. The label is a frozen interop-load-bearing constant of the X-Wing c
- **F4-004** [MAJOR] F-LC-2/F-LC-3 vs F-AAD-2 `f_aad_2_nine_tuple_injectivity_opaque_boundary.rs (AAD_VERSION)` — The two envelope-level AAD encoders DISAGREE on the leading aad_version prefix byte — Layer-C's plaintext_aad_bytes() pushes ENVELOPE_FORMAT_VERSION (=2) as byte 0, while MembershipSet's assemble_aad_9tuple() pushes AAD_VERSION (=0x01) as b
- **F4-005** [MAJOR] F-LC-2 vs F-AAD-2 `f_aad_2_nine_tuple_injectivity_opaque_boundary.rs (lp() helper)` — The two 'canonical-TLV length-injective' encoders use DIFFERENT length-prefix integer widths — Layer-C length-prefixes recipient-DIDs and recipient-count with u16 BE, while MembershipSet's lp() and DID-count use u32 BE. Both cite R0.4 §4.1'
- **F4-006** [MAJOR] F-LC-3 `f_lc_hpke_encrypt_to_recipient_sealed_sender.rs:140,149,269,283 (coarse_epoch field)` — The Layer-C drop BindingContext::{DropPlaintextSender, DropSealedSender} carries a coarse_epoch: u64 field and the seal fns take a coarse_epoch arg (F-LC-3 PIN 1 passes a coarse epoch bucket; comment says 'on-wire AAD = audience + coarse-ep
- **F4-007** [MAJOR] F-AAD-1 / F-MS-3 `f_aad_1_members_table_canonical_cbor_length_injective.rs:72,120 (+ f_ms_3:24)` — The frozen golden vector encodes member_ref as a CBOR text string (MemberRef::UserDid -> externally-tagged unit variant 67 55736572446964) while role is frozen as an integer ordinal (#[serde(into="u8")] -> 04) — an asymmetric, un-ratified r
- **F4-008** [MAJOR] F-CRATE-1 `f_crate_1_2_ep1_roster_and_boundary.rs` — TIER1_OPEN_SEAMS pins SEVEN object-safe OPEN backend seams including DeviceAuthBackend (asserts len()==7 + contains("DeviceAuthBackend")), but R0.4 §2.7 (the authoritative m-15 GNC-4 precision edit, line 264) freezes the Tier-1 OPEN roster 
- **F4-009** [MAJOR] F4-030 / META #629 bounded-decode ` abuse TokenBindingAad)` — No test anywhere in the corpus asserts a wire DECODER rejects a hostile declared length-prefix before allocating/reading — e.g. an HpkeMultiBase envelope declaring recipient_count=0xFFFF (u16) with zero actual stanzas, or a DropBundlePayloa
- **F4-010** [MINOR] F-NQA1-1 `f_nqa1_1_frozen_surface_additive_extensibility.rs:59-62` — The test invents specific reserve codepoint integers CP_RESERVED_RECOVERY_ARTIFACT=0x6330, CP_RESERVED_ROTATING_GROUP_KEY=0x6611, CP_RESERVED_EXECUTE_WORKFLOW=0x6321 that are ABSENT from the canonical R0.4 §4.0 table — §4.0 names RecoveryAr
- **F4-011** [MINOR] F-LD-3/5/6/8 (NQ-T2/T3/T4/C5) `f_ld_5` — All four NQ-gated Layer-D arms carry STALE 'OPEN-SPEC: unresolved at R2 / gated on NQ-Xn / R5 finalizes against the ratified NQ-Xn answer' docstrings AND #[ignore] messages, even though R0.4 §10.5/§10.6 RATIFIED all four (Ben 2026-06-02): N
- **F4-012** [MINOR] F-W0-3 `f_w0_envelope_v2_migration.rs:~118 (wire_path_to_le_bytes_count) + :294` — The to_le_bytes-survivor scanner is a stub returning a hardcoded 13; the test asserts ==0 (fires RED). But the file header's R5 un-ignore instruction is 'DELETE the local f_w0_stub + INSERT real imports' — which names no destination for THI
- **F4-013** [MINOR] F-NQC4-1 `f_nqc4_1_did_key_hybrid_pubkey_multicodec.rs:60-61` — The reserved placeholder multicodec prefixes HYBRID_SIG_MULTICODEC_RESERVED=[0x12,0x01] and HYBRID_KEM_MULTICODEC_RESERVED=[0x13,0x01] are not valid unsigned-varint multicodec encodings — 0x12/0x13 have the high bit clear, so a real varint 
- **F4-014** [MINOR] F-INV21-4 vs F-LB-1 `f_lb_1_structural_kdf_on_real_k_principal.rs` — K(V) is the same logical encryption-substrate keying function keyed by the immutable Version-Node-CID (Inv-19), but Inv21 derives it via blake3::derive_key("benten-membership-set:K(V):v1", cid) while Layer-B's structural-KDF chain derives K
- **F4-015** [MINOR] F-AAD-2 vs F-LC-4 `f_lc_dual_cid_local_cid_and_generations.rs (type Cid=[u8;32])` — The body_cid/CID concept is modeled as a 36-byte CIDv1 framing (0x01 0x71 0x1e 0x20 + 32-byte digest) in F-AAD-2's stub_cid, but as a bare 32-byte digest ([u8;32]) in the dual-CID stub and in Layer-C (BodyCid=[u8;32]). The AAD 9-tuple's fro
- **F4-016** [MINOR] F-MST-3 (NQ #52 revocation-priority) `f_mst_membership_anti_entropy_backstop.rs:~232-238` — The apply_in_revocation_priority_order(revocation_hlc, stale_write_hlc) production-stand-in collapses #52's revocation-priority semantics into a bare 'if revocation_hlc > stale_write_hlc' timestamp compare, and the two assertions feed the I
- **F4-017** [MINOR] F-NAT-2 `f_nat_2_unlinkability_scope_honesty_network_observer_only.rs` — The file header and the body assertion message attribute per-recipient unlinkability to Inv-22 ('Inv-22 unlinkability SCOPE-HONESTY', 'Inv-22 m-7; Compromise #58'), but unlinkability is Inv-20 clause-d per R0.4 §5.1/§3.8 m-7 (Inv-22 is memb
- **F4-018** [MINOR] F4-005-adjacent (length-prefix width spec) ` spec — R0.4 §4.1 length-prefix width + byte-pin docstrings (f_lc_hpke:206-209, abuse:183 u16; f_drop_no_kset:146-156, f_ld_2 be_u32_len:80 u32)` — The per-field length-prefix WIDTH is a frozen wire choice the corpus pins INCONSISTENTLY (u16 in Layer-C stanzas + Layer-A AAD; u32 in Drop + Layer-D) yet R0.4 §4.1 specifies only 'canonical-TLV length-injective (U3)' — it never canonicaliz
- **F4-019** [MINOR] F-W0-1 / F4-035 `f_w0_envelope_v2_migration.rs:262-265` — legacy_hkdf_combine is invoked with scrambled/duplicated arguments — feeding SS_MLKEM into three slots that should carry distinct values (ek_mlkem, pub_mlkem). The assert_ne!(real, legacy) still fires red against the stub (real [0;32] != le
- **F4-020** [MINOR] F-LD-6 / F4-034 `f_ld_6_remote_permission_six_class_mini_review.rs:317-327` — The roll-up arm f_ld_6_all_six_pass_classes_have_executable_arms asserts assert_eq!(classes.len(), 6) over a 6-element LITERAL array of GrantRejection variants — a tautology (a fixed-length array literal always has its literal length) that 
- **F4-021** [OBS] matrix-trust lesson (F-DROP-NO-KSET mint) `f_drop_no_k_set.rs (lines 100-210)` — R2's '89/89 families, zero uncovered' coverage claim was independently FALSIFIED by the R4-fix pass itself, which had to MINT a brand-new family (F-DROP-NO-KSET) to close a BLOCKER the R4 completeness critic raised — the Drop primitive's ce
- **F4-022** [OBS] F-CP-2 / Inv-18 (sig namespace vs IANA HPKE overlap) `f_cp_codepoint_registry_dispatch.rs` — The collision/IANA-disjoint scanner's input set all_assigned_envelope_codepoints() (and the iana_hpke_reserved_ranges stub) covers only the envelope band 0x6100..0x6FFF; the sig namespace 0x0001/0x0002/0x0003 is deliberately excluded but li
- **F4-023** [OBS] F-SM-2 / F-CP-3 / F-CP-5 `f_sm_inv17_hybrid_floor_swap_matrix.rs vs f_cp_codepoint_registry_dispatch.rs` — F-SM-2's bidirectional cipher swap matrix round-trips {HybridDefault, ClassicalOnly, Nf1PqPq} + the no-encryption verbatim arm but omits non_pq_encryption() — verified NOT a cipher-axis gap (non_pq_encryption maps to ClassicalOnlyX25519/0x6
- **F4-024** [OBS] F-GOSSIP-2 `f_gossip_transport_placement_and_blinded_topic.rs:266-291` — The absolute golden topic vectors (TOPIC_GEN7_HEX, TOPIC_GEN_0X01020304_BE_HEX) are frozen against the stub's blake3::keyed_hash, but R0.4 §3.9 specifies the construction as HMAC(K_Set, ·) / truncate_32(HMAC(K_Set, set_id‖BE(gen))). BLAKE3-
- **F4-025** [OBS] F-W0-2 / F-KAT-1/3/4 / F-NQC4-1 `4, f_nqc4_1)` — External-vector arms assert against synthesized sentinels (draft_connolly_x_wing_kat_for_fixture() -> [0x11;32], etc.) living inside the deletable stub module. At R5 the combiner is wired to real SHA3-256 but these KAT helpers must ALSO be 
- **F4-026** [OBS] F-CRDT-1/2 `f_crdt_membership_set_convergence.rs` — merge_membership_ops is NOT convergent under a FULL HLC tie — two ops on the same DID with byte-identical (physical_ms,logical,node_id) but different roles resolve to whichever was seen first (existing.hlc>=hlc short-circuit keeps first-see
- **F4-027** [OBS] F-INV21-3 `f_inv21_fork_tie_break_totality_version_node_cid.rs` — The F-INV21-3 proptest body checks forward + reverse reduction only, but the docstring claims 'total + deterministic + commutative + associative'. Forward/reverse cover commutativity-of-reduction; associativity (verified to hold for min-ove
- **F4-028** [OBS] F-GOSSIP-2 arm 3 / F-DROP-NO-KSET PIN 3 (unlinkability/independence scope) `f_drop_no_k_set.rs:347-378` — The gossip unlinkability check is !contains_subslice(topic, set_id) — a substring scan catching only a literal plaintext-embedding leak, not a structural one (a hash leaking set_id via XOR/partial bits would pass). Similarly F-DROP-NO-KSET 
- **F4-029** [OBS] F-MST-3 / F-FED-1 `depth precedence)` — F-MST-3's 'order-independent' second assertion overclaims relative to the body (verbatim-copy of the first, no real permutation — overlaps F4-016). F-FED-1's 'V2 + big-endian from first commit' claim is structurally vacuous in red-phase (ev
- **F4-030** [OBS] F-DISC-2 `f_disc_2_invariant_and_doc_registration_catch_net.rs` — PIN 2 asserts 'highest registered invariant == 22' via a parser that scans EVERY Inv-N mention in INVARIANT-COVERAGE.md including prose. If the R5 doc-wave author writes any forward-looking prose mentioning a higher number (e.g. 'future Inv
- **F4-031** [OBS] Inv-20 clause-traceability (clauses a/b/g/h) `cross-cutting (Inv-20 clause citations across corpus)` — Inv-20 clauses a (K_Set via multi-stanza-HPKE-Encap), b (FORK-ONLY rotation), g (TransportConfig+gossip), h (per-member K(N) walk-scope) are NOT cited by clause-letter anywhere in the corpus (grep: only clauses c,d,e,f,i,j,k,l carry explici
- **F4-032** [OBS] F-MS-7 `f_ms_6_7_moderator_subset_ucan_templates.rs` — ms7_no_permission_flags_bitfield's 'bitfield-free' grep-defense is structurally weak — it only asserts ability tokens are independently-removable strings containing an alphabetic char, which any BTreeSet<&str> trivially satisfies regardless
- **F4-033** [OBS] F-MS-4/5 `f_ms_4_5_roleid_ordinal_and_invitee_zero.rs` — The RoleId ordinal byte is pinned as role as u8 (ms4_roleid_serialized_ordinal_hex_pin) but no arm in THIS file pins that the ordinal byte is actually bound into the AAD 9-tuple (the 'keying-AAD-bound' claim is the load-bearing freeze prope
- **F4-034** [OBS] F-MS-2 `f_ms_2_constructor_cardinality_memberref.rs` — The stub does not model R0.4 §3.6.A's nuance that a DeviceMesh's exactly-1 admin is a USER-DID while its members are device-DIDs — admin_count is a bare usize disjoint from member_refs, so the 'user-DID admin' typing is not pinned. Red-phas
- **F4-035** [OBS] F-NAT-1 `f_nat_1_member_nature_derived_never_stored.rs` — The grep-defense arm grep_defense_no_member_type_or_member_kind_nature_field_in_membership_set_src is a non-#[ignore] #[test] that early-returns (passes vacuously) if the scanned src dir is absent; at this SHA the scaffold src/lib.rs DOES e
- **F4-036** [OBS] F-INV16-1 / F-W0-4 / F-LC-* `cross-cutting (BindingContext variant sets)` — The four stub models of the single frozen BindingContext enum enumerate DIFFERENT variant sets — f_inv16_stub={Vault,WholeContent,Recipient}; f_w0_stub={Vault,WholeContent}; layer_c_stub={DropPlaintextSender,DropSealedSender}. All are #[non
- **F4-037** [OBS] F-KAT-4 (NQ-C3) `f_kat_4_lamps_composite_mldsa_interop.rs` — F-KAT-4's freeze-gating-vs-v1-GM-deferred status (NQ-C3) is left UNRESOLVED in the corpus — the inbound real-fixture acceptance arm is enumerated freeze-gating-by-default but the file explicitly surfaces the gating decision to R2/Ben rather
- **F4-038** [OBS] F-LC-1/2/3 `serialize call sites)` — Several arms (e.g. f_lc_1_hpke_base_single_recipient_round_trips, the F4-003 PIN-5 default-group wire-scan part (c), F-LC-3 PIN 1) drive production entry points that are unimplemented!() in the stub (seal_sealed_sender/open_single/seal_grou
- **F4-039** [OBS] F-INV21-2 / F-DROP-NO-KSET (correct-by-construction class) `f_drop_no_k_set.rs (Inv invariant arms)` — A class of correct-by-construction arms (the 3-fork transitivity rides on Rust's derived Ord for (Hlc, Vec<u8>), transitive by construction; the Drop-no-K_Set positive scan passes GREEN against the deliberately-correct stub) do not fire RED
- **F4-040** [OBS] F-CRATE / CI feature-list `ci.yml (+ coverage.yml, msrv.yml, multi-arch-cargo-check.yml, cross-process-graph.yml, host-error-wire-safety.yml, phase-2a-exit-criteria.yml)` — benten-membership-set/testing is absent from the CI --features list (the nextest/clippy/build/coverage/msrv invocations enumerate 11 crates' /testing features but not the 15th crate's), and benten-membership-set is not named in any wasm/mul
- **F4-041** [OBS] F-VA-3 `f_va_3_constant_time_wrong_password.rs:198-231` — The coarse-timing arm becomes a LIVE wall-clock test at R5 un-ignore; under parallel nextest contention a CPU-scheduling skew on one of the two measured paths could in principle approach the 8x band, but at R5 the real Argon2id KDF (~tens o
- **F4-042** [OBS] F-AAD-1 `f_aad_1_members_table_canonical_cbor_length_injective.rs:255-280` — Arm 4 (length-injectivity / U3) asserts only !is_prefix(truncated, extended) for one specific truncated/extended pair; the general U3 claim ('no shorter snapshot's bytes are a prefix of any longer one') rests on the outer CBOR map-header ca
- **F4-043** [OBS] F-LB-2 `f_lb_2_aad_truncation_defense_be.rs:97` — The stub signatures aad_per_chunk_be(_, chunk_index: u64, total_chunks: u32) and aad_per_recipe_be(_, recipe_index: u32, total_recipes: u32) correctly mirror the in-tree production widths (verified at 2172cb6d:aead.rs:240,273), so the BE pi
- **F4-044** [OBS] F-AUDIT-1..4 `f_audit_*.rs (green-now substrate arms)` — The green-now substrate arms invoke REAL Engine::audit_sequence() / caps().create_principal / grant_capability (all confirmed to exist at the f-full-r4-fix baseline) and faithfully clone the validated in-tree inv_13_dedup_path_does_not_adva
