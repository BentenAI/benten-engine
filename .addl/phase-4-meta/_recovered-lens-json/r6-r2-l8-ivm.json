{
  "lens": "L8 IVM + CanonicalViews + materializer + Version DAG + code-as-graph (CLAUDE.md #1 preservation)",
  "round": "R6 R2",
  "reviewed_artifact_sha": "3c80546f",
  "branch": "phase-4-meta-core/r6-r2-l8-ivm",
  "surfaces_reviewed": [
    "crates/benten-ivm/src/lib.rs",
    "crates/benten-ivm/src/strategy.rs",
    "crates/benten-ivm/src/algorithm_b.rs (1970 LOC)",
    "crates/benten-ivm/src/subgraph_spec.rs",
    "crates/benten-ivm/src/subscriber.rs",
    "crates/benten-ivm/src/views/{capability_grants, content_listing, event_handler_dispatch, governance_inheritance, version_current}.rs",
    "crates/benten-platform-foundation/src/materializer.rs (1882 LOC)",
    "crates/benten-core/src/subgraph.rs (PrimitiveKind enum)",
    "crates/benten-core/src/subgraph_spec/walker.rs",
    "crates/benten-core/src/version_dag.rs",
    "crates/benten-core/src/version.rs + version_chain.rs",
    "crates/benten-engine/src/engine.rs (set_manifest_envelope_rechecker call sites)",
    "crates/benten-engine/src/production_engine_builder.rs",
    "crates/benten-engine/src/production_manifest_envelope_rechecker.rs",
    "crates/benten-engine/src/engine_views.rs (ViewStrategyARefused / ViewStrategyCReserved arms)",
    "tests/phase_3_workspace/for_test_symbols_are_feature_gated.rs (EXEMPT_PUB_ITEMS table)"
  ],
  "disposition": "APPROVE",
  "summary": "R6 R2 L8 deep-verify of IVM + materializer + VersionDag substrate at HEAD 3c80546f (post R6 R1 FP integration including F4 production-rechecker substrate cascade). Verified against the L8 catalog questions in R6-LENS-CATALOG.md:100-114 + the R6 R1 baseline (prior round r1.5-ivm-materializer-versiondag.json was clean APPROVE / 0 findings, so this is a substantive deep-verify pass against actual code state, not just JSON-re-read per Ben's R2 deep-verify shape). FINDINGS: (1) CLAUDE.md baked-in #1 12-primitive irreducibility PRESERVED: `PrimitiveKind` enum at crates/benten-core/src/subgraph.rs:67-94 carries exactly 12 variants (Read/Write/Transform/Branch/Iterate/Wait/Call/Respond/Emit/Sandbox/Subscribe/Stream); `#[non_exhaustive]` is the SemVer hatch; canonical_tag/error_edges/is_deterministic match-arms enumerate all 12. SubgraphSpec walker (crates/benten-core/src/subgraph_spec/walker.rs:183-202 walker_as_subgraph) decomposes BFS into exactly 4 primitives (READ/TRANSFORM/BRANCH/ITERATE) per Spike F finding; `walker_as_subgraph_uses_only_four_primitives` test asserts the constraint at walker.rs:224-237. Materializer (materializer.rs:1782-1800 + :1866+) enumerates the exact 12 PrimitiveKind variants in its dispatched-kinds assertion — no NEW variant minted across the substrate. (2) IVM 5-arm Strategy dispatch (G-CORE-4 D1): Strategy enum at strategy.rs:51-84 carries closed {A, B, Reserved} (NOT #[non_exhaustive] per D8 ratification — closed set, future maintenance algo lands as new enum); `CanonicalViews::dispatch` at algorithm_b.rs:303-309 routes 5 canonical view ids (capability_grants/event_dispatch/content_listing/governance_inheritance/version_current) → Strategy::A; everything else → Strategy::B. CANONICAL_VIEW_META single-source-of-truth table at algorithm_b.rs:90-127 carries the 5 entries; `canonical_meta_for` is the dispatch primitive consulted by all 4 collapsed helpers. Strategy::A refusal arm wired at engine_views.rs:684-701 (`register_user_view` Strategy::A → EngineError::ViewStrategyARefused; Strategy::Reserved → EngineError::ViewStrategyCReserved; Strategy::B → accepted). Both error variants present at error.rs:108 + :462 + :568 + :687 with stable ErrorCode mapping. (3) CanonicalViews A2 seam (RATIFIED D1 A2 + ivm-r1-2 DISAGREE preserved): the 4 pre-collapse helpers (hardcoded_label_for_id / canonical_typed_output_projection_for / is_canonical_view_id / dispatch_for) are narrowed to `pub(crate)` at algorithm_b.rs:334/343/352/358 — external callers query through `CanonicalViews::registry().lookup()/is_canonical()/dispatch()` (the registry-query type at algorithm_b.rs:193). Arch-1 boundary preserved: `benten-ivm`'s Cargo.toml declares only `benten-graph = { path = ... }` (no benten-engine dep); `benten-graph`'s Cargo.toml has ZERO benten-ivm references; ZERO `benten_ivm::` imports anywhere in crates/benten-graph/src/. The 'lift to a lower crate' premise from ivm-r1 is RATIFIED-rejected per the algorithm_b.rs:140-178 module-comment + the `CanonicalViews` doc-comment :172-178 codifies the rejection (future agent proposals to lift the registry type must be rejected with reference to that anchor). (4) §4.6 canonical-views vocabulary stable: `VOCAB_LABEL_PROPERTY_KEY` + `VOCAB_LABEL_NAMES` (vocab.rs:73) consumed by materializer walk at materializer.rs:882 + :984 + schema_compiler::emit at emit.rs:91 + :461 — one-source vocab; the materializer recursive walk threads it through HtmlJson + Plaintext impls uniformly. (5) Materializer walk (G-CORE-4 D1): handles all 5 IVM strategies via the typed-output-projection mapping (None → Rows; Some(Rules) → Rules; Some(Current) → Current) at algorithm_b.rs:1330-1355 `materialize`; mat-r1-1 register-time TypedOutputProjectionMismatch gate at algorithm_b.rs:1143-1149 prevents wrong-variant materialization. The §4.31 5-arm byte-equivalence seam at algorithm_b.rs:1504-1525 `materialize_inner_kernel_read` emits canonical bytes that are byte-identical across the two registration paths (`AlgorithmBView::register` G15-A + `AlgorithmBView::register_subgraph` G23-0a) over the same (view_id, label_pattern, projection, budget) triple — the wrapper-side walk_observable canary surface is the single source for the canary bytes (algorithm_b.rs:1294-1304 + 1399-1398 canonicalize_cids_set), avoiding the inner-kernel ViewQuery-keyed read surface that the 4-of-5 canonical kernels reject for the default query. (6) VersionDag D3 unification (G-CORE-5): `VersionDag` at version_dag.rs:247-267 carries Mode::{Strict, Dag} immutable post-construction; `VersionChain` trait at :186-207 is the object-safe canonical composability surface (current/set_current/walk are the 3 trait methods; append is intentionally NOT on the trait because strict-mode prior-head-threading + DAG-mode parent-child carry different invariants — anchor pattern preserved). Branching supported in Dag mode (version_dag.rs:235-244 doctest demonstrates `chain.append(&root, &a); chain.append(&root, &b)` producing 2 tips). VersionDagError (:106-148) unifies 4 arms across both modes with stable ErrorCode mapping. Coexistence with pre-D3 surfaces (`crate::version::Anchor` + `crate::version_chain::DagVersionChain`) preserved per the module-comment :48-57. (7) IVM views correctly composed with F4's production-rechecker installation — NO view-staleness risk: ProductionEngineBuilder.build() at production_engine_builder.rs:75-80 calls `inner.build()?` THEN `set_manifest_envelope_rechecker(Arc::new(ProductionManifestEnvelopeRechecker::new()))` synchronously before returning the engine value — no async between build + set, no other thread can observe the engine pre-set. The rechecker (engine.rs:1456+) is consulted ONLY at the `apply_atrium_merge` sync-merge WRITE boundary as a gate that decides whether a row commits; it never reads IVM view state. IVM views update via the ChangeSubscriber fan-out from COMMITTED writes (subscriber.rs); since the rechecker gates writes pre-commit, IVM only ever observes admitted writes — the rechecker swap is causally upstream of IVM updates, not orthogonal-with-coupling. ProductionManifestEnvelopeRechecker (production_manifest_envelope_rechecker.rs) has ZERO IVM/materializer/views references in its impl. Default Noop wired at engine.rs:1931-1933 admits-via-NotApplicable (Phase-3 baseline) until production replaces; production replaces synchronously inside `build()`. The materializer (materializer.rs) has ZERO rechecker references — its subscribe seam (materializer.rs:486 `subscribe_with_gate`) attaches via `on_change_as_with_cursor` cap-rechecking pathway which is independent of the manifest-envelope rechecker port. (8) Cadence-tuning paths + `with_budget_for_testing` exempt items: all 5 canonical view constructors carry `with_budget_for_testing` at views/{capability_grants:83, content_listing:118, event_handler_dispatch:76, governance_inheritance:73, version_current:74}.rs — all 5 are listed verbatim in `EXEMPT_PUB_ITEMS` at tests/phase_3_workspace/for_test_symbols_are_feature_gated.rs:108-128 (production-shaped despite the `_for_testing` suffix; consumed by `AlgorithmBView::for_id_with_budget` at algorithm_b.rs:892-938's canonical lane). Row D-22 (workspace `_for_test`/`_for_testing` cfg-gating sweep) was CLOSED at R6 R1 FP-A Bundle F1 per V1-FROZEN-INTERFACE-DEFERRED.md:935 — the EXEMPT table is the forward-protection surface; no-regression pin at the same test file enforces that any NEW `_for_test`/`_for_testing` declaration carries `#[cfg(any(test, feature = \"testing\"))]` unless it's in the exempt list. (9) F4 S3c rename (check_write → check_write_with_audience) does NOT regress L8 surfaces: IVM crate has ZERO direct `check_write` calls (composition is via opaque CapRecheckFn closures threaded by the engine); materializer crate uses `MaterializerCapRecheck = Arc<dyn Fn(&Cid, &str, &Cid) -> bool>` (materializer.rs:182) which is independent of the cap-policy method name. (10) Compile-clean verification: `cargo check -p benten-ivm -p benten-platform-foundation` succeeded clean against HEAD 3c80546f (1m44s, zero warnings/errors). All 8 L8 catalog questions answered positive; the underlying RATIFIED dispositions on this surface (D1 A2 / D3 / §4.31 5-arm byte-equiv / handler-cycle no-new-PrimitiveKind / item 15(h) walker-IS-a-Subgraph / item 15(j) resolver live-per-request / D-4M-R5 additive-consumer / G-CORE-4-CanonicalViews-subscription reuse + the Phase-4-Foundation-era D1/D2/D3 design records) carry through unchanged from R1.5 baseline. CONVERGED-AT-R6-R2: 0 BLOCKER / 0 MAJOR / 0 MINOR substantive findings on the L8 surface.",
  "findings": []
}
